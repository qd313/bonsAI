{
  "version": 1,
  "hooks": {
    "afterFileEdit": [
      {
        "matcher": "src/**/*.{ts,tsx}|main.py|plugin.json",
        "command": "echo After Deck-facing edits: run pnpm run test:preview (requires Decky: Open Preview) or pnpm run build",
        "enabled": false
      }
    ]
  }
}
