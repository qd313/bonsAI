{
  "row": "PERM-JUMP-01",
  "date": "2026-09-05",
  "build": "fourth build of plan 32, code 517804a (dist md5 72dfa6c50d8a6f3feb1862d27210d64d)",
  "plan": "docs/planning/32-bugfix-session.md, bug 1",
  "setup": "Steam ban lookup off; a fresh `bonsai:vac-check` typed with scripts/deck_send_ask.py and sent with A on the ask button, so the reply is live (a restored history turn carries neither the deny surface nor the thumbs row)",
  "tool": "deck_assertFocusMove, after runs/PERM-JUMP-01-build4-a-down-chain-to-open-permissions.json",
  "steps": [
    {
      "step": "Open Permissions is a stop on the way down",
      "detail": "last answer section -> Mark reply helpful -> Retry same prompt -> Open Permissions, every stop visible",
      "verdict": "PASS"
    },
    {
      "step": "Open Permissions is a stop on the way up",
      "detail": "from the ask button: attach screenshot, choose AI character, a chip, Session context, Open Permissions, Retry same prompt, Mark reply helpful",
      "verdict": "PASS"
    },
    {
      "step": "A on Open Permissions opens the Permissions tab",
      "detail": "tab bar reads Permissions; the tab's controls are Back to Main, Ollama, Read game & screenshot context, Save files to Desktop, Steam ban lookup, Voice input (microphone)",
      "verdict": "PASS"
    },
    {
      "step": "the ring lands on the matching toggle",
      "detail": "the ring landed on BUTTON \"Back to Main\" (rect 64,118 100x34), the tab's first control, not on the Steam ban lookup toggle",
      "verdict": "FAIL",
      "cause": "src/utils/permissionJumpRegistry.ts:49-73 focusOwnerById calls a plain target.focus() on panel/toggle/button in turn - a cross-container DOM focus, which moves activeElement while Steam's ring stays put; it also focuses every candidate rather than stopping at the first that lands, and returns true unconditionally, so restorePermissionJumpFocusWithRetry's retry can never see the failure"
    },
    {
      "step": "Back to Main returns to the previous tab",
      "detail": "A on Back to Main returned to the Main tab with the ring back on the Open Permissions button in the deny row, visible",
      "verdict": "PASS"
    }
  ],
  "verdict": "PARTIAL: four of five steps pass; the ring does not land on the matching toggle",
  "note": "the deny row itself carries no tabindex now (the stamping helper was corrected), and the button is inside `.bonsai-chat-vac-deny-row`"
}
