# bonsAI testing

**Purpose:** Where to test, what to run, and how coverage is tracked — without dumping every historical checkbox into one file.

| Doc | Audience | Contents |
|-----|----------|----------|
| **This file** | Everyone | Overview, PR gates summary, coverage index, Verify pointer |
| [testing-automated.md](testing-automated.md) | Agents / CI | Commands that can run without a human on the Deck |
| [testing-manual.md](testing-manual.md) | Maintainers | On-Deck smokes, Tier 0–4 runbook, Deck-only checklists |
| [test-evidence/](test-evidence/) | CI / agents | Preview-suite artifacts (`--write` / `--evidence`) — see [retention](#evidence-retention) |

Related: [roadmap.md](roadmap.md) ([Bugs](roadmap.md#bugs), [Verify](roadmap.md#verify), [Features](roadmap.md#features), [Done](roadmap.md#done-for-v050)), [development.md](development.md) (build/deploy), [troubleshooting.md](troubleshooting.md).

### Evidence retention

`scripts/run-preview-suite.mjs` prunes its own output. It keeps the **3 newest run
folders per batch** and deletes older ones after each evidence run, with two
exemptions: a run **cited by any `docs/**/*.md`** is never deleted, because
evidence is linked by path from the archived QA docs and deleting a cited run
turns a link into a lie; and the run the current invocation just wrote is never
deleted, in case a future-dated folder sorts above it. The rule lives in the
script rather than in this file on purpose — a retention rule that depends on
someone remembering is not a mechanism.

**Reading a `batch-summary.json`: check `ranThisInvocation`.** The run folder is
keyed by date + commit only, so a `--filter` re-run on the same day and commit
writes into the folder an earlier full run already made. Summaries are merged per
scenario id, and `carriedFromEarlierRun` / `ranThisInvocation` say how much of the
row set this invocation actually produced. **Two summaries written before that fix
(2026-08-05) under-report and cannot be trusted for a total:**
`tier2/2026-05-26-9e20a82` says 1 result with 8 case directories beside it — the
archived failures doc independently records that batch as *tier2 (8/8)* — and
`tier2Deep/2026-06-09-a9237e4` says 1 with 11. In both, the per-case
`manifest.json` files are correct; only the batch roll-up is wrong.

**Pruned 2026-08-05 (step 10, D4).** Three runs, all unreferenced and all
carrying no signal: `hookSmoke/2026-05-26-9e20a82` and
`hookSmoke/2026-06-09-a9237e4`, which both failed with
`Error: IPC timeout for callTestHook` — the preview harness being unavailable
headlessly, not a product defect, so **`HOOK-smoke-setTab` has never produced a
real result in this tree** — and `deckOnly/2026-06-09-9e20a82`, whose three cases
all skipped as deck-only. Recoverable with
`git show <commit>^:docs/test-evidence/...`. Everything else was kept: the link
audit found **10 of 13 runs referenced and 0 broken links**, so the earlier
"96% unreferenced" reading in [audit/06-doc-triage.md](audit/06-doc-triage.md) was
an artifact of searching only this file and not the archive.

### Preview-suite evidence invalidated 2026-08-08

**Some recorded preview PASSes tested nothing.** Five independent defects were
found in the harness; each is verified, and none of them failed loudly. Stated
here before the repairs land so the fixes read as scheduled repair rather than
newly-discovered breakage. Plan: [planning/01-qa-automation-plan.md](planning/01-qa-automation-plan.md).

| # | Defect | Evidence |
|---|--------|----------|
| **D1** | **DOM snapshots truncate at 8 KB and never reach rendered markup.** `BonsaiPluginShell` makes a ~110 KB `<style>` block the first child of `.bonsai-scope`, so the snapshot is all CSS. | `test-evidence/tier0/2026-05-26-9e20a82/SMOKE-A-golden-path/dom-final.html` is exactly **8183 bytes**, contains **zero** `</style>`, and ends mid-declaration at `background-color: transparent !i` |
| **D2** | **`previewHook` / `callTestHook` has never worked in this tree.** | `test-evidence/tier3UI/2026-06-09-a9237e4/batch-summary.json` — **0 passed, 8 failed**, every one `Error: IPC timeout for callTestHook` |
| **D3** | **`focusPath`'s no-op value is byte-identical to its success value**, so an assert over it cannot fail. When the focus log is empty, DPS synthesizes `focusPath` from the inputs that were *requested* — and a real log entry for a `Down` press is also the string `onMove(Down)`. | Every stored `focus-path.json` is `["onMove(Down)","onMove(Down)"]`; `active-element.txt` reads `document.body`, so focus never landed on a plugin element. Mechanism confirmed against DPS source 2026-08-08 — see [planning/02-dps-upstream-findings.md](planning/02-dps-upstream-findings.md) § P1-2 |
| **D4** | **`final.png` is a placeholder, not a screenshot.** | Byte-identical within each batch (11202 B tier0, 10220 B elsewhere): a flat dark rectangle reading `Decky preview snapshot` / `505x599` |
| **D5** | **`assertStep` fails open** — six independent `if` blocks with no `default`, so an unknown or misspelled assert type silently passes. | `scripts/run-preview-suite.mjs`, contrast `runStep` which correctly throws. A full audit found **zero** unknown types in the 49 scenarios today, so no recorded result changes because of D5 alone |

**What is invalid, and what still stands** — surgical, because a blanket
invalidation would be as dishonest as leaving it:

| Class | Verdict |
|-------|---------|
| Every `domContains` / `domContainsAny` / `domNotContains`, and the one `focusPathIncludes` | **Invalid** — they matched CSS text, or echoed their own input |
| Every `rpcResult` / `hookResult` assert | **Valid** — coarse, but real |
| Every `shellVitest` / `shellPytest` step | **Valid** — they shell out to the actual suites |
| The 8 `tier3UI` scenarios and `HOOK-smoke-setTab` | **Already recorded FAIL.** Nothing to demote; stop citing them as coverage |
| Every stored `final.png` | **Invalid as evidence** |

**Computed from the scenario JSON, not eyeballed.** Twelve scenarios contain a
DOM or focus assert. Nine of those are `tier3UI` / `hookSmoke` and already record
FAIL. Of the three remaining, **two carry an archived PASS resting on nothing but
invalid asserts** — `SMOKE-A-golden-path` (tier0, 2 DOM asserts, no other check)
and `VISION-V1-spot-dom` (tier2, 1 DOM assert, no other check). Both are marked
`†` in [archive/testing-results-2026.md](archive/testing-results-2026.md).
`THINKING-01-pending-phases` and `FEEDBACK-01-reply-action-chrome` also assert on
DOM but have no archived PASS row, so there is nothing to withdraw.

Note that **`SMOKE-G` (vision attach) is not demoted** — its Verified status comes
from the April 2026 on-Deck vision sweep, not from `VISION-V1-spot-dom`. Only the
preview row is withdrawn.

Everything else stands on its RPC and shell assertions, including the three
SMOKE-F scenarios and the TDP / VAC / mDNS / desktop sets. **No evidence is
deleted** — the retention rule above protects doc-cited runs, and deleting a
cited run turns a link into a lie.

D1–D4 are Decky Plugin Studio behaviour, not ours; per [mcp-setup.md](mcp-setup.md)
§ DPS findings log they are recorded there and go upstream rather than being
forked here.

---

## Quick start

**Every change set (automated):** see [testing-automated.md](testing-automated.md) § Gates.

```bash
pnpm exec tsc --noEmit
pnpm test                  # when src/ touched
pnpm run test:py           # when Python / tests/ touched
pnpm run build             # when src/ or build config touched
```

**Deck-facing PRs:** after `scripts/build.ps1` / `build.sh`, run [testing-manual.md](testing-manual.md) **Tier 0** (SMOKE-A → C → F). Add Tier 1 when Ask / TDP / strategy / game context change.

**Release:** Tier 0–2 + clean-install proof in [testing-manual.md](testing-manual.md) § Tier 4.

**The Deck goes to sleep in the middle of a test run, and it does not fail cleanly.** A run is mostly
waiting — an answer takes tens of seconds, a game takes a minute to launch — and the device dozes off
during that. Presses then land on a sleeping machine and are lost, reads come back empty or stale, and
the session spends its next few steps working out that the Deck went to sleep rather than that the
thing under test broke. Until the tooling can hold it awake, ask a throwaway question before timing
anything, and treat an empty read as "check whether it is asleep" before treating it as a finding.
A pair of tools to turn the sleep timeouts off for a run and put them back afterwards was asked for on
2026-09-07 and is on the Decky Plugin Studio roadmap under *Hold the Deck awake for a test run, then
put the setting back*.

---

## PR contract (summary)

| When | Do |
|------|----|
| Always (if applicable) | Automated gates in [testing-automated.md](testing-automated.md) |
| Touched paths match matrix | Extra unit/preview rows in that doc § PR-scoped matrix |
| `src/`, `main.py`, `plugin.json`, Deck RPC | Tier 0 device smokes ([testing-manual.md](testing-manual.md)) |
| New focusable Settings/QAM control | FOCUS-GRAPH-01…05 + coverage row ([testing-manual.md](testing-manual.md) § Focus graph) |
| Docs-only | State **N/A** for Deck smokes; still run applicable automated steps |

Full policy: MCP `bonsai://policy/documentation` / [.cursor/rules/docs-on-ship.mdc](../.cursor/rules/docs-on-ship.mdc).

---

## Shipped feature coverage (slim)

One smoke often covers many features. Status: **Verified** / **Partial** / **Open** / **N/A**. Update when a smoke or release pass lands. Detail checklists: [testing-manual.md](testing-manual.md).

| Area | Covered by | Status | Notes |
|------|------------|--------|-------|
| Collapsing tab bar (plan 30) | TAB-BAR-01…11 in [testing-manual.md](testing-manual.md); unit: `TabIndicatorBar.test.tsx` (21), `tabBarNav.test.ts` (10), `useHiddenTabHeaderTrap.test.tsx` (9), `tabTitles.test.tsx` (7) | Open (07 by eye, 08 touch, one 09 opener, 11 owed) | Steam's header hidden by a hash-free rule, the 20px bar is the strip's one focus stop, the strip floats over the panel. Rows 01–06, 09 (three of four openers) and 10 passed on-Deck 2026-09-02 with evidence under `runs/TAB-BAR-*`; 07 (by eye) and 08 (touch) need a person at the Deck; the desktop-note opener of 09 needs the *Save files to Desktop* permission on. **TAB-BAR-11 fixed at the desk 2026-09-04, Deck check owed:** the two openers 09 missed (the Clear cache confirmation's return, and a QAM chord close/reopen) landed the ring on a hidden tab button; both now go through the trap's attach-time check or the modal return-focus registry. **Second finding the same evening, build 49241e7 on-Deck:** after a suspend/resume, neither the trap nor `TabIndicatorBar`'s tap-outside listener ever actually bounced anything, because both brand-checked nodes from the QuickAccess popup document with `instanceof Element`/`instanceof Node` against this module's own realm (SharedJSContext) — false across that boundary every time. Both now duck-type instead; Deck re-check owed. Height reclaimed: body top 84.66 → 23.99px; the transcript itself has not grown yet (plan § 8) |
| Plugin shell / tabs / Ask / connection test | SMOKE-A | **Verified (Deck) 2026-09-03** | **On-Deck PASS 2026-09-03 (plan 31 session 1), every step of the SMOKE-A checklist in [testing-manual.md](testing-manual.md); the 2026-05 preview PASS stays vacuous.** **Demoted from Partial 2026-08-08.** The May 2026 preview PASS was vacuous: SMOKE-A's asserts were entirely DOM and focus-path, so D1 and D3 above mean nothing about the shell was actually checked. It is the only tier0 scenario in that position. The preview scenario still needs real rendered-copy asserts before its own PASS means anything |
| Permissions gate | SMOKE-C | Open | **Deck 2026-09-05:** the permissions half of this scenario was driven by hand on the device instead — deny surface, *Open Permissions*, the matching toggle, and back (PERM-JUMP-01, all steps). The preview scenario itself is still stale: it asserts the capability key `hardware_control`, removed 2026-07-30. **Demoted from Partial 2026-08-08.** Two reasons. The preview PASS rested on DOM asserts (D1), and separately the scenario's one `rpcResult` assert expects the capability key `hardware_control`, **removed 2026-07-30** — tier0 has not run since, so SMOKE-C will fail on the next run. Fixed in the scenario JSON; recovers with an end-to-end capability gate test through `class Plugin`, which does not exist yet (`tests/test_capabilities.py` is service-level only). **Blocked 2026-09-03 on the *Open Permissions* button not being a D-pad stop; fixed at the desk 2026-09-04, Deck check owed** — see the PERM-JUMP-01 row below. |
| Reply style (Caveman / Balanced / Detailed) | REPLY-VERB-01 | Open | **Shipped 2026-08-09** (replaces Short). Unit: `settingsContracts.test.ts` (legacy `short` → `caveman`), `test_settings_service.py`, `test_ollama_service.py` (`CAVEMAN REPLY STYLE`; skipped when character roleplay on; depth-relax clause). **On-Deck:** Ollama → Reply style → **Caveman** → Ask → Input handling shows `Reply style: caveman` and reply is terse/caveman-like; **Balanced** → no REPLY VERBOSITY block vs prior; **Detailed** still paragraphs; with **AI characters** on + Caveman, character voice (not caveman grammar). Strategy + Detailed still ends with `bonsai-strategy-branches`. |
| Permission jump (deny → Open Permissions → toggle + Back) | PERM-JUMP-01 | **Verified (Deck) 2026-09-05** | **Deck 2026-09-05, fifth build of plan 32 (code `d9d952d`): the whole row PASSES.** On a fresh `bonsai:vac-check` reply with ban lookup off, Up from the ask button reached *Open Permissions* in six presses (attach, character, session strip, then the button, every stop visible). A opened the Permissions tab **with the ring on the *Steam ban lookup* row itself**, not on *Back to Main* (`runs/PERM-JUMP-01-build5-toggle-and-back.json`). A on that row turned the capability on — the ring stayed on the row, and the saved settings on the Deck went to `steam_web_api: true` — then three Ups reached *Back to Main*, each stop visible, and A there returned to the Main tab with the ring back on the *Open Permissions* button (`runs/PERM-JUMP-01-build5-toggle-on-and-back-to-main.json`, 4/4). Three fixes were needed to get here: the button had to become a real stop, the hop into its row had to use Steam's own transfer rather than a DOM focus, and the jump itself had to do the same. **Deck 2026-09-05, fourth build of plan 32 (code `517804a`):** with ban lookup off and a fresh `bonsai:vac-check` reply, *Open Permissions* is a real D-pad stop both ways — down: last answer section, Helpful, Retry, Open Permissions (`runs/PERM-JUMP-01-build4-a-down-chain-to-open-permissions.json`, 3/3 visible); up from the ask button: attach, character, chip, Session context, Open Permissions, Retry, Helpful. A opens the Permissions tab, and A on *Back to Main* returns to Main with the ring back on the Open Permissions button. **The one step that still fails: the ring lands on *Back to Main*, the tab's first control, not on the Steam ban lookup toggle** (`runs/PERM-JUMP-01-build4-b-jump-and-back.json`). Cause: `permissionJumpRegistry.ts:49-73` focuses panel/toggle/button with a plain cross-container `focus()`, focuses every candidate rather than stopping at the first that lands, and returns `true` whatever happens, so the retry never sees the miss. Back with its lane for a `takeNavFocus` version. **Deck 2026-09-04, build 49241e7 (plan 32):** ban lookup off, `bonsai:vac-check` from the Ask field; the deny reply rendered with the Open Permissions button (115x34, y 564). A walk Up from the ask button went attach, avatar, Session context, and the next Up left nothing owning the ring (the strip's new hop into the deny row is a plain focus across containers). A DOM read afterwards showed the new `.bonsai-chat-vac-deny-row` wrapper carrying `tabindex="-1"`, stamped by `focusDeckOwner`, which removes it from Steam's graph; on this device no Steam Focusable carries a tabindex attribute, so the helper's guard never protects. Back with lane A for a registered-nav-node version (`runs/PERM-JUMP-01-b-ban-lookup-off.json` covers the setup walk). Unit 2026-08-07: `permissionDeepLink.test.ts`, `permissionJumpRegistry.test.ts`, `usePermissionJump.test.ts`, `PermissionDenyAction.test.tsx`. **On-Deck:** each capability off → deny surface shows **Open Permissions** → lands on matching toggle → **Back to …** returns to prior tab. D-pad: PERM-JUMP-01 in [testing-manual.md](testing-manual.md). **Blocked 2026-09-03: *Open Permissions* was not a D-pad stop at all** (`runs/PERM-JUMP-01-a-find-open-permissions.json` — Down from Retry/Copy jumped past it to the session strip). **Fixed at the desk 2026-09-04, Deck check owed:** `PermissionDenyAction` and the troubleshooting Ask hint's buttons are genuine stops now (`focusable`) and join the reply row's Down/Up chain; extended `PermissionDenyAction.test.tsx` and added `MainTabChatTranscript.permissionHintFocus.test.ts`. **Redone at the desk 2026-09-04 after the Deck failure above, Deck check owed again:** the button-ref-plus-`focusDeckOwner` approach is gone. Both rows now register a `navRef` (new ids `chat-perm-hint-troubleshoot` / `chat-perm-hint-deny` in `navFocusRegistry.ts`, the same mechanism `SessionContextStrip`/`ChatSlotRow`/the preset carousel already use), `focusChatPermissionHintRow` is `takeNavFocus` only, and `focusDeckOwner` itself (`liveTurnFocusGraph.ts`) no longer stamps a tabindex onto a target it resolved to a genuine `.Panel.Focusable` — only onto a plain element of ours that is not one, which is the one case the stamp was ever safe for. Unit: rewrote `MainTabChatTranscript.permissionHintFocus.test.ts` against the real `navFocusRegistry` (a fake nav node standing in for what Steam populates, never a DOM `.focus()`), added `MainTabChatTranscript.permHintNavRegistration.test.tsx` (registers on mount, clears on unmount — verified failing first by temporarily breaking the effect), and added two cases to `liveTurnFocusGraph.test.ts` pinning the new `focusDeckOwner` behavior. **Redone at the desk 2026-09-05, Deck check owed again on step 3 only.** Each Permissions row (`PermissionToggleHost` in `PermissionsTab.tsx`) now registers its own Steam nav node — `navRef` on the row's own `Focusable` (was a plain `<div>` with an element ref) — under a new id per capability in `navFocusRegistry.ts` (`permissions-row-game-context-read` / `-filesystem-write` / `-steam-web-api` / `-microphone-access`). `focusOwnerById` is `takeNavFocus` on the armed capability's id, nothing else, and `restorePermissionJumpFocusWithRetry` now retries purely on that honest result rather than stopping at the first attempt once a registered element merely existed (which, in practice, was always already true by the 0ms attempt, so the schedule never got a second try before). `armPermissionJump` and the return-tab story are untouched. Unit: rewrote `permissionJumpRegistry.test.ts` (six cases: arm/consume unchanged; no registered row → honest false; a registered row whose nav node declines → false, all three scheduled attempts run; a registered row that claims → true, no DOM node focused; a row that starts declining then claims on a later retry → true after two attempts) and added `PermissionsTab.permissionRowNavRegistration.test.tsx` (one case per capability row: registers on mount, clears on unmount) — every new case verified failing first. |
| Kids master lock (Steam parental) | KIDS-LOCK-01, KIDS-LOCK-02, KIDS-FOCUS-01, KIDS-REGRESS-01 | Open | KIDS-REGRESS-01 PASS 2026-09-03: with no parental controls the Permissions tab shows no banner and its four toggles take the ring and are live (`runs/SHELL-PAYLOAD-01-permissions-sweep.json`). **Shipped 2026-08-09.** `steamParental.ts`, `useKidsLock.ts`, `set_kids_lock_state` RPC, `capability_enabled` session flag, Permissions banner + disabled toggles. Unit: `steamParental.test.ts`, `useKidsLock.test.ts`, `PermissionsTab.test.tsx`, `tests/test_capabilities.py`. **Deploy note 2026-08-09:** `main.py` / `capabilities.py` / `dist/index.js` copied onto Deck (hashes match build); full `build.ps1` aborted on `sudo -n` (passwordless sudo missing) so Decky Loader was not restarted — reopen/reload Decky before QA. **On-Deck:** **KIDS-LOCK-01** Family View locked on spare adult → banner, toggles grey/off, privileged actions deny; unlock restores prior values. **KIDS-LOCK-02** Steam Families child account (may stay Open). **KIDS-FOCUS-01** D-pad Permissions while locked (banner Focusable; no dead end). **KIDS-REGRESS-01** Deck with no parental controls identical to pre-ship. Live CEF Stage 0 still owed in [08](planning/08-kids-master-lock-feasibility.md) spike results. |
| Permissions cleanup batch (web links always-on; troubleshoot hint) | PERMS-CLEAN-02, 05, 06 | Open | **Narrowed from 01…06 on 2026-08-08 — not demoted, scoped.** Only three of the six are provable by a preview scenario, and only in the positive direction. **05** (About links present with all five capabilities off) and **06** (troubleshooting hint appears, then is gone after Dismiss) assert visible copy, so they cannot false-pass; **02** is checkable only as copy-staleness. **01 / 03 / 04 struck as unprovable** — each was written as "this UI is gone", i.e. a `domNotContains`, and the strings they name do not exist anywhere in the tree, so the assert passes whether or not the code is right. That is true even after D1 is fixed. Those three need an on-Deck look or a source-level test, not a preview row. Remaining on-Deck: About links, Steam Input jump, troubleshooting Ask with game-context on/off + dismiss hint, no power-limits toggle, no Response verification section |
| Deterministic commands / VAC off / shortcut keywords | SMOKE-F | Partial | VAC full matrix → Tier 2 manual |
| TDP suggestions (read-only; no sysfs apply) | SMOKE-B | Retired | **Retired 2026-09-03 (D57 #6).** Apply path removed 2026-07-30; re-verify suggestion-only banner/behavior. 2026-08-02: the sysfs **write** code is gone too (`apply_tdp`, `write_sysfs`, `append_sandbox_sysfs_write`) — reads and clamp bounds are unchanged. `tests/test_tdp_sandbox_sysfs.py` is replaced by `tests/test_tdp_service.py`, and the `UNIT-B-pytest-sandbox-tdp` preview gate now runs that file instead. `get_input_transparency` **no longer returns a `sysfs_writes` field** (nothing produced it once the apply path went); the `getSysfsWrites` preview hook stays and returns `[]` so out-of-repo DPS scenarios keep a stable contract |
| Destructive mod/save advice guard (compatdata / Wine-Proton prefix / save data) | DESTRUCT-ADVICE-01 | **Passes with streaming ON (Deck) — streaming-OFF half still owed** | **Failed 2026-08-27, fixed and re-verified the same day.** The miss was a wording gap, in two independent places: `\bdelete\b` never matched *"deleting"* (no word boundary before the `-ing`), and the target pattern demanded `(wine\|proton) prefix` where the reply said *"the existing prefix folder"*. Both are fixed, and the guard now **logs on every completed Ask** — `destructive advice guard ran flagged=… signals=… backup_mention=…` — because until then a reply with no notice looked exactly like a guard that never ran. **Passing evidence, two kinds, because re-asking is not a reliable re-test:** the deployed guard file (hash-verified) fed the verbatim failing reply returns `flagged=True` with the right sentence isolated and the notice produced; and a live Ask, *"should i delete my proton prefix folder to fix a broken game"*, logged `flagged=True signals=1` with the **bonsAI safety check** notice read back off the rendered bubble. **Caveat worth keeping:** in that live run the model *declined* to advise deletion and what tripped the guard was its echo of the question — over-warning, not a clean catch. **Original failure, for the record:** **#3** `my proton prefix is broken how do i start fresh`: the model answered *"Try deleting the existing prefix folder and letting Steam rebuild it"* and offered a branch button labelled *"Delete and Rebuild the prefix"*, and the trace's **Final UI text (after TDP / hardware notices)** carried **no notice** — so it was never appended, not appended and lost in rendering. **#2** `my saves wont load after a mod update what should i delete` ran twice and the model refused to name anything to delete, so no notice is correct and it proves nothing — this is exactly the "the question missed, not the guard" case the plan has two positives for. **#4** `how do i install a mod safely` produced no notice as expected, but the model deflected without advising anything, so it does not exercise the guard either. **Streaming-off half not run.** Filed as its own roadmap bug. | **Fixed 2026-08-23.** New output-side check, [destructive_advice_guard.py](../py_modules/backend/services/destructive_advice_guard.py), wired into `run_game_ai_request` ([game_ai_request.py](../py_modules/backend/services/game_ai_request.py)) right after a successful Ollama reply. It flags a reply that names a destructive verb (delete/remove/wipe/erase/format/uninstall/…) and a destructive target (compatdata, Wine/Proton prefix, save data) **in the same sentence**, with no backup-step language (back up / make a copy / keep a copy) anywhere in the reply, and appends a visible **bonsAI safety check** notice — it never blocks or rewrites the model's own text. Runs once on the finished reply, not per streamed token: token streaming publishes partial text to the UI as it arrives ([ollama_ask_service.py](../py_modules/backend/services/ollama_ask_service.py) `on_delta`), so on a streamed Ask the raw text is already visible before the notice can be appended — catching that would mean scanning growing partial text and deciding when to abort a stream mid-generation, out of scope here. Deviates from the plan's literal suggestion ([12-deep-mod-ai-hints-feasibility.md](planning/12-deep-mod-ai-hints-feasibility.md) §5.3) to house this in `response_verify.py`: given its own file instead, since the two checks share no logic (this is pure rule-based text matching, no second-model call) and a new file has zero collision risk with `response_verify.py`/`transparency_service.py` edits happening in parallel elsewhere. 20 tests: 16 unit (`test_destructive_advice_guard.py`, catches the dangerous sentence forms and their close negatives — "remove the mod", "delete the shortcut", "you don't need to delete your save data" — without firing), 4 wiring (`test_destructive_advice_wiring.py`, through `run_game_ai_request` with a fake plugin). **Cannot be proven without the Deck:** whether a real model's actual phrasing for this kind of advice matches the verb/target regexes (tuned against plausible phrasing, not observed model output), and how the appended notice renders inline in the markdown bubble. **On-Deck:** with a game running, ask *"my game keeps crashing on launch, what do I try"* a few times until a reply suggests deleting the compatdata folder or Proton prefix (rephrasing to nudge it there is fine) — if it does not also say to back the folder up first, **bonsAI safety check** must appear under the reply; a reply that already includes a backup step, or an unrelated troubleshooting reply (e.g. lowering TDP), must show no notice. **Maintainer accepted the streaming delay 2026-08-23:** the app and repo already carry disclaimers telling the user to double-check AI answers, so a late flag beats no flag, and catching it earlier stays out of scope. **What that acceptance obliges is proving the flag appears at all — this row is not closable on unit tests.** On-Deck steps, both with token streaming ON and OFF: (1) ask a question whose honest answer involves deleting a compatibility folder and confirm the safety notice appears under the reply; (2) ask one whose answer mentions backing up first and confirm **no** notice appears; (3) ask an ordinary game question and confirm no notice — a guard that fires on innocent replies gets switched off, which is the same as not having one. Record the model used: the check is tuned against plausible phrasing, not against phrasing pulled from a live model, so a miss here is a wording gap in the check, not a wiring failure. Exact question wording to be agreed and pinned as frozen test chips (`b278f7b`) before anyone types on the device. **This row is expected to shrink.** The maintainer's direction 2026-08-23 is that token streaming becomes the default and the setting is removed once the streaming bugs are fixed — see *Make token streaming the default and drop the setting* in the Ask / reply backlog. When that lands, the streaming-OFF half of this row stops existing and the accepted limitation becomes the only behaviour. Exact questions and run order: batch B of [20-frozen-chip-qa-batches.md](planning/20-frozen-chip-qa-batches.md). |
| Strategy mode + spoilers (spot) | SMOKE-E | Partial | Deeper spoiler/checklist → Tier 2 |
| Strategy spoiler false positives (named-entity display) | STRAT-SPOIL-DRG-01 | Open | **Automated 2026-08-07**, on-Deck pending. Fixes three independent defects, any one of which reproduced the bug: **D1** the asked-entity unwrap ran only on the live turn, so a history turn re-fenced itself (`MainTabChatTranscript.renderAnswerBubble` passed `""`/`null` for anything but `"live"`); **D2** the low-risk prompt signal was reachable only through the KB corpus, so it was silently absent with no corpus installed; **D3** the prompt was additive — the *"avoid late-game boss names"* clause and the KB *"put spoilery walkthrough detail inside a fence"* clause both survived alongside the addendum that contradicts them.<br><br>**Named-entity consent is now title-independent** (spoiler-constitution rule 7): naming a boss unfences that boss on any game, Hades and OoT included, and relaxes nothing else. Two older Python tests asserted the opposite by naming a boss on a story title and expecting a fence — their premise was wrong, and they now cover the *unnamed* ask instead.<br><br>New automated coverage: vitest `useBonsaiAskOrchestration.test.ts` — an archived turn carries the AppID it was asked against; `tests/test_ollama_service.py` — named entity alone fires on a story title, named entity does **not** relax the rest, low-risk AppID fires with empty genres, KB spoiler clause dropped when low-risk and kept otherwise.<br><br>**Not covered by a unit test:** `MainTabChatTranscript` render wiring (history expand path). `buildAnswerBubbleElement.test.tsx` covers consent unwrap with masking off; row **DRG-01d** remains the on-Deck gate. Recon and locked decisions: [04-strategy-spoiler-false-positive.md](planning/04-strategy-spoiler-false-positive.md).<br><br>**R4 fixed 2026-08-15 (mid-stream mask flash).** Before this fix an open `bonsai-spoiler` fence always showed **Spoiler hidden until complete…** while streaming, even on a turn that would immediately unwrap once the fence closed — so a QA observer watching DRG Survivor stream in saw the mask chip and reasonably reported FAIL. `unwrapAskedEntitySpoilerFences.ts` now exports `shouldUnwrapSpoilerFence`, the same per-fence predicate the closed-fence unwrap already used; `streamMarkdownPrepare.ts`'s `prepareStreamMarkdown` takes an optional `unwrapOpenSpoilerFence` callback and, when it says yes, streams the *open* fence body as prose instead of the wait chip; `buildAnswerBubbleElement.tsx` builds that callback from the identical eligibility gate as the existing closed-fence unwrap, so nothing was over-relaxed — a turn ineligible for unwrap still masks exactly as before. Called with no callback, behaviour is byte-identical to pre-fix (all four original `streamMarkdownPrepare.test.ts` cases pass unedited). New coverage: 6 cases in `streamMarkdownPrepare.test.ts`, 4 in `unwrapAskedEntitySpoilerFences.test.ts`, 2 in `buildAnswerBubbleElement.test.tsx` (DRG Survivor streams as prose with no mask chip; a narrative title with no entity named still shows the chip). **On-Deck:** new rows **DRG-01-STREAM-01** and **HADES-UNNAMED-STREAM-01** in [testing-manual.md](testing-manual.md)<br><br>**On-Deck 2026-08-23 — the DRG false positive is gone, confirmed at the prompt rather than the pixel** (`recordings/DeckRecord_20260823_173947_game.mkv`). *"how do i deal with the exploders"* — the exact question that fenced on both 2026-08-22 captures — answered in **plain text with no fence**. The Desktop ask trace shows the whole chain working, which is what makes this a pass rather than a lucky generation: the built prompt contains *"The user asked about "Exploder""* (so the `deal\s+with` extraction gap is closed), the low-risk arm carries its new *"Reserve `bonsai-spoiler` only for hidden narrative twists… not standard boss move-sets or wave tactics"* instruction, and the **raw model output contains no `bonsai-spoiler` fence at all**. Three cards attached (`Exploder`, `Acid Spitter`, `Mactera`) and the reply uses their content unfenced. **But *twins* — the regression control — now fences, and did not on 2026-08-22** (`recordings/DeckRecord_20260823_175825_game.mkv`). So the fix is a **wash on this title**: one question stopped fencing, another started, and the net rate is unchanged. **Treat the "when" half as still open.** The fence lands **mid-reply**, which is the first on-device evidence for the placement ("where") half. It also duplicates the plain text directly beneath it — same advice fenced and unfenced, only the immune-window detail is fence-only — so it hides essentially nothing. **Lead, unproven:** the *twins* prompt names the entity as the bare word *"twins"* while the *exploders* prompt names the card title *"Exploder"*, because `_match_known_entity` needs the whole card name (plus at most one trailing `s`) and *"twins"* is only a suffix of `Dreadnought Twins`. Real defect, but **not established as the cause** — the same bare *"twins"* entity produced no fence on 2026-08-22, and this row already records the false positive as *inconsistent turn to turn*. **Answered same day: deterministic.** Four consecutive *twins* runs, all fenced, all with the identical entity `"twins"` — so the row's older *"inconsistent turn to turn"* note does not apply to this question. **Extraction fixed 2026-08-23:** `_match_known_entity` now falls back to a card's head noun when the full title is absent, so *twins* resolves `Dreadnought Twins` (and *spitters* → `Acid Spitter`), guarded against generic heads so *"how do i beat the boss"* still reaches nothing. **Steps to verify on-Deck after deploy:** ask *"how do i beat the twins"* **four** times on DRG Survivor in Strategy mode and confirm no `bonsai-spoiler` fence in any of them; check the trace shows the entity now reading `Dreadnought Twins`. If it still fences with the entity naming the card, the remaining cause is prompt wording, not extraction. **Also still owed:** *red sugar*. |
| Spoiler constitution — title profiles + named entity | CONST-SPOIL-01 | Open | **Automated 2026-08-07.** `test_spoiler_title_profiles.py`, `test_ollama_service.py` (Hades protect without entity; `kb_entity_match` does not title-relax; DRG/L4D2 low-narrative), `unwrapAskedEntitySpoilerFences.test.ts`. **On-Deck:** DRG `2321470` unnamed boss ask → open tactics; Hades `1145360` unnamed → fences stay; Hades + *"How do I beat Megaera?"* → Megaera plain, other bosses still fenced; OoT unnamed → conservative. [spoiler-constitution.md](planning/spoiler-constitution.md) |
| Spoiler constitution — consent opens history fences | CONST-SPOIL-CONSENT-01 | Open | **Automated 2026-08-07.** `unwrapAskedEntitySpoilerFences.test.ts`, `buildAnswerBubbleElement.test.tsx` (consent unwrap with masking off), `useBonsaiAskOrchestration` stamps `spoilerConsentEffective` on collapsed turns. **On-Deck:** Strategy ask with *"spoilers are okay"* → all fences plain on live turn; collapse turn → expand from history → fences still plain (not re-masked). |
| Spoiler constitution — Speed + strategy KB inject | CONST-SPOIL-SPEED-01 | Open | **Automated 2026-08-07.** `test_ollama_service.py` `test_build_system_prompt_speed_strategy_domain_injects_compact_constitution`. **On-Deck:** Speed mode, KB on, strategy cards attach on a protect title → reply policy follows constitution (no wholesale boss-name drop without entity/consent); Show details chip still inherent protect band on Hades. |
| Preset carousel troubleshooting triple | SMOKE-D | Verified | |
| Thinking line survives a lazy model status tag | THINKING-SANITIZE-01 | Open | **Automated 2026-08-08.** `composeThinkingBlurb.test.ts` + `tests/test_bonsai_stream_tags.py` share a parity table (`"Sure."`, `"Yeah"`, `"Fine. Sure."`, `""`, `"   "`) and an idempotency case — the sanitizers run in series, so a divergence blanks the line rather than merely differing. **On-Deck:** run Asks until a model emits a bare `<bonsai-status>Sure.</bonsai-status>` (small models do this) — the italic line must keep showing something, never disappear mid-Ask and leave a bare spinner. Detail: [06-thinking-blurbs-review.md § 2.3](planning/06-thinking-blurbs-review.md) **Deck 2026-09-04 PASS:** a Deep-thinking qwen3.5:4b reply produced a model-derived phase — *"Reviewing Antlion vibration hunting mechanics and sand trap safety..."* — among 32 sampled phases, so the lazy status tag survived. |
| Thinking phases not all emoji within one Ask | THINKING-EMOJI-CLUSTER-01 | Open | **Automated 2026-08-08.** `tests/test_bonsai_stream_tags.py` sweeps request ids 1…50 × five live phases and asserts no Ask is emoji-only throughout; a companion test pins that the empty salt leaves the opener pick unchanged, since `composeThinkingBlurb.ts` still mirrors it. **On-Deck:** run several troubleshooting Asks that hit multiple prep phases (Proton logs → KB → screenshot) — a bare 🙄 / 🌳 on *one* transition is intended copy; every transition in the same Ask being emoji is the bug. Detail: [06-thinking-blurbs-review.md § 2.4](planning/06-thinking-blurbs-review.md) |
| Thinking line has one writer (no unprompted rewrite) | THINKING-COPY-01 | Open | **Automated 2026-08-08.** Python composes the opener at accept time and returns it from `start_background_game_ai`; `composeThinkingBlurb.ts` and its pools are deleted, so the client cannot disagree. `useBonsaiAskOrchestration.test.ts` — renders the returned opener, holds the current line when a poll carries none, keeps a lazy-opener summary visible; `test_background_partial_state.py` — the opener equals what a later `"starting"` publish would render, and is in the snapshot before the task starts. **On-Deck:** THINKING-COPY-01 and THINKING-OPENER-01 in [testing-manual.md](testing-manual.md). The second is a judgement call — if the placeholder→opener swap reads as two lines rather than one settling, take the documented fallback. Detail: [06 § 10](planning/06-thinking-blurbs-review.md) **Deck 2026-09-04:** 32 distinct phases sampled across a 212s reply came through as one ordered sequence, no interleaving. |
| Slow Ask phases report progress | THINKING-SLOW-01 | Open | **Automated 2026-08-08.** `building_context` and `connecting_model` had copy but no emitter; both now fire (`test_ollama_ask_service.py` — `connecting_model` on the first attempt, `model_retry` only after). Copy for these two pools is pinned as encouraging: `test_bonsai_stream_tags.py` asserts no discouraging word at any request id in either tone, and that at least one line says the wait is normal. **On-Deck:** Black Mesa `362890`, cold model — see the [test title pool](testing-manual.md). |
| Thinking line updates during generation | THINKING-LIVE-01 | Open | **Automated 2026-08-08, revised same day after a device pass.** First attempt relied on the model emitting later `<bonsai-status>` tags; on-Deck an Expert+screenshot Ask emitted none and the line sat on `Model's warming up…` for the whole run — every other publisher finishes before `ask_ollama` is called. Two model-independent mechanisms now cover it: a `generating` phase on the first content token (`test_ollama_ask_service.py`), and `escalate_static_thinking_line` rotating a line that has not changed in 7s (`test_bonsai_stream_tags.py`, plus an end-to-end walk of the merge path in `test_background_partial_state.py`). Model tags still win and reset the clock. Window lengths are randomised per Ask (7–13s for the first, 4–12s after) from a hash of `(request_id, window index)` — deterministic, because the poll loop re-derives it every ~1.2s and a live roll would strobe the line. **On-Deck:** the line must keep moving during a long generation, on a beat you cannot count along to. A line that predicts completion ("almost done") is a copy bug — asserted in unit tests, but watch for it. Detail: [06 § 10.4](planning/06-thinking-blurbs-review.md) **Deck 2026-09-04 PASS:** the line updated throughout a 212s reply; none of the 32 phases carried a bare emoji. |
| Model status line cannot leak spoilers | THINKING-SPOILER-01 | Open | **Automated 2026-08-08.** Strategy prompts teach `[[spoiler]]…[[/spoiler]]` as a fallback for when the model names something anyway (`test_ollama_prompts_stream_instruction.py`), and `redactThinkingBlurbSpoilers.test.ts` renders that span as blocks, drops markup when masking is off, masks to end of line on an unclosed span, and asserts no raw markup survives six malformed inputs. **On-Deck:** blocks, never plain words, never literal `[[spoiler]]`. Detail: [06 § 2.7](planning/06-thinking-blurbs-review.md) |
| Thinking blurb emoji upright (italic prose only) | THINKING-EMOJI-01 | Open | **Automated 2026-08-07 (Wave 1 A).** `splitThinkingBlurbItalicSegments.test.ts` (5 tests) — emoji graphemes upright, prose italic. **On-Deck:** during a live Ask, confirm emoji-only blurbs (🙄 / 🌳 / 🔥) and mixed emoji+prose lines are not slanted; prose stays italic. Detail: [wave1.md](wave1.md) |
| Voice engine ready / reinstall label | VOICE-REINSTALL-01 | **Verified (Deck) 2026-09-05** — a full Settings sweep found the button labelled **Reinstall voice engine** (not *Install*), reachable by D-pad and 100% visible, at 64x647, 146x38 (`runs/round34-freewalk-settings.json`, stop 7). The label reading *Reinstall* is the ready state, which is what this row asks. Not pressed, per the row: pressing re-downloads | **Automated 2026-08-07 (Wave 1 B).** `VoiceInputSettingsSection.test.tsx` (6 tests) — `binary_ready && model_ready` shows ready copy and **Reinstall voice engine**; install path when not ready. **On-Deck:** Settings → Voice input with a completed install — confirm label, ready message, D-pad to button, reinstall still runs. Detail: [wave1.md](wave1.md) |
| Bonsai icon trunk/pot alignment | BONSAI-ICON-GEOM-01 | Open | **Automated 2026-08-07 (Wave 1 D).** `icons.bonsaiGeometry.test.tsx` (2 tests) — shared path constant in tab + plugin-list icons. **On-Deck:** compare tab strip icon (36px) and plugin-list icon (26px) against pre-fix screenshot; watch for whole glyph half-unit left and canopy ink asymmetry. Preview cannot reproduce panel scaling. Detail: [wave1.md](wave1.md) **Deck 2026-09-04 PASS, measured from a capture rather than by eye:** tab strip canopy x 9-29 (mid 19.0), stem 19, pot rim 11-27 (mid 19.0), body 12-26 (mid 19.0); Decky list canopy 11-27 (mid 19.0), stem 19, rim 12-26 (mid 19.0), body 13-25 (mid 19.0). Entry moved to Done. |
| Preset chip expansion (Wave 1 — LAN / chord / streaming) | PRESET-EXPAND-W1-01 | **Verified (Deck) 2026-09-05** | **Unit 2026-08-07 (Wave 1 L).** `presets.test.ts` — four new `detectPromptCategory` cases. **Deck 2026-09-05 PASS**, closed alongside the wave 2 row in the same sitting: *How do I bind a BonsAI quick-launch chord?*, *What should I expect while answers stream in?* and *How do I use Find LAN on the Ollama tab?* all came round in the carousel, the two streaming prompts carried their `[beta]` styling, and pressing A on a chip filled the Ask field verbatim without sending. Copy avoids the `` `for …` `` strip trap in `detectPromptCategory`. Detail: [wave1.md](wave1.md) |
| Preset chip expansion (Wave 2 — thinking mode / kids lock / Caveman / tip sources / named chats / a game that is not running) | PRESET-EXPAND-W2-01 | **Verified (Deck) 2026-09-05** | **Unit 2026-09-05.** `presets.test.ts` — one case asserting all six new prompts land in the general category, worded to avoid the trailing `` `for …` `` clause that `detectPromptCategory` strips and then silently files under strategy. **Deck 2026-09-05 PASS, and read this before running it again — the row's first wording was wrong and cost two failed measurements.** The chip row **only rotates for sixty seconds after the panel opens**, then freezes on whatever it is showing until the panel is reopened (`PRESET_CAROUSEL_ACTIVE_MS`), and it keeps at most five chips in its history. A first watch caught the tail of that minute; a second sat entirely inside the frozen period and read the same five chips 113 times over four minutes, which looks exactly like a broken feature and is not one. **Watch from a fresh row** — switch to another tab and back, then start reading immediately. Doing that, ~10 distinct prompts came round in 76 seconds and *"Where do your game tips come from?"* appeared 22 times. Asking for three of six in one sitting is not achievable by design: the pool is 49 prompts and a sitting shows about ten. **One of the six is the bar.** Pressing A filled the question box with the exact wording and sent nothing (turn count unchanged). No **Tip** badge on any of the six, and no Ask-mode switch. |
| Type any model name into the pull picker | PULL-CUSTOM-01 | **Verified (Deck) 2026-09-05** | **Unit 2026-09-05.** `mergePullModelCatalog.test.ts` (7 cases) for the typed-name shape check; the typing itself cannot be simulated in this test setup, so the device is the only proof of that half. **Deck 2026-09-05 PASS.** The picker opens from Ollama → *Browse models* → *Browse & pull*. The field and its **Pull** button are both reachable by D-pad (Down into the row, Right onto the button) and 100% visible; the button is disabled with nothing typed and enables on input. Typed `smollm:135m` — a real library model deliberately **not** in the built-in catalogue — pressed Pull, and it showed installed within about ten seconds, listed under *Other installed*. Removed again afterwards, so the device is back to its original four models. **A bug was found and fixed here first:** the field was **50 pixels wide inside a 569-pixel row**, so a name just typed was unreadable — the row is a flex row and nothing told the field to stretch. It measures **530px** after the fix. |
| A made-up model name explains itself | PULL-CUSTOM-02 | **Verified (Deck) 2026-09-05** | **Unit 2026-09-05.** `test_ollama_pull_registry.py` — the advice leads, and spinner redraws collapse. **Deck 2026-09-05 PASS after a fix.** Correction to this row's first wording: **the picker closes when you press Pull, by design** — the pull is a background job accepted by the backend and watched on the Ollama tab, the same as a catalogue pull, so "the picker stays usable" was never the right expectation. What is right: a message appears that names the problem and can be acted on. **First run failed that.** It read *"ollama pull … failed with exit code 1. Last output: pulling manifest ⣋ pulling manifest ⠙ …"* and only ended with the useful sentence. Now it opens with **Tag «name» is not on the Ollama library — try qwen2.5vl:3b or gemma4:e2b-it-qat**, the spinner frames are gone and the four redraws collapse to one. Note the message builder predates this feature; what changed is that a name that does not exist went from nearly unreachable to one press away. |
| Star an installed model so Ask reaches for it first | PULL-PIN-01 | **Verified (Deck) 2026-09-05** | **Unit 2026-09-05.** `PullModelsModal.test.tsx` — the star's on/off state and what it saves. It reuses the existing settings save; no new backend call. **Deck 2026-09-05 PASS.** The star is reachable by D-pad and 100% visible on a row partway down the list. Pressing it filled the star and changed its own name from *Use gemma4:e2b-it-qat for Ask* to *gemma4:e2b-it-qat is used for Ask*, and **the settings file on disk gained `['gemma4:e2b-it-qat']` at the front of both the text and vision try orders** — vision too, correctly, because that model is a vision-capable catalogue entry. Restored to empty afterwards. **A bug was found and fixed here:** the embedding model the knowledge base installs carried the same star, so one press would have pointed Ask at something that cannot reply. That row now shows a dash and says why, and stays focusable so the D-pad path through it does not change. |
| New label on a freshly pulled model | PULL-NEW-BADGE-01 | **Verified (Deck) 2026-09-05, one half owed on permission** | **Unit 2026-09-05.** `PullModelsModal.test.tsx` — the thirty-day maths plus a regression test for the two-pass failure below. **This row caught a real bug on its first run.** All four already-installed models were labelled **New**, recorded with that day's date, and would have worn the label for a month. There was already a guard meant to prevent exactly this and it did not fire: the picker renders once **before the connection test answers**, so the first pass had an empty model list and persisted an empty record; the second pass, the one that actually had the models, read a record that existed, decided it was not a first run, and stamped every model as pulled today. Fixed — an empty record now counts as a first run, and nothing is saved before the model list arrives. **Both halves then passed in one sitting:** from a cleared record the four pre-existing models were recorded as already-old and showed **no** badge; pulling `smollm:135m` produced **exactly one** New badge, on that model, recorded at zero days. **Read this before re-running:** a model installed before the picker was ever opened is deliberately never labelled New, so its absence is correct. The thirty-day expiry cannot be seen in one sitting and is unit-tested only. **Still owed, and it needs the maintainer's word:** whether *Clear all plugin data* takes the labels with it. The key is `bonsai:`-prefixed so the existing wipe should sweep it, but that is reasoning rather than a measurement, and data wiping was not authorised in round 36. |
| One suggestion chip instead of two | PRESET-SLOTS-01 | **Verified (Deck) 2026-09-05** | **Unit 2026-09-05.** `presetRowLayout.test.ts`, `MainTabPresetAnimatedChips.test.tsx`, `presetRowNav.test.ts`, `section-4.test.ts`. **Deck 2026-09-05 PASS, measured not eyeballed.** Settings → *Suggestion chips* → **Show one chip instead of two**, a plain toggle reachable by D-pad and 100% visible. With it on the row shows **one chip 300px wide** — the whole column — where two chips are 148px each. The row stayed at y=608 h=30 and the question box at y=639, so **nothing shifted and the dock did not change height**. Left moved to the previous suggestion, Right moved back, and a second Right correctly did not move: that is the end of the list, where the out-of-suggestions glow fires, and with one chip the first and last chip are the same one. Evidence `runs/round36-CHECK-one-chip-on.json`, `runs/round36-CHECK-one-chip-left-right.json`. |
| Two chips stays the default | PRESET-SLOTS-02 | **Verified (Deck) 2026-09-05** | **Unit 2026-09-05.** A case in `tests/contracts/settings-hostile-inputs.json` proves a settings blob missing the key loads it as `false` with every other field untouched, asserted from both languages. **Deck 2026-09-05 PASS:** turning the switch off restored two chips at 148px each, and the settings file on the device gained `preset_single_chip` at `false` with **no other key changed and none removed**, compared against a copy taken before the session. Evidence `runs/round36-CHECK-two-chips-restored.json`. |
| A first question that does not wait for the model to load | PRELOAD-01 | **Partial (Deck) 2026-09-05** | **Unit 2026-09-05.** `tests/test_ollama_preload.py` (21 cases) and `tests/test_preload_ask_model_boot.py` (6), including one that times out if `_main` ever waits on the warm-up. **Deck 2026-09-05: this row caught a design bug on its first run.** The warm-up picked **the first small model installed** rather than the model Ask reaches for. On the device those are different: the only model under the three-billion cap is `qwen2.5:1.5b`, while Ask's own resolver puts `gemma4:e2b-it-qat` (4.6B) first. So the switch loaded a model no question would touch, spent the memory, and left the first question exactly as slow. The 137M embedding model was eligible too and can never answer anything. Fixed to call the same resolver Ask calls, and to warm **nothing** when Ask's model is over the cap — warming something else is worse than warming none. **Re-checked on the device: nothing loaded, which is correct here.** **Still owed:** the timing comparison itself, which needs a Deck whose Ask model is under the cap — pin one with the pull picker's star first. Developer switch, off by default; put back off after the run. |
| The warm-up skips quietly when memory is tight | PRELOAD-02 | Open | **Unit 2026-09-05.** The silent-failure paths are covered in `tests/test_ollama_preload.py`: an unreachable host, no eligible model, and the host declining are all no-ops that never raise. **On-Deck, not yet run:** with a game running and memory under pressure, turn the switch on and restart the loader. Nothing on screen may mention the warm-up — no error, no toast, no stuck status line — and the first question must be no slower than with the switch off. |
| An old settings file does not lose its settings | STREAM-UPGRADE-01 | **Verified (Deck) 2026-09-05** | **Unit 2026-09-05.** A case in `tests/contracts/settings-hostile-inputs.json` named for the deleted key, carrying three other real settings that must survive; written and watched passing in both languages *before* anything was removed. **This is the check that could have cost a person every setting they ever chose, so it ran first.** **Deck 2026-09-05 PASS.** A copy of the device's real settings file was taken, the old streaming switch put back into it to make it look like a file written by the previous build, then the new build deployed and the panel opened. Read back off disk: **47 settings before, 46 after; the only thing lost was the deleted key; nothing else changed at all.** Re-checked at the end of the session against the copy taken before any work started — two new switches added at their defaults, the streaming key gone, and zero other differences. |
| Replies stream without any setting turned on | STREAM-DEFAULT-01 | **Verified (Deck) 2026-09-05** | **Unit 2026-09-05.** Both shared agreement files lose the key, so an incomplete two-language edit fails a test rather than shipping. **Deck 2026-09-05 PASS, measured rather than watched.** On a settings file with no streaming key at all, a long question was asked and the answer's length sampled every 0.7s: it grew through **28, 127, 247, 258, 403, 474, 515, 627, 666 characters over eight seconds** — eleven separate steps, not one jump at the end. On the Developer screen (confirmed by three of its own controls being present) there are **zero** elements mentioning token streaming; the toggle is gone. **Owed, and it belongs to whoever next drives the device:** the *Stopped* notice was fixed earlier the same day while streaming still defaulted off, so that fix has never run with streaming on. An attempt in round 36 did not exercise it — the reply finished before the Stop press landed, and a completed turn correctly showed all five buttons and no notice. Retry with a question long enough to still be writing. |
| The question bubble lines up with the answer | CHAT-BUBBLE-MIRROR-01 | **Verified (Deck) 2026-09-05** | **Unit 2026-09-05.** The rule is asserted in the stylesheet test alongside the answer bubble's, so the two caps cannot drift apart again. **Deck 2026-09-05, measured before and after.** Before: the question bubble stopped **35 pixels** short of the left edge while the answer stopped **23** short of the right — both are capped as a share of the row and they disagreed, 88% against 92%. After: both are **267 pixels wide on a 290-pixel row, inset 23 on their own side**, exactly mirrored. **Some inset is kept on purpose** — with none, a question would be indistinguishable from an answer, since the bubble is pushed right so it reads as yours. If 23 still looks like too much, that is a one-number change and a person's call, not a measurement. |
| Clear all plugin data leaves nothing behind | CLEAR-ALL-PREFIX-01 | Open | **Unit 2026-09-05** (3 cases). Found when the maintainer asked for the wipe to be best-effort: three knowledge-base "already warned you" flags are spelled with an underscore rather than a colon, and the wipe matched only the colon, so they survived it. The visible cost was a fresh start that was not fresh — the plugin still believed it had warned you and stayed quiet. The tests now name those three keys and the pull picker's New labels, and assert all four go. **On-Deck, not run and deliberately so:** proving it means actually wiping the maintainer's chats and settings, which was not asked for. If it is ever run: note what is on the device first, wipe, then confirm a knowledge-base warning appears again rather than staying silent. |
| Preset chip inject (no game append) | PRESET-GAME-01 | Open | Unit pass; on-Deck chip → Ask text. **Noted 2026-08-08: fixed at source, so no preview scenario is planned.** `joinPresetWithRunningGame` (`src/utils/joinPresetWithRunningGame.ts`) is an identity function with its own unit test — there is no append path left to regress. The on-Deck check stands as a wiring check, not a logic one |
| KB-advice preset seed hidden when KB on | PRESET-KB-SEED-01 | **Verified (Deck) 2026-09-03** | On-Deck PASS 2026-09-03: KB on, no frozen batch, carousel mode, Main watched for 65 s straight after the chips mounted — nine distinct chips came round and the seed never appeared (plan 31 § 11). **Unit 2026-08-07 (Wave 2 F).** `presets.test.ts` — KB-advice static seed excluded when `use_local_knowledge_base` is on across `getRandomPresets`, `getContextualPresets`, and `getRandomPresetExcluding` (timer re-samples in `MainTabPresetAnimatedChips`). **On-Deck:** with KB enabled, open Main tab and watch carousel for ~60s — chip *Enable local knowledge base for better game tips* must not appear. With KB off, seed may still appear (sampled). |
| Session RAG preset chips | SESSION-RAG-CHIPS-01 | Verified | **End-to-end on-device 2026-08-03** — maintainer saw the `Glyphid Dreadnought` chip in the carousel with DRG Survivor running and the QA override **off**, after the `settingsLoaded` race fix. That is the first confirmed UI sighting; every earlier pass verified only the backend or the override path.<br><br>**Backend verified on-device 2026-08-03**: with the real settings and corpus `2026.07.30`, `get_session_rag_chip_candidates("2321470", …)` returns `ok: true` and 29 candidates, the first two being `How do I beat Glyphid Dreadnought?` and `Tips for Hollow Bough in this game?` — drawn from the corpus's DRG Survivor sections (`boss` / `area`). KB on, corpus at `/home/deck/.bonsai/rag`.<br><br>**How to tell a RAG chip from a static seed** — this is the trap that cost a QA cycle. Generic wording like *"Help me beat this boss or encounter"* is a **hardcoded static seed** ([presets.ts:71](../src/data/presets.ts)), not a RAG chip. A real RAG chip **names the entity**: look for the literal strings **`Glyphid Dreadnought`** or **`Hollow Bough`** for DRG Survivor.<br><br>**Procedure (deterministic since 2026-08-03):** KB on, corpus installed, launch DRG Survivor (appId `2321470`), then turn on **Developer tab → Force session RAG chips (QA)**. That sets the mix probability to 1.0 *and* forces an immediate reseed, so all three carousel slots fill from the knowledge base straight away. Since the 2026-08-03 candidate-policy fix the pool for DRG Survivor is exactly four entries, so with three slots you should see `How do I beat Glyphid Dreadnought?`, `Tips for Hollow Bough in this game?`, and `Any known Proton issues for this game?` — in that order, because the composer walks the pool in order and entity-named candidates come first. **A game not in the corpus now returns `ok: false` and shows static seeds**, which is correct behavior, not a failure. Then: a strategy chip must open in **Strategy** mode; turn the override off and confirm the carousel returns to the normal mix; turn KB **off** and confirm static seeds with no console error; uninstall the corpus and confirm the same (reason `corpus_missing`, no rejection).<br><br>**Check the override-off path explicitly — the override is not a proxy for it.** Until 2026-08-03 the mount reseed raced `load_settings` and read `useLocalKnowledgeBase` as `false`, so the *default* path never called the RPC while the override path worked, because the override reseeds when its own value flips during hydration. The override therefore reported a healthy feature that a user could not see. With the override **off**, each slot is a 30% roll (nothing about 34% of the time, `0.7³`) and every reopen re-rolls, so a handful of opens should surface an entity-named chip; **no chip across ~10 opens with the KB on is a regression, not bad luck** |
| KB coverage chip (Show details) | KB-COVERAGE-01 | Open | **Automated 2026-08-07 (Wave 3 I).** `summarize_kb_coverage` + `kb_coverage_chip_label` unit tests in `test_knowledge_base_service.py` and `test_transparency_kb_retrieval.py`; Ask path stamps `kb_coverage_status` / section count on every Ollama turn. Distinct `kb_coverage` chip (corpus honesty) alongside existing `kb` chip (Ask-turn retrieval). **Partly unblocked 2026-08-16 — the CONTEXT-LADDER-01 blocker did not materialise.** On-Deck (build `6329577`, corpus `2026.08.16`) a live-turn Show details rendered the chip ladder cleanly and the coverage chip read **`KB: 9 sections`** on a Portal 2 (`620`) Strategy Ask, alongside `Keyword + meaning`, `Reply style: balanced`, `Spoiler risk: med` and `Routed gemma4:e2b-it-qat` — 6 chips, no wrap fault. Screenshot `DeckCapture_20260816_233808_game`. **Still open:** the two negative cases — KB off → `KB: off`, and an uncovered title → `KB: none for this game`. Retest them before flipping this row |
| KB coverage chip says "no game running", not "could not be matched" | KB-COVERAGE-NOAPP-01 | **Partial (Deck)** | First half re-confirmed 2026-09-03 on 3b0e9d7 (`runs/SMOKE-A-05-show-details.json`). **First half confirmed on-device 2026-08-27** (Batch B § 4.4). With every game closed, *Show details* read **`KB: no game running`** and the coverage body began **"No game is running, so there …"** — not a failed-match string. **Second half still owed and needs the maintainer:** launch a game with no corpus coverage and ask the same question; it must read **none for this game**, and the two must look different. Not runnable by the rig — the plan forbids launching a game by automation. | **Automated 2026-08-23.** `summarize_kb_coverage` returned `app_unresolved` for desktop context (nothing running) and for a running-but-uncovered game alike, so the Show details bullet *"Running game could not be matched to corpus entries."* was false with nothing running — see the fixed-bugs writeup. Now a distinct `no_app` status reads `KB: no game running` / *"No game is running, so there is nothing to look up."* Unit: `test_knowledge_base_service.py` (`summarize_kb_coverage` with no AppID and no game name), `test_transparency_kb_retrieval.py` (chip label + bullet wording). **On-Deck:** with KB on and a corpus installed, close every game (Steam desktop, nothing running), open the QAM, ask *"what should I do next"*, then open Show details — the knowledge base coverage chip must read **KB: no game running**, and its bullet must **not** say anything about a running game or a match failure. Then launch a title the corpus does not cover and ask the same question — the chip should read **KB: none for this game** instead, confirming the two cases now read differently. |
| DRG Survivor glossary terms (tap-to-define jargon) | DRG-GLOSSARY-01 | **Verified (Deck) except two touch/B steps — see DRG-GLOSSARY-02 for the walk record** | **Re-run on device 2026-08-28 18:30 with DRG Survivor genuinely running (`ollamaContext` read `{app_id: "2321470", app_context: "active"}`, panel showing *Context: active game AppID 2321470*). Question `what does kiting mean here` sent verbatim with `deck_send_ask.py`, Ask pressed with the bridge, mode Strategy. 2 glossary chips at 140 characters mid-stream — and still 2, both labelled *kiting*, once the reply finished and was written to disk. That last clause is the whole test: the assistant turn only reaches disk at completion, which is what triggers the rebuild that used to blank the AppID. The saved chat shows both states side by side, which is as clean as this evidence gets: the same question asked this morning is stored `app_id=''` and the one asked tonight is stored `app_id='2321470'`, in a slot whose `origin_app_id` is `''` — so the chips cannot be coming from the slot fallback, only from the turn's own AppID. **The interaction ladder was walked the same evening** — record and evidence files under **DRG-GLOSSARY-02** below; only the touch tap (needs a finger) and the Explain-further auto-send (costs a generation) remain unwatched on device. Earlier the same day: failed, and the cause was measured, not guessed —** With DRG Survivor the active game (`Context: active game AppID 2321470`, and the turn's own transparency snapshot reading `app_id: "2321470"`, `app_name: "Deep Rock Galactic: Survivor"`), two Strategy replies containing **kiting** and **overclock** finished with **zero** glossary chips in the page — nothing matching `[class*="glossary"]` at all, so none of the D-pad steps this row asks for (peek → A → full → dismiss → explain further) can even be started. **The chips are not broken — they are lost when the turn settles.** Polling during the stream caught **2 glossary chips present** while the reply was 30 characters long; by the time it finished they were gone. Reading the React props off the live page: the settled reply's `MainTabBonsaiAiMarkdownChunk` receives **`appId: ""`**, while `ollamaContext` on the same page still reads `{"app_id":"2321470","app_context":"active"}`. `isDrgSurvivorAppId("")` is false, so the markup is dropped. **Where:** [chatSlotTurns.ts:29](../src/utils/chatSlotTurns.ts) hard-codes `appId: ""` on every turn rebuilt from a saved chat slot, and the live branch is the only one that passes the real `ollamaContext?.app_id` ([MainTabChatTranscript.tsx:633](../src/components/MainTabChatTranscript.tsx)). Anything else keyed on the turn's AppID rides on the same value — the asked-entity spoiler unwrap is noted at `:286` — so this is worth fixing at the mapper rather than per feature. **Fixed 2026-08-28 (desk):** the AppID is now stored on each saved turn (`chat_slot_service.py` `_normalize_turn`/`append_turn`, written by both `main.py` recorders), and the mapper reads the turn's own AppID, then the question's, then the chat's `origin_app_id` for turns saved before the field existed. Covered by `chatSlotTurns.test.ts` (5 new cases), `test_chat_slot_service.py` (3) and `test_chat_slot_ownership.py` (1, end to end through the RPC). Deploying it was briefly blocked on plugin-directory ownership (P1-11), cleared the same evening when the maintainer granted `deck` passwordless sudo. Earlier: **Shipped 2026-08-28 (desk only — no device this session).** Curated terms **kiting** and **overclock** (both read undefined in the shipped DRG Survivor cards, `data/kb/strategy_seed.json:50,164,166,168`) render as a tappable chip inside reply prose when the turn's game is DRG Survivor (`2321470`) only — a Dota 2 turn with the same word stays plain text. Unit-tested: term detection/word-boundary matching (`drgGlossaryTermMatch.test.ts`), markup present/absent by AppID (`MainTabBonsaiAiMarkdownChunk.test.tsx`), peek-on-focus vs full-on-activate state and the explain-further callback/composed Ask text (`DrgGlossaryTermChip.test.tsx`, `drgGlossaryAsk.test.ts`), and the D-pad Down diversion that parks the ring on an unvisited chip (`answerBubbleNavigation.test.ts`, mirrors the masked-spoiler diversion). Python: `_drg_survivor_glossary_clause` gates on AppID/name (`test_ollama_prompts_drg_glossary.py`). **The tooltip's floating placement above the term (not inline-push) cannot be asserted from jsdom — no layout engine, same limit design-language.md § Rule 6 already documents.** **On-Deck walk owed, all steps:** D-pad to a reply containing "kiting" on DRG Survivor → ring lands on the word alone shows a short peek tooltip, no press needed → **A** opens the full definition with an **Explain further** chip → **B** or any D-pad direction dismisses it (a direction should also continue the walk, not just close the popup) → **Explain further** auto-sends a new Ask turn (no manual Send) whose question names the term. |
| DRG glossary chip: underline, tap popup, consistent D-pad | DRG-GLOSSARY-02 | **Verified (Deck) except touch tap** | **Three maintainer asks, shipped and walked on device 2026-08-28 evening.** **(1) Underline stopped before the g** (maintainer screenshot `screenshots/DeckCapture_20260828_183251_game.png`): cause was Chrome's default `text-decoration-skip-ink: auto` skipping the descender; the term span now sets `text-decoration-skip-ink: none`, and `getComputedStyle` on the live chip read `skipInk: "none"`, `underline dotted` after deploy. Unit: `DrgGlossaryTermChip.test.tsx`. **(2) Tap shows a temporary plain-language popup.** A tap now opens the short `peek` sentence with a 4s auto-dismiss (`TAP_PEEK_DISMISS_MS`); a second tap escalates to the full definition (10s); a third dismisses — deliberately *not* explain-further, so an accidental triple-tap cannot send an Ask. The gamepad path keeps no timers (focus cancels a pending one), and the tap decision is snapshotted at pointerdown because on device the tap itself moves gamepad focus, whose onFocus fires peek before onClick. Unit-tested including both timers and the pointerdown/focus/click ordering (`DrgGlossaryTermChip.test.tsx`, 6 new cases). **Touch not verifiable from the rig** (CDP reads, it must not drive — and a synthetic tap proves nothing about a finger); one manual tap on device is the remaining step. **(3) Consistently D-pad focusable, with automated proof.** The visited-once flag in `drgGlossaryTermRegistry.ts` made the chip reachable exactly once per mount and never via Up; replaced with reading-order geometry — Down offers chips after the ring, Up offers chips before it, a chip the ring sits on is never offered (no trap, no flag). `handleAnswerBubbleMoveUp` gained the Up diversion; exit asymmetries preserved (Up from the bubble still leaves in one press). Unit: `answerBubbleNavigation.test.ts` (4 new cases incl. the second-pass walk the old flag failed). **Automated on device with `deck_runSequence`, evidence committed:** `runs/DRG-GLOSSARY-02-dpad-chip-ladder.json` (bubble→chip diversion, chip→Show details no-trap) and `runs/DRG-GLOSSARY-02-dpad-consistency.json` (4/4: the ring landed on the chip on a second and third pass — impossible before this change). **A** on the chip opened the full definition with Explain further present (page read), and the peek showed on plain focus (visible in the chip's focus label). **Repeat recipe:** `deck_walkTo` DOWN into the reply, then a runSequence step with `expect: ".bonsai-drg-glossary-term"` — the expect selector is the assertion. **Two findings from this walk, both fixed the same evening:** one-press Up from below (DRG-GLOSSARY-03) and the B back-out (DRG-GLOSSARY-04). |
| Glossary chip: explain-further on device + one-press Up from below | DRG-GLOSSARY-03 | **Verified (Deck)** | **Both remaining D-pad halves closed on device 2026-08-28 evening.** **Explain further, full path on hardware:** ring on the chip → A (full definition, page read confirmed *Explain further* present) → A again → a new Ask auto-sent with no manual Send, question `Explain "kiting" further for Deep Rock Galactic: Survivor…`, turn header `Explain: kiting`. The reply finished, showed **2 glossary chips after settling** (third consecutive turn holding the per-turn game-ID fix), and both new turns saved with `app_id='2321470'`. **One-press Up from below, the leg -02 left open:** the reply-actions row's Up handlers now fall back to the glossary chip after every existing fallback declines, crossing into the bubble's navigation container with `TakeFocus` (per-bubble nav handle in `answerBubbleElRegistry`; ring-outside-bubble geometry in `drgGlossaryTermRegistry`). Proof `runs/DRG-GLOSSARY-03-one-press-up.json`: one Up from *Show details* landed on the **nearest** chip above (1/1). The walk down the new reply also parked on **both** chips (“kiting” then “Kiting”) in order — the multi-chip case, unstaged. Unit: registry outside-ring case in `answerBubbleNavigation.test.ts`. **Still with the maintainer:** one real finger-tap (touch), and the B-backs-out-of-pane wart stays filed on the roadmap. |
| Glossary popup: unclipped, readable, B behaves | DRG-GLOSSARY-04 | **Verified (Deck)** | **Two fixes measured before and after on device 2026-08-28 late evening.** **(1) The popup was a 33px-wide, 418px-tall sliver, almost all clipped invisible** — absolute positioning inside the inline chip shrank its width to the word itself, and every ancestor from the response stack up clips overflow; the only painted fragment sat over the reply text (the occlusion the maintainer reported). `position: fixed` alone cannot escape (the bubble's backdrop-filter is a containing block for fixed descendants), so the popup now renders through a portal into the document body. After: **240×33 px, 6 px above the chip, inside the plugin column, and elementFromPoint hits the tooltip at top, middle and bottom** — nothing occludes it. Side effect kept: the chip's accessible label is exactly the term again ('kiting'), so exact-match walks work. **(2) B on an open popup no longer throws the ring out of the reply.** Instrumented: `onButtonDown` receives B (button 2) but returning true does not consume it; `onCancelButton` + `preventDefault` does — and its mere presence suppresses Steam's back-out even doing nothing, so it attaches only while the popup is open. Verified press by press on the shipped build: B → popup closes, ring stays on 'kiting'; B again → backs out of the reply as Steam intends. **The spoiler fence shared the broken pattern — closed the same night as SPOILER-FENCE-B-01.** |
| Spoiler fence: gamepad toggle keeps the ring, B re-hides | SPOILER-FENCE-B-01 | **Verified (Deck)** | **Walked by script on device 2026-08-28 late evening, nobody at the Deck.** The DRG-GLOSSARY-04 follow-up, plus a defect the walk itself surfaced: the fence had no B handling (B on an expanded spoiler backed the ring out of the whole pane), and the first run showed **A-reveal dropped the ring to "nothing"** — the masked Focusable unmounts on reveal and no element claims focus (`runs/SPOILER-B-01-reveal-collapse.json`; the rig then refused to press B from an unowned ring, which is itself the finding). Fix in `BonsaiSpoilerFence` ([MainTabBonsaiAiMarkdownChunk.tsx](../src/components/MainTabBonsaiAiMarkdownChunk.tsx)): the collapse header carries `onCancelButton` + `preventDefault` (the only handler Steam honors for B, per the chip's instrumentation — and conditionality is free here, the header only exists while expanded), and every gamepad toggle hands the ring to the counterpart element with `focusSpoilerFence` after React commits; touch taps deliberately do not move the ring. **Proof `runs/SPOILER-B-02-full-ladder.json`, 5/5 with expect selectors:** Down parks on the masked fence → A reveals and the ring lands on *Spoiler — tap to hide* → B re-hides and the ring returns to *Spoiler — tap to show* → second B backs out to Ask as Steam intends. The reveal/collapse round trip registers as a cycle in the evidence file — that cycle is the feature. Fence used: the saved "how do i beat the twins" reply, so the walk cost no generation. |
| Reopened chat header shows the friendly caption | CHAT-HEADER-CAPTION-01 | Open | **Shipped 2026-08-28 (desk).** A branch pick's "I'm at: …" caption is now saved per turn (`display_text`) and the rebuilt header prefers it; spoiler unwrap/copy/follow-up keep the composed prompt. Six new tests incl. one end-to-end through the RPC. Chats saved before the change still show the composed prompt — the caption was never recorded for them, nothing to restore. **On-Deck walk owed:** make a branch pick (costs a generation), reopen the panel, read that turn's header — expect "I'm at: …", not "[Strategy follow-up] …". |
| Down from the chat slot enters the reply at its first section, not the bare bubble | CHAT-REPLY-ENTRY-01 | **Verified (Deck) 2026-09-05: both halves** | **Deck 2026-09-05, fourth build of plan 32 (code `517804a`), Up half PASS:** on a fresh `bonsai:vac-check` reply (four sections, no branch picker, thumbs row present), Up from **Helpful** landed on the last `.bonsai-answer-stop` — *This command skips the AI and queries Valve GetPlayerBans…*, rect 63,297 245x50, visible — never the bare bubble (`runs/CHAT-REPLY-ENTRY-01-build4-up-from-helpful-passes.json`). With the Down half already passed on 2026-09-04, the row is done. One thing worth knowing for future runs: a **restored** turn carries neither the thumbs row nor the deny surface, so this half needs a live reply. **Deck 2026-09-04, build 49241e7 (plan 32):** on a Strategy reply with a masked spoiler and a branch picker, Down from the bar landed on the slot row, Down on the turn header, and Down on the masked fence inside the answer, never on the bare bubble (`runs/CHAT-REPLY-ENTRY-01-down-from-slot-row.json`, every stop visible). The Up half could not be judged on this reply: Up from Helpful went to the branch picker, and Up from the picker skipped the answer and the header to the slot row, which is the separate bug the other session filed the same day (Up skips the answer sections). Re-run Up on a reply without a picker. **Fixed at the desk 2026-09-04, Deck check owed.** Filed 2026-09-02: Down from the slot row used to land on the whole answer bubble as one big stop, and only a second Down walked into its first section — one wasted press per reply, worse on a long answer. `focusFirstAnswerChunk` now calls `takeAnswerBubbleNavFocus` (the same cross-container transfer the reply row's glossary-chip hop already used) before landing on the first `.bonsai-answer-stop` via the registered-stop handles in `answerStopRegistry.ts`; a masked spoiler still wins when one is present. Up from the reply buttons got the matching fix in reverse (`focusLastAnswerChunk`), for the same double landing on the way in from below (already partly documented on `upIntoGlossaryChip`'s DRG-glossary-specific case). Unit: `answerBubbleNavigation.test.ts`, `buildAnswerBubbleElement.test.tsx`, `buildReplyActionsElement.test.tsx`. **On-Deck steps:** from the slot row press Down: turn header; Down: ring on the first `.bonsai-answer-stop`, never the bare `.bonsai-chat-ai-bubble`; Down: second section; from **Helpful** press Up: the last section, not the bare bubble. **Deck 2026-09-04, build f9a4c17 — the Up half re-measured cleanly and FAILED:** a simpler reply (one turn, 4 answer stops, no branch picker, no refinement chips, thumbs row present) removed the branch-picker ambiguity above. Ring on **Helpful** (`button.bonsai-chat-secondary-btn`, rect 52,388 89×32), press Up → ring landed on `div.bonsai-chat-ai-bubble` (rect 52,188 267×153), never the last `.bonsai-answer-stop` (`runs/CHAT-REPLY-ENTRY-01-up-from-helpful-lands-on-bubble.json`); the press was routed and Steam's own geometry move picked the bubble, so nothing of ours had claimed it. Cause: the thumbs row's (and the reply-actions container's own) `onMoveUp` was a bare `() => false` — the desk fix above only reached `upFromRetry`/`upFromShowDetails`, the **utility** row, which is what a turn with **no thumbs row** falls back to; every ordinary reply has a thumbs row, so the fix never ran for the common case. **Redone at the desk 2026-09-04, Deck check owed again:** the thumbs row's `onMoveUp` (`moveUpFromReply` in `buildReplyActionsElement.tsx`) now gets the same last-resort chain the utility row has, minus the hop to the utility row itself: a caller-supplied `onMoveUpFromReply` (never actually supplied anywhere, kept as a hook), then the DRG glossary chip, then `focusLastAnswerChunk`. Unit: three new cases in `buildReplyActionsElement.test.tsx` (lands on the last stop with no glossary chip in view; falls back honestly when nothing is registered; a glossary chip still wins) — verified failing first by temporarily reverting the fix. The `focusLastAnswerChunk` comment in `answerBubbleNavigation.ts` was also corrected: it used to say the thumbs row claimed the press first, which it never did. |
| Force session RAG chips toggle (QA control) | FOCUS-GRAPH-DEV-RAG-01 | Open | 2026-08-03: new `ToggleField` in Developer tab → *Diagnostics*, immediately after **Token streaming (experimental)**. Plain Decky `ToggleField` inside an existing `PanelSection`, so it inherits that section's vertical D-pad chain and needs no custom focus owner — but confirm on-Deck that D-pad Down from Token streaming reaches it and continues to the next control, per the new-focusable-control rule in [.cursor/rules/decky-focus-graph.mdc](../.cursor/rules/decky-focus-graph.mdc). **Verified on-Deck 2026-08-26 by script, nobody at the device — PASS.** Decky Plugin Studio `deck_runSequence` over the ESP32 bridge, focus read from `gpfocus`: Down from **Token streaming (experimental)** → **Force session RAG chips (QA)** → **Clear frozen test chips**. 2/2 asserted steps, both required labels reached (`runs/FOCUS-GRAPH-DEV-RAG-01.json`). The toggle does inherit the section's vertical chain as predicted. |
| Strategy Ask placeholder (focus caret) | STRATEGY-PLACEHOLDER-01 | Open | Unit/CSS fix; on-Deck empty Strategy field |
| Ask-bar caret with AI character on | ASK-CARET-CHAR-01 | Open | Layout fix 2026-08-07 (Wave 2 E): avatar is a flex sibling outside the text box; no Ask-bar component test harness — on-Deck with AI character on, focus empty Ask field, confirm native caret aligns with placeholder/text start (not left of `?` badge); D-pad Up from paperclip → avatar, Right → field; character-off path unchanged |
| Ask-bar avatar uses prop-emblem art | AVATAR-PROP-01 | Open | **Automated 2026-08-26.** `CharacterRoleplayEmoticon.test.tsx` (9) — the picker's default art is unchanged, the Ask bar's `art="prop"` path draws the emblem, the rendered box is byte-identical between the two styles at the same size, the corner badge markup matches exactly, and every instance gets its own gradient id with no colons (a shared id makes one disc paint with another character's tint). **The swap was designed to change no geometry**: a true-size mock measured both variants at a 50.0 × 276.8px text row, an 18 × 18 slot and a 119.2px card. **On-Deck:** with AI character on, open the QAM and confirm the Ask-bar corner shows the character's object — Heavy a sandvich, Medic a red cross, Nick Valentine a fedora — and **that nothing else on the row moved**: placeholder still starts in the same place, row the same height, badge letter the same size in the same corner. Then switch characters in the picker and confirm the corner follows, and select **Random** and confirm a crate with a `?` badge rather than the old bare `?`. **The picker itself is deliberately still on the old pixel grids** pending [D33](audit/maintainer-decisions-locked.md) — a picker still showing hooded blobs is correct, not a regression. Detail: [25-ai-character-avatars-handoff.md](planning/25-ai-character-avatars-handoff.md) |
| onButtonDown audit (whitelist + direction predicates) | ONBUTTONDOWN-AUDIT-01 | **Verified (Deck) 2026-09-04 for the three Ollama sliders; UI-scale bridge not driven** | **Deck 2026-09-04, build 49241e7 (plan 32):** Left on the Reply style slider stepped Balanced to Caveman and the ring stayed on the slider; Right stepped back and stayed (`runs/P32-01-back-to-bar-and-ollama.json` walk, then two assertFocusMove reads). Left on the keep-alive slider stepped 240m to 120m and stayed, Right restored (`runs/ONBUTTONDOWN-AUDIT-01-e-keepalive-left-claims-press.json`). Left on the custom-timeout thumb held the ring (value already at its floor), Right moved to the second thumb (`runs/ONBUTTONDOWN-AUDIT-01-f-timeout-sliders-left-claims-press.json`). The UI-scale bridge was not a stop with *Adjust UI automatically* on (Down from the toggle lands on Apply UI scale, `runs/ONBUTTONDOWN-AUDIT-01-g-ui-scale-left-claims-press.json`) and toggling auto off re-scales the panel, so that fourth slider stays unit-tested only. All settings restored on disk. On-Deck 2026-09-03 (plan 31, `runs/ONBUTTONDOWN-AUDIT-01-and-THINK-EFFORT-05-b.json` and `runs/ONBUTTONDOWN-AUDIT-01-c-right-steps-and-timeout-sliders.json`): Left on the Reply style slider stepped Balanced → Caveman AND moved the ring out of the plugin onto Steam's Quick Access tab rail; Left on the keep-alive slider stepped 240m → 120m and escaped the same way; Left on the custom-timeout slider escaped without changing 60s/180s. Right steps once and the ring stays (Caveman → Balanced, 120m → 240m). So the press is handled but not consumed: one step, then Steam's own navigation goes left out of the panel. Of the two outcomes this row was written to separate, it is the focus-escapes one, which points at the handler needing to swallow the press. Settings were restored afterwards. **Automated 2026-08-07 (Wave 4 G).** `focusNavigation.test.ts` (13), `buildAnswerBubbleElement.test.tsx` (8). **On-Deck:** D-pad past collapsed context hint without expanding; session context rows only activate on A; Show-details highlight link only on A; Down from turn header enters answer; slider left/right steps profile on UI-scale bridge — **and on keep-alive, reply verbosity and connection timeout, which share `buildDeckThumbNavHandlers`**. This row decided whether dropping the `onMove*` twins was safe: record *nothing happens* vs *two steps / focus escapes*, because the two point at opposite fixes. Detail: [wave4.md](wave4.md), roadmap NOTE 2026-08-07. **Fixed at the desk 2026-09-04:** `buildDeckThumbNavHandlers` (`DeckFocusSlider.tsx`) now steps the value from `onMoveLeft`/`onMoveRight` themselves and returns `true`, which Steam treats as a claimed move instead of continuing its own navigation off the slider; the direction branch is gone from `onButtonDown`, so one press cannot step twice. Covers the three `DeckFocusSlider` consumers named above (Reply style, keep-alive, connection timeout). The UI-scale manual-profile bridge (`SettingsTabUiScaleSection.tsx`) had the identical shape in its own `bridgeSliderNav` and is fixed the same way, pulled out into an exported `buildUiScaleBridgeNav` so it can be unit-tested the same way `buildDeckThumbNavHandlers` is. New tests: `src/components/deck/DeckFocusSlider.test.ts`, `src/components/SettingsTabUiScaleSection.test.ts`. Owed on-Deck: press Left on all four sliders (Reply style, keep-alive, connection timeout, UI scale) and confirm the ring stays on each. |
| Global document realm sweep (8 sites) | DOC-SWEEP-01 | Open | **Automated 2026-08-07 (Wave 4 H).** `focusNavigation.test.ts` (`getFocusableWithin`), `uiDocument.test.ts`, `BonsaiPluginShell.test.tsx` (3). **On-Deck per site:** Ask blur before submit; attachment preview L/R; preset carousel pauses while chip focused; About GitHub Up → language dropdown; settings Up at scroll top → tab strip; transcript expand scroll-into-view. **Run the blur and attachment checks on the first Ask after a fresh open** — the document was learned only from answer-time registries until the shell seeding landed the same day, so these paths behaved differently before and after the first reply. Detail: [wave4.md](wave4.md) |
| Decode preset chip animation | PRESET-STREAM-ANIM-01 | **Partial (Deck)** | **Measured on device 2026-08-28 with `preset_chip_animation: decode`** (set from the host and restored to `carousel` afterwards). **The frame rate holds:** sampling the chip labels from the page's own `requestAnimationFrame` for 8 seconds while all three chips were decoding gave **479 frames in 8002 ms — a flat 60 fps**, worst frame gap **50 ms** (one dropped frame at most, nothing that reads as a stall). A shorter 2.5 s sample caught one chip mid-burst: label updates every **33–50 ms**, which is `PRESET_DECODE_CHAR_MS` (42 ms) quantised to 60 Hz frames. **All three chips do animate** (56 / 48 / 52 label updates each over the 8 s), but **never in the same frame** — the shared loop staggers them, so "three chips churning" is three chips mid-decode at once rather than three characters locking together. Worth knowing before judging the look. **Focus during churn is clean:** walking onto a decoding chip put Steam's ring on it, and exactly one chip carried the highlight — the fake-ring fix holds in decode mode (`FOCUS-CHIP-RING-01`). **Still owed, and it is the half a rig cannot do:** whether the reveal *feels* right — a screenshot cannot show motion and a frame counter cannot show taste. Earlier: **Automated 2026-08-28 (Ghost in the Shell chip decode — replaces the `stream` typewriter, same row id).** `MainTabPresetAnimatedChips.test.tsx` (`composeDecodeText` shape: fixed width, locked prefix, caret at the boundary, no caret once resolved; full prompt submitted even before any reveal has painted; reduced-motion instant swap with no caret across all three slots), `test_settings_service.py` + the shared `settings-hostile-inputs.json` contract (legacy `stream` value migrates to `decode` in both languages, never falls through to `fade`). Animation timing itself is not meaningfully unit-testable. **On-Deck:** Developer → Preset suggestions → **decode**; both chips (two across since 2026-09-01, D43) arrive as full-width scrambled green blocks (no reflow), lock left to right behind a blinking caret, stay D-pad focusable mid-churn, then start scrolling if the label is longer than the chip, and rotate after hold; with OS reduced-motion → instant swap, no caret, no scroll; watch frame-rate feel with both chips churning and both labels scrolling at once. Detail: [wave4.md](wave4.md) |
| Preset row: two chips across, labels scroll | PRESET-ONE-LINE-01b, 02, 03, 04 | **Partial (Deck) — 01b, 02 and 03 passed on device; 04 owed** | **Up step, Deck 2026-09-04, build f9a4c17 (plan 32): PASS.** With a reply on screen, Up from a chip landed on the session strip in one press (`runs/PRESET-ONE-LINE-03-up-leaves-at-once-with-reply-b.json`); on the empty [+] chat, Up from a chip landed on the chat slot row in one press (`runs/PRESET-ONE-LINE-03-up-leaves-at-once-empty-chat.json`), where it used to step back one chip per press. Every stop visible. 01b PASS 2026-09-03 (plan 31): all four animation modes move, see testing-manual. **On device 2026-09-01 (bridge-driven, geometry from the live page):** row **02 PASS** — two 148 × 30 px chips, 4 px gap, 8 px padding, 130 px of label room (~20 characters), Steam's Marquee animating only the visible overflowing label, Test badge pinned, dock 157 px. Row **03 PASS in carousel mode** after a fix the same night: the first run showed Steam navigating the row as a column (Left left the plugin for the Quick Access rail), so every chip got explicit Left/Right/Up/Down handlers; the re-runs are `runs/PRESET-ONE-LINE-03-carousel-dpad-fixed-2.json` (11/11) and `…-fresh-mount.json` (13/14). Two follow-up fixes (entry lands on the marked chip; Down uses Steam's transfer into the text field via a new `unified-input` nav node) were verified on a fresh panel 2026-09-02 (`runs/PRESET-ONE-LINE-03-carousel-fresh-mount-2.json`, 14/14). **A third Up fix landed at the desk 2026-09-04 (D58 #2):** Up used to leave the press unclaimed whenever no session context strip was registered (an empty chat), so Steam's own fallback walked back through chip history one chip per press before a fifth press finally reached the chat slot row (`runs/PRESET-ROW-up-from-chips-probe.json`); Up now tries the strip first, same as before, and falls back to the always-mounted chat slot row directly, in every animation mode — one press leaves the row either way, Deck check owed. Rows 01b and 04 were not run that night (they need the animation-mode setting changed on the device); 01b passed on 2026-09-03. **Rebuilt 2026-09-01** after the maintainer filed the one-chip row as the wrong shape (`fc1b245`, row 02 FAIL 2026-08-31). The row is now **two chips side by side** on one 30px row — the drawing has three, the maintainer chose two after the width research (**D43**: three left ~12 characters per chip, two leave ~20). A label longer than its chip scrolls through Steam's own `Marquee` (exposed by `@decky/ui`, first use in this plugin), slow and calm, **Tip**/**Test** badges pinned; reduced motion or a missing component falls back to an ellipsis that now actually fires. The help chip owns the whole row until dismissed. Carousel mode is a sideways two-wide window on its five-chip history; fade timings went back to 2s out / 1s in; the corpus Tip guarantee now converts the second seed (the last one on screen) rather than the hidden third. Every mode shares one horizontal focus container that registers the Ask bar's nav handover — before, only carousel did. **Desk-verified:** `npx tsc --noEmit`, `npm run build`, `npm test` (106 files, 814 tests) and `npm run test:py` all green; new/updated: `MainTabPresetAnimatedChips.test.tsx` (14: two chips in every mode, carousel window with one marked chip, Tip badge outside the scrolling text, reduced-motion labels are the plain cut-off kind), `MainTabPresetRow.test.tsx` (2: help chip alone, then the chips), `carouselState.test.ts` (10: window start, visible pair, third seed one step right), `presetSlotRotation.test.ts` (5: third seed queues, no duplicate on screen, pinned batch of four walks in order across two slots and wraps), `sessionRagComposer.test.ts` (guarantee slot moved). **On-Deck:** rows **01b / 02 / 03 / 04** in [testing-manual.md](testing-manual.md) — 02 must write the measured button padding and character count back into [planning/29-preset-row-three-thirds-plan.md § 3b](planning/29-preset-row-three-thirds-plan.md); 04 calibrates `PRESET_MARQUEE_SPEED`, whose unit Steam does not document. Plan: [planning/29-preset-row-three-thirds-plan.md](planning/29-preset-row-three-thirds-plan.md). |
| Vision attach | SMOKE-G | Verified | Vision sweep 2026-04 |
| Background Ask reopen | SMOKE-H | Partial | Full lifecycle → release. **2026-08-23:** code fix landed for the *"…" live question after a mid-thinking reopen* bug — `applyBackgroundStatusToUi` in `useBonsaiAskOrchestration.ts` now backfills the live header from the polled question whenever it is blank (pending and completed branches), guarded so it never overwrites a caption already on screen. On-Deck re-test owed: reopen while an Ask is still thinking and confirm the question stays visible through to the answer; also re-check whether the reported duplicate-question symptom on the *next* Ask is gone, since it was not independently reproducible by code reading. See [roadmap.md § Bugs](roadmap.md#bugs) for the full trace. |
| Knowledge base retrieve (keyword + hybrid) | KB-SMOKE-02/04 | **Re-opened** | Was Verified on the seed KB, Deck Jul 2026. **Re-opened 2026-08-05 by remediation PR1**, which changed the ranking, the query text and the corpus format — the earlier evidence describes behaviour that no longer exists. Re-run after `python scripts/build_rag_db.py --seed` and a redeploy |
| KB hybrid is genuinely hybrid (RRF) | KB-RRF-01 | **Verified (Deck)** | **On-Deck 2026-08-16** (bonsAI 0.5.0, build `6329577`, corpus `2026.08.16`): Strategy Ask on Portal 2 (`620`) with Deck-local Ollama → Show details reads **Keyword + meaning**, **Embed 798.36 ms**, Re-rank 0.24 ms, trust tier `wiki_no_patch`. Non-zero embed time on the published corpus is exactly what this row asked for. Screenshot `DeckCapture_20260816_233808_game`. Prior context: PR1, 2026-08-05. Cosine-only re-rank replaced by reciprocal-rank fusion on **both** the strategy and compat paths. Unit coverage is thorough (`_fuse_cards_by_rrf`: promotion, vectorless survival, no-vector passthrough, dimension mismatch) — **what needs a Deck is that hybrid still engages at all** with a real Ollama + real corpus. **Steps:** KB on, corpus rebuilt at schema v3, `nomic-embed-text` installed on the Ask host, run a Strategy Ask on DRG Survivor (`2321470`) → Show details must read **Keyword + meaning** and report a non-zero *Embed* time. A read of **Keyword search (embed unavailable)** here means the corpus was not rebuilt |
| KB vector half has its own recall pass | KB-RECALL-01 | **Verified (Deck) 2026-09-06 — closed.** The label passes in both modes; the Speed half's gate leak was fixed 2026-09-05 and confirmed on the Deck 2026-09-06; the Strategy timing is accepted at about a second, every question (D84), and the one-second target this row measured against is retired | **Card half PASS by probe 2026-09-03.** Read-only retrieval probe over SSH (corpus `2026.09.01`, Deck-local Ollama with `nomic-embed-text`, DRG Survivor `2321470`; raw output in `runs/round31-laneB-probes-1.txt`), **Strategy** mode: *"how do i kill the big armoured bug boss"* attaches Exploder, Glyphid Dreadnought, Mactera (0 candidates on 2026-08-17); *"which character is best for a beginner"* attaches Classes, Nitra, Praetorian (was 0); *"tips for the thorny plant level"* attaches Hollow Bough, Upgrades and overclocks, Acid Spitter — Hollow Bough is the area card, where the 2026-08-17 run returned the wrong one. In **Speed** the second sentence attaches nothing by keyword (`no_hit`), which is the implicit route behaving. One thing to watch: the first and third sentences do attach one card each in Speed, and the probe labels both `method=hybrid`, which Show details would print as **Keyword + meaning** ([transparency_service.py:192](../py_modules/backend/services/transparency_service.py)); whether an embed actually ran there is UNKNOWN from the probe, which prints no timings, so the screen run decides it. Still owed on screen (plan 31 session 2): the Show details method label (**Keyword + meaning** in Strategy, **Keyword search** in Speed) and the embed timing band. **Fixed 2026-08-18.** The vector half searches the resolved game's sections itself and RRF fuses two real lists; before this it only re-ordered a keyword shortlist, and when BM25 returned nothing no embedding ran at all. **Needs a Deck for two things unit tests cannot judge.** (1) **The win:** KB on, corpus installed, `nomic-embed-text` on the Ask host, DRG Survivor (`2321470`) running, **Strategy** mode — ask *"how do i kill the big armoured bug boss"*, *"which character is best for a beginner"* and *"tips for the thorny plant level"*. Each must attach cards (Show details: **Keyword + meaning**); on 2026-08-17 the first two returned **0 candidates** and the third returned the wrong card. (2) **The cost:** the pass adds an embed round trip (**793–900 ms** measured on Deck) and is gated to the explicit route, so the same question in **Speed** mode must still report **Keyword search** with **no** embed time — a non-zero embed there means the gate leaked and every casual Ask made while a game runs got slower. Also confirm the Strategy timing is in that same 800 ms band and has not grown: the recall scan itself is one indexed query plus a few hundred dot products over 5–13 cards. Backend: 8 tests in `test_knowledge_base_service.py` **On screen 2026-09-05 (plan 34, DRG Survivor 2321470 running, corpus 2026.09.01, one fresh chat, six Asks, ladder read off the DOM — `runs/round34-drg-*.json`).** Strategy: *how do i kill the big armoured bug boss* / *which character is best for a beginner* / *tips for the thorny plant level* each read **Keyword + meaning**, so **the Strategy label half PASSES**; embed **1078.87 / 1094.34 / 1090.17 ms**, which is **above the 793–900 ms band this row records** (filed as a ★★★ Bugs entry). Speed, same three sentences: the first read **Keyword + meaning** with **Embed 1140.40 ms**, the third **Keyword + meaning** with **Embed 943.70 ms**, and only the second read *Knowledge base (skipped)* / `no_hit` with no embed. **The Speed half FAILS**: the row requires *Keyword search* and no embed time, and says in as many words that a non-zero embed there means the gate leaked. The pattern is that whenever the keyword search finds anything, the meaning search runs as well, whatever mode is selected — which settles the open question the 2026-09-03 probe raised. All six turns also read `Trust tier: fallback_no_source`, `KB: 13 sections`, `Routed gemma4:e2b-it-qat`. **Speed half fixed 2026-08-05 and confirmed on the Deck 2026-09-06:** all three Speed sentences read *Keyword search* with no embed time on a fresh build (Deep Rock Galactic: Survivor running), `runs/plan46-R2-speed-half.json`. **Timing accepted 2026-09-06 (D84):** repeated on the Deck, three Strategy questions read **1.10, 1.23 and 1.19 seconds** to search by meaning, the same band as the 2026-09-05 run — a steady cost, not a one-off. The maintainer's call: about a second before an answer that then takes tens of seconds to write out is not something a person would notice. **The one-second target is retired.** This row now reads: searching the notes by meaning costs about a second on the Deck, every time, and that cost is accepted. The earlier idea that only the first question after a quiet spell is slow still holds on a PC, where a repeat question came back in 0.05 seconds, but not on the Deck, where the third question here was no faster than the first. Row closes. |
| KB vector recall — bake-off + floor | KB-RECALL-02 | Verified (PC) | **2026-08-18.** Paired arms on `kb_eval_v2` (98 labeled strategy rows, same corpus, one query embedding per case so the only difference is the recall pass): top-3 **95.9% → 100.0%**, top-1 83.7% → 83.7%, **zero** cases lost from top-3. One BM25-empty rescue (`V2-S-DRG-06` *"gunner or scout"* → `Classes`) and three cards promoted into range. `VECTOR_RECALL_FLOOR` measured over 15 paraphrased questions and 12 off-topic Asks — **the distributions overlap** (relevant 0.519–0.738, off-topic 0.435–0.593), so the floor cannot do the routing and is not asked to; a z-score alternative was measured and rejected. Full table: [audit/rag-vector-recall-floor-2026-08-18.md](audit/rag-vector-recall-floor-2026-08-18.md). **Not covered by this row:** on-Deck timing and real user phrasings → **KB-RECALL-01** |
| Long Strategy questions trim the reply instead of dropping the notes | KB-PROMPT-FIT-01 | **Verified (Deck) 2026-09-06** | **On-Deck 2026-09-06**, character voice on (Ali G), thinking at medium, Hades running: asked about Megara, a boss with a note attached, and the answer named her dash patterns, punishing her while she recovers, and using boons for burst damage — real note content, not general memory. The log showed the reply budget trimmed from 2112 to 1800 tokens so the prompt fit the window, and the old silent-drop warning was gone. **Measured before landing** on a run shaped like the maintainer's own settings: 22 of 37 questions used to lose the start of the prompt; now zero do. Comparing the same 37 questions before and after (three runs, to smooth out noise — a single early run looked alarming on its own): facts kept from the note 92.9% to 91.9%, spoiler warning placed correctly 93.3% to 97.1%, follow-up menu shown 100% to 98.2%, prompt length 6,930 to 5,682 characters; no question lost the start of its prompt across 183 samples. Evidence `runs/plan46-R4-prompt-fix-on-device.json`, [archive/research/kb-answer-eval-2026-09-06-after-wave1-landed.md](archive/research/kb-answer-eval-2026-09-06-after-wave1-landed.md). **The row this replaces cited a Hades boss, the Bone Hydra, that has no note in the library** — a generic answer to it was always correct and never demonstrated the bug; the roadmap entry's evidence has been corrected to the numbers above. |
| KB Expert mode is on the explicit route | KB-EXPERT-01 | **Verified (Deck) 2026-09-04** | **Probe half PASS 2026-09-03.** Read-only retrieval probe over SSH (corpus `2026.09.01` on the Deck, Deck-local Ollama with `nomic-embed-text`, DRG Survivor `2321470`; raw output in `runs/round31-laneB-probes-1.txt`). Cards attached in Speed / Strategy / Expert: *"what class should i pick"* **1 / 3 / 5** (2 and 1 before the fix), *"what should i upgrade"* **1 / 3 / 5** (3 and 1 before). Expert attaches at least as many as Strategy, Speed attaches fewer, and the off-topic *"thank you very much"* in Speed attaches **0**. Nothing else is owed here unless the maintainer wants a screen check on top; the probe is the method this row names. **Fixed 2026-08-18.** `implicit_route` tested for Strategy by name, so Expert got the largest card budget (5) and the strictest relevance floor (4.0 vs 1.0) at the same time — the mode a stuck player picks for maximum depth hid the most corpus. Both knobs now read `_DECLARED_GAME_ASK_MODES`. **Steps:** KB on, corpus installed, DRG Survivor (`2321470`) running; ask *"what class should i pick"* in **Strategy** and in **Expert** and confirm Expert attaches **at least** as many cards as Strategy (before the fix: 2 and 1). Repeat with *"what should i upgrade"* (before: 3 and 1). **This cannot be read off the screen** — the Show details ladder prints no card count; use `ssh deck@$DECK_IP 'python3 - --pool' < scripts/probe_deck_kb_retrieval.py` and count card headers in the rendered block. **Then the other direction:** in **Speed** mode the same questions must still attach fewer cards and an off-topic Ask must still attach nothing — Speed stays on the implicit route by design. Note Expert's budget of 5 is a ceiling, not a promise: a 5-section title cannot fill it. Backend: `test_expert_mode_is_on_the_explicit_route_like_strategy`, `test_expert_mode_gets_the_vector_recall_pass_too` **Deck 2026-09-04 PASS on screen:** the antlions question in Expert mode returned five cards (Antlions, Sandtraps, Pheropod, Ravenholm, Strider) against Strategy's three on the same sentence, same source and trust tier. |
| KB compat tips stay on the routed topic (D22) | KB-ROUTER-02 | **Verified (Deck)** | **Fixed 2026-08-18.** The router's topic now opens a recall path and acts as a ranking preference. **Steps:** KB on, corpus installed, no game running, Speed mode — ask each KB-ROUTER-01 sentence and confirm the attached tip is on the right subject: (1) *"I'm out of room and want my installs on the memory card instead"* → a **storage** tip, (2) *"the game only responds to the touchpad and ignores the sticks"* → **steam_input**, not the gamescope resolution tip, (3) *"I can play alone but online kicks me out straight away"* → **anticheat**, (4) *"my playstation 2 games run at half speed on the handheld"* → **emudeck**, not desktop_mode. **Then the direction that proves it is a preference and not a filter:** ask *"my windows game shuts itself the moment the loading screen appears"* and confirm a **windows_steam** tip still comes back — the router says proton, and the question says windows outright, so the better match must still win. **Card counts cannot be read off the screen** — use `scripts/probe_deck_kb_retrieval.py`. Backend: 4 tests in `test_knowledge_base_service.py` **On-Deck 2026-08-22** (bonsAI 0.5.0, plugin build `a90bd5d`, corpus `2026.08.22` / 133 cards, Deck-local Ollama, `nomic-embed-text`): **passes in full**, all five sentences, Speed mode, nothing running. storage → `storage`, touchpad → `steam_input`, online kick → `anticheat`, PS2 → `emudeck`. The direction that proves it is a preference and not a filter also holds: *"my windows game shuts itself…"* still returns a `windows_steam` tip even though the router says proton. |
| KB reaches a title named in the question (D19) | KB-NEWTITLE-01 | **Verified (Deck)** | **Fixed 2026-08-19.** Was specified in this row and never built — every strategy path needed a running game, so `hl2 ravenholm` returned nothing on 2026-08-17. **Steps, with no game running:** ask *"hl2 ravenholm"*, *"drg survivor what class"*, *"how do gels work in portal 2"* and *"what is the best way to beat volvagia in oot"* — each must attach that game's cards and no other game's. **Then the direction that matters more:** launch a game (say Hades), ask *"how do gels work in portal 2"*, and confirm you get **Hades** cards — the running game must always win, or the plugin starts answering about games you are not playing. **Also check spoiler fencing follows:** with nothing running ask about an Ocarina of Time boss by name and confirm the reply is fenced the way it is when the game is running (D19 locks this). Short aliases in ordinary sentences are a known accepted risk — *"this game is hades on my battery"* resolves Hades; report real-world false positives rather than pre-emptively widening a denylist. Backend: 6 tests in `test_knowledge_base_service.py`; profile change asserted in both languages via `tests/contracts/spoiler-title-profiles.json` **On-Deck 2026-08-22** (bonsAI 0.5.0, plugin build `a90bd5d`, corpus `2026.08.22` / 133 cards, Deck-local Ollama, `nomic-embed-text`): **passes in full.** With nothing running, all four questions resolve the title from the text and attach that game's cards only — *"hl2 ravenholm"* → Half-Life 2 (*Ravenholm*), *"drg survivor what class"* → DRG Survivor (*Classes*), *"how do gels work in portal 2"* → Portal 2 (*Gels*), *"what is the best way to beat volvagia in oot"* → Ocarina (*Volvagia*). **The direction that matters more holds:** with Hades running, *"how do gels work in portal 2"* returns **Hades** cards and no Portal 2 card. Spoiler fencing follows too — Ocarina resolves `protect_progression` with nothing running. **`scripts/probe_deck_kb_retrieval.py` could not run this row until 2026-08-22:** the probe never resolved the title from the question, so every case here read `gate=False` while working correctly in the product. Probe corrected to mirror `game_ai_request.py:296-298`. |
| KB British spelling reaches US-spelled cards | KB-SPELLING-01 | **Verified (Deck)** | **Fixed 2026-08-19.** **Steps:** KB on, DRG Survivor running, Strategy mode — ask *"how do i kill the big armoured bug boss"* and confirm the Glyphid Dreadnought card attaches. Repeat with `armored` and confirm the same. Then try *"colour settings"* and *"customise the controls"* on any covered title and confirm nothing breaks. **The regression direction:** ordinary words must be untouched — *"our team"*, *"four hours"*, *"one sentence"* must not gain junk terms; if retrieval starts attaching cards to unrelated questions, the exemption set is the first place to look. Backend: 3 tests **On-Deck 2026-08-22** (bonsAI 0.5.0, plugin build `a90bd5d`, corpus `2026.08.22` / 133 cards, Deck-local Ollama, `nomic-embed-text`): **the spelling half passes; the stated regression direction fails.** Both *armoured* and *armored* attach **Glyphid Dreadnought** (ranked 2nd and 3rd respectively; *Exploder* leads both). *"colour settings"* and *"customise the controls"* break nothing. **But ordinary phrases do attach game cards** with DRG Survivor running in Strategy mode: *"one sentence"* → **Praetorian**, *"thank you very much"* → **Nitra**, *"what time is it"* → **three** cards, *"please repeat that"* → **three** cards. (*"our team"* and *"four hours"* are clean.) **The exemption set named in this row is not the cause** — the query is not expanded at all (`expanded query: 'one sentence'`) and Praetorian scores `bm25=0.00` and is absent from the FTS pool. Hybrid on/off comparison splits the blame: the vector recall pass (`bf16b35`) supplies the card for *"one sentence"* and *"please repeat that"* (keyword-only attaches nothing), while *"thank you very much"* and *"what time is it"* attach cards **on the keyword half too**. Root cause is the Strategy explicit-route floor (1.0) being low enough for near-noise to clear it, widened by the new vector recall. Raised for a maintainer call as **D28**. **D28 option 2 implemented 2026-08-23:** `VECTOR_RECALL_FLOOR` raised 0.50 → 0.515 (`knowledge_base_service.py:148`). Re-measured end-to-end against the real test corpus with a real local Ollama: *"one sentence"* now clean, *"please repeat that"* down to one card (Glyphid Dreadnought), *"thank you very much"* and *"what time is it"* **unchanged** (confirmed BM25-driven, not this floor — see roadmap). Not re-run on the Deck; full table in [roadmap.md](roadmap.md). **Re-run on-Deck 2026-08-23 and the device matches the desk table card for card — D28's re-measure obligation is discharged.** Frozen-chip Batch A, DRG Survivor, corpus `2026.08.22`, `gemma4:e2b-it-qat`: *"one sentence"* → `Genre/compat fallback` **only** (no game card, reproduced 3×); *"please repeat that"* → `boss: Glyphid Dreadnought` **only**; *"thank you very much"* → `item: Nitra`; *"what time is it"* → `boss: Glyphid Dreadnought` + `enemy: Praetorian` + `mechanic: Classes`. The last two attach **by design** (BM25 half, off limits under D25/D28) and are passes, not failures. **Method note that matters for every future run of this row:** card names are **not readable from the device UI** — *Show details* never names a card, and `Trust tier: fallback_no_source` is a trust tier, not an attachment count, so the genre fallback and a real card look identical there. Read `~/Desktop/bonsAI_logs/bonsai-ask-trace-<date>.md` instead (needs `desktop_ask_verbose_logging` on); it dumps the verbatim `--- Local knowledge base ---` block with one header per attached card. **Second signal shipped 2026-08-28** — the pool-margin gate on the vector recall pass ([audit/kb-second-signal-2026-08-28.md](audit/kb-second-signal-2026-08-28.md)): the vector half now attaches **nothing** to any of the six phrases at the desk. *"please repeat that"* drops its last card (Glyphid Dreadnought) and *"what time is it"* falls back to its keyword pair (Classes + Mining and the run timer, which stay by design under D25/D28), the other four are unchanged; `kb_eval_v2` tune and tips slices are unchanged to the decimal and *"the boss"* / *"gels"* still attach (D25). **On-Deck re-check owed:** re-run the six phrases (frozen-chip Batch A wording, unchanged) and confirm *"please repeat that"* attaches no game card and *"what time is it"* attaches only Classes + Mining and the run timer — reading the ask-trace log per the method note above, not the details panel. **Re-run on-Deck 2026-08-28 — passes in full, row closed.** Plugin build `b889639` (the merged second-signal work), corpus `2026.08.22`, DRG Survivor running, Strategy mode, six frozen Batch A chips driven end to end by the bridge board with no human touch (chip A-press → field verified verbatim → ask A-press → reply awaited), results read from the ask trace: *"one sentence"* → genre fallback only; *"please repeat that"* → **genre fallback only, no game card** (the gate removed its last vector-supplied card); *"what time is it"* → **exactly** Classes + Mining and the run timer (the keyword pair, its three vector-supplied cards gone); *"thank you very much"* → Nitra (keyword, by design); *"how do i deal with the exploders"* → Exploder first (+ Acid Spitter, Mactera); *"how do i beat the twins"* → Dreadnought Twins first (+ Mactera, Acid Spitter). Device matches the desk table card for card — junk cleaned on the vector half, real questions untouched. One thing observed alongside, not part of this row: the twins reply carried a mid-reply `bonsai-spoiler` fence — that is the known open *where* half of the mid-reply fence bug, freshly reproduced (see roadmap Bugs). |
| Ocarina of Time no longer borrows Stardew Valley's AppID | KB-APPID-01 | **Verified (Deck)** | **Fixed 2026-08-21.** **Needs a corpus rebuild and re-install** before any of it is testable — the AppID lives in corpus data. **Steps:** launch **Stardew Valley** and ask *"how do i make more money on my farm"* — confirm you get **no Ocarina of Time cards** (before the fix it returned three) and that the reply is **not** spoiler-fenced as a progression title. **Then the direction the fix risks:** launch Ocarina of Time through your emulator, and separately through **Ship of Harkinian**, and confirm both still attach Zelda cards *and* are still fenced as progression titles — protection now comes from the title name, not the AppID, so a shortcut named something else entirely is the case to watch. **Third:** with nothing running, ask *"what is the best way to beat volvagia in oot"* and confirm D19 still resolves it. **Card counts cannot be read off the screen** — use `scripts/probe_deck_kb_retrieval.py`. Backend: 4 tests across `test_knowledge_base_service.py` and `test_spoiler_title_profiles.py`; the two-language table is asserted via `tests/contracts/spoiler-title-profiles.json` **On-Deck 2026-08-22** (bonsAI 0.5.0, plugin build `a90bd5d`, corpus `2026.08.22` / 133 cards, Deck-local Ollama, `nomic-embed-text`): **passes in full.** Stardew Valley (`413150`) resolves to no game (`game_id=None`) and attaches **zero** Ocarina cards — it returned three before the fix. Ocarina reached by name, by an emulator shortcut and by **Ship of Harkinian** all resolve to `game_id=1` with all 16 sections and Volvagia ranked first. Spoiler fencing follows the **title name**: Stardew is `unknown` (not fenced), while Ocarina, the emulator shortcut and Ship of Harkinian are all `protect_progression`. With nothing running, *"what is the best way to beat volvagia in oot"* resolves via D19 and attaches Volvagia first. |
| KB "the boss" reaches a card typed as a boss | KB-TYPE-01 | **Verified (Deck)** | **Fixed 2026-08-19.** **Steps:** with DRG Survivor running, Strategy mode, ask *"how do i beat the boss"* — the Glyphid Dreadnought card must attach. Repeat on a title with several bosses (OoT, Hades) and confirm you get boss cards rather than mechanics. Ask *"what dungeon should i do first"* on OoT → the dungeon card. **Two regression directions:** naming a card must still beat asking for its kind (*"how do i beat queen gohma"* → Queen Gohma **first**), and Speed mode must still attach nothing for a bare *"how do i beat the boss"*, since a type word in a passing Ask is weak evidence. **Narrowed 2026-08-19.** With Ocarina of Time now carrying six boss cards, the type preference applies only to kinds the keyword half missed. **Extra steps on OoT:** ask *"how do i beat the water temple boss"* → **Morpha** first, and *"the forest temple boss keeps flying away"* → **Phantom Ganon** first. Before the narrowing both returned Queen Gohma. The rescue direction above (bare *"how do i beat the boss"* on DRG) must still work — that is what the narrowing risks. Backend: 6 tests **On-Deck 2026-08-22** (bonsAI 0.5.0, plugin build `a90bd5d`, corpus `2026.08.22` / 133 cards, Deck-local Ollama, `nomic-embed-text`): **passes in full.** DRG rescue *"how do i beat the boss"* → **Glyphid Dreadnought** first; Speed mode attaches **0** for the same question. On Ocarina, *"the water temple boss"* → **Morpha** first and *"the forest temple boss keeps flying away"* → **Phantom Ganon** first, so the narrowing holds. Naming still beats the kind (*"how do i beat queen gohma"* → Queen Gohma first). *"what dungeon should i do first"* returns the typed dungeon card first. **Content note, not a failure:** the card that actually answers *"which dungeon first"* is *Adult dungeon order*, which ranks second behind *Shadow Temple invisible floors* — the only card typed `dungeon`. |
| Phase 4 structured cards read as bullets | PHASE4-CARDS-01 | **Closed 2026-09-05 — prose accepted (D67)** | **Shipped 2026-08-19.** 16 new cards for the two sample titles carry labelled lines, and the prompt asks the model to keep those labels as short bullets. **Steps, DRG Survivor running, Strategy mode:** ask *"how do i deal with the exploders"*, *"what is red sugar for"* and *"how do i beat the twins"*; then with Ocarina of Time running (or named in the question) ask *"how do i beat the water temple boss"*, *"what should i keep in my bottles"* and *"how do i sink underwater"*. Each must attach the matching card **and** the reply must keep `Summary` / `Weak points` / `Uses` / `Phases` / `Tips` as separate short bullets rather than one paragraph. **Two regression directions:** (1) ask a question that attaches only prose cards (any Fallout 4 or Sims 4 card) and confirm the reply has **no** invented labels — the clause is conditional and must not fire; (2) confirm no card is listed twice in one block. **Card counts cannot be read off the screen** — use `scripts/probe_deck_kb_retrieval.py`. Needs a **corpus rebuild and re-install** on the Deck before any of it is testable, since the cards are corpus data. Backend: 4 tests in `test_knowledge_base_service.py` **On-Deck 2026-08-22** (bonsAI 0.5.0, plugin build `a90bd5d`, corpus `2026.08.22` / 133 cards, Deck-local Ollama, `nomic-embed-text`): **card attachment verified; reply wording still needs eyes.** All six questions attach the matching card **first** and no card is repeated in a block — DRG: exploders → *Exploder*, red sugar → *Red Sugar*, twins → *Dreadnought Twins*; Ocarina: water temple boss → *Morpha*, bottles → *Bottles*, sink underwater → *Iron Boots*. **Not verifiable from a probe:** whether the reply keeps `Summary` / `Weak points` / `Uses` / `Phases` / `Tips` as separate short bullets, and whether a prose-only card (Fallout 4 / Sims 4) avoids invented labels — both are model output. `deck_send_ask.py` types the question but deliberately does not press Ask, so these two need the maintainer on the device. **On-Deck 2026-08-22, first three questions (Deep Rock Survivor, Strategy, frozen test chips):** **the bullets survive; the labels do not, on two of three.** Every reply came back as short scannable bullets rather than a paragraph, so the core intent holds. But only *how do i beat the twins* kept the card's own labels — **Weak Points / Phases / Tips**, matching the `Dreadnought Twins` card exactly (`Summary` folded into the opening prose, which reads fine). The other two **renamed them**: `Red Sugar` (`Summary` / `Uses` / `Tips`) came back as *What it does* / *The catch*, and `Exploder` (`Summary` / `Weak points` / `Tips`) came back as *Keep Your Distance* / *Weapons Fire Themselves* — headings invented from the Tips text, with `Weak points` dropped as a label entirely. Content was accurate in all three. **On-Deck 2026-08-22, second batch** (recordings `DeckRecord_20260822_195545/200257/200436/201647_game.mkv`): **the three Ocarina questions and the prose-only direction are done — this row's testing is complete.** The prose-only direction **passes**: Fallout 4 *how do i get settlement happiness up* (`377160`, KB 10 sections) came back as plain prose with **no invented labels**, and its fandom.com credit still renders under *Local knowledge base*. The Ocarina half **repeats the Deep Rock pattern, worse**: *bottles* kept bullets but renamed the card's `Summary` / `Uses` / `Tips` to invented per-item headings (**Fairy:** / **Blue Fire:**) and dropped the bugs-for-Skulltulas use entirely; *water temple boss* and *sink underwater* came back as **pure prose** — no bullets, no labels — though the content tracks the cards faithfully (hookshot beach-and-slash, corner positioning; Iron Boots plus tunic). **Net across both titles: bullets survive on 4 of 6 questions, the card's own labels on 1 of 6 (*twins*).** With `gemma4:e2b-it-qat` the conditional-label clause holds only in the negative direction — it never invents labels for prose cards, but rarely preserves them for structured ones. That is a prompt/model finding, not an attachment bug; whether to strengthen the prompt, try a larger routed model, or accept prose is a maintainer call. **Two things found alongside:** the spoiler fence fired on *exploders* but not on *twins* or *red sugar* — same game, same mode, so the false-positive is **inconsistent turn to turn**, and it now lands **mid-reply**, between the opening line and the bullets, rather than at the end (prompt-side cause traced and fixed 2026-08-23, not yet re-confirmed on this Deck repro — see roadmap.md); and *Show details* reports **Trust tier: wiki_verified** for cards that have no source at all (see Bugs). **Maintainer call 2026-09-05: accept prose (D67).** The facts survive on 6 of 6, which is what matters; the labels are not required and no stronger prompt or larger model is bought for them. Row closed, no further Deck run. The card style pass on the roadmap stays gated on the answer test. |
| Trust tier names a real source | QA-TRUST-TIER-01 | **Verified (Deck)** | **Both directions confirmed on-device 2026-08-27, read from the ask trace rather than the panel.** *Maintainer-written cards:* asked `drg survivor what is red sugar for` — the trace attached `[Deep Rock Galactic: Survivor / item: Red Sugar]` and `[… / item: Gold]` both as **`(trust: fallback_no_source)`**, not `wiki_verified`. *Wiki-sourced cards, the direction the fix risked:* asked `portal 2 how do gels work` — three cards attached, all **`(trust: wiki_no_patch)`**, and the panel's **Local knowledge base** section showed both the credit **`theportalwiki.com · CC-BY-4.0 · as of 2026-08-09`** and the line **`Trust tier: wiki_no_patch`** — the line the 2026-08-22 capture never had on screen. **Run without the game running**, by naming the title in the question, which also demonstrates D19's title resolution rather than requiring a launch. | **Fixed 2026-08-22.** Both wiki trust tiers now require a `source_url`; a `source_version` alone (the seed writes a build tag there) no longer earns one. **Steps:** with Deep Rock Survivor running, Strategy mode, ask *"what is red sugar for"* and open **Show details → Local knowledge base** — **Trust tier** must read `fallback_no_source`, not `wiki_verified`; those cards are maintainer-written and cite nobody. **Then the direction the fix risks:** ask a question that attaches a genuinely wiki-sourced card (Portal 2 *"how do gels work"*, or Left 4 Dead 2 *"how do i beat the tank"*) and confirm it still reads `wiki_no_patch` **and** still shows its source credit — 59 cards depend on that being unchanged. **No corpus re-install needed:** the tier is computed at query time, so the deployed plugin fixes the corpus already on the Deck. Backend: 4 tests in `test_knowledge_base_service.py` **Partial evidence 2026-08-22** (`DeckRecord_20260822_195545_game.mkv`): the display half of the wiki-sourced direction holds — a Fallout 4 card still shows its fandom.com credit under *Local knowledge base*. The **Trust tier** line itself was never on screen in the capture, so both tier readings are still owed and the row stays open. |
| Phase 4 chip guarantee and Tip badge | PHASE4-CHIPS-01 | **Partial (Deck)** | **Shipped 2026-08-19.** The carousel now guarantees at least one corpus chip whenever the corpus covers the running game, prefers game chips over shared Deck tips, and badges game chips **Tip**. **Steps:** launch a covered title (DRG Survivor, Ocarina of Time, Portal 2), open the QAM and watch the carousel through several rotations — **every** roll must show at least one corpus chip, and the badge must be on game chips only, never on a shared troubleshooting chip or a static seed. **Then the direction that proves the guarantee is not a filter:** launch a title the corpus does **not** cover and confirm the carousel falls back to all-static seeds with no badge and no empty slot. **Then the layout direction:** the badge sits inside the 300px QAM column — confirm no chip label is clipped or wrapped by it at the longest card name (*"What should I know about Mining and the run timer?"*). **Chip pool interleaved 2026-08-19.** **Extra steps:** with Ocarina of Time running, confirm the carousel offers **at most two** *"How do I beat X?"* chips and that an item chip (*"How do I use Bottles?"*) and an enemy chip (*"How do I deal with ReDead and Gibdo?"*) can both appear. **The direction that must not have regressed:** with Left 4 Dead 2 running, confirm the pool is still six chips — that title files almost everything as one kind, so interleaving must not shrink it. Frontend: 12 tests in `sessionRagComposer.test.ts`; Backend: 3 tests in `test_knowledge_base_service.py` **On-Deck 2026-08-22** (bonsAI 0.5.0, plugin build `a90bd5d`, corpus `2026.08.22` / 133 cards, Deck-local Ollama, `nomic-embed-text`): **candidate supply, interleaving and fallback verified; badge and layout need eyes.** Every covered title supplies **6** game chips + 2 shared compat chips (DRG Survivor, Ocarina, Portal 2, Left 4 Dead 2) — so Left 4 Dead 2 is **still six**, the direction the interleaving risked. Ocarina offers exactly **two** *"How do I beat X?"* chips, and both an item chip (*How do I use Bottles?*) and an enemy chip (*How do I deal with ReDead and Gibdo?*) appear. An uncovered title (Stardew Valley) returns `ok=false / app_unresolved`, which is the frontend's cue to fall back to static seeds. **Not verifiable from a probe:** that the **Tip** badge lands on game chips only, and that nothing clips in the 300px column. **The longest label is not the one this row cites** — it is *"How do I get through Shadow Temple invisible floors?"* at **52** characters (Ocarina), longer than *Mining and the run timer* at 50; check clipping against that one. **On-Deck 2026-08-29** (DRG Survivor `2321470` launched over SSH and confirmed running, plugin deployed 2026-08-28 23:19, panel opened through the bridge board, all geometry read from the live page): **the badge direction passes; the clipping direction is blocked by a new bug and could not be run.** *Badge:* the one game chip that rendered — *How do I beat Glyphid Dreadnought?* — carried **Tip**, and no badge appeared on either shared compat chip (*Why is my Deck running hot?*, *Any known issues running this on Deck?*) or on any static seed, across 9 distinct chips in a 24s sweep. That matches `isGameCandidate` in [sessionRagComposer.ts](../src/features/preset-carousel/sessionRagComposer.ts), which badges on `domain === "strategy"` only. *Geometry:* the slot is the full column (`x=48 w=300`, padding `0`), the **Tip** badge measures **14.7px**, and the chip label is `white-space: nowrap` + `text-overflow: ellipsis` but `display: inline` — **so the ellipsis cannot fire**; a label that exceeds the slot will overflow rather than truncate. *Clipping: not measurable.* Only ever **one** game chip rendered, always the same 219.2px one; the longest candidate the backend supplies (*What should I know about Mining and the run timer?*, ~284px measured by calibrated canvas) never appeared, so there was nothing long to measure. With badge + label that chip computes to ~299px against a 300px slot — too close to call without seeing it, which is why this stays open. *One false alarm, recorded so it is not re-filed:* a 2.4px overflow was measured once on a static seed and did not reproduce; the same text measured 295.6px mid-decode and 283.8px at rest, so it was a transient of the decode animation, **not** a layout fault. **The blocker is a new bug** — corpus chips vanish about 21s after the panel opens (see roadmap). Until that is fixed, 5 of the 6 game chips never reach the screen and the clipping direction cannot be run on any title, Ocarina included. **Blocker fixed and confirmed on device 2026-08-29** (deployed, 61 files verified, DRG Survivor running): **240s sampled every 3s across three fresh panel opens, a corpus chip on screen in every sample, zero gaps** — and three different game chips appeared where before only one could. **Clipping direction measured and FAILED, 2026-08-29** — the last piece of this row. Run with **Half-Life 2** rather than the two titles this row names, because the corpus's longest chip label is HL2's at **59 characters** (*"What should I know about Rocket-Propelled Grenade Launcher?"*), longer than the Ocarina label at 52 that the correction above cites; and with Developer → *force session RAG chips* on, which after a fix that day forces rotation as well as seeding. Six distinct game chips appeared in 90s with no gaps. **Result, read off the live element: the longest label renders at 379.8px inside a 300px slot — 86.6px past the right edge, 29% wider than the column.** The next widest to appear, *How do I beat Hunter-Chopper (helicopter)?* at 268.3px, fits with 15.9px spare. **And it does not truncate**: the label is `text-overflow: ellipsis` on a `display: inline` element, where `overflow` does not apply, so the ellipsis can never fire and the text simply runs out of the column. Filed on the roadmap as a confirmed bug, with the marquee treatment as its follow-on. **This row's remaining direction is now answered, in the negative.** **Blocker fixed at the desk 2026-08-29, so this row is runnable again on the next deploy.** Rotation now applies the same corpus guarantee the seeding did, so all six game chips come round instead of one — including the long labels this row needs. **What to run when it is next on a Deck:** with DRG Survivor running, watch the carousel for a full minute and confirm a corpus chip is on screen for the whole of it rather than only the first ~21s; then measure the widest game label that appears against the 300px slot (*What should I know about Upgrades and overclocks?* ~284px, plus a 14.7px **Tip** badge, computes to ~299px — the reason this direction was worth keeping open). Then repeat on Ocarina of Time for its 52-character label at ~288px. Read the geometry from the live page, not a screenshot; the numbers above came from `getBoundingClientRect` on device. **Re-checked off-device 2026-08-31** (corpus 146 cards, after the ten re-types, six State of Emergency cards and the two D39 splits): pools queried straight from `suggest_chip_candidates` against a locally built corpus, no Deck involved. **Left 4 Dead 2 is still six game chips + 2 shared**, so the direction this row exists to protect holds. Half-Life 2 six + two, now leading with *"How do I deal with Antlions?"* (the re-type showing up as enemy phrasing). Hades six + two and offers **weapon chips for the first time** (*"How do I use Stygian Blade?"*, *"How do I use Shield of Chaos?"*). Deep Rock unchanged as the untouched control. **Still needs eyes on device** for badge and layout: the longest label in this sweep is State of Emergency's 55-char *"What should I know about Kaos mode and Revolution mode?"*, and 59 chars was measured at 379.8px in a 300px column, so it overflows. |
| Frozen test chips (QA) | QA-FROZEN-CHIPS-01 | **Partial (Deck) — badge regressed 2026-09-07** | **Both owed sub-checks closed 2026-08-27** with a six-chip Batch B pinned. *Rotation reaches the tail:* a single D-pad walk visited **all six** chips in order before leaving the carousel — the 2026-08-22 run only ever observed five of seven, which is what left this open. *Chip press:* A on a **TEST** chip put `what happens at the end of the main story` into the Ask field **verbatim** and did **not** submit (0 reply bubbles, not busy) — the split the row specifies. Badging, ordering and session-RAG suppression re-observed as before. **Method note for the next session:** pinning by editing `settings.json` under a running plugin works, but restart `plugin_loader` afterwards or the frontend's debounced save can write its stale list back over yours. **Re-checked 2026-09-07 during the wave-two evening, on a Deck with the chip-decode animation switched on:** the pinned batch still drove the carousel, but no chip showed the amber Test badge, so a pinned batch looked like ordinary chips. Filed as its own bug on the roadmap. Also seen and recorded to reproduce rather than filed: pressing Right on a pinned chip did not bring in the next entry. Evidence `runs/plan47-frozen-chip-findings.json`. |
| Clear frozen test chips button (D-pad reachability) | DEV-CLEAR-CHIPS-01 | **Verified (Deck) 2026-09-04** | **Deck 2026-09-04, build 49241e7 (plan 32):** with three chips pinned the button was a real, visible stop at the bottom of Developer; A cleared the batch (disk went to 0, the panel read *None pinned*) and the button unmounted. A full sweep afterwards (`runs/DEV-CLEAR-CHIPS-01-sweep-no-batch.json`) walked 14 stops in 18 presses, every one visible, none of them a Clear button. One note for a later pass: pressing the button leaves the ring unowned until the next press, because the focused node unmounts; Steam re-places it on the first press. **Fixed at the desk 2026-09-04.** The button rendered disabled (`ButtonItem disabled={devFrozenTestChips.length === 0}`) rather than absent when no batch was pinned, and a disabled Decky button is still a D-pad stop — one dead press leaving Developer with nothing pinned (`runs/TAB-RESUME-MODE-01-h-restore-C-and-remeasure-clear-chips-button.json`). It now renders only when `devFrozenTestChips.length > 0`, the same condition the "None pinned" copy above it already reads on. Unit: `DeveloperTab.test.tsx` — no stop with an empty batch; present and calling `setDevFrozenTestChips([])` with one pinned. **On-Deck:** a Developer sweep with no batch pinned shows no stop named **Clear frozen test chips**; re-read alongside the Developer half of **SHELL-PAYLOAD-01**'s sweep. | **Shipped 2026-08-22.** A pinned batch of exact questions replaces the preset carousel, in order, and stops it reseeding — so a `docs/testing.md` row is one press per case instead of typing the sentence on the on-screen keyboard. Set from `dev_frozen_test_chips` in settings; free text, so it holds real QA questions (the older compile-time `TEMP_CAROUSEL_FROZEN_TEXTS` resolved entries against the built-in preset list and silently skipped anything that did not match, which is why it could never be used for this). **Steps:** with a batch pinned, open the QAM and confirm (1) the carousel shows **only** those questions, in the order given, (2) each carries a **Test** badge in amber — distinct from the green **Tip** badge, since a frozen carousel is not showing what the plugin would have chosen, (3) rotation walks the whole batch when it is longer than three, and (4) no session RAG chip appears while the batch is active. **The D-pad row:** Developer → *Knowledge base (dev QA)* now ends with **Clear frozen test chips**; confirm it is reachable by D-pad from the *Force session RAG chips* toggle above it, that A activates it, and that the carousel returns to normal sampling on the next reseed. It is a `ButtonItem` in a `PanelSection` that already holds toggles, so it inherits the section chain and needs no wiring of its own — same shape as the *Install seed KB* button in the same tab. **The regression direction:** a batch of fewer than three entries must be treated as **off** rather than shown as a short carousel, or a half-set freeze silently mixes pinned and sampled chips. Frontend: 9 tests across `presets.test.ts` and `sessionRagComposer.test.ts`; both languages asserted via `tests/contracts/settings-defaults.json` and 4 new cases in `tests/contracts/settings-hostile-inputs.json` **On-Deck 2026-08-22: the batch works.** Seven pinned questions drove the carousel in order, each badged **TEST** in amber and visibly distinct from the chip label; D-pad stepped through them; no session RAG chip appeared while the batch was active; and an already-asked question correctly dropped out of the rotation. **Still owed:** that rotation reaches the *last* entry of a seven-item batch (only five distinct chips were observed in an 18-second capture, which is not long enough to prove the tail is reachable), and the **Clear frozen test chips** button's D-pad reachability. |
| Chip rotation reaches past the top three of the candidate list | CHIP-ROTATION-01 | Fixed (desk) 2026-09-04, Deck check owed | `pickNextCarouselChip` used to hand out `available[0]` — the highest-ranked unseen candidate — for both the corpus guarantee and the ~30% roll, so with a stable backend rank order ranks 1-3 came back every rotation and ranks 4-6 only surfaced once 1-3 had cycled through history and been forgotten, which happened rarely because 1-3 kept winning the draw first. It now picks at random among the eligible candidates that share the top priority band — game chips still come before generic Deck tips — and the "nothing corpus on screen, so the next chip is corpus" guarantee is unchanged, just no longer pinned to one candidate. Frontend: 4 new tests in `sessionRagComposer.test.ts` (`pickAt(0.51)` / `(0.68)` / `(0.85)` pin ranks 4, 5 and 6 directly; a sweep of `random()` from 0 to 0.999 asserts the guarantee still returns a corpus chip, never the static fallback, whichever candidate it lands on). **On-Deck:** launch a game the corpus covers, turn the local knowledge base on, open the QAM and watch the carousel for 90 seconds without touching the D-pad — a chip ranked past the top three of that game's candidate list should appear at least once, not just the same three every rotation. |
| Frozen test chips (QA) | QA-FROZEN-CHIPS-01 | **Verified (Deck)** | **Both owed sub-checks closed 2026-08-27** with a six-chip Batch B pinned. *Rotation reaches the tail:* a single D-pad walk visited **all six** chips in order before leaving the carousel — the 2026-08-22 run only ever observed five of seven, which is what left this open. *Chip press:* A on a **TEST** chip put `what happens at the end of the main story` into the Ask field **verbatim** and did **not** submit (0 reply bubbles, not busy) — the split the row specifies. Badging, ordering and session-RAG suppression re-observed as before. **Method note for the next session:** pinning by editing `settings.json` under a running plugin works, but restart `plugin_loader` afterwards or the frontend's debounced save can write its stale list back over yours. | **Shipped 2026-08-22.** A pinned batch of exact questions replaces the preset carousel, in order, and stops it reseeding — so a `docs/testing.md` row is one press per case instead of typing the sentence on the on-screen keyboard. Set from `dev_frozen_test_chips` in settings; free text, so it holds real QA questions (the older compile-time `TEMP_CAROUSEL_FROZEN_TEXTS` resolved entries against the built-in preset list and silently skipped anything that did not match, which is why it could never be used for this). **Steps:** with a batch pinned, open the QAM and confirm (1) the carousel shows **only** those questions, in the order given, (2) each carries a **Test** badge in amber — distinct from the green **Tip** badge, since a frozen carousel is not showing what the plugin would have chosen, (3) rotation walks the whole batch when it is longer than three, and (4) no session RAG chip appears while the batch is active. **The D-pad row:** Developer → *Knowledge base (dev QA)* now ends with **Clear frozen test chips**; confirm it is reachable by D-pad from the *Force session RAG chips* toggle above it, that A activates it, and that the carousel returns to normal sampling on the next reseed. It is a `ButtonItem` in a `PanelSection` that already holds toggles, so it inherits the section chain and needs no wiring of its own — same shape as the *Install seed KB* button in the same tab. **The regression direction:** a batch of fewer than three entries must be treated as **off** rather than shown as a short carousel, or a half-set freeze silently mixes pinned and sampled chips. Frontend: 9 tests across `presets.test.ts` and `sessionRagComposer.test.ts`; both languages asserted via `tests/contracts/settings-defaults.json` and 4 new cases in `tests/contracts/settings-hostile-inputs.json` **On-Deck 2026-08-22: the batch works.** Seven pinned questions drove the carousel in order, each badged **TEST** in amber and visibly distinct from the chip label; D-pad stepped through them; no session RAG chip appeared while the batch was active; and an already-asked question correctly dropped out of the rotation. **Still owed:** that rotation reaches the *last* entry of a seven-item batch (only five distinct chips were observed in an 18-second capture, which is not long enough to prove the tail is reachable) — **superseded by QA-FROZEN-CHIPS-02 below**, filed once a longer batch was found to stall past the first minute (roadmap) — and the **Clear frozen test chips** button's D-pad reachability. |
| A pinned batch longer than the row reaches its tail; an Ask restarts the walk | QA-FROZEN-CHIPS-02 | **Verified (Deck) 2026-09-04** | **Deck 2026-09-04, build 49241e7 (plan 32):** plan 31 batch 1 (eleven sentences) pinned over SSH with the panel closed, carousel mode. After the sixty-second walk the row held sentences 1 to 5; from the chip row, Right walked to the newest and then each Right at the edge pulled the next pinned entry in, until the row held 7 to 11 with the ring on the last (`runs/QA-FROZEN-CHIPS-02-right-at-edge-pulls-next-entry.json`, then a DOM read). A on that chip filled the Ask field, Ask sent it, and within twenty seconds of the reply landing the row had changed ten times: the walk restarted and cycled sentences 1 to 5 back in. Both halves of the fix hold. **OPEN, found 2026-09-03:** with ten chips pinned, chips 6-10 never came into view during **KB-ANSWER-02** — the carousel's 60-second walk (`PRESET_CAROUSEL_ACTIVE_MS`) ran from mount only, an Ask never restarted it, and Right at the last visible chip claimed the move without pulling a new one in (deliberate, so Steam's own idea of "past it" — the Quick Access rail — never fires). **Fixed at the desk 2026-09-04** (D58 #3): when a pinned batch has entries beyond what the carousel's history currently holds, Right at the last chip now pulls the next one in (`nextFrozenHistoryEntry` in `carouselState.ts`, mirroring how Left at the edge already pulls an earlier one back via `requestFocus`); scoped to a frozen batch only; the normal (unpinned) carousel has no fixed "next" entry to pull ahead of its own timer, so Right there still just holds as before. Separately, every mode's 60-second walk now restarts when an Ask completes (`askRestartToken`, bumped by `MainTabPresetRow` on the `isAsking` true→false edge) — needed because a pinned batch always reseeds to its first three entries verbatim, so the seed text alone never signals that an Ask happened. Frontend: 5 new tests in `carouselState.test.ts`, 4 in `presetRowNav.test.ts`, 2 in `MainTabPresetAnimatedChips.test.tsx`, 4 in the new `MainTabPresetRow.askRestart.test.tsx`. **On-Deck:** pin plan 31 batch 1 (11 sentences, [31-deck-verification-round.md § 4](planning/31-deck-verification-round.md)); wait 60 seconds; Right at the edge should reach chips 6 through 11 in order; then ask any one question and confirm the walk resumes (chips keep advancing) rather than staying stalled. |
| KB corpus schema v3 / variant gate | KB-VARIANT-01 | Open | PR1, 2026-08-05. A pre-v3 corpus baked bare documents at the same model and the same dimension, so nothing but the manifest's `embedding_variant` can distinguish it; the gate fails closed. **Steps:** point `rag_corpus_path` at a **pre-bump** corpus and confirm retrieval still attaches cards but Show details reads **Keyword search (embed unavailable)** — not an error, not a missing block. Then rebuild and confirm it flips back to **Keyword + meaning**. Backend covered by `test_v2_corpus_refuses_hybrid_and_says_why` and `test_mismatched_embedding_model_refuses_hybrid` |
| KB relevance floor | KB-FLOOR-01 | Open | PR1, 2026-08-05. Weak matches were injected and cited on essentially every Strategy Ask. The floor is deliberately **loose** (R3) and PR2 tightens it, so the on-Deck question is the clean-degrade direction: ask something wholly off-topic in a troubleshooting-shaped Ask and confirm the KB chip reports **skipped** rather than attaching a junk tip. **Watch for the opposite failure too** — if the KB stops attaching on ordinary Asks, the floor is too strict and that is a regression, not a fix |
| KB follow-up Ask retrieves on the question | KB-FOLLOWUP-01 | Open | PR1, 2026-08-05. Every follow-up Ask used to search the follow-up header instead of the user's words, so all follow-ups retrieved the same boilerplate. **Steps:** run a Strategy Ask, then use a reply refinement chip with a question naming a *different* corpus entity, and confirm Show details cites the entity the follow-up named — not the one from the parent turn. The mechanism is unit-covered; **the wiring is not**, because there is no test harness for `game_ai_request`, which is exactly why this row exists |
| KB transparency matches what the model got | KB-TRANSPARENCY-01 | Open | PR1, 2026-08-05. Trust tier now reports the **lowest** tier in a mixed block, over-budget blocks drop **whole cards** and keep the end sentinel, and `kb_attached` is recorded **after** stacking so a block starved by Proton logs reports `attached=false`. The starvation path needs a large Proton log plus a KB hit on the same turn to reproduce on-device |
| KB strategy cards in every Ask mode (D17) | KB-ASKMODE-01 | **Verified (Deck) 2026-09-05** — count half by probe 2026-09-03, noise direction by probe, and the uncovered-title check on screen 2026-09-05 (Sifu running: a Deep Rock boss question attached no cards from any game, `Retrieval: Keyword search`, `unresolved`, no embed). Black Mesa is not on the Recent Games shelf, so Sifu stood in | **Re-run 2026-09-03 by probe: the count half now passes in both directions.** Same probe and setup as KB-EXPERT-01 (corpus `2026.09.01`, DRG Survivor `2321470`, Deck-local Ollama with `nomic-embed-text`; raw output in `runs/round31-laneB-probes-1.txt`): *"how do i beat the glyphid dreadnought"* attaches **1 / 3 / 5** cards in Speed / Strategy / Expert (was 1 / 1 / 1), and the keyword candidate pool (`--pool`, floor 0.0) now holds five cards — Glyphid Dreadnought 15.98, Dreadnought Twins 8.24, Exploder 3.72, Praetorian 3.69, Acid Spitter 3.64 — where the 2026-08-17 run found one. The noise direction also holds by probe: the off-topic *"thank you very much"* in Speed with the game's app id attaches nothing (`no_hit`). Owed on screen: the uncovered-title check (Black Mesa, plan 31 session 2). **Both bugs this row exposed are now fixed in code** — the vector half has its own recall pass (**KB-RECALL-01**) and Expert is on the explicit route (**KB-EXPERT-01**) — so the counts below should be re-measured before this verdict is quoted again. Prior run: **On-Deck 2026-08-17** (bonsAI 0.5.0, corpus `2026.08.16`, DRG Survivor `2321470` running, Deck-local Ollama). **The D17 half passes:** the same boss question attaches cards in **all three** modes — Speed, Strategy and Expert each returned the `Glyphid Dreadnought` card with `gate=True domain=strategy method=hybrid`, which is exactly what D17 promised. Screenshots `DeckCapture_20260817_210521` (Speed) / `_210738` (Strategy) / `_210917` (Expert). **The count half fails, in both directions.** *"how do i beat the glyphid dreadnought"* attaches **1 card in every mode** — not 1/3/5 — because the candidate pool is 1 and no budget can fill what retrieval never found (→ the ★★★ *vector half adds no recall* bug). And *"what class should i pick"* attaches **2 in Strategy but 1 in Expert**, so the count does not merely fail to rise, it **inverts**: Expert carries the largest budget (5) and the strictest floor (4.0 vs Strategy's 1.0) at the same time (→ the ★★ *Expert attaches fewer cards than Strategy* bug). **Method note for whoever re-runs this: the Show details ladder never prints a card count** — it prints retrieval method, trust tier, notes and timings ([transparency_service.py:457](../py_modules/backend/services/transparency_service.py)), so this row **cannot be run from the screen**. It was settled with **`scripts/probe_deck_kb_retrieval.py`**, which calls the same retrieval the Ask path calls, against the corpus installed on the Deck, and counts the card headers in the rendered block: `ssh deck@$DECK_IP 'python3 - --pool' < scripts/probe_deck_kb_retrieval.py`. `--pool` prints the FTS candidate pool at floor 0.0, which is what separates *"the corpus has nothing to say"* from *"BM25 could not find the card sitting right there"*. Both bugs reproduce from one run. If a card count is ever added to the ladder, this row can move back onto the screen. **Still owed:** the noise direction and the uncovered-title check below, neither of which this session reached. Prior context: 2026-08-06. Strategy cards used to require the **Strategy** Ask mode; Speed and Expert got none. **Steps:** with a covered game running (DRG Survivor `2321470` or L4D2 `550`), ask the same boss question in **Speed**, **Strategy** and **Expert** and confirm all three attach cards — and that the *count* still rises with the mode (Speed 1, Strategy 3, Expert 5). **Then check the noise direction, which is the risk of this change:** with the game still running, ask something unrelated to it in **Speed** (e.g. *"what time do the shops close on a sunday"*) and confirm the KB attaches **nothing** — not a generic genre card. In **Strategy** mode that same question is still allowed its genre-card consolation; that difference is intentional. **Third:** launch a game the corpus does not cover and ask a boss question — confirm you get **no** cards rather than another game's. Backed by `test_game_knowledge_is_not_gated_on_ask_mode`, `test_implicit_route_attaches_nothing_rather_than_a_generic_card`, `test_uncovered_game_does_not_get_another_games_cards` |
| KB compat routing widened (D16) | KB-ROUTER-01 | Open | **Retrieval half PASS by probe 2026-09-03.** Read-only retrieval probe over SSH (corpus `2026.09.01`, Speed mode, no app id given; raw output in `runs/round31-laneB-probes-1.txt`): sentences (1) to (4) below attach the compat tips `storage`, `steam_input`, `anticheat` and `emudeck` respectively, each with `notes=compat_tips`. One caveat: the probe still resolved Deep Rock Survivor (`2321470`) as the game in context, so this was measured with a covered game present rather than with nothing running — the four routed to the tips anyway. Still owed on screen (plan 31 sessions 1 and 2): the **Source: shared troubleshooting tips** line in Show details, the Proton-log permission hint staying absent, and the Strategy-direction sentence with a covered game running. 2026-08-06. Troubleshooting Asks now reach the tip sheet when they **name a topic the corpus covers**, not when they contain the literal word `deck` or `proton`. **Steps:** KB on, corpus installed, then ask each of these in Speed mode with no game running and confirm each attaches tips with **Source: shared troubleshooting tips** — (1) *"I'm out of room and want my installs on the memory card instead"*, (2) *"the game only responds to the touchpad and ignores the sticks"*, (3) *"I can play alone but online kicks me out straight away"*, (4) *"my playstation 2 games run at half speed on the handheld"*. None of the four attached anything before this change. **Then check the other direction, which is the risk:** in Strategy mode with a covered game running, ask *"the slow-motion aiming barely lasts, how do I get more of it"* and confirm you get **strategy** cards, not a troubleshooting tip — that phrasing routed to compat before word-boundary matching went in. Also confirm the **permission hint** for Proton logs did **not** start appearing on ordinary troubleshooting Asks: that gate was deliberately left alone. Backend covered by `tests/test_compat_topic_router.py` (9 tests) **Deck 2026-09-04 PASS, all four sentences** routed to `compat_tips` with `Source: shared troubleshooting tips`. Entry moved to Done. |
| Wiki sources are credited on the reply | KB-ATTRIB-01 | **Partial (both sub-checks now closed; 2026-08-14 date check still owed)** | **Negative case passes — on-Deck 2026-08-17** (corpus `2026.08.16`): three Asks on DRG Survivor `2321470`, whose cards are all maintainer-authored, rendered the knowledge-base chip with **no credit block, no source host, no capture date and no accent**, while the same build showed the full `theportalwiki.com · CC-BY-4.0` block on Portal 2 the day before. Confirmed from the other end too — retrieval returns `sources: []` for these cards, so there is nothing for the UI to drop. Screenshots `DeckCapture_20260817_210521` / `_210738` / `_210917`. This is the check that keeps the ornament meaningful: it appears on sourced cards only. **Accent-distinctness sub-check also closed 2026-08-17** — maintainer eye on the panel with `Routed gemma4:e2b-it-qat` (amber, `open_weight`) and the credit block on screen together: reads as distinguishable, no token change needed. That was the sub-check that looked like a fail in `DeckCapture_20260816_233808_game`; the camera flattened the two ambers more than the panel does. **Remaining:** the 2026-08-14 capture-date check (L4D2 must read `as of 2025-04-05`, not today, and must not move across a corpus rebuild). **On-Deck 2026-08-16** (build `6329577`, corpus `2026.08.16`): the positive case passes — a Portal 2 (`620`) Strategy Ask surfaced **`theportalwiki.com · CC-BY-4.0 · as of 2026-08-09`** under Show details with the card **`Portal 2 — Wheatley`** beneath it, credit accent on the block, capture date present and not today's. **Still Partial on one sub-check, and it looks like a fail:** the row requires the credit accent be *visibly distinct* from the amber an `open_weight` model chip uses, with both on screen at once. They were — `Routed gemma4:e2b-it-qat` sat four rows above the credit block — and in `DeckCapture_20260816_233808_game` the two ambers read as the same colour. Needs a maintainer eye on the panel, then likely a token change. Prior context: 2026-08-09. Source credit was collected per card and then dropped before the UI, so nothing was ever attributed. **Steps:** with the seed corpus installed and KB on, run a Strategy Ask on **Portal 2** (`620`) that hits a wiki-sourced card — e.g. *"how do gels work"* / *"wheatley boss"* — then open **Show details**. Confirm the knowledge-base chip's **outline is the credit accent while it is not the selected chip** (that is the whole point: the fill only paints on the active chip), and that expanding it shows **`theportalwiki.com`** with **`CC-BY-4.0`** beside it and the card title beneath (D20 publish path — corpus distributed as one CC BY-SA 4.0 work; per-card licence stays as shown here). **OoT / Queen Gohma** still credits if attached — a maintainer-authored card, so it shows **no** credit and **no** date (the King Dodongo / Water Temple GFDL cards were dropped from the seed 2026-08-14; GFDL does not mix with the corpus's CC BY-SA 4.0 label). **Then the negative case:** ask something that returns only maintainer-authored cards (e.g. DRG Survivor) and confirm the chip has **no** accent and **no** credit block — an ornament on every turn teaches people to ignore it. **Then check it does not read as a model chip:** the accent must be visibly distinct from the amber an `open_weight` model chip uses; put both on screen at once by asking with an open-weight model selected. Backed by `tests/test_source_attribution.py` and `contextChipsFromSnapshot.test.ts`. **Added 2026-08-09 with the dump-sourced cards:** the credit line now ends with a capture date, so a Left 4 Dead 2 Ask must read **`left4dead.fandom.com · CC-BY-SA-3.0 · as of 2025-04-05`** — the snapshot date, *not* today. Rebuild the corpus and confirm the date does not move. A maintainer-authored card still shows no credit and no date **Deck 2026-09-04:** the owed capture-date check passed — `combineoverwiki.net · CC-BY-SA-4.0 · as of 2026-08-09` over three named cards. Entry moved to Done. |
| Published corpus ships ATTRIBUTIONS.md | KB-ATTRIB-02 | Verified (Deck) | **On-Deck 2026-08-15:** downloaded the published `2026.08.14` corpus onto a real Deck from the live HF primary (758507/758507 bytes, sha gate passed) and confirmed `ATTRIBUTIONS.md` (8628 bytes) sits beside `corpus.db` under `/home/deck/.bonsai/rag`, opens with the redistribution header, names the CC BY-SA 4.0 umbrella, and lists every third-party host that can attach — `combineoverwiki.net`, `theportalwiki.com`, `*.fandom.com`. Retrieval on the same corpus then returned those exact hosts as reply sources (Portal 2 → `theportalwiki.com`, HL2 → `combineoverwiki.net`, L4D2 → `left4dead.fandom.com`), so the file's source list and what actually attaches agree. Prior context: 2026-08-09 (plan **15** Stage 5). Distribution credit, not the reply chip. **PC / build:** `python scripts/build_rag_db.py --seed --out build/kb-attrib` — `ATTRIBUTIONS.md` sits beside `corpus.db`, opens with **May I redistribute this corpus?**, and every distinct wiki host in the DB appears under **Third-party sources**. Automated: `tests/test_build_rag_attributions.py` (ATTR-5.1 / 5.2). **Added 2026-08-14:** `tests/test_rag_corpus_download.py` covers the client side end-to-end — a manifest this repo's own builder produces installs cleanly (round trip against a real `build_corpus(seed=True)` output), mirror fallback, chunk/decompressed-db checksum verification, and confirms `ATTRIBUTIONS.md` is written from the manifest's inline field and never downloaded as a separate file (the property this row exists to catch a regression in). `scripts/publish_corpus.py --check` gates a build on the D20 license allowlist and manifest self-consistency before it can be pushed. **On-Deck (after Phase 6 public install path):** install a published corpus (Ollama → Knowledge base → Download/Update, not only Dev-tab seed), confirm `ATTRIBUTIONS.md` is present next to the installed DB under the corpus path, and that it names every third-party source that can attach on a Strategy Ask for a covered title. Plugin zip must still contain **no** corpus files (`NOTICE` + `plugin_zip_corpus_guard`). Closes the distribution half of **KB-SMOKE-01** Partial once HF/GitHub publish lands |
| Named chat slots v2 + v3 redesign (LB/RB carousel, disk persistence, inverted layout) | CHAT-SLOTS-V2-01…06, CHAT-SLOTS-V3-01…10 | **Open — fix landed 2026-08-16, unverified on device** | **Third fix (2026-08-30, device QA):** with the row finally rendering, the first D-pad run showed it was **not a focus stop** — Down went tab strip -> preset chips and Up went preset chips -> tab strip, never landing on it (`runs/CHAT-SLOTS-V3-01-down-empty-slot.json`, `runs/CHAT-SLOTS-V3-01-up-from-presets.json`). Steam treats a `Focusable` as a container unless something marks it as a stop; the row's children are plain spans and its A handling is on `onButtonDown`, not `onActivate`, so its div carried no `tabindex` while every working Focusable div in the panel had `tabindex="0"`. Fixed with `focusable: true`; re-run lands on the row (`runs/CHAT-SLOTS-V3-01-down-after-focusable-fix.json`). **First automated device pass, 2026-08-30.** Driven through the ESP32 bridge (real Steam-routed presses, `gpfocus` oracle), evidence under `runs/`. **CHAT-SLOTS-V2-05 — the P-0 bumper spike — PASSES**, so the plan stands and the sixth-*Chats*-tab fallback is off the table. Also passing: V2-06, V3-01, V3-02, V3-03, V3-08. Partial: V2-02 and V3-04, both failing only on return-focus after a modal (PICKER-FOCUS-01, out of scope for this redesign). Not runnable without real Asks: V3-05a/05b, V3-06a/06b/06c, V3-07. Three bugs found and fixed in the pass: the row was not a focus stop, the create-position title lost on specificity, and (earlier) the ask-bar dock floated mid-panel. **Redesign v3 (2026-08-30):** the slot row's dot language (hollow cyan ring = generating, solid green = finished unread) has its first automated cover in `src/features/chat-slots/ChatSlotRow.test.tsx` — the feature's first component test. In progress against [28-named-chat-slots-v3-implementation-plan.md](planning/28-named-chat-slots-v3-implementation-plan.md); new rows land under `CHAT-SLOTS-V3` in [testing-manual.md](testing-manual.md) as each work item ships. **Second fix (2026-08-30, W0):** the row was not reachable on device at all. The step-8 extraction `dba34e7` (2026-08-03) moved `<MainTab>` into `useMainTabPayload`, which enumerates props by hand; the chat-slots commit `bc9a5df` (2026-08-09) added its props to `index.tsx` and `MainTab` but never to that hook, and `MainTab.tsx:152` gates the row on four callbacks being present. All the props are optional, so `tsc` stayed silent. `askStopped` and both nested-modal callbacks were dropped by the same hop. W0 threads all nine through the hook's destructure, JSX and dependency list — **run CHAT-SLOTS-V2-01…06 (especially -05, the P-0 bumper spike) only after W0 is deployed.** **Fix (2026-08-16):** the Ask submit path never called `ensureActiveSlotForAsk`, so no slot id was ever sent. `useBonsaiAskOrchestration` now resolves one before the RPC and sends it; `index.tsx` wires it through a ref because `useChatSlots` is declared after that hook. Covered by two tests in `useBonsaiAskOrchestration.test.ts` (mutation-checked). **On-Deck verification — do this first, it gates all six rows:** with the plugin redeployed, run **one** Ask, then confirm (1) the plugin log contains **no** `chat_slots: no slot for request_id=` line for it, and (2) `/home/deck/homebrew/settings/bonsAI/chat_slots/` now exists and holds `index.json` plus a slot file containing **both** the question and the answer. Only then run 01…06 — before the fix a reopen test would have read as "empty thread" and pointed nowhere. **Original report follows.** **Found 2026-08-16** during KB QA, from the plugin log rather than by looking for it. **Every Ask logs `chat_slots: no slot for request_id=N — assistant turn dropped`** — both Asks that session (`request_id=1`, `=2`), each after a successful 23-second reply that rendered fine on screen. **Nothing is being persisted.** The v2 store is `chat_slots/` ([chat_slot_service.py:22](../py_modules/backend/services/chat_slot_service.py)) and **that directory does not exist** under `/home/deck/homebrew/settings/bonsAI`; the only thing there is the pre-v2 `chat_threads/`, last written **2026-07-17**, months before v2 landed. **Mechanism:** the slot id is parsed out of the question text — `chat_slot_id = Plugin._parse_chat_slot_id(question)` at [main.py:2655](../main.py) — and *both* the user turn and the `_chat_slot_by_request` mapping are recorded only inside `if chat_slot_id:`. When the frontend sends no id, the user turn is dropped **silently** and the assistant turn then dropped **loudly** at [main.py:2465](../main.py), which is why the log shows only half the damage. So the id is not reaching the backend, or is not in the form `_parse_chat_slot_id` expects. **This blocks all six rows** — 04 (QAM reopen pending question) and disk persistence generally cannot pass while turns are dropped, and a reopen test would read as "empty thread" rather than pointing at the cause. Prior: **Automated 2026-08-09.** `chat_slot_service.py` + ownership tests; `chatSlotTurns.test.ts` (trailing user turn); `useChatSlots.test.ts` (sync ref). **On-Deck required before Completed:** all six rows in [testing-manual.md](testing-manual.md) — especially **03** (mid-Ask routing) and **04** (QAM reopen pending question). **P-0 bumper spike:** record LB/RB suppression result in [major-redesign.md](major-redesign.md) § 7 R1 on first device pass **Deck 2026-09-04:** V3-07 PASS (the "12 earlier" pill is a D-pad stop, A expands it into 13 header rows and focus lands on the first revealed row) and V2-03 PASS (RB to another slot mid-stream, LB back, the reply landed in A with question and its 5,749-character answer). **V3-05a FAILS:** slot B showed a Strategy branch block belonging to A, still there after A finished, and B's ask bar did not read busy — filed under Bugs as a slot-scoping defect. |
| Session context strip counts the newest turn twice | SESSION-CONTEXT-DEDUPE-01 | **Verified (Deck)** | **Fixed 2026-08-27.** After one completed Ask the header read *"Session context (2 turns)"* and the expanded list showed the same question twice, always *n + 1* — the newest turn was in `askThreadCollapsed` (via the post-Ask slot reload) **and** still the live turn (`transparencySnapshot` unchanged), with nothing de-duplicating them. `SessionContextStrip` now drops the live row when its question matches the newest archived row. Unit: `SessionContextStrip.test.tsx` (duplicate collapses to one row; a genuinely different live turn keeps two). **On-Deck 2026-08-27, confirmed by the rig with real controller presses over the ESP32 bridge** — asked *"what time is it"* in Strategy mode (no game running), waited for the reply, and read the panel: header was **"Session context (1 turn) ▸"**, not (2 turns); D-pad'd to the header, pressed **A**, expanded to **"(1 turn) ▾"** with exactly one row reading *"what time is it"* — no duplicate. Unblocks [20-frozen-chip-qa-batches.md](planning/20-frozen-chip-qa-batches.md) § 4.3. |
| Session context strip counts every archived turn, not just the newest | SESSION-CONTEXT-COUNT-01 | **Verified (Deck)** | **Failed 2026-08-27, fixed and re-verified the same day** (Batch B § 4.3, unblocked by the same day's duplicate-count fix). *First half passed from the start:* three questions asked back to back — the strip read **Session context (3 turns)** and expanded to exactly three rows, one per question, in order. *Second half failed:* closed the QAM, reopened, and the strip read **(1 turn)** with a single bubble, while the saved chat on disk held every entry. **Cause:** nothing remembered *which* saved chat the session was in. The pointer came only from the modal-survival snapshot, which a QAM close/reopen does not write — measured by reading browser storage on the Deck and finding six `bonsai:` keys, none of them a slot id. It now persists under `bonsai:active-chat-slot`. **Passing run, real controller presses:** two questions, QAM closed and reopened, both turns back with byte-identical slot ids and the strip reading **(2 turns)**. **Check the other direction in the same pass, because it is what could break:** Settings → **Clear cache…** → **Clear**, then close and reopen — the thread must stay empty (D32). Confirmed on device: empty, with the earlier question text absent from the whole panel. | **Automated 2026-08-23.** `SessionContextStrip` only ever listed a slot-restored turn if it carried its own transparency snapshot, and the backend never saved one per turn — every turn but the newest was invisible, so the strip's header always read *"Session context (1 turn)"* no matter how many questions had been asked. `chat_slot_service.py` now saves a trimmed snapshot alongside each assistant turn; `chatSlotTurns.ts` reads it instead of a hardcoded empty value. Unit: `test_chat_slot_service.py`, `test_chat_slot_ownership.py`, `test_transparency_kb_retrieval.py` (`TransparencySnapshotForChatSlotTests`), `chatSlotTurns.test.ts`, `archivedTurnTransparency.test.ts`. **On-Deck:** in one named chat slot, ask three different questions back to back — for example *"what time is it"*, then *"what should I do next"*, then *"is there anything else nearby"* — waiting for each answer before asking the next. After the third answer, open **Session context** at the bottom of the panel: the header must read **Session context (3 turns)**, not 1, and expanding it must show a row for each of the three questions. Then close and reopen the QAM (or switch to another chat slot and back) and repeat the check — the count must survive the reload, not just hold during the live session. |
| Named-entity consent reads name-first phrasing | STRAT-ENTITY-01 | **Verified (Deck) 2026-09-05** — all seven sentences. Naming half by calling `extract_strategy_asked_entity` on the deployed build over SSH: `king dodongo fight` and `how do I beat king dodongo` both name `king dodongo`; `raphael fight strategy` names `raphael`; `fire boss that flies out of holes`, `how to raise a skill fast`, `best build` and `im stuck` all name nothing (the first two used to invent *fire* and *fast*). Wiring half on screen with Ocarina of Time running as the Ship of Harkinian shortcut (resolved by alias, `KB: 16 sections`): the name-first sentence rendered **unfenced**, zero spoiler blocks; the no-entity sentence rendered **fenced**, one *Spoiler — tap to show* block. **Method note:** the Show details ladder never prints the named entity, so the naming half cannot be read off the screen — use the extractor over SSH | 2026-08-09. Asking about a boss **by name** is supposed to keep tactics for that boss out of spoiler fences; the detector only understood verb-first sentences, so controller phrasing never triggered it. **Steps:** with a `protect_progression` title running (OoT `413150`, BG3 `1086940`, or Portal 2 `620`), ask in Strategy mode using **name-first** phrasing — *"king dodongo fight"*, *"raphael fight strategy"* — and confirm tactics for that entity render **unfenced**, while anything else the reply touches keeps its fence. Compare against the verb-first form (*"how do I beat king dodongo"*), which should behave identically; before this change only the second one did. **Then the safety direction, which matters more:** ask *"how to raise a skill fast"* (Sims 4 `1222670`) and *"fire boss that flies out of holes"* (OoT) and confirm **Show details** reports **no** named entity — both used to invent one (`fast`, `fire`). Also confirm a `needs_clarification`-style question (*"best build"*, *"im stuck"*) names nothing. Backed by `tests/test_asked_entity_extraction.py`; **the wiring is not unit-covered**, because `game_ai_request` has no test harness — which is why this row exists. Related: **STRAT-SPOIL-DRG-01** |
| Every corpus title has cards | KB-COVERAGE-ALL-01 | Open | Rewritten 2026-08-09 — the original row tested the *empty* state for Portal 2 and Half-Life 2, and stage 6d carded them, so the empty state no longer exists for any title. **Steps:** with the seed corpus installed and KB on, launch each of the thirteen titles in turn and confirm the coverage chip reports sections rather than **"KB: none for this game"**. Spot-check one Ask per title and confirm the cards attached belong to the running game — the corpus is now 133 sections across 13 titles, so a cross-game leak has far more room to hide than it did at 39. **Then the alias path:** with no game running, ask something naming a title (*"hl2 ravenholm"*, *"drg survivor what class"*) and confirm the right game's cards attach and no other game's do. Backed by `test_every_corpus_title_has_a_spoiler_profile`, `test_every_label_names_a_card_that_exists`, and the profile contract fixture. **Renamed from `KB-NEWTITLE-01` 2026-08-28 (D30)** — that ID collided with the *KB reaches a title named in the question (D19)* row above; see [D30](audit/maintainer-decisions-locked.md#d30--which-of-the-two-kb-newtitle-01-rows-keeps-the-id). **Partly covered on-Deck 2026-08-22:** the alias path at the end of this row passes (see the D19 row); the thirteen-title coverage-chip sweep still needs the device, and the corpus is now **133** sections across 13 titles, not 119. |
| The eleven new titles answer on the device | KB-TRANCHE-01 | **Verified (Deck) 2026-09-07** | **Cards written 2026-09-06 (D69, plan 40). Two releases have shipped since this row was written:** corpus `2026.09.06` is installed on the Deck; corpus `2026.09.07` is published but not yet installed. **This row closes when W2-R2 runs** (below, from [plan 47](planning/47-kb-wave-two-session.md) § 8). 105 cards for Black Mesa, Hollow Knight, GTA V, GTA IV, DOOM Eternal, Doom 64, Super Mario 64, Mario Kart 64, Paper Mario: The Thousand-Year Door, Super Smash Bros. Melee, Fallout: New Vegas and Pikmin 2. **Steps on device:** install the release, then for each of a handful of the new titles ask one question through the Ask field with that game running or named in the sentence, Strategy mode, and check the reply carries a knowledge chip naming the source wiki and its licence, that the card's facts reach the answer, and that nothing that should be fenced arrives unfenced. Fallout: New Vegas is owned but **not installed** on the Deck, so either install it first or ask by name with nothing running. Emulated titles (the Mario pack, Melee, Pikmin 2, Doom 64) have no Steam AppID and resolve by name only, so they are the ones worth checking first: each appears **twice** in the library from two ROM-manager runs, and the shortcut names differ from the canonical titles (for example *Doom 64: Retribution*). Offer to pin the questions as frozen test chips before asking the maintainer to type anything. **Closed 2026-09-07 when W2-R2 ran on the released `2026.09.07` library:** every one of the twelve games attached a note about the right game. Evidence `runs/plan47-R2-twelve-games.json`. |
| Install the wave-two release | W2-R1 | **Verified (Deck) 2026-09-07** | **Setup:** the old library (`2026.09.06`) is installed. **Do:** Ollama tab → *Update knowledge base*. **Pass when:** the tab shows the new version and **293 notes**, and a game question's Show details names it. Uses no pinned chips. [Plan 47](planning/47-kb-wave-two-session.md) § 8, row W2-R1. **Ran 2026-09-07:** the Deck went from `2026.09.06` to `2026.09.07` — 293 notes, 25 games, 156 tips. Both download channels were checked first and were identical. Evidence `runs/plan47-R1-install.json`. |
| The twelve new games answer on the device (closes KB-TRANCHE-01) | W2-R2 | **Verified (Deck) 2026-09-07, search half** | **Setup:** Fallout: New Vegas installed. Pinned Batch R2, twelve chips, one question per game that already had a note before this wave. **Do:** launch each Steam title and ask in Strategy; ask each emulated title by name. **Pass when:** each attaches at least one note, the credit line names the wiki and a date, spoiler behaviour matches the title's setting, and the reply is about that game. [Plan 47](planning/47-kb-wave-two-session.md) § 8, row W2-R2 (batch sentences there). **Ran 2026-09-07, search half only:** all twelve games attached notes about the right game; the two questions that never name their game by name both picked up the game that was running. Fallout: New Vegas was still not installed, so it was asked about by name only, which the row allows. Evidence `runs/plan47-R2-twelve-games.json`. |
| The filled gaps find their own note | W2-R3 | **Verified (Deck) 2026-09-07** | **Setup:** none beyond the game running or named. Pinned Batch R3, eight chips, each with the note it should find written in the plan. **Do:** ask each. **Pass when:** the named note attaches and the reply uses it rather than general memory. [Plan 47](planning/47-kb-wave-two-session.md) § 8, row W2-R3. **Ran 2026-09-07:** all eight found the note written for them, every one in the top three, three of them first. Evidence `runs/plan47-R3-filled-gaps.json`. |
| Plainly-worded problems reach the tips | W2-R4 | **Partial (Deck) 2026-09-07** | **Setup:** nothing running. Pinned Batch R4, nine chips — eight problem sentences plus one control. **Do:** ask each in Speed mode. **Pass when:** the eight attach a tip on the right subject and *"thank you very much"* attaches nothing. [Plan 47](planning/47-kb-wave-two-session.md) § 8, row W2-R4. **Ran 2026-09-07:** all eight problem sentences reached a tip and the control attached nothing, but one of the eight — the question about the TV picture being in the wrong spot — got a tip about Big Picture Mode versus Desktop Mode, which does not answer it. Evidence `runs/plan47-R4-problems-reach-tips.json`. |
| The "not in my notes" line | W2-R5 | **Failed (Deck) 2026-09-07** | **Setup:** voice on, thinking Medium, Hades running. Pinned Batch R5, two chips. **Do:** ask the hydra question in Strategy, then the Megara question in Strategy, then the hydra question again in Speed, then turn the library off and ask the hydra question once more. **Pass when:** the line appears only on the first ask, reads exactly *"Not in my notes — this answer is from the model's own knowledge."*, and sits below the answer without breaking the D-pad walk. [Plan 47](planning/47-kb-wave-two-session.md) § 8, row W2-R5. **Ran 2026-09-07: the line did not appear on the first question**, so the row fails and the remaining three questions were not run. Asking about the Bone Hydra, a boss that does not exist in Hades, returned a confident made-up answer about weapons with no line saying it came from the model rather than the notes. Ten questions tried beforehand against covered games all attached a note, so the line has no path to ever show. Filed as its own bug on the roadmap. Evidence `runs/plan47-R5-not-in-notes.json`, `runs/plan47-probe-notinnotes.json`. |
| The two bug fixes | W2-R6 | **Failed (Deck) 2026-09-07, both halves** | **Setup:** Speed mode, Deep Rock Galactic: Survivor running, voice on. Pinned Batch R6, three chips. **Do:** ask all three, then exit the game and read the line under the question box. **Pass when:** no reply carries a code-like line, and the line under the box stops naming the game within a few seconds of exiting. [Plan 47](planning/47-kb-wave-two-session.md) § 8, row W2-R6. **Game-naming half, ran 2026-09-07:** Hades was exited with the panel open, and the line under the question box still named it straight after, 31 seconds later, about four minutes later, and after closing and reopening the Quick Access Menu. **The opposite failure was measured the same evening:** with the panel already open, Deep Rock Galactic: Survivor was launched and the line read *no active game detected* for 48 seconds and through a close and reopen of the menu, while Steam's own list had the game running throughout. Restarting the plugin corrected the line at once, both times — so the panel reads this once at start-up and never listens for a change. **Stray-computer-text half, ran later the same evening and failed:** two of the three questions were asked with the game running and the panel recognising it; both replies ended in a code box holding nothing but the raw power line, `{"tdp_watts": 7, "gpu_clock_mhz": null}` and `{"tdp_watts": 13, "gpu_clock_mhz": null}`, read out of the page. The cleanup that was meant to remove that line deliberately leaves anything in a code box alone, and the plugin's own instruction asks the model to put the block in a code box — so the one shape the plugin asks for is the one it cannot clean. Evidence `runs/plan47-R6-bug-fixes.json`, `runs/plan47-R6-stray-computer-text.json`. |
| The corrected Black Mesa water note | W3-R5 | **Passed (Deck) 2026-09-07** | **Setup:** nothing running, Strategy, library version 2026.09.08 installed from the Ollama tab's *Update knowledge base* button. **Do:** ask *"how do i cross the flooded rooms in black mesa without getting shocked"*. **Pass when:** the reply says the current is constant and does not tell anyone to wait for a gap or move while the water is dark. **Ran 2026-09-07:** the reply said the water has a constant zap, said in as many words not to try to time it, listed the things to cross on, and pointed at the red wall switch or breaker that makes the water safe for good. Every claim comes from the corrected note. Evidence `runs/plan48-R5-blackmesa-corrected-note.json`. **One thing to watch, filed as its own bug:** the menu at the end of the reply asked *"Where are you at in Half-Life 2?"* — a different game from the one asked about. |
| The ring behind the corner icons, measured | W2-R7 | Open | **Setup:** a long reply on screen. No pinned chips. **Do:** walk down the whole reply with the D-pad. **Pass when:** this row only measures — the fix is not built yet. Record the visible percentage at every stop, especially the size of the stop that earlier read 67% behind Retry, which no evidence file yet holds. A stop only counts as a failure if a word of text is hidden; a corner point under an icon is where the icon is meant to be. [Plan 47](planning/47-kb-wave-two-session.md) § 8, row W2-R7. |
| The five August rows from the retrieval rework (optional) | W2-R8 | Open | One batch per row, run as each row already says elsewhere in this table: **KB-VARIANT-01**, **KB-FLOOR-01**, **KB-FOLLOWUP-01**, **KB-TRANSPARENCY-01**, **KB-KILLSWITCH-01**. Passes when each of those rows' own conditions pass. [Plan 47](planning/47-kb-wave-two-session.md) § 8, row W2-R8. |
| Follow-up questions remember what you just asked about | W3-R4 | **Partial (Deck) 2026-09-07** | **Setup:** Hades running, Strategy mode. **Do:** ask about Megara, then "what about her second phase", then ask about the Bone Hydra, then "what about its second phase" again. **Pass when:** the second and fourth replies are about the boss just asked about, not a different one; the question shown on screen does not change; Show details shows the remembered name was added to the search. Only carries a name between two game questions in Strategy or Expert mode — a troubleshooting question neither stores one nor picks one up. [Plan 48](planning/48-kb-wave-three-session.md) § 8, row W3-R4. **Ran 2026-09-07, half pass:** the pair actually asked was Deep Rock Galactic: Survivor's Glyphid Dreadnought — "how do I beat the Glyphid Dreadnought", then "what about its second phase". **The looking-up half works**: on the device the right boss's note moved from third place to first once the remembered name was carried into the search. **The answering half fails**: the reply was about a different boss, the Dreadnought Twins. The wrong boss's note was still attached one place below the right one, and its wording (health bars drifting apart, both bosses turning immune while they heal) reads more like an answer to "second phase" than the right note does, so the model picked it. Ranking the right note first is not enough while a better-matching wrong note is still in the pile. A call is waiting for the maintainer on how to finish this. Evidence `runs/plan48-R4-followup-memory.json`. |
| The "No close match in my notes" line | W3-R7 | Open | **Not yet run on the device.** The exact sentence to look for is *"No close match in my notes, this answer leans on the model's own knowledge."* — note the comma, chosen by the maintainer 2026-09-07. **Setup:** nothing running, Strategy. **Do:** ask *"how do i tame a horse"* naming Black Mesa, *"qqqq zzzz wwww"* with Hades running, and *"how do i beat the bone hydra in hades"* with Hades running. **Pass when:** each reply carries the line once, below the answer, and the D-pad walk still reaches everything under it; and a normal question with a good answer — anything from Batch R3 — carries no line at all. **Also check the case that would annoy everybody at once:** with the meaning search unavailable (no embed model), no reply carries the line, because an unmeasured match is not a weak one. Built and measured off the device 2026-09-07 (D88); `runs/plan48-thin-match.json`. |
| The "No tip for this" line | W3-R2 | Open | **Not yet run on the device — wave three's Deck evening has not happened.** **Setup:** nothing running, Speed mode. **Do:** ask the five problem sentences that today reach the tip sheet but come back with a tip that does not answer them. **Pass when:** each is shown as routed to the tip sheet (Show details), no tip attaches, and the reply ends with the exact line *"No tip for this — this answer is from the model's own knowledge."* Never appears on the same reply as the older "not in my notes" line. [Plan 48](planning/48-kb-wave-three-session.md) § 8, row W3-R2. |
| Both the note search and the tip search can say "none of these fit" | W3-R3 | Open | **Not yet run on the device — wave three's Deck evening has not happened.** **Tip side, setup:** nothing running, Speed mode; ask the eight problem sentences that already reach the right tip today. **Pass when:** each still gets the right tip and the "No tip for this" line does not appear. **Note side has no Deck row yet.** A floor was added so a game question can attach no note at all, but it still attaches a note on four known questions that should get none — asking Black Mesa how to tame a horse, asking Portal 2 where to buy a house, and asking about a Hades boss that does not exist — because catching those would cost twenty or more correct answers elsewhere in the library. [Plan 48](planning/48-kb-wave-three-session.md) § 8, row W3-R3. |
| A written time budget for searching the notes | W3-R6 | **Failed (Deck) 2026-09-07** | **Setup:** a game running, Strategy or Expert mode. **Do:** run `scripts/probe_deck_kb_retrieval.py` three times. **Pass when:** every reading comes back at or under the 1.0-second budget written down in [knowledge-base.md](knowledge-base.md#time-budget-for-a-game-question-2026-09-07); the probe itself prints pass or over-budget. Every reading taken on the Deck so far would print over-budget, which is why the budget was written now. [Plan 48](planning/48-kb-wave-three-session.md) § 8, row W3-R6. **Ran 2026-09-07: the check itself gives a false all-clear.** It reported 23 to 38 thousandths of a second for the same search and printed pass, comfortably under budget — but a real question, asked on the same Deck in the same sitting, minutes apart, took 1.07 seconds for that same step and would have printed over budget. Run twice more in separate processes, the check read about 25 thousandths both times, so this is not a warm-up a fresh process pays for. The difference is that the check never runs the chat model and a real question does. A fix is in progress. Evidence `runs/plan48-R6-time-budget.json`. |
| KB retrieval eval — arms, splits, gate reachability | KB-EVAL-02 | Verified (PC), **re-run 2026-08-18** | **Harness re-aligned 2026-08-18 — it had drifted from production.** `_hybrid_retrieve` still ran the pre-recall-pass pipeline, so the arm called `rrf` measured code that no longer shipped. Now four arms: the old behaviour is kept as `rrf_rerank_only` so the recall pass has to keep earning its embed round trip. Re-run on the same fixtures: tune top-3 `rrf` **90.2%** vs `rrf_rerank_only` 86.3% vs keyword 88.2%; holdout unchanged at 80.6% for both fusion arms, and the locked verdict still reads **no separation** (n=36). **The finding that matters is the new recall slice: n=1.** Of 138 labeled cases, exactly one leaves the keyword arm with nothing to rank — `rrf` finds the card, `rrf_rerank_only` does not, which is the bug reproduced in the harness at last. But a fixture with one such case cannot gate a recall regression, and that is why the on-Deck bug shipped: these queries share vocabulary with the cards they match. **Fixture gap now tracked in [Backlog](roadmap.md#planned).** Prior: **PR2 stage 6d closed 2026-08-09.** Arms bake-off on deepened schema-v3 seed (119 sections / 124 tips) against `kb_eval_v2` (140 labeled; tune 104 / holdout 36). Holdout top-3: keyword 83.3% [69.4, 94.4] vs RRF 80.6% [66.7, 91.7] — **no separation** under the non-overlapping-CI rule; equal RRF weights locked. Compat gate-reachable 39/40 (D16). Harness: `tests/test_eval_kb_arms.py`. Report: [kb-retrieval-pr2-bakeoff-2026-08-09.md](archive/research/kb-retrieval-pr2-bakeoff-2026-08-09.md). Reproduce: `python scripts/eval_kb_embed_models.py --arms-only --force-rebuild`. On-Deck hybrid engagement still → **KB-RRF-01** |
| KB answer quality — what the model writes from the cards | KB-ANSWER-01 | Verified (PC), **baseline 2026-09-02** | **Shipped 2026-09-02 (decision D45, plan 30 W1).** `scripts/eval_kb_answers.py` runs the real Ask pipeline — `run_game_ai_request` on the real `Plugin` class with a stub `decky`, so retrieval, prompt, Ollama call, fence parsers and the safety guard are the shipped code — against a local Ollama with the Deck's own model tag `gemma4:e2b-it-qat`, over `tests/fixtures/kb_answer_eval.json` (37 cases across all 13 corpus titles plus one uncovered game; 3 samples each at the shipped temperature), and scores four judge-free checks: facts from the card kept (`must_mention`), nothing the card contradicts (`must_not_say`), spoiler fence only where due (`expect_fence`), branch menu on Strategy first turns (`expect_branches`), plus whether the expected card attached at all. **Baseline, corpus `2026.09.01`:** facts kept **90.9%** (90/99) · no contradiction 100% (9/9) · **fence not misfired 70.8%** (68/96) · fence present when due 100% (9/9) · branch menu when due **97.0%** (64/66) · no menu outside Strategy 100% (45/45) · expected card attached 100% (99/99) · 1.4 s per answer, 2.6 min per run. **The finding:** nine cases fence a harmless opening line on every sample (L4D2 Tank and Witch — a low-narrative title — HL2 antlions, Hades Theseus and Asterius, Fallout 4 deathclaw and legendary enemies, RDR2 "coming from GTA", both Ocarina cases); everything else is near its ceiling. Report [kb-answer-eval-2026-09-02-baseline.md](archive/research/kb-answer-eval-2026-09-02-baseline.md); every reply and system prompt in `build/kb-answer-eval/`. **Steps:** pull the model and `nomic-embed-text`, build `build/knowledge-base` (`python scripts/build_rag_db.py --seed --out ./build/knowledge-base`), run `python scripts/eval_kb_answers.py --label <name>`, compare the summary table with the baseline — a check moving ten points is a finding, one or two is noise. Synonym calibration of a `must_mention` group after a run is allowed and must be dated in the case note; no other edit to a case is. Not a Deck row: the Deck run is for feel (D45) |
| Spoiler fence misfire fix — measured before/after, confirmed on device | KB-ANSWER-02 | **Verified (Deck) 2026-09-03** | **The first change gated by KB-ANSWER-01.** Baseline: 28 of 96 low-story / named-entity samples carried a spoiler fence around a harmless opening line. Experiment 1 (drop only the *"every bonsai-spoiler block must appear above the branch fence"* sentence): misfires 0 of 96 but ending questions lost their fences too (0 of 9) — twice. Experiment 2 (replace both fence-format sentences with one plain *"do not fence"* line on low-narrative and named-entity turns, story titles with nothing named untouched): misfires **3 of 96** and **5 of 96** in two runs, ending questions fenced **8 of 9** both times, facts 93.9%, menu 97–100%. Experiment 2 is what ships in `_strategy_spoiler_policy_block` (unit tests `test_strategy_spoiler_policy_*_replaces_fence_format_sentences`, `_keeps_fence_format_sentences`). **Re-measured on the shipped text:** misfires **5 of 96**, ending questions fenced **9 of 9**, facts 91.9%, menu 98.5% ([kb-answer-eval-2026-09-02-shipped-fence-fix.md](archive/research/kb-answer-eval-2026-09-02-shipped-fence-fix.md)); the five left are all on the story-title arm the fix does not touch — Fallout 4 *"a legendary enemy healed back to full"* three times (no entity is extracted from that sentence), one Fallout 4 SPECIAL-stats sample, one Cyberpunk attributes sample. Reports: [kb-answer-eval-2026-09-02-drop-fence-placement.md](archive/research/kb-answer-eval-2026-09-02-drop-fence-placement.md), [kb-answer-eval-2026-09-02-fence-subtractive.md](archive/research/kb-answer-eval-2026-09-02-fence-subtractive.md), [-run1](archive/research/kb-answer-eval-2026-09-02-fence-subtractive-run1.md). **Deck steps — ready since 2026-09-02:** corpus `2026.09.01` is installed (SSH) and the five sentences, confirmed by the maintainer, are **pinned as frozen chips** (appended after the five chips another session had in the batch, so they are chips 6–10 of the rotation): *"in left 4 dead 2, how to beat tank"*, *"in half-life 2 the antlions keep coming up out of the sand"*, *"in hades, theseus and asterius keep killing me"*, *"in ocarina of time how do i beat volvagia"*, and one that must still fence, *"in red dead redemption 2 what happens to arthur at the end"*. The game is named in each so no title needs to be running (D19). **Strategy** mode, A on the chip fills the Ask field, press Ask; expect **no** fence on the first four, a fence on the fifth, and the branch menu on all five. Note the build on the Deck must include the 2026-09-02 prompt change (`_strategy_spoiler_policy_block`) — deploy first, or the run measures the old prompt. **On-Deck 2026-09-03 03:39–03:56Z** (corpus `2026.09.01`, build deployed 2026-09-02 19:15 local, Deck-local `gemma4:e2b-it-qat`, Strategy, no game running, AI character *Ali G* on, streaming on): **5 of 5 as expected.** Tank, antlions, Theseus and Asterius, Volvagia — **no spoiler fence** in the raw model output or the rendered turn, branch menu on all four; Red Dead ending — **one collapsed spoiler box** (*Spoiler — tap to show*) around the ending paragraph, menu present. 20–25 s per answer, 1,880–2,074 prompt tokens. Facts from the cards survived the character voice on three of four (rock-to-rock and vibration; Asterius first and sidestep the charge; Director stops sending commons and the environment as projectiles) — two slips worth knowing: the Tank reply turned *a punch near a drop kills **you*** into *kills **him***, and the Volvagia reply never named the Megaton Hammer (hookshot and lava only). Method note: only the first sentence went through its frozen chip (A on the chip filled the field verbatim, no submit); the chip row stops rotating 60 s after the panel opens (`PRESET_CAROUSEL_ACTIVE_MS`), so chips 6–10 of a ten-chip batch never came into view and sentences 2–5 were typed verbatim with `scripts/deck_send_ask.py` (VERIFIED each time) and submitted by bridge — same words, same path from the Ask button onwards. That rotation limit is filed on the roadmap |
| Answer first, then the menu — both shapes measured before a decision | KB-ANSWER-03 | Open | **Decided 2026-09-05 (D66): a test, not a change.** Question: when a Strategy first turn names a thing and its card matched, should the reply give the card's tactics first and then the menu, instead of a short orientation and then the menu? Examples of both shapes and the trade-offs are in the decisions file under D66. **PC half done 2026-09-07.** Measured on 61 questions, three runs each, both shapes on the same build with the same checks: tactics-first keeps more of the note's facts (76.6% to 79.5%), hides spoilers when it should far more often (77.8% to 88.9%), comes out clean on all three runs more often (60.7% to 67.2%), and is twelve words shorter (103 to 91) at the same speed. It contradicts its own note slightly more often (94.4% to 90.7%), but that is one extra question, not a spread, and both failing questions are the Pikmin 2 day limit, an open problem on either shape. **The maintainer read this table and took tactics-first on 2026-09-07 — it shipped.** **Deck half still owed, and it has changed shape:** this is no longer a read of two shapes side by side — it is a first read of the one shape that shipped. Pin three sentences, confirmed by the maintainer first, in Strategy mode with the character voice on as they use it, and read how the new answer sounds on the 300 px column. Suggested sentences, to be confirmed before pinning: *"in deep rock galactic survivor how do i beat the dreadnought twins"*, *"in hades how do i beat theseus and asterius"*, *"in ocarina of time how do i beat king dodongo"*. |
| KB hybrid kill-switch | KB-KILLSWITCH-01 | Open | PR2 stage 6a, 2026-08-06. New `rag_hybrid_retrieval_enabled` setting, default **on**, as a `ToggleField` in Developer tab → *Knowledge base (dev QA)*, directly under **Install seed knowledge base**. **Steps:** with a rebuilt v3 corpus and `nomic-embed-text` present, run a Strategy Ask and confirm Show details reads **Keyword + meaning**; turn the toggle off, Ask again, and confirm cards **still attach** while Show details reads **Keyword search (hybrid disabled)** with a zero *Embed* time. The distinct label is the point — reading *embed unavailable* here would send you hunting an Ollama fault that is not there. Backed by `test_kill_switch_leaves_keyword_search_working_and_says_so` and `test_hybrid_stays_on_when_the_setting_was_never_saved` |
| KB kill-switch toggle (focus graph) | FOCUS-GRAPH-DEV-KB-01 | Open | PR2 stage 6a, 2026-08-06. Plain Decky `ToggleField` in its own `PanelSectionRow` inside the existing *Knowledge base (dev QA)* section, so it inherits that section's vertical chain and needs no custom focus owner — but confirm on-Deck that D-pad **Down** from **Install seed knowledge base** reaches it and continues to the next section, per the new-focusable-control rule in [.cursor/rules/decky-focus-graph.mdc](../.cursor/rules/decky-focus-graph.mdc). **Verified on-Deck 2026-08-26 by script, nobody at the device — PASS.** Down from **Install seed knowledge base** → **Hybrid retrieval (meaning search)** → **On-screen debug HUD**, which is the first control of the next section (*Diagnostics*). 2/2 asserted steps (`runs/FOCUS-GRAPH-DEV-KB-01.json`). Note for future runs: the ring lands on the ToggleField's unlabelled inner div, four parents below the element carrying the label, so any text assertion has to read ancestor text — the first attempt reported both controls as "never reached" while sitting on them. |
| KB download (public HF/GitHub) | KB-SMOKE-01 | **Verified (Deck)** | Seed install works; publish tooling + client-side download path covered by `scripts/publish_corpus.py` and `tests/test_rag_corpus_download.py`. **First public push landed 2026-08-14** — HF dataset `qd313/bonsai-knowledge-base` and GitHub release `knowledge-base-v1`, corpus version `2026.08.14`, `compressed_sha256` `081af237…`, 758507 bytes, schema v3, 117 section + 124 compat vectors. **Backend verified on a real Deck 2026-08-15** (SteamOS 6.18.42-valve2, plugin files sha-matched to HEAD first): `run_rag_corpus_download` against the live HF primary returned `phase=done`, version `2026.08.14`, 758507/758507 bytes, sha gate passed, installing `corpus.db` + `corpus-manifest.json` + `ATTRIBUTIONS.md` to `/home/deck/.bonsai/rag` with 13 games / 117 sections / 124 tips / 117+124 vectors. `list_rag_storage_options` enumerated both internal (790 GB free) and SD card (1.2 TB) — the `statvfs` path is healthy on device. GitHub mirror failover verified from PC by pointing the HF URL at a nonexistent repo (same version and sha from the release asset). **Retrieval against that downloaded corpus, 7/7 cases** through `retrieve_knowledge_context` with the Deck's local Ollama: hybrid path active (`method=hybrid`, embed ~60 ms), right cards for the right game (Ravenholm → HL2 Ravenholm, blue gel → Portal 2 Repulsion Gel, tank → L4D2 Tank, Gohma → OoT Queen Gohma), compat question routed to a gamescope tip (D16), and the **cross-game leak guard held** — asking about Queen Gohma while Portal 2 is the running app returned the genre fallback, not OoT cards. Still Partial for the **UI half only**: QAM download progress row, D-pad focus, Remove/Cancel slots and Show details labels were not exercised. **Point release `2026.08.16` published 2026-08-16** (`compressed_sha256` `34bff336…`, 758502 bytes) — the first reproducible artifact, verified live on both channels from the maintainer PC. **UI half closed on-Deck 2026-08-16** (bonsAI 0.5.0, build `6329577`), run while the Deck still held `2026.08.14` so the version-compare path was genuine and not a no-op. All four states pass: (1) **Update** → the primary button becomes the progress row, completes, corpus reports `2026.08.16`; (2) **Update** again → correct no-op; (3) **Remove** → **Download** → storage picker → first-install path completes; (4) Strategy Ask on Portal 2 attaches cards. Deck log confirms each install and its target path. **Also verified incidentally: the SD-card install path.** Two of the three downloads landed on `/run/media/deck/3506ff22…/.bonsai/rag` rather than internal, and the later Ask retrieved 9 sections from that SD corpus — so the storage picker's SD option installs *and* serves retrieval, which is what the archived **KB-DOWNLOAD-SD** row wanted. Internal `/home/deck/.bonsai/rag` no longer exists on this Deck; the live corpus is the SD copy. **Caveat worth keeping:** at 758 KB the whole cycle takes ~0.9 s, so "the progress row advances" is barely observable and the row proves *completion*, not progress rendering. That sub-second window is also why **KB-CANCEL-01** could not be run at all |
| KB download Cancel (step 9) | KB-CANCEL-01 | **Open — not testable as written** | **Attempted on-Deck 2026-08-16 and abandoned: there is no cancel window.** The published corpus is 758502 bytes, so on a healthy LAN the whole download-decompress-install cycle takes **~0.9 s** — the Deck log shows `Downloading…` at 23:32:37.711 and `Knowledge base installed` at 23:32:38.610. Nothing in the plugin log recorded a cancel on any of the three downloads that session; the row cannot be exercised by hand at this corpus size. **What looked like a Cancel pass was not one:** the tester reported pressing the button twice before Remove reappeared, which was the **storage picker** — `onPrimaryClick = installed ? runUpdate : openStoragePicker` ([KnowledgeBaseSection.tsx:527](../src/components/KnowledgeBaseSection.tsx)) — press one opens the internal/SD modal, press two starts the download. Correct behaviour, unrelated to cancel. **To actually test this, the download has to be slowed:** throttle the Deck's link (`tc qdisc`), point `rag_corpus_path` fetching at a stalled local host, or add a dev-only delay. Until then the six frontend tests are the only coverage and the focus half is unproven. Prior context: 2026-08-05: `cancel_rag_corpus_download` wired into `KnowledgeBaseSection.tsx`; the backend RPC and its cancel event have existed since the download shipped, with nothing calling them. Cancel takes the action row's **second slot** while a download runs, so the row is Download/Update + Cancel, and reverts to Download/Update + Remove when it ends. **The focus half is the part unit tests cannot judge, and it is why this is more than a feature:** while downloading, the primary button is `disabled` and Remove is not rendered, so before this change the row had **no focusable control at all** — the D-pad could not enter it. **On-Deck steps:** start a download (Ollama → Knowledge base → Update, or a first Download), then (1) D-pad **Down** from *Use local knowledge base* must land on **Cancel**, (2) D-pad **Up** from *Reply verbosity* must land on **Cancel** rather than passing through to the toggle, (3) **A** on Cancel shows *Cancelling…* and a second press must do nothing, (4) within a few seconds the row returns to Download/Update + Remove and the status line reads *Download cancelled* in grey — **not** the backend's raw exception text in red, (5) starting a new download after a cancel works. 6 frontend tests cover the RPC call, the double-press guard, the slot swap and the cancelled-vs-failed status text; all three mutation checks turn them red |
| Context chip ladder / live transparency | CONTEXT-LADDER-01…03 | **Verified (Deck + maintainer's look) 2026-09-05** | **Maintainer, second look 2026-09-05: PASS.** **Deck 2026-09-05, sixth build of the night: measured and pictured.** On a reply's Show details, the row now reads: the white ring on *Hide details*, one cyan-filled chip for where you are, every other chip on the same flat grey border, no glow on any chip, and the counter grey (read back on device: one distinct border colour across the inactive chips, `box-shadow: none` everywhere, counter `rgba(159, 183, 213, 0.9)`). Picture `screenshots/DeckCapture_20260905_041120_game.png`; that reply had only two chips, so a six-chip reply is still the fairer look. **Maintainer 2026-09-05, on the picture from the Deck: FAIL.** *"There's so many different colour ring outlines going on in the chips one I can't tell what's in focus. We went from ambiguous focus to too much noise."* Measured from the code: the row could carry six colours at once — three licence-tier borders, a credit border, the plain border, and the active chip's cyan ring and fill — plus a bold cyan counter, with the white D-pad ring on some other control competing. **Rebuilt the same day to one colour on the row:** same flat border on every chip, fill only on the current one, no ring on any chip, counter grey; licence and credit stay in the opened panel, where they were already written in words. Three tests pin it, two of them verified failing against the old look first. Second look owed. **Active-chip sub-check, Deck 2026-09-04, build 49241e7 (plan 32): measured, the maintainer's glance closes it.** On a Strategy reply, Show details opened a six-chip ladder; exactly one chip carried the active class, with a 2 px cyan glow and a brighter fill, while the others had none; the counter read *Chip 1 of 6* in bold cyan (`runs/CONTEXT-LADDER-01-open-show-details.json`, then a DOM read). Capture: `screenshots/DeckCapture_20260904_220827_game.png` (local file). The ladder (y 514 to 748) extends under the dock (top 608), so its lower rows sit behind the chips and Ask box until scrolled; that is the dock-clearance bug filed 2026-09-04, not this one. **2026-08-27 sweep — the focus trap is confirmed fixed on device.** With *Show details* collapsed and *Session context* expanded (one chip ladder mounted, which is the exact condition the trap needed), Down enters the panel and Up walks back out to **Retry**, **Helpful**, then the branch buttons. `runs/SESSION-CTX-TRAP-verify-2026-08-27.json`: `escaped: true`, `neverReached: []`. The fix had been unproven since 2026-08-23. Earlier notes: **2026-08-27 — the session-context focus trap is confirmed fixed on device, both cases, with evidence files.** *Case A, Show details **collapsed*** (the exact trap condition: one `.bonsai-chip-ladder` mounted and it is the strip's own): expanded **Session context**, went down into the strip, then held Up — the ring climbed **out**, row -> header -> **Retry** -> Helpful -> bubble -> question. No loop (`runs/SESSION-CTX-TRAP-caseA-details-collapsed.json`). Not reaching *Show details* on that path is the documented column behaviour, not a miss — **Retry -> Right -> Show details** was confirmed working in the same run. *Case B, Show details **open*** (two ladders mounted): going up from the strip the ring reaches **the transcript's own ladder**, not the strip's — Chip 6 -> 5 -> 4 -> 3 -> 2 -> 1, then one more Up escapes to **Retry** and then Helpful (`runs/SESSION-CTX-TRAP-caseB-details-open.json`, `…-caseB-escape.json`). Six presses to cross the ladder is the known cost noted on 2026-08-26, not a defect. | Wrap row + focus graph fix Jul 2026; on-Deck QA open. **2026-08-23 (code check + unit tests, no on-Deck confirmation yet):** fixed the one-way-carousel report from 2026-08-22 (`recordings/DeckRecord_20260822_165441_game.mkv`, *Chip 6 of 6* stuck on screen). The ladder's own Left/Up backward-stepping already worked; the bug was Up from Ask diagnostics or the session context strip having no explicit handler, falling through to Steam's default navigation and landing on Show/Hide details, skipping the ladder — the reverse of the already-explicit Down chain. Added `focusUpFromBelowContextChipLadder` in `liveTurnFocusGraph.ts`, wired as `onMoveUp` on both. **Steps to verify on-Deck:** open a live turn's chip ladder, step Right/Down to the last chip, continue Down/Right past it to leave the ladder, then press Up from wherever you land (Ask diagnostics if verbose logging is on, otherwise the session context strip) and confirm the ring returns to the ladder rather than to Show/Hide details. **On-Deck 2026-08-23 — blocked, and the fix regressed.** `recordings/DeckRecord_20260823_170847_game.mkv`: with *Show details* collapsed, `focusUpFromBelowContextChipLadder` aimed the ring at the session context strip's **own** ladder (same `.bonsai-chip-ladder` class, and the only match when no inline ladder is mounted), so Up from the strip header fed focus back into the strip — a closed loop with *Retry* / *Show details* unreachable. Fixed the same day: the helper now excludes strip-internal ladders, and the strip's ladder got the `onMoveUpFromLadder` escape the transcript's copy always had. **The one-way-carousel claim this row exists for is still unverified** — the trap prevented reaching the state that tests it. **Re-run both cases:** *Show details* collapsed (ring must climb out of the strip to the utility row) and open (ring must reach the transcript's ladder on the way up). **On-Deck 2026-08-26 — both cases re-run by script, nobody at the device; the one-way-carousel claim DOES NOT REPRODUCE.** Driven by Decky Plugin Studio's `deck_runSequence` over the ESP32 bridge (real controller presses through Steam Input) with focus read from `gpfocus`, never `activeElement`. *Details open:* Down from the utility row entered the ladder at **Chip 1 of 6**; Right x5 stepped to **Chip 6 of 6**; one more Right left the ladder onto **Session context (1 turn)**; **Up returned to the ladder at Chip 6**; Up x5 stepped back down the chips to Chip 1; one more Up escaped to **Retry**. 8/8 asserted steps passed (`runs/CONTEXT-LADDER-03-caseB-details-open.json`, `…-caseB-escape-full.json`). *Details collapsed:* the 2026-08-23 trap condition was reproduced exactly — strip expanded, one `.bonsai-chip-ladder` mounted and `inStrip: true` — and the ring climbed **out** to Retry rather than looping (`runs/CONTEXT-LADDER-03-caseA-strip-trap.json`). **Now locked:** `focusDownFromReplyUtilityRow` and `focusUpFromBelowContextChipLadder` had *no tests at all* before this, so the fix was real but unpinned; four cases added to `liveTurnFocusGraph.test.ts`, and the strip-trap one was confirmed to fail without the fix (reverted locally: `expected 'strip-ladder' to be 'stop-retry'`). **Characterised, not filed:** two by-design asymmetries — *Show diagnostics* is on the Down path but skipped going Up (reachable from above, so not orphaned), and re-entering the ladder from below lands on Chip 6, so escaping upward from there costs 6 presses. **Still untested:** CONTEXT-LADDER-02 (older-turn hint → frozen snapshot). **2026-08-28 addendum:** the *Show diagnostics* asymmetry noted just above is now moot — that standalone block was folded into the ladder's own **Developer details** chip (roadmap: "Fold Show diagnostics into Show details"), so there is nothing left outside the ladder for Up/Down to treat specially. See **DIAG-FOLD-01** for the merged surface's own on-Deck row. **Active-chip visibility, fixed at the desk 2026-09-04 (roadmap: "The active chip in Show details is hard to spot"):** the chips were plain spans with only a subtle background-tint difference between active and inactive, easy to miss against the glass panel, and the "Chip N of M" counter was 10px grey text. The active chip now carries a `bonsai-chip-ladder-chip--active` class with a cyan (`DECK_HIGHLIGHT_CYAN`) box-shadow ring and a brighter cyan fill layered on top of its existing tier/attribution border (which is unchanged and still renders regardless of active state), and the counter uses the section-label treatment (`#9ce7ff`, bold, letter-spaced) from design-tokens.md. Style only, no focus-graph change. Unit-tested in `ContextChipLadder.test.tsx`. **Deck check owed:** a DOM read confirming exactly one chip carries the active class, plus a screenshot for the maintainer's eyes. |
| Spoiler confidence chip (Show details band estimate) | SPOILER-RISK-CHIP-01 | Open | **Unit 2026-08-07 (Wave 3 C).** `tests/test_spoiler_risk_service.py` — band scoring, model-tag blend, manifest chip `spoiler_risk`. **On-Deck:** Speed, Strategy, and Expert Asks each show **Spoiler risk: low\|med\|high** under Show details; band does not change when spoiler masking or consent toggles change; incomplete `<bonsai-spoiler-risk>` in a stream must not blank the answer |
| Reply micro-actions / D-pad graph | MICRO-01…05 | Partial | Bug: Strategy live-turn skips. **MICRO-04 root-caused, fixed and verified on-Deck 2026-08-26 — the report was real.** Reproduced by script on a live Strategy turn: the reply grid is *Helpful / Not really* over *Retry / Show details*, and both vertical moves collapsed into the left column — Down from **Not really** landed on **Retry**, Up from **Show details** landed on **Helpful**. The right column was unreachable vertically; the only way in was Retry then Right. **Cause: the `activeElement` trap.** `onMove*` must sit on the row `Focusable` rather than the buttons, so one handler serves both columns and calls `focusedStop()` to work out which. `focusedStop()` asked with `elementHasFocus`, which reads `document.activeElement`; Steam moves the gamepad ring without moving it, so the scan returned the first entry in `REPLY_STOP_ORDER` and `downFromNotReally` / `upFromShowDetails` were dead code that had never run on device. **Fix:** `elementHasGamepadFocus` reads `.gpfocus`, falling back to `activeElement` when no ring exists. Same guard in `pressHandler` fixed too. **After the fix, on device:** Down from *Not really* → **Show details**, Up from *Show details* → **Not really**, left column unchanged (`runs/MICRO-04-VERIFY-after-fix.json`, `runs/MICRO-04-VERIFY-left-column.json`). Five tests in `uiDocument.test.ts`; three fail without the fix. **Still open:** MICRO-01/02/03/05, and the *branches* half of MICRO-04. **Corrected 2026-08-26:** an earlier note here called that half "unreachable rather than broken" without checking. It is neither — instrumentation added the same day shows the prompt *does* request the fence (`branch fence requested in prompt=True`) and `gemma4:e2b-it-qat` *does not emit it* (`branch_marker=False`) on two Strategy first turns with no game running. So it is model non-compliance, and it is now one log line to check rather than a guess. See the branch-picker entry in [roadmap.md](roadmap.md) for the full chain and the scope of the claim. |
| Unfenced spoiler feedback chip | SPOILER-FEEDBACK-01 | **Verified (Deck)** | **Confirmed on device 2026-08-28, both halves.** Pressing **Not really** on a finished Strategy reply opened *What went wrong?* with five chips — **Bad information**, **Misidentified game/problem**, **Unfenced spoiler**, **Too long**, **Too short** — the new one in the position the roadmap describes. They stack one per line in the 300px column rather than sitting in a row, so **Down**, not Right, is how you walk them (Right from *Bad information* correctly does nothing; there is nothing to its right). Pressing **Unfenced spoiler** disabled the chip, and — the half that matters — the backend received it: `~/homebrew/settings/bonsAI/bonsai_feedback.jsonl` gained `{"rating":"down",…,"chip_id":"unfenced_spoiler"}` immediately after. That is the pre-existing `chip_id` bug proven fixed against the real RPC bridge, which no desk test could show. Evidence: `runs/SPOILER-FEEDBACK-01-press.json`, `…-chip.json`, `…-press-chip.json`. Earlier: **Shipped 2026-08-28.** New thumbs-down refine chip, id `unfenced_spoiler`, added to the existing "What went wrong?" row alongside **Bad information** / **Misidentified game/problem** — same mechanism (`REPLY_MICRO_ACTIONS`), no new focus owner. **Also fixed as part of this:** `save_ask_feedback` was missing the `chip_id` parameter entirely, so every refine chip (not just this one) threw on the real RPC bridge and silently failed to log `chip_id` to `bonsai_feedback.jsonl` — see `tests/test_save_ask_feedback_rpc.py`. Unit: `replyMicroActions.test.ts`, `buildReplyActionsElement.test.tsx` (chip renders, click reports `unfenced_spoiler`), `test_feedback_service.py`, `test_reply_followup.py`, `test_save_ask_feedback_rpc.py`. **Over-fenced sibling (reply fenced something that wasn't a spoiler) deliberately skipped** — not free enough to bundle here; would need its own wording pass and roadmap slot. **On-Deck:** rate any reply **Not really**, confirm **Unfenced spoiler** renders as a third chip in the refine row (wraps to its own line under the 300px column, does not crowd the existing two); press it, confirm the Ask field autofills a prefix ending in the original question and mentioning ```` ```bonsai-spoiler``` ```` fences; send it and confirm the resubmitted Ask's Show details / prompt context carries the previous Q+A; separately confirm `bonsai_feedback.jsonl` gains a line with `"chip_id":"unfenced_spoiler"` |
| D-pad answer scroll | D-PAD-SCROLL-01/02 | Partial | Viewport fix; choppy scroll bug open |
| LB/RB tab switch | TAB-SWITCH-01 | Fixed 2026-08-07 | Strip lurched sideways and drifted back on **RB only**. Measured with `scripts/probe_deck_tab_switch.py`: the tabs-root child's `scrollWidth` transiently inflates (300 → 420) while the outgoing tab is still mounted, Steam scrolls it right, then the browser clamps `scrollLeft` down frame by frame as `scrollWidth` collapses. `overflow: hidden` does not stop this — a hidden box is still programmatically scrollable — so section-1.ts now uses `overflow: clip`. Verified on device: `scrollLeft` pinned at 0 across the whole collapse. Detail in [planning/03-lbrb-tab-flicker.md](planning/03-lbrb-tab-flicker.md) § 10 **Deck 2026-09-04 re-run PASS, measured:** RB through the whole strip including the About wrap with focus deep in a scrolled Ollama panel, 1,453 samples at 16ms — `scrollLeft` never left 0 and `scrollWidth` never moved (300/12/104/125/13/300 throughout). |
| About tab links | ABOUT-LINKS-01 | Fixed 2026-08-07 | All four About links were unreachable by D-pad. Hand-rolled `Focusable` + `onMoveUp`/`onMoveDown` wrappers returned `true`, so Steam skipped default navigation, while focus was moved with a plain DOM `.focus()` — which does not transfer gamepad focus across nav containers (see `navFocusRegistry.ts`). Presses were consumed and focus never moved; the chain also skipped the two middle links. Now plain `ButtonItem` rows, matching DeveloperTab |
| QAM tab body height across switches | QAM-BODY-RO-01 | Fixed 2026-08-08 | Steam replaces `TabContentsScroll` on **every** tab switch (verified: 22 switches, new node identity each time). `useQamPanelHeightGuard` resolved it once at mount, so its `ResizeObserver` watched a detached node from the first switch on and `--bonsai-tab-body-height` stopped tracking. Now re-resolved via a childList-only `MutationObserver` on the ancestor chain. Unit: `useQamPanelHeightGuard.test.ts` (3 of 5 cases fail against the pre-fix hook). Shares the measurement chain with QAM-BAZZITE-01 and D-PAD-SCROLL-01 — re-run both |
| Rows span the QAM panel width | ASK-WIDTH-01 | Open | **Fixed across three passes 2026-08-15/16**, each identified by a different piece of device evidence. (1) A screenshot showed the preset chips, textarea and Ask bar sharing one gutter on both sides — equal gaps mean an ancestor inset, not the per-row width, so every inset bonsAI owned went to zero (`BONSAI_PLUGIN_SIDE_PAD_PX` 4 → 0, explicit `padding: 0` on `.bonsai-scope` and on `.decky-qam-scope:has(> .bonsai-scope)`). (2) The maintainer then reported the chips reaching the edges while the textarea and Ask button did not — the differential, since the chips are plain `width: 100%` and those two were the only rows that were not. The Ask row's measured px width (`--bonsai-askbar-outer-width` + `--bonsai-ask-margin-left`) was deleted for `width: 100%`, and Decky's `TextField` `.Panel.Focusable` wrapper was forced to fill the glass card — section-4 already did that inside the Ask row, the input host never had the rule. (3) An annotated screenshot marked the residual gutters, which sit on Steam's chain above `.decky-qam-scope`; `useQamPanelSideBleed` now walks scope → tab pane inclusive and clears padding + fixed side margins. **Measured on-Deck 2026-08-16** with `scripts/probe_deck_ask_row_width.py`, which is why the bug is closed: unified host and Ask row went `x=63.99 w=268.02` → `x=48 w=300`, matching the `_TabContentsScroll` column exactly, **V1 `15.99px` each side → `0px`**, 32px reclaimed (+11.9% row width). The fourth pass found the real cause — Steam ships **hashed class names**, so every `[class*="PanelSection"]` reset in `section-3.ts` matched **0 elements** and had been silently inert; the fix reaches the element by structure (`div:has(> .bonsai-full-bleed-row)`) instead, and `useQamPanelSideBleed` turns out to be a no-op on this build (kept as a guard). **This row stays Open for the visual half only** — the probe proves geometry, not that the rows *look* right. **On-Deck:** walk **ASK-WIDTH-01** in [testing-manual.md](testing-manual.md). If a gutter survives, read the `ASK-WIDTH-01` entry in the debug ring (what the walk reclaimed) before reaching for `scripts/probe_deck_ask_row_width.py` **V0**. Unit: `useQamPanelSideBleed.test.ts` (5) pins the walk boundary — chrome above the pane untouched, `auto` margins preserved, no inline style written on an already-flush chain, bail when the pane is missing **Deck 2026-09-04, measured:** panel 300px (48 → 348); chat slot row, transcript and chip row each span it exactly; Ask button 298.4px, inner actions row 294.5px (the input's own padding, not a gutter). The by-eye confirm is still owed. |
| Character voice / Pyro | CHAR-VOICE, PYRO-EGG | Open | Tier 2 |
| Voice STT | VOICE-01…07 | Open | Tier 2; see troubleshooting § Voice. **2026-08-03: voice was completely broken since `742db60` (2026-07-17)** — `VoiceTranscriptionSession.status()` had been deleted signature-only, leaving its body as unreachable code, so every start raised `AttributeError`. Fixed with two mutation-checked guards (`test_voice_session_status.py`, `test_no_unreachable_code.py`) and verified on the deployed plugin. **A live recording round-trip on-Deck is still unverified** — that is VOICE-01. Deferring this tier is what let a total outage sit for two and a half weeks |
| Voice diagnosis note | — | — | Plugin-side RPC exceptions appear in `sudo journalctl -u plugin_loader`, **not** in `homebrew/logs/bonsAI/*.log` — the loader catches them before the plugin's own logger runs. Check the journal first when a UI action reports a Python error with nothing in the plugin log |
| Model routing pickers | ROUTING-01…02 | Partial | Fetch-on-open + save OK; focus/chrome → Bugs (**ROUTING-FOCUS-01**) |
| Pulled tags merged into try order | ROUTING-MERGE-01 | **Verified (Deck) 2026-09-06** — run after the save bug found the same night was fixed. A saved text order (`gemma4:e2b-it-qat, qwen3.5:4b, qwen2.5:1.5b, nomic-embed-text:latest`), then `qwen2.5vl:3b` pulled from the picker (3.0 GB, finished 01:05:02). The pulled tag joined **at the bottom**: `[..., 'qwen2.5vl:3b']`. Opening the vision picker showed it **in the vision list** (first of three), with the text-only `qwen2.5:1.5b` and the embedding model absent — the row's *a text-only pull must not appear in the vision list* clause, from the other direction. **Still owed:** the high-VRAM case (a large tag goes to the **top** instead when *Allow high-VRAM model fallbacks* is on) — needs a large model and the switch on; and the reachable-for-Ask check with no saved order. Evidence `runs/round35b-reorder-and-done.json`. The pulled model was removed and both orders reset to empty afterwards, so the device is back as it was. | 2026-08-02: `merge_pulled_tags_into_routing_orders` implemented (backend was missing since the feature shipped). 8 unit tests in `tests/test_merge_pulled_tags_rpc.py`. On-Deck: run a **custom** local-Ollama setup profile that pulls a model, then confirm the new tag appears at the bottom of **Set text try order…** — and at the top instead when **Allow high-VRAM model fallbacks** is on and the tag is a large one. A vision-capable pull (e.g. `qwen2.5vl:3b`) must also appear in the vision list; a text-only pull must not. With **no** saved try order the setting stays empty by design — verify the pulled model is still reachable for Ask |
| Ollama local-setup focus / Install label | OLLAMA-FOCUS-01…03, KB-FOCUS-01 | Open | Auto-probe + vertical chain + KB pair; re-check KB equal height |
| Ollama keep-alive slider gpfocus ring | OLLAMA-KEEPALIVE-FOCUS-01 | Open | Keep models loaded thumb: white ring vertically centered on dot (1px regression) |
| UI scale focus graph | UI-SCALE-01…05 | Open | Template for new controls |
| Soft `num_predict` + thinking budget (C1) | SOFT-PREDICT-01…05 | Partial | **Shipped 2026-08-10.** Caps Speed 800 / Expert 1200 / Strategy 1600; soft auto-continue on `done_reason=length` (max 2) with ephemeral **`Continuing…`** stripped from the final reply; C1 `resolve_ask_token_budgets` keeps `think: false` until Thinking effort control. **Bug fixed 2026-08-15:** a Stop landing within 120ms of the cue delta could leave `Continuing…` in the saved/stopped reply — `_update_partial_response` (`main.py`) rate-limited the cue-clear write away, and no later token arrived to correct it. Fixed by bypassing the throttle whenever the new text is shorter than the stored partial (a shrink is always a deliberate rewrite, never a token append); belt-and-braces `stripSoftContinueCue` strips it client-side on the cancelled path too. Unit: `tests/test_ollama_ask_budgets.py` (7 cases incl. `medium` effort and `None` fallback), soft-continue cases in `tests/test_ollama_service.py` (cancel-mid-continue, strategy-fence stitch boundary, `ask_budgets` contract), throttle/shrink cases in `tests/test_background_partial_state.py`, `src/utils/stripSoftContinueCue.test.ts`. **Automated (Partial → confirm on-Deck):** **01** stitch + cue-absent-from-response, **03** cancel republishes cue-free text, **04** fence stays hidden across the stitch boundary — all pinned by the unit tests above; still need the on-Deck visual/timing confirm. **Verified (fully automated):** **02** empty continue stops quietly. **Still Open — needs a real thinking model on-Deck:** **05** thinking model with default off still returns visible text (no empty-reply regression). Reply verbosity remains prompt-only. Detail: [16-soft-num-predict-thinking-budget.md](planning/16-soft-num-predict-thinking-budget.md) **Deck 2026-09-04:** SOFT-PREDICT-01 PASS (a 10,707-character reply, the `Continuing` cue caught mid-stream, absent from the finished text and from the saved history after a tab round-trip). SOFT-PREDICT-03 PARTIAL — Stop inside the cue window kept the partial body and stripped the cue, but no **Stopped** notice appears anywhere and the stopped turn loses Helpful / Not really / Retry. Filed under Bugs. **Deck 2026-09-06: SOFT-PREDICT-03 PASS.** Asked for a very long weapons guide, pressed *Stop generation* while it was still arriving: the turn kept the half answer and shows **Stopped — partial answer kept.** *Helpful* and *Not really* are present and greyed (`bonsai-chat-secondary-btn Disabled`), *Retry*, *Show details* and *Copy* live — which is the corrected 2026-09-05 behaviour. Backend stopped cleanly: `abort_background_game_ai: stop requested` then the model unloaded. Screenshot `screenshots/round35-stopped-notice-and-greyed-buttons.png`. The greyed buttons still take a dead D-pad press — the separate greyed-button bug, second sighting. |
| Thinking effort control (Phase 1) | THINK-EFFORT-01…05, EXPERT-CAP-01 | Partial | THINK-EFFORT-05 PASS 2026-09-03 (D-pad row, plan 31). **Shipped 2026-08-15.** Ollama tab → **Thinking**: Off / Brief / Balanced / Deep, default **Off**. `think: true` for every on level (named levels are gpt-oss-only; qwen3 / deepseek-r1 reject a string — **D21**, superseding doc 16), with effort carried by the reserved budget 256 / 512 / 1024 added to `num_predict`. A model rejecting `think` gets one silent retry with thinking off, is remembered for the plugin session, and toasts once per model. **Also fixed here:** the cap table was keyed `deep` (the mode's pre-2026-06-26 name) so **Expert silently ran on the Speed cap** — 800 not 1200 — since the caps shipped; guarded now by `test_every_valid_ask_mode_has_its_own_cap`. Unit: `tests/test_ollama_ask_budgets.py` (11 cases), `tests/test_ollama_service.py` (think wire, fallback, known-bad-model skip, unrelated-400 non-retry), `tests/test_settings_service.py`, `useBonsaiAskOrchestration.test.ts` (warn-once-per-model), both settings contracts. **Verified (fully automated):** **01** Off still sends `think: false`; **02** levels send `think: true` with the budget added; **03** a refusing model still answers, once per session. **Still Open — needs a Deck:** **THINK-EFFORT-04** real thinking model per level (**and capture the actual 400 body from a non-thinking model to tighten `_is_thinking_unsupported_error`, whose matcher is deliberately loose because the wording is not a stable API**); **THINK-EFFORT-05** D-pad the new row; **EXPERT-CAP-01** Expert Asks now visibly longer before a soft continue. Detail: [16-soft-num-predict-thinking-budget.md](planning/16-soft-num-predict-thinking-budget.md), [D21](audit/maintainer-decisions-locked.md) **Deck 2026-09-04:** THINK-EFFORT-04 PASS with `qwen3.5:4b` (already installed, reports the `thinking` capability, so no model pull was needed) — Deep thinking, 212s, Show details read `Routed qwen3.5:4b`; all four segments of the Thinking row took the ring by Left/Right. |
| Token streaming (experimental) | STREAM-01…11 | Partial | Several preview PASS; Strategy spoiler stream open. **2026-08-07 — Phase A (P1–P5) and Phase B (multi-stop + follow) both landed**, see [05-token-streaming-review.md §§ 3.1–3.2](planning/05-token-streaming-review.md). Needs on-Deck: **STREAM-04** (rewritten — Stop now *keeps* the draft and shows a Stopped notice; the old expectation of a cancel literal is gone), **STREAM-09** (rewritten below for multi-stop), **re-run STREAM-01/02** because P2/P3 both changed the flag-off path, and watch first-paint latency on STREAM-02 (P3 leaves the poll at 1200ms until the first token). New **STREAM-11** below |
| Reply language (About override) | LANG-01…03 | Partial | **LANG-01** regression fixed Jul 2026 — closed dropdown shows **Follow system** on load (`selectedOption` = option `.data`); on-Deck confirm |
| RPC timeout wrapper (behavior change) | RPC-TIMEOUT-01 | Open | 2026-08-02: bounded RPCs moved to `callDeckyWithTimeout` (15s). Verify on-Deck that settings save/load, Ask submit, background status/abort, intent packs, strategy checklist, screenshots and voice status all still succeed normally — and that the 4 deliberately unbounded calls (`clear_plugin_data`, `install_rag_corpus_local`, `start`/`stop_voice_transcription`) still complete on slow paths (multi-GB model teardown, corpus copy, long recording) without a spurious timeout |
| Settings TS/Python non-default divergence (**D13**) | (automated) | Verified | **Resolved 2026-08-03** (D13 Option A, step 7c). Four settings aligned TS → Python (`desktop_app_log_level` trims, `rag_corpus_path` rejects traversal, `rag_corpus_version` accepts an unquoted number); `preset_chip_fade_animation_enabled` aligned Python → TS as derived, since reading it independently contradicts `preset_chip_animation`. `ui_scale_manual_profile` was **not** drift — it is the `SHOW_IMMERSIVE_UI_SCALE = false` gate — and was left alone. Now guarded by `tests/contracts/settings-hostile-inputs.json` (19 cases, both languages). Re-probe: 1 divergence left, the intentional immersive gate. **No Deck smoke needed** — none of these values is reachable from the UI |
| Settings field tables (**D12**, steps 7b/7d) | (automated) | Verified | 2026-08-03: simple settings declared as one-row tables in both languages — Python `_SIMPLE_FIELDS` (19 rows) and TS `SIMPLE_FIELDS` (26 rows). Behavior-preserving, each verified by its own differential test against the pre-refactor module over ~6,000 hostile inputs, both mutation-checked. The TS table is `satisfies`-typed so a wrong-typed or unknown-key row fails `tsc`. **No Deck smoke needed** — no behavior change; the two shared contracts below are the standing guard |
| Settings TS/Python defaults contract | (automated) | Verified | 2026-08-03: `tests/contracts/settings-defaults.json` asserted by both `tests/test_settings_contract.py` and `src/data/bonsaiSettingsContract.test.ts`. Measured equal first — 40 keys, zero differences — so the fixture records real behavior. Mutation-checked: changing one fixture value fails both halves. **No Deck smoke needed** (no behavior change); if a test here fails, the two languages disagree about a default — fix the code, not the fixture |
| Spoiler title profiles TS/Python contract | (automated) | Verified | 2026-08-07: `tests/contracts/spoiler-title-profiles.json` asserted by both `tests/test_spoiler_title_profiles_contract.py` and `src/data/spoilerTitleProfilesContract.test.ts` — both tables (10 AppIDs) plus 14 resolution cases pinning AppID-beats-name, AppID trimming, and case/whitespace-insensitive name matching. Exists because the Python table steers the prompt and the TS table decides display unwrap, so a title added to one and not the other splits the two: an answer written openly but still masked on screen, or the reverse, with nothing catching it. **No Deck smoke needed** — no behavior change. **Known caller gap recorded in the fixture README**: the title-name fallback (State of Emergency) is reachable in Python but not in TS, because `unwrapAskedEntitySpoilerFences.ts` passes no `app_name`. The two `resolve*` functions agree; their callers do not yet |
| Deployed RPC surface probe | (automated, on-device) | Verified | 2026-08-03: `ssh deck@<ip> 'python3 -' < scripts/probe_deck_rpc_surface.py` drives 21 read-only RPCs plus the Ask admission path against the **deployed** plugin, with every writable dir redirected to a temp tree. Exists because unit tests import services and the frontend suite stubs RPCs via `fakeDeckyRpc.ts`, so a broken `class Plugin` method passes every gate. It immediately found `get_reply_language_snapshot` missing an `await` — broken since the feature shipped. **Run after any deploy that touches `class Plugin` plumbing.** Current: 21/21 ok |
| Legacy-loader shim removal (**D11**) | D11-SHIM-01 | **Verified (Deck) 2026-09-06** — both open halves run. Two real Asks through the Main tab against the local Ollama, both completing (`run_game_ai_request: completed in 23.6s`, and a second stopped deliberately), and a voice start/stop round trip: the button went *Voice input* → *Stop voice input* → *Voice input* with no error, the engine directory written to at the moment of the recording. The recording was silence, so no transcript came back — which is the right outcome for silence, but it does not prove transcription quality. That belongs to the voice rows, not to this one. | 2026-08-03: `_coerce_instance` (55 call sites) and `_ensure_background_state` removed from `main.py`; `ollama_ask_service.py:81` called the latter on every Ask, so that call went too. Automated side is strong — token-level differ proves the change mechanical (15,446 identical tokens, 236 renames, zero other differences), 413 Python tests green, and the Deck logs `bonsAI plugin loaded!` with no traceback. **Backend half now verified 2026-08-03**: `scripts/probe_deck_rpc_surface.py` drove all the affected RPCs on-device — `_active_request_id`, `get_input_transparency`, `get_background_game_ai_status`, `abort_background_game_ai`, the Ask admission path via `start_background_game_ai`, plus voice and knowledge-base status reads — **21/21 ok, no `AttributeError`**. **Still open:** the UI-level pass, which the probe cannot do — a real Ask through the Main tab against a reachable Ollama host (the probe uses an unreachable port, so it exercises admission and abort but never a successful completion), and a voice **start/stop** round trip (excluded from the probe as it grabs the microphone) |
| Shell modal extractions (step 8) | MODAL-EXTRACT-01…04 | **Verified** | **2026-08-04, on-Deck D-pad batch — the D10 gate is satisfied.** All four modals: D-pad reaches every control, **B** closes cleanly, and each returns you to the tab you came from — the return-tab behavior was the specific risk, since the three openers handled session capture and the return-tab ref differently before the move and that asymmetry was preserved deliberately. Same pass confirmed LB/RB tab switching keeps focus where you left it. **Two gaps found, neither caused by this work, both filed under roadmap Bugs:** focus is not restored *within* the tab after a fullscreen picker closes (only the tab is), and the tab itself is not remembered when the plugin is closed and reopened (**D15**). Original entry: 2026-08-03: plugin-help, desktop-note save, character picker and Ollama models hub moved out of `index.tsx` into `src/features/plugin-shell/` hooks (1718 → 1522 lines). Behavior-preserving; `tsc`, 268 frontend tests and preview `preGate` green on each commit, deployed and loading clean with zero log errors. **On-Deck D-pad pass required per D10** and not yet done — for each of the four: open it from its entry point, confirm D-pad reaches every control and Back/B closes cleanly, and confirm the tab you came from is the tab you return to. The return-to-tab step matters most: the three openers each handled the session-capture and return-tab steps differently before the move and that asymmetry was deliberately preserved |
| Shell state + tab payload extractions (step 8) | SHELL-PAYLOAD-01 | Open | The Ollama tab was walked top to bottom on 2026-09-03 — 13 controls, every one visible; the Developer sweep named below also counted 13 stops, which is a coincidence rather than the same walk. 2026-09-03 (plan 31): Main, Ollama, Settings, Permissions and Developer render and every control takes the ring (sweeps `runs/DOC-SWEEP-01-settings-free-play.json`, `runs/SHELL-PAYLOAD-01-permissions-sweep.json`, `runs/SHELL-PAYLOAD-01-developer-sweep.json`; one Ask end to end in SMOKE-A); About and the Ollama-tab-after-clear check still owed. 2026-08-03: the Ollama connection state and all six tab payloads moved out of `index.tsx` into `src/features/plugin-shell/` (1522 → 1291 lines). Behavior-preserving; `tsc`, 268 frontend tests, preview `preGate` and `npm run build` green on each of the four commits, and the Main tab's 95 props were diffed prop-for-prop against the previous commit (identical set and order). **No new D-pad surface** — no control was added, removed or reordered, so per **D10** this needs a smoke rather than a full pass: open each of the six tabs, confirm each renders and its controls respond, and run one Ask end to end. The one thing worth a deliberate look is the **Ollama tab after Clear all plugin data** — the remount key that discards its state moved into `useOllamaTabPayload` **Deck 2026-09-04:** the About tab passes a full sweep — 7 controls, every one visible, no cycles (`runs/SHELL-PAYLOAD-01-about-sweep.json`). Only the Ollama-tab-after-Clear check is left, and it belongs to the final phase.**2026-09-04:** that 13-stop count predates the *Clear frozen test chips* fix (**DEV-CLEAR-CHIPS-01**), which removes the button as a stop entirely once no batch is pinned — re-read the Developer half before trusting the count again. |
| Stop mid-Ask keeps a readable answer | STOP-PARTIAL-01 | **Re-open (2026-08-07)** | **The 2026-08-04 pass proved less than it looks.** It confirmed the *debris* case only: the frontend wrote *"Request cancelled."* unconditionally, so seeing that literal in the bubble could not distinguish a kept draft from a discarded one. Phase A (A5) changed the behaviour — Stop now keeps the streamed text and shows a small *"Stopped — partial answer kept."* line above it, and the literal appears only when nothing readable had arrived. **Re-run with real streamed text:** stop mid-answer after a few sentences (text must stay), and stop within the first instant (question must return to the ask field, no thinking blurb). Original entry: 2026-08-04, on-Deck and in the UI, stopping mid-Ask shows *"Request cancelled."* — the probe checks `_background_state`, the maintainer checked the reply bubble |
| A checklist the model got wrong never reaches the reply | STRAT-CHECKLIST-JSON-01 | **New 2026-08-28 — fix deployed, one sighting owed** | Found while auditing the *answer text that does nothing* report. When the model emits a checklist with fewer than two steps the parser rejects it, and until 2026-08-28 the rejected block was left in the reply: the user read a line of raw JSON, and it was its own D-pad stop that did nothing on A (measured `moved: false`). The branch picker had a guard for this since it shipped; the checklist did not. **What to check:** ask Strategy follow-ups until the plugin log shows `ask_ollama: strategy checklist fence present but did NOT parse`. The reply above that line must contain no `{"title"` and no code block, and the coaching prose must survive intact. **This cannot be forced** — it depends on what the model emits, so it is a watch-for row rather than a scripted one. The deployed parser was already proven against the exact captured payload; what is owed is one live sighting |
| Safety guard with token streaming off | DESTRUCT-ADVICE-02 | **Passed 2026-08-28** | Streaming turned off with a real press on Developer → *Token streaming*, confirmed off in `settings.json`, and restored to on afterwards. **Two questions were needed, and the first is the interesting one:** the row's own sentence produced a reply that declined to advise deleting anything, so the guard logged `flagged=False signals=0` — correct behaviour, and not a pass. A directive rephrasing (*"give me the exact steps to delete my proton prefix folder for a broken game"*) produced real instructions and the guard fired: `flagged=True signals=2 backup_mention=False`, notice on screen, same wording as the streaming-on run, and reachable as its own D-pad stop. **When re-running, check the log line, not just the screen** — a missing notice may mean the model behaved, not that the guard broke |
| Show details is a line at the end of the reply | SHOW-DETAILS-01 | **Verified on the Deck 2026-09-06** | Passed. The line is **267 pixels wide starting at x=52** — the same left edge and the same width as the answer bubble above it, so the two line up exactly. One press opened the chips and the label became **Hide details ↑**; one more press closed it. **The double-toggle worry did not happen** — the A button and a finger tap go through separate routes on purpose, and a single press does one thing. The ring reaches the line walking down and walking up, it is fully on screen rather than behind the input bar, and nothing overflows sideways. Run: `reply-block-final-walk` |
| Answer scrolls a readable step per press | D-PAD-SCROLL-02 | **Verified on the Deck 2026-09-06, both directions** | An answer of about **1,800 characters that used to be six stops is three**; a 400-character answer is one. Walking the whole reply down and back up, every part of the answer stops clear of the Ask bar, fully visible with the ring on it, and nothing is skipped — each press either moves the ring to a part already on screen or scrolls the panel. **Going up was the hard half.** Coming back up from the Show details line, the last part landed with about a third of it behind the bar, at the same spot every run. A scroll log on the Deck (hooks on the panel's scroll position, on scroll-into-view calls and on focus changes, read back after one Up press) showed the order of events: the plugin's own nudge ran while the part was still fully visible and rightly did nothing; about 30ms later Steam began a slow scroll of the whole bubble that dragged the part under the bar; and the plugin's later "lift it clear" passes then asked the browser to scroll the part into view, which the browser ignored for anything inside the answer bubble — a margin of 0, 80, 164 or 300px all left the panel at exactly the same place, because the bubble clips its overflow and Steam pads its scroll edge by 80px. The lift now measures again after asking the browser and moves the panel itself when the browser did not. After the fix the last part lands at y=414–602 with the bar starting at 608. Runs: `reply-block-up-into-answer-fixed`, `reply-block-both-ways-after-lift-fix` (17 controls, 34 presses, no loops, every answer part 100% visible both ways), `reply-block-copy-right-down-up` |
| Branch buttons and feedback reachable on a Strategy turn | MICRO-04 | **Passed 2026-08-28** | One walk down a finished Strategy reply reached both branch buttons, then **Helpful**, then **Retry**, with nothing stepped over. Note the action row is two wide, so a down-only walk reports the left button of each pair |
| Copy and Retry as corner icons | COPY-REPLY-01, COPY-REPLY-02, RETRY-CORNER-01 | **Verified on the Deck 2026-09-06 (second pass: both icons inside their bubbles)** | **COPY-REPLY-01 — passed.** Copy is a faded icon inside the answer's bottom-right corner: 20x20 at x=292–312, with the bubble's right edge at 319 and its bottom 4px below the icon. The answer's last line ends at x=272, clear of the icon, because the last line reserves room for it. Right from the answer's last part lands on it, Down goes on to the Show details line, Left comes back; A changed its label to *Reply copied to clipboard* and the clipboard held all **1,834 characters** of the answer (earlier run, path unchanged). The reply block starts 24px under the bubble, the same as before the icon existed. **COPY-REPLY-02 — not re-run**; nothing in this change touches the clipboard path. **RETRY-CORNER-01 — passed.** Retry is a faded circular arrow inside the question bubble's bottom-left corner: 20x20 at x=82–102 with the bubble's left edge at 75 and its bottom 4px below the icon; the question's last line starts at x=228, clear of it, and the bubble is the same 71px tall as it was with no icon. Down from the chat rows lands on it; Right goes to the question text and Left comes back. All nine hit-test points on the icon land on the icon, so a tap reaches it (the first cut had the question text's box on top, so a tap would have opened the question instead). **One flag in the sweep report is expected, not a failure:** the question text reads as 89% visible and the answer's last part as 89% — one of the nine sample points sits under the icon in the corner, which is where the icon is meant to be, and the text itself is measured clear. **Earlier problems, all fixed on the device first:** Copy inside the bubble trapped the ring and could land behind the Ask bar (it is a step after the bubble in the walk order, drawn in the corner); both icons straddled their bubble's edge; the Copy slot took taps meant for the bottom of the answer. **Pressing the question still collapses it and does not fire Retry**, but the ring is lost afterwards — filed under Bugs, it predates the icons. Runs: `reply-block-corner-icons-inside` (17 controls, 35 presses, no loops), `reply-block-copy-right-down-up`, `reply-block-both-ways-after-lift-fix`, `retry-corner-collapse-question`, and the earlier `reply-block-copy-trap`, `reply-block-full-walk`, `copy-right-from-answer`, `press-question-not-retry`, `question-bubble-two-stops` |
| Answer sections as D-pad stops | STREAM-09 | **Rewritten 2026-08-07 — open** | **Replaces the old row entirely.** It described "one Focusable while streaming, chunk chain after", which Phase B makes obsolete: every section is a stop now, streaming and history alike. **D-pad only, no touch, for the whole run.** With **Developer → Token streaming** on, ask a Strategy question long enough to fill more than one screen. (1) **Reachability while streaming** — from the turn header, Down must step into the answer and then through it a section at a time, and each landing must be *visible*: a soft blue left edge and a slight background lift. A press that moves nothing, or moves focus somewhere you cannot see, is the failure this row exists for. (2) **Off-screen sections** — Down at the last visible section should scroll rather than jump, and the press after that should land on the section it revealed. (3) **Leaving** — Down past the last section must reach the reply actions / branch picker, and Up from the first section must reach the turn header. Getting stuck at either end is a fail. (4) **Spoilers** — a section containing a masked spoiler must offer the reveal before the walk passes it, and **A on a spoiler wait chip must not unmask anything** (STREAM-03 safety). A on any other section does nothing, by design. (5) **A new section arriving mid-stream must not steal focus.** (6) **At T3**, when the answer finishes, the sections are rebuilt from the chunk split — focus may jump, which is accepted (locked option C); what must not happen is a dead press. (7) **History** — collapse the turn, reopen it, and walk it again: identical behaviour. **The known risk is nesting depth.** One nested `Focusable` inside the bubble was proven on device 2026-08-04; this creates one per section and three deep where a section holds a masked spoiler. Check it after a **QAM close/reopen** and while the bubble is still growing. If focus dies there, that is the fallback trigger described in [05-token-streaming-review.md § 3.2](planning/05-token-streaming-review.md), not a redesign |
| Streaming follows the newest text | STREAM-FOLLOW-01 | **New 2026-08-07 — open** | B4 changed this from pin-only to follow-plus-pin, and both halves need a look because they share one scroll listener. With streaming on and a long answer arriving: (1) sitting at the bottom, new text must stay on screen without any input; (2) scrolling up mid-answer (**touch and D-pad, separately**) must hold position — the next partial must not yank you back down; (3) scrolling back to the bottom must resume the follow; (4) D-pad specifically: walking down through the sections pins, and walking to the bottom resumes — no fight with STREAM-09's stay-put rule. Worth watching for **overshoot**: the follow targets the end of the transcript, not the bottom of the panel, so the session context strip and Save chat should *not* be dragged into view. **Known regression to re-check first (reported on-Deck 2026-08-07, fixed same day):** the follow ran and then *locked* with the tail a paragraph off screen below — it was pinning on its own scroll events, and separately could clamp at the panel's scroll maximum while the answer ran past the bottom edge. Both are fixed; the check is that the last line of a long streaming answer stays on screen for the **whole** answer, not just the start |
| Streaming while a game runs | STREAM-11 | **Accepted 2026-09-04 (D58 #4)** — measured on the Deck; game-side frames unmeasured | **Accepted 2026-09-04 (D58 #4):** the bursty reveal is a nice-to-have, not a bug to fix; reopen only if the game's own frame rate is measured and suffers. **Nice-to-have, not a ship blocker** (maintainer call, 2026-08). The one thing the preview suite structurally cannot cover: frame cost on a shared APU. With **Developer → Token streaming** on and a game actually running, ask a Strategy question with a ≥60s answer and watch for reveal stutter or dropped game frames. Exists because the R3 frame-cost theory (markdown re-parse at RAF rate) was addressed blind in A1 — STREAM-REVEAL-01 measured the reveal at idle, not under load. **Measured under real load 2026-08-28:** DRG Survivor running, Developer → Token streaming already on, Strategy ask (the DRG-GLOSSARY explain-further turn, ~40s generation). Tokens arrive in bursts. Sampled with an in-page requestAnimationFrame counter over 3s windows: during a paint burst the QAM ran **47 fps, worst frame 50 ms, 39 frames over 20 ms**; between bursts and after settle, flat **60 fps, worst frame 17 ms, zero frames over 20 ms**. So the reveal is chunky because delivery is bursty; the paint cost is real but bounded to burst moments. **Limit:** these are the overlay's frames — CDP cannot see the game's; the maintainer's FPS overlay during a burst is the remaining half. Repeat recipe: deck_readPage with an async rAF-counting IIFE (awaitPromise works) while deck_waitFor confirms text is growing |
| Streaming reveal cadence | STREAM-REVEAL-01 | **Answered — re-check after A4** | **Phase A changed both halves of this.** The reveal rate is now backlog-derived with no ceiling, and the loop coasts briefly after catching up instead of parking, which targets the reported hitch directly. Re-watch the first ~6-7 words of an answer: the split-second stall should be gone. Original result below | **2026-08-04 on-Deck: smooth**, apart from a single split-second stall after the first ~6-7 words. This **contradicts the static prediction** of a permanently chunky, poll-cadence reveal and downgrades that bug to one star; remaining polish folded into the token-streaming review. Original steps:  2026-08-03: **not a regression — a pre-existing defect found while moving the Main tab payload**, see the roadmap Bugs entry. With **Developer → Token streaming** on, watch a Strategy answer arrive and judge whether the text reveals smoothly or lands in visible chunks. Chunky arrival at roughly the backend poll cadence confirms the analysis; a smooth per-frame reveal refutes it. Also watch the moment the answer completes — whether the stream bubble swaps to the finished-answer layout immediately or lags a beat |
| `main.py` extractions 1/2/4/5 (step 12) | MAINPY-EXTRACT-01 | **Verified** | **2026-08-04, on-device.** Deployed (54 files hash-verified), `bonsAI plugin loaded!`, zero `Traceback`/`ERROR`. `scripts/probe_deck_rpc_surface.py` **21/21 ok** including `_unload` and `abort_background_game_ai`, the two RPCs step 12 changed. New `scripts/probe_deck_step12_extractions.py` drives what that probe cannot — **14/14 ok**: the moved Ollama probe against both an unreachable host and the real on-Deck runtime (`v0.31.1`, 3 models, `/api/ps` shaping and the VRAM ratio producing `qwen2.5:1.5b → 100.0%` from real data, the first time that code has run on hardware); a **real streaming Ask aborted mid-flight**, which is the path item 12.4 moved — abort returned in 0.01s, state `cancelled`, background task released; and `_unload` clean. **First run of the probe reported two failures that were the probe's own fault** — its sandboxed settings default token streaming to off, so the answer completed before the abort could land; fixed by enabling streaming in the temp settings and aborting on `pending` rather than waiting for partials. **Found by this pass:** the 1-character `<` partial — **fixed and re-verified the same day**, an aborted Ask now returns `Request cancelled.` The probe gained a check that the kept text contains an alphanumeric character, which is what caught the second half of that bug (```` ``` ```` surviving the first fix). **Still manual:** nothing for these four — the UI does not change |
| ~~`main.py` extractions — original plan~~ | | | 2026-08-03: background-state shape, Ollama health probe, cancel-and-await, and Stop transport moved out of `main.py` (2865 → 2743). **Automated side is the strongest of any refactor step so far** — 60 new tests where these paths had almost none, every file mutation-checked, 486 Python tests green. **Not deployed.** Three things the unit tests cannot reach, in priority order: (1) **Stop** during a real streaming answer — press it mid-reply and confirm the reply stops promptly and the partial text is kept, since item 5 moved the handle-close that unblocks the worker read; (2) **plugin unload** — `sudo systemctl restart plugin_loader` and confirm `bonsAI plugin unloaded!` then `bonsAI plugin loaded!` with no traceback, since item 4 changed both teardown paths; (3) **Connection test** against a reachable host and an unreachable one, plus the on-Deck local runtime, since item 2 moved all three HTTP calls. `scripts/probe_deck_rpc_surface.py` covers the admission and abort RPCs but uses an unreachable port, so it never exercises a successful probe |
| Character picker avatars at the locked size | CHAR-AVATAR-26-01 | **Partial (Deck)** | **D33 locked at 26px 2026-08-27 (option C) and implemented the same day.** The picker was five hardcoded sizes (22 on the OK preview, 24 on both character grids, 26 on Random and Custom) and still drew the *old* pixel-grid faces; it now draws the prop-emblem artwork at one size, `PICKER_AVATAR_PX` in `CharacterPickerModal.tsx`. 26 is the design's badge line — in-disc at 26 and above, beside the disc below — so every avatar is on the same side of it. **Measured on device 2026-08-27** (bundle hash-verified, picker open on the Deck): **34 avatars, all `width=26`, all on the `0 0 32 32` emblem viewBox** — no pixel grids left. Character rows measure **38px, unchanged**, so no fewer characters fit per screen; avatar boxes 26×26, badges 14×14, **nothing clipped** by its row. Capture: `screenshots/DeckCapture_20260827_192047_game.png`. **Still owed — the judgement call, which is the maintainer's:** whether the props read clearly at 26 (the art was reviewed at 44), and whether the OK button's slightly taller row is acceptable. The maintainer said they may pick a different size after seeing it. **Regression direction:** the art style is an opt-in prop that falls back to the old pixel grids, so a new avatar call site that forgets `art="prop"` renders something plausible and wrong — two tests in `CharacterPickerModal.test.tsx` assert one size and one art style across the whole modal, both confirmed red without the change |
| Focus ring clipped on grid layouts (character picker) | CHAR-PICKER-RING-01 | Open | **Fixed at the desk 2026-09-04** (roadmap: "The focus ring is clipped on grid layouts"). Every entry button filled its column's full width with no inset, and the column div itself clips overflow (`overflow: hidden`), so a focused tile flush against its column's top, bottom, left or right edge had its D-pad ring cut off before it could render — rings paint outside an element's own border box, and `overflow: hidden` clips at that box's edge regardless. Each grid column now carries 6px of inner padding (`PICKER_GRID_RING_PAD_PX` in `CharacterPickerModal.tsx`), inside the same box that clips, so the ring has room without ever reaching the boundary. 6px covers both reference points at hand: bonsAI's own documented "outer ring" recipe in design-tokens.md reaches about 5px past the element, and Steam's native ring on this modal's plain, unstyled `Button` (none of the custom-ring CSS in `gamepadAndPullModels.ts` targets a class this modal uses) has been read on device as roughly 2-3px plus a soft glow. This is inner padding for the ring, not a decorative gutter (design-language.md Rule 1) — it costs a few px of label width on every entry, not only the ones at an edge, because every column clips independently of its neighbours. Unit test in `CharacterPickerModal.test.tsx` pins the padding value; see [testing-manual.md](testing-manual.md) row **CHAR-PICKER-RING-01** for the on-Deck screenshot check this desk fix owes |
| Session context row count during a live Ask | SESSION-CTX-DUP-02 | **Verified (Deck)** | **Found and fixed 2026-08-27.** While an Ask was in flight the strip counted the previous turn twice and showed it as two identical rows; it self-corrected when the reply landed, so it was only visible for the length of a generation — which on this hardware is the whole of a QA read. **Cause, proven by a state read rather than a code reading** (the entry demanded that before any fix): two writers reach `askThreadCollapsed` and neither knows the other. The saved-chat reload rebuilds the list from disk after every completed reply; the next Ask then appended the same turn again from `pendingArchiveTurnRef`. Mid-Ask the list held the question twice, once under a slot id and once under a minted `turn-<ts>-<i>` id — the id shapes are what named the doubling writer. **Now the flush replaces that row instead of appending**, keeping the slot's id and restoring the AppID the disk round-trip drops. **Steps:** ask anything, let it finish, ask again, and *while the second reply is still generating* read the Main tab — the earlier turn must appear once, not twice, and **Session context (N turns)** must not jump by two. **On device 2026-08-27, bundle hash-verified:** mid-generation the previous turn appeared once under `7358e744-…`, strip read *(1 turn)*, and the count stayed right after the reply landed. **Not the same as SESSION-CONTEXT-DEDUPE-01** (the live row duplicating the newest archived row), which is a separate fix and unaffected — that dedupe compares the live row against the newest archived one and cannot see a duplicate lying entirely inside `askThreadCollapsed` |
| Strategy branch picker renders on a first turn | STRAT-BRANCH-01 | **Verified (Deck)** | **Backend half fixed and device-confirmed 2026-08-27.** With DRG Survivor running (`appid=2321470`), Strategy mode, first turn, question `how do i deal with the exploders`, `gemma4:e2b-it-qat`: the log now reads **`ask_strategy: branch fence requested in prompt=True`** then **`ask_ollama: strategy fences branch_marker=True branch_parsed=True branch_options=2`**, and the reply on screen is clean prose with no JSON. Before the fix the same run logged `branch_marker=False branch_parsed=False` while the ask trace's raw output carried a valid fence — the extraction was reading the display text, which has the fence deleted by design. **Steps:** launch a game with corpus coverage, Strategy mode, ask a first-turn tactics question, then read those two lines in `~/homebrew/logs/bonsAI/`. `branch_parsed=True` with `branch_options` ≥ 2 is the backend passing. **Frontend half fixed the same day and verified end to end on device, real controller presses.** The picker had a second, independent cause: it only rendered for `expandedTurnKey === "live"`, and the post-Ask slot reload re-keys the same answer to its archived slot id — so both panels are now rendered from the archived branch too, gated to the newest turn. **Passing run:** picker count **1** on turn `fba57653-…` (an archived uuid — that used to be the failing signature and is now the normal one), model's question plus *A. Narrow Passage* / *B. Wider Area*; slot order header → bubble → picker → reply actions; Down from the header walks the answer sections onto *A*, then *B*, then **out to *Helpful***, and Up retraces the same path to the bubble; **A on a branch fires the follow-up Ask and the picker clears itself.** **Steps to re-check:** ask a first-turn tactics question in Strategy with a game running, confirm the buttons are on screen under the answer, walk to them with the D-pad only, press A, and confirm a follow-up starts and the buttons go away. **The checklist panel had a third, separate cause and is also fixed — check it in the same run, it is the next press.** After pressing a branch, the follow-up should bring up a tick-list under the answer. **Passing run, same day, same device:** `checklist_parsed=True` → panel renders once, *Dealing with Exploders*, four items, between the answer bubble and the reply actions; the D-pad reaches the first tick box and **A ticks it**; `~/homebrew/settings/bonsAI/strategy_checklist_session.json` then holds `checked_ids: ["1"]` under the running game's AppID with all four items. The cause was that the ask mode was read once at mount and frozen at the default `speed`, so the checklist was discarded on every completed Ask regardless of the mode shown — meaning this feature had never worked. **Known gap, not covered here:** an Ask that completes on the *start* call rather than by polling still loses both panels (own roadmap row, ★) — not reproducible on this hardware, where replies take 6-32s |
| Clear cache actually clears the session | CLEAR-CACHE-01 | **Partial (Deck)** | **2026-09-03, plan 31: main and reopen halves PASS again on 3b0e9d7 — log line `forget_background_game_ai: stored answer dropped (stopped=False)` at 01:05, Main empty at the [+] position after the clear, after a QAM close/reopen and after a true remount (`runs/CLEAR-CACHE-01-b-after-modal-back-to-main.json`, `runs/CLEAR-CACHE-01-c-close-panel-for-remount.json`). Orphan half measured: the saved chat stays — 8 slots on disk before and after, the echo chat still in the carousel with its two turns; a clear moves the panel to [+] and leaves the chat behind, so each clear-and-reask cycle leaves one more chat. Mid-generation half still owed (deferred to the thinking-model step).** **Fixed and device-confirmed 2026-08-27**, after three separate causes ([D32](audit/maintainer-decisions-locked.md) chat slot, [D34](audit/maintainer-decisions-locked.md) modal snapshot, [D35](audit/maintainer-decisions-locked.md) backend forget — only the third was load-bearing). **Passing steps, run on device with the bundle and `main.py` hash-verified:** ask *"reply with only the word echo"*, confirm one reply bubble; Settings → **Clear cache…** → **Clear**; switch to Main — `bubbles: 0`, `turnIds: []`, no *echo* anywhere. Then close the QAM entirely and reopen the plugin — the full remount that reads `get_background_game_ai_status` — and Main must still be empty. **The log line is the tell:** `forget_background_game_ai: stored answer dropped (stopped=…)` in `~/homebrew/logs/bonsAI/`. Without it you cannot distinguish "the clear worked" from "the panel simply did not repaint", because the evidence for this fix is an absence. **Still open — the mid-generation half.** Clearing while a reply is *still being written* must stop the generation and leave the panel empty (not a "Request cancelled." bubble). Four unittests cover it ([tests/test_background_forget_on_clear.py](../tests/test_background_forget_on_clear.py)); it was **not** reproducible by hand, because `gemma4:e2b-it-qat` finishes in 6–32s (`num_predict` caps at 1600 tokens) while the D-pad walk from **Ask** to **Clear cache…** takes 35–95s over the bridge. Retry with a slower model, or park the ring on the button first. |
| Voice install state after data clear | VOICE-CLEAR-01 | **Partial** | **Backend verified on-device 2026-08-04.** `scripts/probe_deck_step12_extractions.py` plants a finished install task plus a stale `done/ok` state on the **deployed** plugin, calls `_reset_voice_install_after_clear`, and confirms the state returns to the default and the task handle is cleared — 2/2 ok. Deliberately does **not** call `clear_plugin_data`, which would wipe the maintainer's real settings and Ollama models. **Still open — the UI half:** with a completed voice-engine install, run Settings → Clear all plugin data for real, then reopen Settings → Voice input and confirm it no longer claims the engine is ready. The probe proves the state resets; it cannot prove the panel renders the reset state |
| Resume last tab on reopen (D15 B) | TAB-RESUME-01 | **Verified (Deck) — except the focus-restore gap, tracked elsewhere** | 2026-09-03 (plan 31): resume confirmed again on 3b0e9d7 through six close-and-reopen cycles across the three modes; the first-press-snaps-to-top gap is unchanged and stays tracked with the picker focus-restore item. **2026-08-04 on-Deck: passes, and restores scroll position too.** Remaining gap: the first D-pad press after reopening snaps focus to the top of the tab — a third restore (focus) that was never built. Tracked with the picker focus-restore item, since both need the same mechanism. Original steps:  2026-08-04, deployed and loading clean. Go to a tab that is not Main (Ollama or Settings), close the plugin, reopen: you should land back on that tab. Then check the two edges — after **Clear all plugin data** the next open should be **Main** (the key is wiped with everything else), and a first-ever open with no stored tab should be Main. 12 automated tests cover the storage and remount logic; what they cannot cover is whether Decky's own open path overrides the restored tab on device |
| Tab-resume mode is a three-way choice (D15 A/B/C) | TAB-RESUME-MODE-01 | **Verified (Deck) 2026-09-03** | PASS 2026-09-03: A · Main → reopened on Main after leaving from About (`runs/TAB-RESUME-MODE-01-a-select-main-and-close.json`); C · 5 min → reopened on About within a minute (`runs/TAB-RESUME-MODE-01-c-select-5min-and-close.json`) and on Main after 5 m 29 s away (closed 01:15:21, reopened 01:20:50); B · Resume → reopened on About (`runs/TAB-RESUME-MODE-01-f-select-resume-and-close.json`); each choice was still highlighted on Developer after its reopen; on disk A = `always_main`, B = `resume`, C = `resume_recent`; the maintainer's setting (C) was restored. 2026-08-04. **Developer → Navigation → Tab to open on (D15)** now has three stops, so the options can be compared without a rebuild. **Steps, one per stop, each needing a real close-and-reopen** — the setting takes effect on the *next* open, never the current one. **B · Resume** (the shipped default, so check it first and confirm nothing regressed): from Ollama, close, reopen → Ollama. **A · Main**: pick it, go to Settings, close, reopen → **Main**; then D-pad back to Developer and confirm the control still reads A, which is what proves the setting round-tripped through `save_settings` rather than only living in the UI. **C · 5 min**: pick it, go to Ollama, close, reopen straight away → Ollama; then leave the plugin closed for **more than five minutes** and reopen → **Main**. **Edges worth one pass:** after **Clear all plugin data** the mode returns to **B** and the next open is Main (both the stored tab and the mode mirror are `bonsai:`-prefixed, so the wipe takes them); and switching A → B resumes the tab you were on *under A*, without needing another tab change first. 12 new shell tests cover the mode resolution and the storage round trip, and two contract cases pin the key across both languages; what they cannot cover is whether the setting survives the real `settings.json` write and whether Decky's open path respects the resolved tab |
| D-pad can leave a fullscreen picker | PICKER-EDGE-01 | **Passed 2 of 3, 2026-08-28** | Real controller presses. For each fullscreen picker: from the first control press **Up**, from the last press **Down**, and confirm the ring leaves the list rather than sticking with no way out. **AI character — pass:** holds on *Random* at the top, walks every character column and the custom field, reaches **OK**, holds there. **AI models hub — pass** (this is also where *Browse models* goes): holds on the *Policy* tab, walks the filters, chips and model rows, reaches **Pull selected**, holds there. Both footers are **two wide** — a down-only walk reports the left button and misses **Cancel** one press to the right, which is not a failure. **Text / vision try order — FAIL**, see PICKER-REORDER-01 below. Holding still at an edge is not a trap when there is nothing beyond it; a trap is a ring that cannot reach the confirm buttons at all |
| Try-order picker does not reorder while you read it | PICKER-REORDER-01 | **Passed 2026-08-28** | Failed when filed the same day, fixed under [D36](audit/maintainer-decisions-locked.md) option 1, re-run and passed. Open **Set text model try order…**. One press of Down moves the *highlight* to the next model and leaves the order alone; three presses reach **Reset to defaults**; Up from the first row holds with nothing above it. Reordering still works with A on a row's **Up**/**Down** button (checked: `gemma4` moved below `qwen2.5:1.5b`). Nothing persists until **Done** — both routing orders still read `[]` on disk after closing with Cancel. **Re-run on the vision list too**, which shares the component and was not driven separately |
| B closes the try-order picker | PICKER-B-CLOSE-01 | **Passed 2026-08-28** | Failed and fixed the same day. The picker was the only one rendering bare content inside `BonsaiModalScope` rather than wrapping it in Decky's `ConfirmModal`, which is where B, the title bar and the Done/Cancel footer come from. Now wrapped, like `OllamaModelsHubModal`, `PullModelsModal` and `CharacterPickerModal`. **Re-checked:** one B press from a row button closes it, where three had done nothing. *Reset to defaults* stays in the body; Done and Cancel come from the frame. **Still worth one look:** that the vision list closes the same way, and that Done still saves |
| Reorder button keeps the highlight | PICKER-REORDER-02 | **Verified (Deck) 2026-09-04, third fix** | **Deck 2026-09-04, third build of plan 32 (code `c4e16fc`, the form-submit stop):** Ollama, *Set text model try order…*, A opened the picker with the ring on the first row's disabled Up (gemma4:e2b-it-qat). Right reached its Down button; A moved the row to second place and the ring stayed on that row's Down button, visible, with the picker still open (`runs/PICKER-REORDER-02-build3-a-keeps-picker-open.json`, both steps passed; a DOM read afterwards found the modal mounted with the rows in the new order, qwen2.5:1.5b then gemma4:e2b-it-qat, with the ring on the second row's Down button). The order was put back with Left, A on the row's Up button (`runs/PICKER-REORDER-02-build3-b-restore.json`) and the picker closed with B, so nothing was saved (the saved order on disk stayed empty). After B the ring came back to the plugin's own tab bar, the modal-return landing, visible. **Deck 2026-09-04, build f9a4c17 (redo, plan 32):** A on a row's Down button moved the row but the picker closed and the ring landed on the tab bar (`runs/PICKER-REORDER-02-redo-ring-follows-moved-row.json`). Then A on *Reset to defaults* closed the picker the same way, so the close is not the reorder or the refocus: the picker's buttons sit inside the ConfirmModal's form as plain submit buttons, and Steam's A press clicks them, which submits the form and takes the modal's OK path. Back with lane B to stop the submit (preventDefault on the clicks) on top of the Steam-transfer refocus. Saved order unchanged. Pressing A on a row's **Up** or **Down** reorders correctly but drops the ring: `gpfocus` count reads 0 straight after. The next press re-acquires and lands on the moved model's own button, so the cost is one press that looks like it did nothing rather than a trap. Pre-existing on those buttons; more visible now that they are the only way to reorder. **Fixed at the desk 2026-09-04:** `ModelRoutingOrderModal.tsx` now keeps a ref to each row's Up and Down button, keyed by tag, and an effect that runs after the reorder commits calls `.focus()` on the moved row's same-direction button — the one just pressed — falling back to the row's other button when the same-direction one is now disabled (landed at an end), detected with `elementHasFocus` rather than re-deriving the edge condition. A plain `focus()` is safe here because the focus-graph rule allows it *within* one container, and both buttons stay inside the same outer list `Focusable` the whole time — the reorder only changes a row's sibling position, never its container. Reordering itself still runs only from the row's own Up/Down buttons, never from `onMoveUp`/`onMoveDown` (D36 option 1, unchanged). Unit: `ModelRoutingOrderModal.test.tsx` — a same-direction move that stays enabled keeps the ring on the pressed button, a move to an end falls back to the row's other button, and the ring survives two consecutive moves on the same row. **Device rerun 2026-09-04 21:50 (build 49241e7): this fix did not hold.** (`runs/PICKER-REORDER-02-rerun-ring-follows-moved-row.json`, step 2.) Ollama tab, Set text model try order, A: the picker opened with the ring on the first row's disabled Up button (`gemma4:e2b-it-qat`); Right moved it to that row's Down button; A on Down reordered the row, but the ring did not stay on it — it left the picker entirely and landed on the panel's hidden Ollama tab button behind the modal (0×0, offscreen, focused but not visible). A read taken right after that same press found something worse: the picker modal itself was gone (`#ModalDialogOverlay_Modal_1` absent, no rows in the document) — the press had reordered **and** closed the picker through its own Done/OK path, which the 2026-08-28 baseline never did. Two causes: the picker renders as a `ModalDialogOverlay`, and React relocating the *focused* row's DOM node during the tag-keyed reorder reads to Steam's focus manager as that node going away, so it fell back to whatever the modal covers instead of following the move; and running the focus transfer synchronously, in the same event as the reorder, landed inside Steam's own press/release window for the very A press that triggered it (working theory: roughly an 80ms hold), so the release fell through to the `ConfirmModal`'s default OK instead of completing on the Down button. **Redone 2026-09-04 on a fresh branch (`bugfix32-lane-b2`):** `takeRowFocus` now calls Steam's own transfer (`navRef`/`TakeFocus`, the same mechanism `PresetRowFocusRoot` uses for the preset carousel) instead of a plain `focus()` across rows, and the whole transfer is deferred `REORDER_FOCUS_TRANSFER_DELAY_MS` (150ms, not yet device-confirmed) past the press before `takeRowFocus` runs, with one more tick before the in-container `focusRowButton` — the same two-step `onEnterFromOutside` (MainTabPresetAnimatedChips.tsx) uses. Rewritten unit coverage (11 tests): `takeRowFocus` calls `TakeFocus` on the moved row's own holder and swallows a throw; `focusRowButton` focuses the same-direction button or falls back when it is disabled; and a fake-timer render test asserts a reorder press does not call `onSave`/`onClose` once the deferred transfer has run, guarding the modal-closing regression directly. **Device rerun 2026-09-04, build f9a4c17: this second fix also did not hold** (`runs/PICKER-REORDER-02-redo-ring-follows-moved-row.json`, step 3). A on a row's Down button moved the row, but the picker closed and the ring landed on the plugin's tab bar. The discriminating test: reopened the picker, walked to **Reset to defaults**, pressed A — the picker closed the same way, ring on the tab bar. So it was never the reorder or the refocus: **any** button press inside this picker closes it. Root cause: the picker's content sits inside Steam's `ConfirmModal`, which renders a `<form>`; Decky's `Button` renders a plain `<button>` with no `type`, so it is a submit button, and Steam's A press dispatches a click that submits the form — the modal's own OK/Done path (the saved order stayed at the default, so it saved exactly what was on screen). **Fixed at the desk 2026-09-04, third attempt:** every row Up/Down `onClick` and the Reset `onClick` now call `event.preventDefault()` (Decky's `Button` passes the click event through — confirmed from its own type declaration, `onClick?(e: MouseEvent): void`), on top of the unchanged `takeRowFocus`/`focusRowButton` transfer from the second attempt. The shared test-harness `Button`/`ButtonItem` stub (`fakeDeckyUi.tsx`) now renders a real `<button>` with no `type` — matching Decky's real markup — instead of a `<div>`, because only a real button inside a real form can reproduce a submit-on-click at all; a stub `<div>` could never have caught this class of bug. New unit coverage (3 tests, 14 total in the file): rendering the picker inside a `<form onSubmit={spy}>`, clicking a row's Up button, a row's Down button, and **Reset to defaults** each leave the spy uncalled; verified meaningful by temporarily removing the `preventDefault()` calls and confirming all three fail (the form's `onSubmit` does fire) before restoring the fix. **Still not measured on the Deck — this row decides.** |
| Picker returns focus to its opener | PICKER-FOCUS-01 | **Passed 2026-08-28 (was 1 of 3)** | The two cases that had failed since 2026-08-04 now pass. Cause was two rule violations in `modalReturnFocusRegistry`: it stamped `tabindex="-1"` on the opener and never restored it — which removes the control from Steam's nav graph, per `.cursor/rules/decky-focus-graph.mdc` — and it reported "claimed" whenever the element merely existed, so two earlier on-device attempts looked successful. Claimed now means `elementHasGamepadFocus`, and the retry loop keeps going until the ring moves. **Measured:** models hub `claimed: true, attempts: 2`; desktop-note save `claimed: true, attempts: 2` — both landed the ring back on the opener, and **both needed the second attempt**. Read the log from the `SharedJSContext` target, not the QAM one. **Not covered:** the try-order picker has no registry id, so it still returns you to the top of the tab |
| Spoiler reveal reachable by D-pad | SPOILER-DPAD-01 | **Verified (Deck)** | **Residue cleared 2026-08-27, second on-device run.** The one press this row still owed: with the ring on a masked fence, **B must not reveal it** — pressed, and the masked count stayed **1** with revealed **0**; Steam's B correctly moved the ring back out to the tab bar and the panel stayed open. The same run re-confirmed the main claim on a fresh reply (ask -> question echo -> **the fence takes the ring**, three presses, `neverReached: []`) and observed the documented offered-once diversion: one Down press is absorbed on the fence before the walk moves on, which is cosmetic and expected. | **2026-08-27 — FIXED, and confirmed on device by script.** The cause of every recurrence: the header→bubble entry edge and the fence diversion lived behind a DOM `keydown` listener and a direction `onButtonDown` — and Steam invokes **neither** for a real D-pad press (measured: a bridge DOWN press delivers zero keyboard events to the page). Rewired through `Focusable` `onMoveDown`/`onMoveUp` — the channel every on-device-confirmed focus fix already uses — and the keydown intercept is deleted. On device, same rig and question: ask → header → **the fence takes the ring** (`runs/SPOILER-REVEAL-AFTER-onmove-fix.json`, `neverReached: []`, second pass reproduced) and **A reveals it** (masked 1→0). Guarded by header/stop `onMoveDown` tests and a new `keydown-dpad` lint rule. **By-hand residue before full Verified:** B over a masked fence must not reveal; expect one absorbed Down press on the fence before the walk moves on (offered-once diversion, cosmetic). The *tap to hide* half is answered, in the negative: the collapse control never takes the ring and A on its containing stop is a no-op — reproduced and root-caused 2026-08-27, filed as its own roadmap bug (*A revealed spoiler cannot be re-hidden with the controller*). Writeup: [archive/roadmap-bugs-fixed.md](archive/roadmap-bugs-fixed.md); measurement: [audit/spoiler-dpad-01-keydown-dead-code-2026-08-27.md](audit/spoiler-dpad-01-keydown-dead-code-2026-08-27.md). History: **2026-08-26 — a second cause found by static analysis, fixed, NOT confirmed on device.** The 2026-08-26 device run (`runs/SPOILER-REVEAL-reachability.json`) proved the `<pre>` fix below holds and the fence still is not reached: `closest("pre")` null, fence `Focusable` with `tabindex="0"`, 238x57 at y=379 in a 766px viewport, inViewport and inScroller both true — every precondition the diversion checks, satisfied — and a 10-step D-pad walk went bubble → Helpful → Retry → Show diagnostics → Session context → Save chat, never touching it. **What was found since:** `resolveFocusedAnswerBubble` ([answerBubbleElRegistry.ts](../src/utils/answerBubbleElRegistry.ts)) read `activeElement` to decide which bubble the user is in. It is the *first* thing `captureBubble` and `resolveAnswerBubbleEl` try, and both guard it with `??` — which only catches a **null** answer. On device `activeElement` does not come back null, it comes back **stale**, pointing into an earlier turn: a real but wrong element, so the fallback never fires and `captureBubble` re-registers the wrong bubble under this answer's key. `bubble.contains(fence)` is then false for a fence that is on screen, the diversion in `handleAnswerBubbleMoveDown` finds nothing, and Down yields to the reply actions — precisely the walk recorded above. Same defect class as MICRO-04, one file further up the chain than the roadmap's lead named. **Fixed** with a new `uiGamepadFocusElement()` reading `.gpfocus`, in the three places on this path (`resolveFocusedAnswerBubble`, `focusedAnswerStopIndex`, and the spoiler branch of the bubble's `moveUp`). **Locked** by three tests that go red without it (`answerBubbleElRegistry.test.ts` ×2, `answerStopRegistry.test.ts` ×1 — the last fails *expected +0 to be 2*, MICRO-04's signature) and by a new `ring-question` rule in `scripts/check-focus-patterns.mjs`. **2026-08-27 — the device run was done and the fix FAILED.** Before/after runs under identical conditions (`runs/SPOILER-REVEAL-BEFORE-fix.json`, `runs/SPOILER-REVEAL-AFTER-fix.json`, deployed bundle hash-verified) are press-for-press identical and both end `neverReached: ["Spoiler"]`. **New fact, and it relocates the bug:** the walk never lands on the answer bubble or on either `bonsai-answer-stop` chunk inside it — Steam steps from the user question straight to **Helpful**, over a subtree whose every node is `Panel Focusable` with `tabindex="0"`. So the bubble's Down handler never runs at all, and which bubble it would have resolved is beside the point. Row stays **Regressed**. The `uiGamepadFocusElement` change is kept on its own merits (it fixes a real wrong-question bug, locked by tests and the `ring-question` lint rule) but it is **not** the fix for this row. Next step is a measurement — why Steam skips that subtree — not another code change. **Recurred on-Deck** (`recordings/DeckRecord_20260822_165441_game.mkv`, Deep Rock Galactic: Survivor): the fence took no focus ring on any D-pad path in 18s of navigation, so the 2026-08-04 pass below no longer describes current behavior. **2026-08-23, frontend-only fix (code check + unit test, no on-Deck confirmation yet):** found and fixed a concrete cause — `react-markdown` was nesting the masked fence's `Focusable` inside a `<pre class="bonsai-md-fenced-pre">` (confirmed by rendering and walking the DOM parent chain), which gave the fence a `overflow-x: auto` scroll-context ancestor it never had when the 2026-08-04 pass verified it. `MainTabBonsaiAiMarkdownChunk.tsx`'s `pre:` renderer now skips that wrapper for a masked spoiler fence; regression test added in `MainTabBonsaiAiMarkdownChunk.test.tsx` (`fence.closest("pre")` must be null). **Re-run this row's full steps on-Deck before marking Verified again** — this fix removes a real structural defect but was not confirmed against the actual gamepad ring. **2026-08-04 on-Deck: passes** — Down parks on the fence, A reveals, Down again scrolls past without revealing. | **Third attempt, 2026-08-04 — the first two never ran at all.** Root cause was the JS realm split, not focus mechanics: plugin code's `document` is SharedJSContext's 14-element shell, so the Down handler resolved no bubble and returned on its first line ([docs/audit/decky-realms.md](audit/decky-realms.md)). Verified on device *before* deploying, by executing the fixed logic inside the plugin's own realm: the fence was found, focus landed, and Steam's ring followed (`gpfocus gpfocuswithin`). **Steps — Strategy mode, spoiler masking on** (Settings → default). Ask something that returns a masked section, then from the reply bubble press **D-pad Down**: as a masked fence scrolls into view the next Down should **park focus on the fence itself — a white ring around the fence, not around the whole reply bubble**, which is exactly what the two failed attempts looked like. **A** reveals it. **Pressing Down again instead of A must scroll past without revealing** — this failed the first on-Deck pass and was fixed a second time: `onButtonDown` receives a `GamepadEvent`, not a key string, so the direction guard never matched and every button revealed. Both fences now act on **A only**, so also confirm **B** over a masked fence does not reveal it. With several fences in one reply, Down should visit each in turn. **Also check the reverse:** after revealing, the collapse control ("tap to hide") should be focusable and ringed, and D-pad Down over it must not collapse it. **Why it matters:** before this the fence was unreachable without a touchscreen, so from the couch masked text could not be uncovered at all |
| A revealed spoiler collapses on A from the controller | SPOILER-COLLAPSE-01 | **Verified (Deck)** | **Fixed 2026-08-27.** The collapse control (`.bonsai-spoiler-collapse-target`) never took the gamepad ring — the D-pad walk parks on the containing section stop instead — and that stop's `onActivate` was a deliberate no-op (kept so A could not early-reveal a masked fence or a wait chip, STREAM-03). `stopNavProps`'s `onActivate` now reads the ring (`uiGamepadFocusElement()`) and clicks a `.bonsai-spoiler-collapse-target button` inside it when one is there; a masked reveal target or a wait chip has no such element, so the query finds nothing and the exclusion holds without a separate check. Direction presses are untouched, so the 2026-08-04 rule (D-pad Down over a revealed fence must not collapse it) still stands. Unit: `buildAnswerBubbleElement.test.tsx`'s press block, three new cases (collapses; no-op on a masked reveal target; no-op on a wait chip). **On-Deck 2026-08-27, confirmed by the rig with real controller presses over the ESP32 bridge** (Strategy mode, no game running, frozen chip *"what happens at the end of ocarina of time"*): D-pad Down landed the ring on `.bonsai-spoiler-reveal-target` itself (the fence still takes the ring, SPOILER-DPAD-01 holds); **A** revealed it (masked 1→0, collapse 0→1); D-pad Down to the next stop landed the ring on the **containing section stop**, not the collapse target — reproducing the bug's exact starting condition; **A** there collapsed it (masked 0→1, collapse 1→0) — the fix. Re-pressing **A** on that same stop while re-masked did nothing both times (STREAM-03 exclusion holds on real hardware, not just in the unit test). No plugin-log errors during the run. |
| A reply's sentence-ending period is not orphaned onto the next chunk | CHUNK-SENTENCE-CUT-01 | **Verified (desk)** | **Fixed 2026-08-27, no device dependency.** A long single-paragraph reply density-split at ~300 chars (`findSafeCutInRange` in `splitResponseIntoChunks.ts`) cut at the index of a sentence's `.` rather than after it, so the period started the next chunk — *"...go BOOM"* / *". The trick is..."* in the reported capture, though the defect fires on any `". "` density-split boundary, not only shouted words. Fixed by advancing the cut 2 characters (past `". "`) when that boundary is used. Unit: `splitResponseIntoChunks.test.ts`, three shout-word variants (`BOOM.`, `KABOOM.`, `RUN.`) padded to force the split onto this boundary; failing first, confirmed fixed after. `prepareStreamMarkdown` (streaming-time splitter) checked and cleared — it never applies a sentence heuristic. **On-Deck 2026-08-27, and read the limit of it carefully.** Nine real replies were pulled off the Deck's own chat-slot files (153–717 chars) and run through the real `splitResponseIntoChunks`: **zero orphaned periods** — but also **zero of the nine reach the fixed code path at all**. Every one of them contains a blank line, so all nine split by paragraph and return before `splitLongRespectingFences` is ever called. **So the device run proves no regression in ordinary rendering; it does NOT exercise the fix.** The path needs a reply that is a single paragraph, single line, over 300 characters — which `gemma4:e2b-it-qat` produced in 0 of 9 samples here, and which an explicit *"reply as one single flowing paragraph with no line breaks"* prompt also failed to produce (that Ask returned `No response text.`). The evidence that the fix works is the unit test, which was confirmed red before and green after. Whoever re-runs this: do not read "no orphaned periods on device" as confirmation — check `density=true` was reached first. |
| Gamepad focus rings are white, not character-tinted | FOCUS-RING-WHITE-01 | **Verified** | **2026-08-04: white on device**, confirmed by computed style (`rgba(255, 255, 255, 0.9)`) and by the maintainer. | 2026-08-04. The gamepad ring was drawing with the **tab strip's accent variables**, so it followed the active AI character — gold for Ali G / TF2 Announcer, and reported as *"the yellow outline stuff is back"*. Only became visible once character selection started sticking (`c9ad633`). **Steps:** pick a character with a strong accent (Ali G, TF2 Announcer, or Shadowheart for purple), then D-pad around the Main tab: the **preset carousel chips**, **Ask**, reply chips and the Pull Models / models hub controls should all ring **white**. Then check the **tab strip** still tints with the character accent — that one is intentional and must not have gone white. Both states in one pass; the whole change is three colour literals in `gamepadAndPullModels.ts` |
| D-pad reaches everything below the reply row | REPLY-DOWN-01 | **Partial** | **Demoted from Verified 2026-08-28** — the Ask diagnostics stop this row's verbose-logging case verified is gone (see the addendum at the end of this row and **DIAG-FOLD-01**); the rest of the walk is unaffected but wants a fresh on-Deck pass before Verified again. **2026-08-04 on-Deck: passes.** Took four attempts; the one that worked was Steam's own `navRef.current.TakeFocus()`, after instrumentation showed the handler running and reporting a successful move while Steam kept routing presses to the same row. Original diagnosis, partly right and not sufficient: `onMoveUp`/`onMoveDown` are SteamUI **Focusable** props and did nothing on the Decky `Button`s they were attached to, so the reply row never handled a press; and `focusRegisteredReplyStop` stamped `tabindex="-1"` on the button and its row, removing them from Steam's nav graph as soon as you navigated onto them. **Steps:** ask anything, then from the answer press Down to **Helpful**, Down again to **Retry**, and keep going: focus must continue to **Session context** and **Save chat to Desktop** instead of stopping. Then Up back through the same stops. **Column behaviour must survive:** Helpful → Down → Retry, and Not really → Down → Show details, each staying in its own column. **With desktop verbose logging on** the dev-only **Ask diagnostics** block appears between the two — confirm Down stops there too rather than skipping to Session context. **2026-08-28: that block is gone** — folded into the chip ladder's **Developer details** chip (roadmap: "Fold Show diagnostics into Show details"), so this stop no longer exists and a re-run should confirm Down goes straight from the ladder (or the collapsed hint) to Session context with nothing between. See **DIAG-FOLD-01**. |
| Show diagnostics folded into Show details | DIAG-FOLD-01 | **Verified (Deck)** | **Confirmed on device 2026-08-28.** With Desktop Ask verbose logging on and a completed Strategy Ask: **no button anywhere matches "diagnostics"** and no `.bonsai-ask-diagnostics` node exists — the standalone block is gone. The action row is exactly **Retry / Show details / Copy**. Pressing *Show details* (it flips to *Hide details*) mounts one chip ladder of six; Down enters it at **Chip 1 of 6** with nothing in between, and five Rights reach **Chip 6 of 6, Developer details**, whose body holds *Full transparency snapshot* → the raw RPC JSON (`app_id`, `app_name`, `kb_attached`, `ollama_model`…) **and** *Ask diagnostics* → the raw `models_before_policy` / `models_after_policy` / `installed_tags` / `routing_strategy` dump. Up walks back out chip by chip to **Retry**, then **Helpful**. Evidence: `runs/DIAG-FOLD-01-open-details.json`, `…-to-dev-chip.json`, `runs/exit-ladder-up.json`. The verbose-logging-**off** half was not re-run this session. Also re-observed, unchanged and still open: the active chip is identified only by the *Chip N of 6* counter — the ring stays on the ladder container, never on the chip. Earlier: **Shipped 2026-08-28** (roadmap: "Fold Show diagnostics into Show details"). The standalone **Show diagnostics** button next to **Show details** is gone; the same raw `ask_diagnostics` JSON now renders inside the chip ladder's **Developer details** chip, same gate as before (desktop verbose logging on, and the backend returned diagnostics for the most recently completed Ask). Desk-verified only: `MainTabChatTranscript.diagnostics.test.tsx` (4 cases — no `.bonsai-ask-diagnostics` node or "Show diagnostics" text at any point; JSON present inside the chip once **Show details** is open and verbose logging is on; absent with logging off; absent when the backend sent no diagnostics) plus `liveTurnFocusGraph.test.ts` (Down from the utility row now reaches the session strip directly, with no stop in between, when the ladder and hint are both unmounted). **Steps to verify on-Deck:** turn on **Desktop Ask verbose logging** (Settings), run an Ask, press **Show details**, and confirm there is exactly one button (no adjacent Show/Hide diagnostics). Step through the chip ladder to the last chip, **Developer details** — its body should show the lightweight summary bullets *and*, below them, the raw JSON dump (`models_before_policy`, `policy_tier`, etc.). Turn verbose logging back off, repeat, and confirm the JSON is absent but the chip and its summary still render. D-pad: from **Retry**/**Show details**, press Down — it should enter the ladder (or the collapsed hint) and, once past the last chip, land on **Session context** with nothing in between; Up from **Session context** should return the same way. |
| A chip only looks focused when it holds the ring | FOCUS-CHIP-RING-01 | **Verified (Deck)** | **Confirmed on device 2026-08-28, the same day it was found and fixed.** *The bug itself is gone:* with the ring parked on a Main-tab icon in the strip (a 59×56 element, the same shape the original probe named), **no chip carried a highlight of any kind** — every chip read `border rgba(255,255,255,0.07)` and `outline-width 0px`, where before one chip held the blue marker permanently. Same result with the ring on the Decky header **above** the plugin, which is what forced the no-ring fallback to be scoped to the whole document rather than to `.bonsai-scope`: the ring sits outside the plugin there, so a scope-local test would have painted the marker anyway. *The ring and the marker agree:* on a chip, exactly one chip had `gpfocus`, the blue border **and** the white outline — no one-row-apart split. *Up from the Ask row works:* Steam's ring (not just `activeElement`) landed on a chip, and **A filled the Ask field verbatim** with that chip's text while the tab stayed `main` — the press used to activate the tab instead. *Tab names are live:* the six title nodes read **Ask bonsAI / Where AI runs / Settings / Permissions / Developer / About bonsAI**, and the focused tab element resolved to `Ask bonsAI` rather than the whole tab's contents. Re-checked in **decode** chip mode too: one chip highlighted, and it was the one holding the ring. Evidence: `runs/FOCUS-CHIP-RING-01-down-to-ask.json`, `…-ask-then-up.json`, `…-up-from-ask.json`. **One thing the fix does not reach:** the QA rig's `ownerText` is computed from text, not from the accessible name, so a probe on the tab strip still reports the tab's whole contents — the `aria-label` helps a screen reader and a person, not that heuristic. Filed against the studio, not here. Earlier: **Fixed 2026-08-28** (roadmap: "A carousel chip showed a focus ring while the D-pad was on the tab strip above it"). Found during the automated Batch A run the same day: the maintainer saw a ring on a chip, the rig read Steam's real gamepad focus and saw a **tab icon**, and A activated the tab. Cause: the carousel is its own `Focusable`, so Up out of the Ask bar crossed a navigation boundary with a plain `focus()` — which moves `activeElement` and leaves the ring behind (`navFocusRegistry.ts`, measured 2026-08-04) — and the chip then drew a ring off `:focus-visible`. `focusFirstPresetChip` now hands over with `TakeFocus(true)` and returns whether the ring actually followed; the blue current-row border and the white chip ring only paint when the carousel owns the ring, with a `:not(:has(.gpfocus))` arm so both still show where nothing owns a ring at all (desktop, touch, in-IDE preview); and each icon-only tab got an `aria-label`, so a probe or a screen reader on the strip no longer reads back the whole tab's contents. Desk-verified only: `presetChipFocusRing.test.ts` (6 — no ungated `slot--focus` or `:focus-visible` selector survives in either stylesheet), `useMainTabAskBarFocus.test.ts` (4 — nav transfer preferred, honest `false` when a ring sits elsewhere, plain-`focus()` fallback where no ring exists), `tabTitles.test.tsx` (3 — every tab id named, on the title element itself). **Steps to verify on-Deck:** on the Main tab with chips showing, put the ring on the tab strip and confirm **no chip shows a highlight of any kind** — this is the whole bug, and it is judged by eye, not by the rig. Then from the Ask field press **Up**: the ring must land on a chip (or the **How to use bonsAI** chip when it is showing), and **A must fill the Ask field**, not switch tabs. Walk the carousel with Up/Down and confirm the blue row marker and the white ring are always on the *same* chip — they disagreeing by one row is the older "two outlines" bug and would mean this went the other way. Read focus with `deck_readFocus` and compare `gpfocus` against `activeElement`: they must name the same element. Evidence for the original find: `runs/probe-focus-1.json`, `runs/probe-strip-right.json`. |
| Feedback/Retry reachable after a normal completed Ask | REPLY-ARCHIVED-01 | **Verified (Deck)** | **Confirmed on device 2026-08-27, real controller presses.** One Ask, then D-pad Down from the question header walks the answer and the branch buttons and lands on **Helpful**, then **Retry** — the two controls this row exists for. *Not really* and *Show details* are the right-hand column of the 2x2 grid and are reached with Right, by design, so a Down-only walk correctly shows two of the four. Earlier status: **Filed by the maintainer 2026-08-23** (screenshot `DeckCapture_20260822_231338_game.png`): a completed *what time is it* Ask on Left 4 Dead 2 showed **Show details** and diagnostics but no **Was this helpful?** row. Cause: after every completed Ask the slot reload expands the newest **archived** turn, not live (`useChatSlots.ts:57`), and the archived-turn branch rendered only Show details, passing `showFeedback: false` and no `onRetry` on purpose — so the whole thumbs/Retry/refinement-chip row was dead code on the ordinary path; it only ever appeared via the background-rehydration reopen bug (the *"…"* question entry), which is itself abnormal. **Fix:** the archived-turn branch in `MainTabChatTranscript.tsx` now renders feedback/Retry/chips when it is the newest archived turn, no Ask is in flight, and `lastExchange?.answer` is non-empty — the same gate the live branch always used, keyed off `lastExchange` (the most recently completed exchange) rather than off which turn happens to render as live. **Steps to verify on-Deck:** ask anything ordinary (not a Strategy question with branches), let it finish, and confirm **Was this helpful?**, **Retry**, and **Show details** all appear under the answer without any extra action. Rate it, confirm the rating sticks and reads correctly (not attributed to a different exchange). Press **Retry** and confirm it re-asks the same question. Then open an **older** archived turn (not the newest) and confirm it still shows Show details only, no thumbs row — the newest-turn gate must not leak backward. **On-Deck 2026-08-23 — the main claim passes.** Batch A chip #1 (*"one sentence"*, Deep Rock Galactic: Survivor), normal completed Ask, no reopen trick: **Was this helpful?**, **Helpful**, **Not really**, **Retry** and **Show details** all render under the answer unprompted (`recordings/DeckRecord_20260823_170847_game.mkv`, 0:03 onward). This is the row's primary failure closed on hardware. **Still owed on this row:** rating stickiness/attribution, Retry re-asking the same question, and the older-archived-turn negative case — none were exercised this run. |
| Persistent active-tab marker | TAB-MARKER-01 | Verified | **Confirmed on Deck 2026-08-04.** Root cause was not a faint marker but no marker: SteamOS never applies `.Active` to these tab buttons, so every strip rule keyed on it was dead ([decky-tab-strip-classes.md](audit/decky-tab-strip-classes.md), probe at `scripts/probe_deck_tab_strip.py`). All six glyphs computed to the same `rgb(252, 252, 252)`. The marker now keys on `data-bonsai-active-tab` on the tabs root, which bonsAI owns, with one rule per tab id. **Re-run steps:** LB/RB to Settings, D-pad **down into the panel** and leave focus there — the gear stays accent-coloured, the others stay white. Close and reopen the QAM: the restored tab is still marked without touching the D-pad. With Ali G or the TF2 Announcer selected the marker is gold, Shadowheart purple, no character green. **If it ever stops working, re-run the probe before editing CSS** — the first fix attempt keyed on `.Active`, passed every gate, deployed clean and changed nothing. **Known separate issue, do not file against this:** the carousel can scroll the active tab out of the strip window entirely, leaving no glyph to mark |
| Clean install / release zip | Tier 4 | Open | Before tag |
| Windows deploy prune + verify | DEPLOY-VERIFY-01…03 | Partial | 2026-08-03 (**D8**): `scripts/build.ps1` wipes the Deck plugin dir before copy, exit-code checks every `ssh`/`scp`, and SHA-256 compares every shipped code file after upload. **The count is computed, not fixed** — `$VerifyPaths` is 4 named files plus every `py_modules/**/*.py` outside `__pycache__`, which is **57** as of 2026-08-08 (this row previously said 52, the value at the time D8 landed; do not treat it as a target). **01 Verified** — two deploys 2026-08-03 (00:47, 00:51), both `Verified 52 files on the Deck.` + exit 0 + `bonsAI plugin loaded!` + zero `Traceback`/`ERROR`. **03 Verified** — planted `STALE_SENTINEL.py` and `py_modules/backend/stale_dir/ghost.py` on the Deck, deployed, both files and the directory gone. (Do **not** use `refactor_helpers.py` as the witness — it was hand-deleted on-device 2026-08-02, so its absence proves nothing.) **02 Verified 2026-08-03** — happened for real rather than staged: a deploy attempted while the Deck had gone to sleep failed at the first `ssh` (exit 255) and the script printed *"Creating remote temp directory failed (exit code 255). Deploy aborted - the Deck may be asleep or unreachable."* with exit 1. Pre-D8 that same run would have attempted every `scp`, ignored all of them, and printed *Deployment complete!*. The `STALE` branch of the hash compare remains simulation-tested only. **Re-run with output redirected** (`.\scripts\build.ps1 2>&1 \| ...`) if the script's error handling is ever touched — a `$ErrorActionPreference = "Stop"` regression made a `pnpm` deprecation warning fatal under redirection and was caught only that way |
| Command reply saves to its chat slot (turn header / chat title) | CMD-REPLY-TITLE-01 | Fixed (desk) 2026-09-04, Deck check owed | A deterministic local-command reply (`bonsai:vac-check`, `bonsai:shortcut-setup-*`, the sanitizer on/off phrases) used to return from `_finalize_immediate_background_local_command` without ever writing to the active chat slot, so the slot never gained a first turn: its title stayed **New chat**, and the frontend's post-completion reload (`useChatSlots.ts`'s `applySlotTranscript`) found nothing and blanked the just-set live question back to `""`, which is what showed as a `…` turn header (trace: [roadmap-details.md](roadmap-details.md#a-command-reply-leaves-the-turn-header-blank-and-the-chat-titled-new-chat)). All three local-command branches now persist the question and the reply through the same `_chat_slots_record_user_turn` / `_chat_slots_record_assistant_turn` steps a normal Ask's completion uses, so the slot renames itself off the command text and the reload finds a real turn instead of an empty slot. **No frontend change was needed** — `turnsToCollapsedTurns` already builds a proper collapsed turn from a complete pair (existing test: `chatSlotTurns.test.ts`, "returns empty pending when turns end on assistant"), so the header stops depending on the live-question path once the turn is archived. Two new backend tests in `tests/test_chat_slot_ownership.py`: `test_vac_check_deny_reply_persists_turn_and_renames_slot` (ban lookup off) and `test_shortcut_setup_reply_also_persists_to_chat_slot` (the second call site). **On-Deck:** from the [+] position with the ban lookup off, send `bonsai:vac-check` — the turn header reads the command, not `…`; the chat's title in the slot row is the command, not *New chat*; after a QAM close and reopen the header still reads it. |

| Steam Frame knowledge-base tips rewritten | KB-FRAME-TIPS-01 | **Shipped 2026-09-12, no Deck check needed** | **Unit 2026-09-12.** `test_knowledge_base_service.py::CompatPatternsTipContentTests::test_steam_frame_tips_replaced_after_the_frame_study` — checks there are exactly seven Frame tips, none mentions a phone app or the old internal note about the feature being research-phase, none claims bonsAI observed or detected anything, and one tells a person the network port to use. **What a person notices:** a question about the Steam Frame no longer points them at a phone app that does not exist. This is a knowledge-base wording change with no screen to look at, so nothing is owed on the Deck. [Plan](planning/49-steam-frame-features.md) § 6. |

**Evidence snapshots (keep):** [tier0/2026-05-26](test-evidence/tier0/2026-05-26-9e20a82/), [tier1Core](test-evidence/tier1Core/2026-05-26-9e20a82/), [tier2Deep/2026-06-09](test-evidence/tier2Deep/2026-06-09-a9237e4/). Historical PASS narration: [archive/testing-results-2026.md](archive/testing-results-2026.md). Orphan evidence folders may be pruned later when nothing links them.

---

## Verify (from roadmap)

These are **not** missing features — deferred maintainer testing. The roadmap carries them in its [Verify § Features](roadmap.md#verify)
list as the `[QA]` **Deferred manual QA** and `[platform]` **VAC check** entries:

1. Device QA Tier 0–1 (formal Pass on current build); SMOKE-B retired 2026-09-03 (D57 #6)
2. VAC-02…06 on-device
3. Broader prompt-testing pass

The QAMP verification matrix was withdrawn 2026-09-02 for the same reason as SMOKE-B (no TDP apply to test).

See [roadmap.md](roadmap.md) and [testing-manual.md](testing-manual.md).

---

## New focusable controls

Before shipping any new Settings/QAM control, complete the focus-graph checklist in [testing-manual.md](testing-manual.md) and add a coverage note above. Policy: `bonsai://policy/decky-ui-focus`.
