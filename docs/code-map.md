<!-- GENERATED by scripts/code_map.py. Do not hand-edit; re-run the script to refresh. -->

# Code map

One entry per app file, grouped by folder: its Title, then the opening of its Purpose. Headers are written at whatever length the file needs, so an entry ending in […] has more in the file itself — open it rather than assuming this is all it says. A file with no entry under Purpose is missing a header line; see `scripts/check_headers.py`, which fails the build on one.

## .

- **main.py** (main.py) — *The plugin's front door*: This is the file Decky loads when the plugin starts, and the only back-end file the screen is able to talk to directly. […]

## py_modules/backend

- **__init__.py** (py_modules/backend/__init__.py) — *Backend package*: Where the plugin's back-end code lives -- the front door in main.py at the repo root, and every service behind it, under services/.
- **constants.py** (py_modules/backend/constants.py) — *The addresses and paths every other file agrees on*: A handful of values need to stay the same everywhere in the backend: the address to try for Ollama when nobody has set their own, the list of names that mean "this same machine" rather than a machine on the network, the folder Decky's Python runs from on the Deck, and the…
- **ollama_connectivity.py** (py_modules/backend/ollama_connectivity.py) — *Telling local Ollama from a network one*: Ollama can run right on the Deck, or on another computer the Deck reaches over the network. This file answers the two questions that come up because of that: does a given address mean "this same machine", and if the plugin ever needs to run the `ollama` program itself, where on…
- **ollama_routing.py** (py_modules/backend/ollama_routing.py) — *Which AI model to ask, and in what order*: Before bonsAI can answer a question, it has to pick which downloaded AI model to send it to, and know what to try next if that one is not on the Deck. […]
- **ollama_urls.py** (py_modules/backend/ollama_urls.py) — *Turning what someone typed into a real Ollama address*: A person can type an Ollama address as almost anything -- just a port number, "host:port", or a full web address. […]
- **tdp_intent.py** (py_modules/backend/tdp_intent.py) — *Spotting "what's my power limit right now" questions*: The Ask box gets power-related questions in several shapes: "what's my TDP right now", "set my TDP to 12 watts", "what TDP should I use for this game". […]
- **tls_ca_fallback.py** (py_modules/backend/tls_ca_fallback.py) — *Fixing "can't verify this secure site" on the Deck's own Python*: The copy of Python that Decky Loader runs the plugin's backend inside is a self-contained bundle, and on a real Deck that bundle never picks up the operating system's list of trusted certificate authorities -- the list every secure (https) address is checked against. […]

## py_modules/backend/services

- **__init__.py** (py_modules/backend/services/__init__.py) — *Services package*: One file per job the plugin does -- talking to the AI, the knowledge base, voice, screenshots, saved chats, settings, and the rest -- kept separate so main.py stays a thin front door instead of one huge file.
- **ai_character_service.py** (py_modules/backend/services/ai_character_service.py) — *AI character voices for Ask replies*: When someone turns on the AI character feature, this file is what makes the reply sound like a chosen game character instead of a plain assistant. […]
- **ask_command_text.py** (py_modules/backend/services/ask_command_text.py) — *Cleaning up what you typed before checking it against a built-in command*: A few things you can type in the Ask box are not questions at all — they are commands, like turning word-filtering off, or the anti-cheat safety check. […]
- **ask_local_commands.py** (py_modules/backend/services/ask_local_commands.py) — *Ask local command detection*: Detect local-only command kinds before Ollama runs.
- **ask_payload.py** (py_modules/backend/services/ask_payload.py) — *Reading what the screen sent for one Ask*: When you type a question and hit Ask, the screen sends it to the back end as a small bundle of fields -- the question text, the address of the PC running the AI, which game you are asking about, any screenshots you attached, and so on. […]
- **ask_topic_instructions.py** (py_modules/backend/services/ask_topic_instructions.py) — *Recognising a question's topic and writing its call-out*: Some questions need extra instructions beyond the base system prompt -- a power or TDP tuning question needs the hardware appendix kept front and center, a display-resolution question needs guidance broken out by 1280x800 / 1080p / 4K, a question about Ollama itself needs…
- **async_background_job.py** (py_modules/backend/services/async_background_job.py) — *Small shared pieces for the plugin's three long-running downloads*: Setting up the AI engine, downloading the knowledge base, and installing the voice engine can each take a while and run in the background while you keep using the Deck. […]
- **async_task_lifecycle.py** (py_modules/backend/services/async_task_lifecycle.py) — *Stopping a background task and waiting for it to actually be gone*: Cancel one background task -- an Ask that is still thinking, the AI engine still installing, the voice engine still installing -- and wait until it has genuinely finished, rather than just asking it to stop and moving on.
- **background_request_state.py** (py_modules/backend/services/background_request_state.py) — *The shape of "how is my Ask coming along"*: An Ask that is still thinking runs in the background while the screen checks back roughly once a second asking "done yet?". […]
- **bonsai_stream_tags.py** (py_modules/backend/services/bonsai_stream_tags.py) — *The "thinking…" line you see while waiting for an answer*: While the AI works on your question, the Deck screen shows a line describing what is happening -- "Searching knowledge base…", "Writing your answer…" -- instead of a plain, silent spinner. […]
- **capabilities.py** (py_modules/backend/services/capabilities.py) — *The five permissions the user grants, and the one question "am I allowed to"*: The plugin can do five things that a person would want to be asked about first: write files to the Desktop, look through the Steam screenshot and video library, read Steam's own logs, talk to Steam's servers over the internet with the user's key, and switch the microphone on. […]
- **chat_memory_service.py** (py_modules/backend/services/chat_memory_service.py) — *What a chat remembers about itself*: Turns a saved chat into a short piece of text the AI can be shown along with the next question, so it answers knowing what has already been said. […]
- **chat_slot_rpc.py** (py_modules/backend/services/chat_slot_rpc.py) — *List, open, start, delete and rename a saved chat*: Saved chats live in their own per-slot files on disk. This file is the five calls the screen makes to manage that list from outside an open chat: see the recent chats, open one, start a new empty one, delete one, and rename one. […]
- **chat_slot_service.py** (py_modules/backend/services/chat_slot_service.py) — *Saved chats you can switch between*: Every separate conversation you keep on the Ask screen -- the ones you can switch between instead of losing your place -- is stored here as one file per chat ("slot"), plus a small index file listing all of them so the switcher can show titles without opening every chat. […]
- **clipboard_service.py** (py_modules/backend/services/clipboard_service.py) — *Reading and writing the Deck's own clipboard*: The panel is drawn in a small embedded browser, and that browser's own copy-and-paste does not reliably reach the Deck's system-wide clipboard -- reading it back in particular is blocked outright. […]
- **compat_topic_router.py** (py_modules/backend/services/compat_topic_router.py) — *Deciding whether a question is a troubleshooting question at all*: The plugin keeps a set of troubleshooting tips -- for things like Proton, a controller not pairing, a stuck update -- separate from its game-strategy notes. […]
- **desktop_note_service.py** (py_modules/backend/services/desktop_note_service.py) — *Saving chats and notes to your Desktop*: This is what writes files onto your Desktop when you ask the plugin to remember something on your own computer instead of only in the Deck's own storage: a note you saved by hand, the daily chat log that autosaves every question and answer, an app activity log for…
- **destructive_advice_guard.py** (py_modules/backend/services/destructive_advice_guard.py) — *Destructive mod/save advice guard*: Output-side check that flags a finished Ollama reply advising the user to delete save data, a Wine/Proton prefix, or compatdata, without a backup step anywhere in the same reply.
- **feedback_service.py** (py_modules/backend/services/feedback_service.py) — *Saving your thumbs up or thumbs down on a reply*: After an Ask finishes, you can rate the reply thumbs up or thumbs down, and on a thumbs-down pick a short reason -- wrong information, too long, too short, guessed the wrong game, or an unmarked spoiler. […]
- **game_ai_request.py** (py_modules/backend/services/game_ai_request.py) — *Running one Ask question, start to finish*: This file is the whole journey of one Ask question, from the moment it arrives to the moment a reply is ready to show. […]
- **input_sanitizer_service.py** (py_modules/backend/services/input_sanitizer_service.py) — *Cleaning up what you type before it reaches the AI*: Every question is checked and tidied up before it goes anywhere near the AI: stray control characters are stripped out, runs of extra whitespace are collapsed, and a question that is empty after cleanup, absurdly long, or reads as random keyboard-mashing rather than a real…
- **intent_pack_rpc.py** (py_modules/backend/services/intent_pack_rpc.py) — *The five settings-search-pack buttons the screen calls*: This is the code behind the Settings screen's manage-packs panel: list what packs exist, turn one on or off, export one as JSON, import one from JSON, and remove one. […]
- **intent_pack_service.py** (py_modules/backend/services/intent_pack_service.py) — *Making settings search understand more words than the exact label*: When you use the search box to find a setting, this is what lets a search for a word like "mic" find the Voice settings even though the setting itself is only labeled "Voice". […]
- **kb_followup_memory.py** (py_modules/backend/services/kb_followup_memory.py) — *Knowledge-base follow-up memory*: Remember the named subject of the last Strategy/Expert question, per plugin process, so a bare follow-up question's search words can carry it forward.
- **kb_not_in_notes_notice.py** (py_modules/backend/services/kb_not_in_notes_notice.py) — *Knowledge-base attribution notices*: Output-side notes telling the user a reply came from the model's own training knowledge rather than the local knowledge base -- one pair for the notes (Strategy/Expert only), one pair for the tip sheet (any Ask mode).
- **knowledge_base_cards.py** (py_modules/backend/services/knowledge_base_cards.py) — *Turning a corpus row into a note the model can read*: The knowledge base keeps its game notes and troubleshooting tips as plain database rows. This file is where a row becomes a `KnowledgeCard` -- the one shape every other piece of knowledge-base code passes around -- and where a set of cards becomes the actual block of text handed…
- **knowledge_base_chips.py** (py_modules/backend/services/knowledge_base_chips.py) — *Suggested-question chips and corpus coverage for the running game*: Two things the rest of the plugin asks the knowledge base for that are not "search for this question" -- a set of ready-made question chips to show under the running game (so a player who does not know what to ask can still tap something), and a plain coverage count ("does the…
- **knowledge_base_game_match.py** (py_modules/backend/services/knowledge_base_game_match.py) — *Working out which game a question belongs to*: Every knowledge-base search is scoped to one game -- searching the whole corpus for "how do I beat the tank" would find Left 4 Dead 2's Tank card while you are playing something else entirely. […]
- **knowledge_base_schema.py** (py_modules/backend/services/knowledge_base_schema.py) — *The knowledge base's table layout and install rules*: The knowledge base is a set of game notes kept on the Deck so the AI can answer from them instead of guessing. […]
- **knowledge_base_search.py** (py_modules/backend/services/knowledge_base_search.py) — *Finding and ranking notes inside one game (or the tip sheet)*: Once knowledge_base_service.py knows which game a question is about (or that it is a troubleshooting question), this file is where the actual search happens -- a keyword pass over the corpus's full-text index, a rescue pass for questions that name a card by kind rather than by…
- **knowledge_base_service.py** (py_modules/backend/services/knowledge_base_service.py) — *Finding the right game notes for a question*: The knowledge base is a set of game notes kept on the Deck so the AI can answer from them instead of guessing -- a short write-up on a boss, a system, or a common problem, stored offline so it works without the internet. […]
- **local_ollama_setup_service.py** (py_modules/backend/services/local_ollama_setup_service.py) — *Installing the local AI on the Deck*: This file is what actually runs when a person taps Install or Update on the Ollama tab. It downloads and installs Ollama (the program that runs the local AI) if it is not there yet, gets it running and ready to accept downloads, then pulls down the model files for whichever…
- **local_ollama_teardown_service.py** (py_modules/backend/services/local_ollama_teardown_service.py) — *Removing the AI engine the plugin installed on your Deck*: When the AI engine (Ollama) is installed and running right on the Deck itself rather than on a separate PC, and you use Clear all plugin data, this is what actually removes it: every downloaded model, the engine program itself, and its cache -- so the Deck is left clean rather…
- **media_desktop_rpc.py** (py_modules/backend/services/media_desktop_rpc.py) — *Screenshots, the Desktop debug log, clipboard, and small one-off reads*: A grab bag of calls the screen makes that do not belong to one bigger feature: browse recent screenshots for attaching to a question, take one on the spot, write to the Desktop debug/chat/app logs a person can open in a text editor, read or write the system clipboard when the…
- **model_policy.py** (py_modules/backend/services/model_policy.py) — *Which AI models you are allowed to use, by how open they are*: The Model policy setting lets you choose how open-source an AI model has to be before the plugin will use it: only fully open-source models, open-source plus models whose weights are published under looser terms, or (once you flip an extra unlock) almost anything installed. […]
- **network_service.py** (py_modules/backend/services/network_service.py) — *Deck network helpers*: Discover the Deck LAN IPv4 address for Ollama remote-access hints.
- **ollama_ask_budgets.py** (py_modules/backend/services/ollama_ask_budgets.py) — *Ollama Ask token budgets*: Per-mode visible ``num_predict`` caps, thinking budgets, and the session record of which models reject ``think``.
- **ollama_ask_extras.py** (py_modules/backend/services/ollama_ask_extras.py) — *What run_ask_ollama needs before its first try*: Two pieces of setup run_ask_ollama() (in ollama_ask_service.py) does before it calls Ollama for the first time. `build_ollama_request_extras()` shapes the one dict of facts about an Ask attempt -- the actual prompt sent, the question, how many images went with it and their…
- **ollama_ask_service.py** (py_modules/backend/services/ollama_ask_service.py) — *Sending one Ask question to Ollama*: This is the file that actually sends a person's question to the AI and streams the reply back. By the time it runs, the question and any screenshots are already gathered — this file's job is to add the roleplay character voice if one is on, work out which model to try and in…
- **ollama_catalog_service.py** (py_modules/backend/services/ollama_catalog_service.py) — *Checking a model name before downloading it*: On the Pull Models screen you can type the name of any Ollama model to download, not just pick from the list the plugin already knows about. […]
- **ollama_connection_test.py** (py_modules/backend/services/ollama_connection_test.py) — *Ollama connection test*: Decide whether the AI is reachable, and on this Deck try once to start it.
- **ollama_embed_service.py** (py_modules/backend/services/ollama_embed_service.py) — *Ollama embed client*: Call Ollama /api/embed for knowledge-base hybrid retrieval vectors.
- **ollama_local_autostart_service.py** (py_modules/backend/services/ollama_local_autostart_service.py) — *Starting the local AI when the Deck starts*: This file is what "Start the AI with the Deck" in the Ollama tab actually does. It writes (or removes) a small startup entry so the Deck's own copy of Ollama, the local AI, turns on by itself every time the Deck boots, instead of needing to be started by hand. […]
- **ollama_local_setup_rpc.py** (py_modules/backend/services/ollama_local_setup_rpc.py) — *Installing AI models onto the Deck itself*: When the AI runs on the Deck rather than a PC, something has to install Ollama there in the first place, pull the starter set of models, let a person pull more by name, delete one, and price a model's download before it starts. […]
- **ollama_mdns_discovery_service.py** (py_modules/backend/services/ollama_mdns_discovery_service.py) — *Ollama mDNS discovery*: User-triggered mDNS browse for _ollama._tcp.local hosts on the LAN.
- **ollama_prompts.py** (py_modules/backend/services/ollama_prompts.py) — *Writing the instructions the AI reads before answering*: Before every Ask question goes to the AI, it needs a full set of written instructions — who bonsAI is, what game is running, what it should and should not talk about, how it should handle spoilers, how long the reply should be, and so on. […]
- **ollama_service.py** (py_modules/backend/services/ollama_service.py) — *Talking to the AI over the network*: This is the file that actually opens the connection to Ollama and reads an answer back, word by word, as it is written. […]
- **plugin_data_reset.py** (py_modules/backend/services/plugin_data_reset.py) — *Clearing all of the plugin's own data*: When someone presses "Clear all data" in Settings, this is what actually does the work. It wipes every saved setting, the local search- knowledge folder if one was installed, the plugin's own temporary and log files, and a leftover file from an old removed feature, then writes a…
- **prompt_budget_service.py** (py_modules/backend/services/prompt_budget_service.py) — *Deciding how the room in a question is shared out*: A question sent to the AI has to hold several things at once -- who the AI is and the rules it follows, the game's cards, what this chat has already covered, the question itself, room for the AI to think, and room for the answer. […]
- **proton_troubleshooting_logs.py** (py_modules/backend/services/proton_troubleshooting_logs.py) — *Pulling in a few lines of Steam and Proton's own logs*: When someone asks the Ask box for help with a game that runs through Proton, Steam and Proton keep their own log files about what went wrong, and a few relevant lines from those logs make a far better answer than guessing blind. […]
- **pull_model_catalog_service.py** (py_modules/backend/services/pull_model_catalog_service.py) — *Downloading fresh hints for the Pull Models screen*: The Pull Models screen groups AI models, shows their license, and gives a short blurb about each one. Those details go stale over time -- a new model comes out, a rating turns out to be wrong, a license changes -- so on top of what ships bundled with the plugin, this file also…
- **rag_corpus_download_service.py** (py_modules/backend/services/rag_corpus_download_service.py) — *Downloading the knowledge base and putting it on disk*: When someone chooses to install the offline knowledge base, this file does the actual work: it looks up where to download from, fetches a small manifest file listing what to download and the checksum each piece must match, downloads the pieces with progress reported back and a…
- **rag_corpus_local_install.py** (py_modules/backend/services/rag_corpus_local_install.py) — *Installing the knowledge base from a folder already on the Deck*: The knowledge base can be installed without downloading anything, by pointing at a folder that is already sitting on the Deck's own disk. […]
- **rag_corpus_rpc.py** (py_modules/backend/services/rag_corpus_rpc.py) — *The knowledge base's own download, update and remove buttons*: The offline knowledge base is a folder of notes downloaded once and kept on disk. This file is the screen's seven calls for that: check its status, start the download, cancel one in progress, check for and pull an update, remove it, install one from a local folder for testing…
- **rag_corpus_status.py** (py_modules/backend/services/rag_corpus_status.py) — *Answering "what is the knowledge base doing right now"*: The Knowledge base section of the Connection tab needs one full answer every time it is shown: is a knowledge base installed, where does it live, has meaning search finished being set up for it yet, and how much room is there to put one. […]
- **reply_followup_blocks.py** (py_modules/backend/services/reply_followup_blocks.py) — *Handling the "refine this reply" chips*: After an answer, the player can tap a chip -- bad information, too long, too short, misidentified game, or an unfenced spoiler -- and ask a follow-up refining it. […]
- **reply_language_service.py** (py_modules/backend/services/reply_language_service.py) — *Reply language resolver*: Detect Steam client language and resolve Ask reply-language settings.
- **reply_style_blocks.py** (py_modules/backend/services/reply_style_blocks.py) — *Shaping how long and in what language a reply comes back*: Two of the player's own settings change how a reply reads rather than what it says -- reply verbosity (Caveman terse, Detailed, or the balanced default) and reply language. […]
- **response_verify.py** (py_modules/backend/services/response_verify.py) — *Ollama response verifier*: Rule-based post-check for hallucination-prone patterns in Ollama replies.
- **screenshot_media.py** (py_modules/backend/services/screenshot_media.py) — *Finding, taking, and preparing screenshots*: This is what handles every screenshot in the plugin: finding the ones Steam already saved or the plugin took earlier, taking a new one of whatever game is running when you press the screenshot button, and shrinking a screenshot down before it is sent to the AI along with your…
- **settings_service.py** (py_modules/backend/services/settings_service.py) — *Reading and cleaning up your saved settings*: Every toggle and choice you make on the plugin's settings screens -- Permissions, Ask behavior, voice, the knowledge base, and the rest -- lives in one file on disk, settings.json. […]
- **shortcut_setup_commands.py** (py_modules/backend/services/shortcut_setup_commands.py) — *Shortcut setup commands*: Deterministic Ask keywords that return Guide-chord setup guidance for Deck/Stadia layouts.
- **spoiler_risk_service.py** (py_modules/backend/services/spoiler_risk_service.py) — *Guessing how spoiler-heavy a reply's topic is, for the "Show details" chip*: Some replies touch things a player might not want to know yet -- a boss's weak point, how a story ends. This file works out a rough low / medium / high guess at how likely a reply's topic is to spoil something, purely so the "Show details" chip under a reply can show it. […]
- **spoiler_title_profiles.py** (py_modules/backend/services/spoiler_title_profiles.py) — *Deciding which games get extra spoiler caution, game by game*: Very different games get asked about in the Ask box. Some, like Deep Rock Galactic or DOOM Eternal, barely have "spoilers" in the way most people mean it -- knowing a boss's attack pattern is not really ruining anything. […]
- **spy_confession_service.py** (py_modules/backend/services/spy_confession_service.py) — *The Spy's after-the-fact confession tag*: At the two heaviest accent settings the Spy character voice (ai_character_service.py) is instructed to give advice that sounds right and is wrong, then end the reply with a short closing block naming what it lied about. […]
- **steam_vac_service.py** (py_modules/backend/services/steam_vac_service.py) — *Asking Steam directly whether an account has a public ban*: Does the real work behind checking an anti-cheat ban: turns whatever a person pasted in -- a raw 64-bit SteamID, or a profile web address that contains one -- into the exact form Steam's own servers expect, calls Steam's ban-lookup address with the plugin's saved key, and turns…
- **strategy_checklist_rpc.py** (py_modules/backend/services/strategy_checklist_rpc.py) — *The three RPCs behind the Strategy checklist's own memory*: A Strategy or Expert answer can come with a checklist, and the game remembers which boxes you ticked the next time you ask about it. […]
- **strategy_checklist_session_service.py** (py_modules/backend/services/strategy_checklist_session_service.py) — *Remembering which strategy checklist boxes were ticked*: When Strategy mode builds a checklist for a game -- what to bring, what to do in order -- the boxes someone ticks off should still be ticked the next time they open that same game's checklist, even after closing the overlay or leaving and coming back later. […]
- **strategy_entity_extraction.py** (py_modules/backend/services/strategy_entity_extraction.py) — *Working out which boss or enemy the player named*: Before the game can decide how much of a spoiler policy to relax, it needs to know whether the player actually named a specific boss, enemy, or other entity by name -- how do I beat the Tank" names one; "how do I get past this part" does not. […]
- **strategy_guide_parse.py** (py_modules/backend/services/strategy_guide_parse.py) — *Reading branch pickers and checklists out of an answer*: When you ask a strategy question, the AI can end its answer with a special block asking which part you are stuck on, or a checklist tracking steps for a longer strategy. […]
- **strategy_spoiler_policy.py** (py_modules/backend/services/strategy_spoiler_policy.py) — *Deciding the spoiler rule for a Strategy Guide turn*: Strategy Guide coaching normally hides story spoilers behind ```bonsai-spoiler``` fences, but that default has to bend in a few known ways -- the player said spoilers are okay, they named a specific boss or enemy by name so that one thing is fair game, or the game itself treats…
- **tdp_service.py** (py_modules/backend/services/tdp_service.py) — *Deck TDP service*: Read current Steam Deck TDP and supply the clamp bounds Ask recommendations use.
- **token_accounting_service.py** (py_modules/backend/services/token_accounting_service.py) — *Knowing the real token numbers instead of guessing at them*: Every question the plugin sends has to fit inside a fixed amount of room, measured in tokens. Until now the plugin guessed at two of the three numbers involved: it assumed the room was always 4,096 tokens, and it worked out how big a question was by counting characters and…
- **transparency_service.py** (py_modules/backend/services/transparency_service.py) — *What "Show details" knows about one answer*: Every Ask reply carries a "Show details" panel that lets a person see what actually happened behind the answer — was a screenshot sent, did it check the local knowledge base, which model answered, and so on. […]
- **vac_check_commands.py** (py_modules/backend/services/vac_check_commands.py) — *Answering "is this account banned" without asking the AI at all*: Typing a command like `bonsai:vac-check 76561198000000000` into the Ask box checks one or more Steam accounts for a public anti-cheat ban flag, straight from Valve's own servers -- no AI model involved, no guessing. […]
- **voice_audio_capture_service.py** (py_modules/backend/services/voice_audio_capture_service.py) — *Finding and opening the microphone*: Before any audio can be recorded, this file works out two things a background plugin process cannot simply assume: where the interactive session's PipeWire/Pulse audio sockets live, and which capture command to run against them (pw-record, parecord or arecord, whichever is…
- **voice_read_aloud_service.py** (py_modules/backend/services/voice_read_aloud_service.py) — *Reading an answer out loud*: This is what happens when you press "Read aloud" on an answer: it speaks the text using the Deck's own built-in voice (a program called espeak-ng), one sentence at a time, through the same sound system a game would play through.
- **voice_rpc.py** (py_modules/backend/services/voice_rpc.py) — *The voice input and read-aloud buttons the screen calls*: This is the code behind two related but separate voice features: typing a question by talking (install the engine, start/stop/poll a recording) and having an answer read back out loud (start/stop/poll a reading). […]
- **voice_transcription_service.py** (py_modules/backend/services/voice_transcription_service.py) — *Turning your spoken question into text, live*: This is what happens between pressing the mic button and seeing your words appear as you talk: it records your microphone, feeds short slices of that recording to a local speech-to-text engine (whisper.cpp) again and again as you keep talking, and stitches those overlapping…
- **voice_whisper_daemon.py** (py_modules/backend/services/voice_whisper_daemon.py) — *The engine that turns your voice into text*: When you press the mic button and speak instead of typing, this is the piece that understands what you said. It manages a small helper program (whisper-server) that does the actual listening, and keeps one copy of it running in the background so the next question does not have…
- **voice_whisper_runtime.py** (py_modules/backend/services/voice_whisper_runtime.py) — *Where the speech-to-text program lives, and two things both of its callers need*: Two different files turn recorded audio into text using the whisper.cpp program: one runs it fresh for a few seconds at a time while you are speaking (voice_transcription_service), the other runs it as one long-lived background server that several recordings can share without…

## src

- **index.tsx** (src/index.tsx) — *Plugin root*: The file Decky loads to build bonsAI's whole panel — the one a person opens from the Quick Access Menu. `definePlugin()` at the very bottom hands Decky the plugin's name, icon, and its whole React tree; everything above that builds the tree. `Content` is that tree: it wires…
- **pluginVersion.ts** (src/pluginVersion.ts) — *Plugin version number*: Holds the version number the plugin shows a person, on the About tab and anywhere else it names itself. Nobody types the number here: this whole file is written from scratch by scripts/sync-version-from-plugin.mjs, reading plugin.json, so what the plugin says about its own…
- **types.d.ts** (src/types.d.ts) — *Image import types*: Tells TypeScript what a line like `import icon from "./icon.svg"` actually hands back. Without this, TypeScript does not know what a .svg, .png, or .jpg file is, and refuses to build any file that imports one.

## src/components

- **AboutReplyLanguageSection.tsx** (src/components/AboutReplyLanguageSection.tsx) — *Reply language picker*: The dropdown on the About tab where you choose what language the AI writes its replies in. This is separate from Steam's own display language — you can run Steam in English and still have the AI answer in Spanish, or leave it on "follow system" so it always matches whatever…
- **AboutTab.tsx** (src/components/AboutTab.tsx) — *About tab*: The About tab — what bonsAI is, a beta warning, the reply-language dropdown (its own file, AboutReplyLanguageSection), and links out to GitHub, the Ollama project, the bug tracker, and support. […]
- **BonsaiChatSecondaryButton.tsx** (src/components/BonsaiChatSecondaryButton.tsx) — *The small button under an AI reply*: One of the small controls that sit under an AI answer — Helpful, Not really, Read aloud, Show details. This file draws the button itself: Steam's own button, made into a real D-pad stop, so a person can reach it with the controller and not just a mouse or touch. […]
- **BonsaiDebugOverlay.tsx** (src/components/BonsaiDebugOverlay.tsx) — *Debug overlay HUD*: On-screen ring buffer of recent bonsAI debug ingest lines and mount counters.
- **BonsaiModalScope.tsx** (src/components/BonsaiModalScope.tsx) — *Plugin look-and-feel wrapper for popups*: When the plugin opens one of Steam's own full-screen popups — the models hub, the pull-models list, and others — that popup is drawn outside the plugin's normal box on screen, so none of the plugin's usual styling reaches it on its own. […]
- **BonsaiPluginShell.tsx** (src/components/BonsaiPluginShell.tsx) — *The plugin's outermost box*: The very first element the plugin draws — every tab (Main, Settings, Permissions, and the rest) is drawn inside this one box. […]
- **CharacterPickerModal.tsx** (src/components/CharacterPickerModal.tsx) — *Character picker modal*: The full-screen picker for choosing your AI's roleplay character, reached from wherever "AI character" is opened in Settings or the Main tab. […]
- **CharacterPropGlyph.tsx** (src/components/CharacterPropGlyph.tsx) — *Character prop glyph*: One small icon — a bat, a fedora, a sandwich, and so on — used as the picture for an AI character. It shows up tiny next to the Ask bar, as the avatar for whoever you are currently talking to, and a little bigger in the character picker where you choose who to talk to next. […]
- **CharacterRoleplayEmoticon.tsx** (src/components/CharacterRoleplayEmoticon.tsx) — *Character roleplay emoticon*: Render preset or synthetic character avatars, as pixel grids or prop emblems, with an optional badge letter.
- **ContextChipLadder.tsx** (src/components/ContextChipLadder.tsx) — *Context chip ladder*: The row of small colored chips that appears once you open Session context" above the chat, or inside a turn's own "Show details" panel — one chip per kind of extra material attached to a question (files read, a screenshot, remembered notes, developer info…). […]
- **DesktopNoteSaveModal.tsx** (src/components/DesktopNoteSaveModal.tsx) — *Save this exchange to a Desktop note*: The popup that appears when someone chooses to save the current question and answer to a file. It asks for a file name, shows where the note will be saved and that new saves are added to the end of the file rather than overwriting it, and hands the name back once the person…
- **DeveloperTab.tsx** (src/components/DeveloperTab.tsx) — *Developer tab*: The Developer tab, only visible once Developer Mode is turned on. It is for troubleshooting and testing bonsAI itself, not for changing how the AI answers: captured crash logs, which tab reopens the plugin, whether app activity gets written to Desktop as a log file, a Steam Web…
- **DrgGlossaryTermChip.tsx** (src/components/DrgGlossaryTermChip.tsx) — *DRG glossary term chip*: One underlined word inside an AI reply, for a Deep Rock Galactic Survivor term the game does not explain well on its own (like "kiting"). […]
- **KnowledgeBaseSection.tsx** (src/components/KnowledgeBaseSection.tsx) — *Knowledge base section*: The "Knowledge base (offline)" panel on the Ollama tab. A toggle turns on grounding Strategy and troubleshooting answers with a downloaded set of offline strategy cards; below it, a status line and a button that reads Download, Update, or Downloading… depending on what is…
- **MainTab.tsx** (src/components/MainTab.tsx) — *Main tab shell*: This is the Main tab a person sees when they open bonsAI: the chat history above, and a dock below it holding the suggestion chips and the question box. […]
- **MainTabAskModeMenuPopover.tsx** (src/components/MainTabAskModeMenuPopover.tsx) — *The Speed / Strategy / Expert menu*: The small pop-up list that opens when someone taps the mode chip next to the Ask bar, letting them pick how the AI should answer: Speed, Strategy, or Expert. […]
- **MainTabAttachMenuPopover.tsx** (src/components/MainTabAttachMenuPopover.tsx) — *The attach menu*: The small pop-up list that opens when someone taps the paperclip next to the Ask bar. It offers two things: close the menu and take a new screenshot, or attach a screenshot already on the system. […]
- **MainTabBonsaiAiMarkdownChunk.tsx** (src/components/MainTabBonsaiAiMarkdownChunk.tsx) — *AI reply markdown renderer*: Turns one chunk of an AI answer's raw markdown text into the actual page — paragraphs, lists, code blocks, links — and handles two special cases along the way: a spoiler fence that a Strategy Guide answer can hide until it is tapped open, and, on DRG Survivor, turning a curated…
- **MainTabChatTranscript.tsx** (src/components/MainTabChatTranscript.tsx) — *The chat transcript*: Everything below the Ask bar: the whole conversation in this chat — the finished questions and answers from before, and the one being asked right now or just finished — plus, underneath all of that, a button to save the chat to the desktop.
- **MainTabPresetAnimatedChips.tsx** (src/components/MainTabPresetAnimatedChips.tsx) — *Preset animated chips*: Draws the row of suggestion chips above the Ask bar, in whichever of four styles the settings picked: fade (one chip fades out as the next fades in), carousel (chips slide in sideways from the right, like a ticker), static (a plain swap, no animation), or decode (each new chip's…
- **MainTabPresetRow.tsx** (src/components/MainTabPresetRow.tsx) — *Main tab preset row*: The row of suggestion chips sitting right above the Ask bar. Tap one and its text drops straight into the question box. […]
- **MainTabScreenshotBrowser.tsx** (src/components/MainTabScreenshotBrowser.tsx) — *Screenshot browser panel*: The screen that opens when someone picks "browse recent" from the attach menu next to the question box. It shows a grid of recent screenshots to tap and attach, a "Paste clipboard" button that drops whatever is on the clipboard into the question instead, and a refresh button. […]
- **MainTabUnifiedAskBar.tsx** (src/components/MainTabUnifiedAskBar.tsx) — *The Ask bar*: The box a person types their question into, and every button around it: the paperclip that attaches a screenshot, the button that picks which AI mode to ask in, the microphone (which turns into a Stop button while a recording or an answer is running), and the big Ask button…
- **MainTabUnifiedAskBar.types.ts** (src/components/MainTabUnifiedAskBar.types.ts) — *The Ask bar's own prop shape*: The prop type MainTabUnifiedAskBar takes, and the one small helper that turns a screenshot media-permission error message into the capability key the Permissions screen needs to jump to the right toggle.
- **ModelPolicyTierPanel.tsx** (src/components/ModelPolicyTierPanel.tsx) — *Model policy tier draft state*: `useModelPolicyTierDraft` — the small draft-until-Done state hook OllamaModelsHubModal uses to hold a pending Licence pick before the person presses Done.
- **ModelRoutingAdvancedPanel.tsx** (src/components/ModelRoutingAdvancedPanel.tsx) — *Model routing advanced panel*: Advanced toggles for non-FOSS unlock and high-VRAM fallback routing in the models hub.
- **ModelRoutingOrderModal.tsx** (src/components/ModelRoutingOrderModal.tsx) — *Model routing order modal*: The full-screen list reached from the Ollama tab's "Set text model try order" and "Set vision model try order" buttons. […]
- **OllamaModelsHubModal.tsx** (src/components/OllamaModelsHubModal.tsx) — *The AI models screen*: The full-screen popup for everything about which AI models this plugin can use: browsing and downloading models (with the model licence choice folded in as one of the Browse screen's own filters), plus a set of advanced routing switches reached from a small "Advanced" link. […]
- **OllamaReplyVerbositySlider.tsx** (src/components/OllamaReplyVerbositySlider.tsx) — *Reply verbosity slider*: Three-stop Deck slider for Caveman / Balanced / Detailed reply prose style.
- **OllamaTab.tsx** (src/components/OllamaTab.tsx) — *Ollama settings tab*: The whole Ollama tab in Settings — where the AI's address lives, how it decides which model to use, and everything about how it replies. […]
- **OllamaThinkingEffortRow.tsx** (src/components/OllamaThinkingEffortRow.tsx) — *Thinking effort row*: The row on the Ollama tab labeled "Thinking," where you choose how hard the AI reasons before it answers: Off, Brief, Balanced, or Deep. […]
- **OllamaWhereAiRunsSection.constants.tsx** (src/components/OllamaWhereAiRunsSection.constants.tsx) — *Where-AI-runs section — shared constants*: The fixed numbers and canned copy the "Where AI runs" panel uses for its connection tests, its mDNS search, and its three local-install setup profiles (Tier 1 essentials, Tier 2 multimodal, update-installed).
- **OllamaWhereAiRunsSection.tsx** (src/components/OllamaWhereAiRunsSection.tsx) — *Where AI runs section*: The "Where AI runs" panel at the top of the Ollama tab, where you choose whether the AI runs on this Deck itself or on a PC somewhere on your home network — and everything involved in setting that up: installing or updating Ollama on the Deck, picking a starting model bundle to…
- **OllamaWhereAiRunsSection.types.ts** (src/components/OllamaWhereAiRunsSection.types.ts) — *Where-AI-runs section — shared types*: The result shapes for the backend calls the "Where AI runs" panel makes (mDNS discovery, the local setup poll, the startup-entry poll), and the props the panel component itself takes.
- **PermissionDenyAction.tsx** (src/components/PermissionDenyAction.tsx) — *"You need to turn this on" message and button*: The small message and Open Permissions button that appears wherever something is blocked because a permission is off — trying to browse screenshots, use the microphone, or read game logs, say. […]
- **PermissionsTab.tsx** (src/components/PermissionsTab.tsx) — *Permissions tab*: The Permissions tab — the screen where you turn on the things the AI is allowed to touch: saving files to your desktop, looking up your Steam ban status, using the microphone, and reading game screenshots and Proton logs to help with troubleshooting. […]
- **PluginHelpModal.tsx** (src/components/PluginHelpModal.tsx) — *"Using bonsAI" popup*: The quick-start popup someone sees the first time they open bonsAI, or whenever they tap the "How to use bonsAI" suggestion chip. […]
- **PullModelsModal.tsx** (src/components/PullModelsModal.tsx) — *Pull models modal*: The full-screen model catalog, reached from the Ollama tab's Browse models…" button (or embedded inline inside the AI models hub). […]
- **PullModelsModal.types.ts** (src/components/PullModelsModal.types.ts) — *Pull models screen — shared types*: The request/response shapes and row/section types PullModelsModal.tsx (and the code it shares work with) needs, kept together so the component file itself reads as flow rather than declarations.
- **ReplyCopyButton.tsx** (src/components/ReplyCopyButton.tsx) — *Reply copy button*: This is the Copy button under a finished reply, or the small icon in the answer bubble's own corner. Press it and the answer's visible text goes to the clipboard. […]
- **SessionContextStrip.tsx** (src/components/SessionContextStrip.tsx) — *The Session tab, inside a turn's own Show details panel*: The list of every turn in this chat that attached anything extra to what was actually sent to the AI — a screenshot, a log excerpt, a memory note — plus the active row's own chips and a Clear button at the end. […]
- **SettingsTab.tsx** (src/components/SettingsTab.tsx) — *Settings tab*: The Settings tab — screen size, whether the Ask bar remembers what you typed, story spoiler masking, how many suggestion chips show, voice input and voice reply settings, the AI's voice & personality picker, whether the Developer tab is visible, and two buttons for clearing…
- **SettingsTabAccentIntensityMenuPopover.tsx** (src/components/SettingsTabAccentIntensityMenuPopover.tsx) — *Accent intensity menu popover*: Inline popover for AI character accent intensity (Light / Default / Strong / Unleashed).
- **SettingsTabConnectionTimeoutSlider.tsx** (src/components/SettingsTabConnectionTimeoutSlider.tsx) — *Warning and timeout slider*: One slider track with two handles that set how long bonsAI waits on a slow AI request. The left, amber handle is the soft warning — past this many seconds, a "this is taking a while" hint appears while the question keeps waiting. […]
- **SettingsTabOllamaKeepAliveSlider.tsx** (src/components/SettingsTabOllamaKeepAliveSlider.tsx) — *"Keep models loaded" slider*: A one-handle slider that sets how long an AI model stays loaded in memory after the last question, before it is unloaded to free up space for other things. […]
- **SettingsTabUiScaleSection.tsx** (src/components/SettingsTabUiScaleSection.tsx) — *UI scale settings section*: The "UI scale" section on the Settings tab. Auto is on by default and picks Handheld sizing (that is all it can ever pick — see uiScaleProfile's own notes); turning it off reveals a slider so you can pick Handheld or Couch yourself, plus a button to jump back to automatic. […]
- **SettingsTabUiScaleSlider.tsx** (src/components/SettingsTabUiScaleSlider.tsx) — *UI scale slider*: Two-stop Deck slider for Handheld / Couch manual UI scale profiles (Desktop was cut 2026-09-21 — it multiplied by the same 1 as Handheld, so it was a choice that did nothing).
- **StrategyChecklistPanel.tsx** (src/components/StrategyChecklistPanel.tsx) — *Strategy checklist panel*: Toggle list for per-game strategy guide checklist items during a live turn.
- **StreamFenceWaitChip.tsx** (src/components/StreamFenceWaitChip.tsx) — *"Still writing…" chip*: A small pulsing status chip shown while an answer is still arriving and the AI is in the middle of something that should not be shown half-finished — a code block that has not been closed yet, or a spoiler-hidden section. […]
- **VoiceInputSettingsSection.tsx** (src/components/VoiceInputSettingsSection.tsx) — *Voice input settings section*: The "Voice input" section on the Settings tab. Lets you choose which speech-to-text model powers the microphone button on the Ask bar (a faster, less accurate one, or a slower, more accurate one), shows whether the voice engine is installed and ready, and has a button to install…
- **bonsaiLogoPath.ts** (src/components/bonsaiLogoPath.ts) — *The plugin's own logo, as one SVG path*: Holds the production logo's outline as a single string, split out of icons.tsx so that file stays readable next to its much smaller icons.
- **icons.tsx** (src/components/icons.tsx) — *The plugin's icon set*: Every small icon drawn anywhere in the plugin, gathered in one file: the tab-title icons (a lock for Permissions, a gear for Settings, a bonsai tree for Main, a bug for Developer, the Ollama mark, a lowercase i" for About), the plugin's own logo as an inline icon (the open tab…

## src/components/deck

- **DeckFocusSlider.tsx** (src/components/deck/DeckFocusSlider.tsx) — *Deck focus slider*: The slider control drawn on several rows in Settings and the Ollama tab — UI scale, how long to keep the model warm, how long to wait for a reply before giving up, how detailed replies should be. […]

## src/context

- **UiScaleContext.tsx** (src/context/UiScaleContext.tsx) — *Passing the UI scale setting down to popups*: bonsAI has a UI scale setting that makes its own text and controls bigger or smaller. Every tab picks that setting up automatically because it is drawn inside the plugin's main box. […]

## src/data

- **aiCharacterAccentIntensity.ts** (src/data/aiCharacterAccentIntensity.ts) — *How strong the AI character's accent is*: When the AI character voice is turned on, Settings has a row of four choices for how far to lean into it: Light, Default, Strong, and Wild. […]
- **askMode.ts** (src/data/askMode.ts) — *The three Ask modes: Speed, Strategy, Expert*: Every question typed into the Ask bar goes out in one of three modes, picked from the small menu above the bar: Speed for a fast plain answer, Strategy for a walkthrough with branching choices, and Expert for the most thorough (and slowest) answer. […]
- **askThinkEffort.ts** (src/data/askThinkEffort.ts) — *How much the AI thinks before answering*: A row in the Ollama tab lets the user choose how much reasoning the AI does before it writes a reply: Off, Brief, Balanced, or Deep. […]
- **bonsaiSettingsNormalizers.ts** (src/data/bonsaiSettingsNormalizers.ts) — *Making sense of a settings file that might say anything*: A saved settings file can hold almost anything — an old value from a version of the plugin that worked differently, a value someone edited by hand, or nothing at all for a setting that did not exist yet when the file was written. […]
- **bonsaiSettingsSchema.ts** (src/data/bonsaiSettingsSchema.ts) — *Every setting the plugin has, in one place*: Everything a person can change in Settings, Permissions, and the Ollama tab — around fifty settings in all — is listed here exactly once, with its starting value and, for anything with limits (a slider, a number field), the smallest and largest value it can hold. […]
- **characterCatalog.ts** (src/data/characterCatalog.ts) — *Character roleplay catalog*: Frontend list of roleplay character presets for the picker and main-tab chrome.
- **characterPlaceholderEmoticonGrids.ts** (src/data/characterPlaceholderEmoticonGrids.ts) — *Tiny pixel-art faces for the AI characters*: Holds a small pixel-art portrait for every AI character preset — the little face shown beside the Ask box and in the character picker when the AI-character feature is on. […]
- **characterPropGlyphAppearance.ts** (src/data/characterPropGlyphAppearance.ts) — *Character prop glyph appearance data*: What CharacterPropGlyph.tsx needs before it draws anything — which named prop and tint color go with which character key, and how to pick a readable line color for a given tint. […]
- **characterUiAccent.ts** (src/data/characterUiAccent.ts) — *Character UI accent colors*: Per-preset main/subtle accent pairs and helpers for character-derived theming.
- **deckEssentialsTags.ts** (src/data/deckEssentialsTags.ts) — *The two one-tap model downloads*: When the AI runs on the Deck itself instead of a PC, the Ollama tab offers two one-tap downloads instead of asking the user to pick a model by hand: "Install Tier 1 essentials" (one small open-source model that can chat, read screenshots, and do OCR) and an optional Tier 2 pull…
- **deprioritizedModels.ts** (src/data/deprioritizedModels.ts) — *Deprioritized Ollama tags*: Sets of model tags deprioritized or blocked from the pull catalog and routing UI.
- **drgGlossaryTerms.ts** (src/data/drgGlossaryTerms.ts) — *DRG Survivor glossary terms*: Curated jargon definitions for Deep Rock Galactic: Survivor, tap-to-define inline in replies.
- **inputSanitizerCommands.ts** (src/data/inputSanitizerCommands.ts) — *The two magic phrases that turn text clean-up off and on*: Before a question is sent to the AI, the plugin quietly cleans it up — this is on by default and most people never notice it happening. […]
- **knowledgeBaseDev.ts** (src/data/knowledgeBaseDev.ts) — *Knowledge base seed path*: Deck filesystem path constant for the deployed seed knowledge-base corpus.
- **modelPolicy.ts** (src/data/modelPolicy.ts) — *The three model policy tiers*: bonsAI limits which AI models it will fall back to, controlled by a three-step tier picker: Tier 1 "Open source only" (the safe default), Tier 2 "Also try open-weight models", and Tier 3 "Any installed model" (locked behind an extra confirmation). […]
- **ollamaKeepAlive.ts** (src/data/ollamaKeepAlive.ts) — *How long a model stays loaded after answering*: A slider in Settings controls how long the AI model stays loaded in memory on the computer or Deck after it answers a question, before Ollama unloads it to free that memory back up. […]
- **pluginQuickStartInstructions.tsx** (src/data/pluginQuickStartInstructions.tsx) — *The help popup's quick-start text*: The actual words shown inside the plugin's "How to use bonsAI" help popup: first, a highlighted box telling a person they must set where the AI runs before anything works, then a short bulleted list covering the rest of the plugin at a glance.
- **presets.ts** (src/data/presets.ts) — *The suggested-question chips on the Main tab*: The Main tab shows a small row of suggested questions — "Why is my Deck running hot?", "How do I fix stuttering?", and so on — that the user can tap to fill the Ask bar instead of typing. […]
- **pullModelCatalog.ts** (src/data/pullModelCatalog.ts) — *The list of models the Pull Models screen can offer*: The Pull Models screen lets the user browse and download AI models to run on their own computer or Deck. This file is the built-in list behind that screen: for every model, its Ollama download name, how big the download is, when it was released, its license and license class…
- **replyLanguage.ts** (src/data/replyLanguage.ts) — *Which language the AI replies in*: The About tab has a setting for which language the AI writes its answers in: "Follow system" (whatever language Steam itself is set to), Always English", or one fixed language picked from a list of every language Steam supports. […]
- **replyMicroActions.ts** (src/data/replyMicroActions.ts) — *The quick fix-it chips under a reply*: Under a finished answer, a row of small chips lets the user ask for a quick correction with one tap instead of typing it out: Bad information, Too long, Too short, Misidentified game/problem, and Unfenced spoiler. […]
- **replyVerbosity.ts** (src/data/replyVerbosity.ts) — *How long the AI's replies are*: A three-stop slider in the Ollama tab controls how long-winded the AI's answers are: Caveman (short, blunt, key points only), Balanced (the default, no particular instruction either way), and Detailed (more explanation, examples, and steps). […]
- **settingsDatabase.ts** (src/data/settingsDatabase.ts) — *Every Steam settings page, by name*: Typing into the Main tab's search can jump straight to a Steam settings page instead of asking the AI a question — typing "brightness", for instance, can jump straight to Settings > Display > Brightness. […]
- **spoilerTitleProfiles.ts** (src/data/spoilerTitleProfiles.ts) — *Which games get extra spoiler protection*: When the AI's answer wraps something in a spoiler fence, the screen normally hides it behind a tap-to-reveal. For some games that tap is pointless — the AI's answers about, say, Deep Rock Galactic almost never contain a real story spoiler, so hiding routine tips behind a tap…
- **steam-input-lexicon.ts** (src/data/steam-input-lexicon.ts) — *Steam Input lexicon*: Versioned catalog of Steam Input jump targets with route confidence metadata.
- **steamLanguages.ts** (src/data/steamLanguages.ts) — *Steam's languages, re-opened under a shorter name*: This file adds nothing of its own — it just hands back, under a shorter import path, the same list of Steam's languages, their English names, and the reply-language helpers that `replyLanguage.ts` already defines. […]
- **steamSettingsNavigation.ts** (src/data/steamSettingsNavigation.ts) — *Steam settings navigation helpers*: Map settings breadcrumb strings to steam:// URLs and QAM QuickAccessTab targets.
- **storageKeys.ts** (src/data/storageKeys.ts) — *Names the plugin remembers things under, on this device*: Some things the plugin remembers are kept on the device itself (in the browser storage Steam's panel runs on) rather than saved to the settings file — the last question typed, whether the welcome disclaimer has been dismissed, which tab to reopen on, which saved chat was open…
- **strategyGuideFollowup.ts** (src/data/strategyGuideFollowup.ts) — *Strategy guide follow-up constants*: Prefix strings and helpers for strategy-branch custom resolution escape hatches.
- **uiScaleProfile.ts** (src/data/uiScaleProfile.ts) — *UI scale profiles*: Profile ids, viewport thresholds, labels, and classifier helpers for QAM density.

## src/features/chat-slots

- **ChatSlotRenameModal.tsx** (src/features/chat-slots/ChatSlotRenameModal.tsx) — *Renaming a saved chat*: The small popup that appears when a person renames one of their saved chats. It is a text box with the current name already filled in, plus Save and Cancel buttons, built with the same popup shell every other bonsAI popup uses.
- **ChatSlotRow.tsx** (src/features/chat-slots/ChatSlotRow.tsx) — *Chat slot row*: The row that always sits at the top of the main tab, above the preset chips. It is a small carousel of your saved chats: press LB/RB (or step the D-pad through it) to flip between them, press A on the middle to rename the current chat, or move onto its × to delete it. […]
- **useChatSlotBumpers.ts** (src/features/chat-slots/useChatSlotBumpers.ts) — *Chat slot bumper handlers*: Cycle the slot carousel on LB/RB when the row owns focus.
- **useChatSlotRenameModal.tsx** (src/features/chat-slots/useChatSlotRenameModal.tsx) — *Rename-a-chat popup*: Opens the small popup a person types a new name into when they tap a saved chat's title, and remembers which button opened it so focus can jump back there once it closes.

## src/features/model-routing

- **useRoutingOrderModal.ts** (src/features/model-routing/useRoutingOrderModal.ts) — *"Which model to try first" popup*: Opens the popup where a person orders their installed AI models from most to least preferred — one order for plain questions, another for questions that include a picture. […]

## src/features/plugin-shell

- **PluginErrorBoundary.tsx** (src/features/plugin-shell/PluginErrorBoundary.tsx) — *Catching a crash while the screen is being drawn*: If something goes wrong while a piece of the plugin's screen is being drawn — not a button press or a background call, but the actual drawing — this shows a plain "Plugin error" panel with the error message instead of the whole Quick Access Menu going blank with no explanation. […]
- **TabBodyFocusRoot.tsx** (src/features/plugin-shell/TabBodyFocusRoot.tsx) — *Tab body focus root*: One `Focusable` around a tab's content that the collapsing tab bar can hand the ring to (Down from the bar) and that hands it back (Up from the tab's first control).
- **TabIndicatorBar.tsx** (src/features/plugin-shell/TabIndicatorBar.tsx) — *The plugin's own tab bar*: Draws the thin bar that replaced Steam's own tab strip at the top of the plugin: a small dash per tab with the current one lit, the current tab's name written out beside them, and shoulder-button marks at each end. […]
- **initialSessionSnapshot.ts** (src/features/plugin-shell/initialSessionSnapshot.ts) — *The empty session snapshot*: The shape `Content` hands to `useRef` for its session-survival snapshot before anything has happened yet — no tab switched, no question asked, nothing typed.
- **jsdom.d.ts** (src/features/plugin-shell/jsdom.d.ts) — *Just enough types for the "jsdom" test package*: Two tests build a second, separate web page in memory (using the jsdom" package) to reproduce a bug that only shows up when checking a focused element with `instanceof` across two different documents. […]
- **modalReturnFocusRegistry.ts** (src/features/plugin-shell/modalReturnFocusRegistry.ts) — *Where to put the D-pad after a popup closes*: When a person opens a full-screen popup — the character picker, the models hub, and so on — and then closes it, this is what sends the D-pad's focus ring back to the exact button that opened it, instead of leaving it wherever Steam happens to land.
- **pluginStorage.ts** (src/features/plugin-shell/pluginStorage.ts) — *Plugin shell local storage*: Read and write the browser-storage values the plugin shell keeps between sessions.
- **tabBarNav.ts** (src/features/plugin-shell/tabBarNav.ts) — *The tab bar's own D-pad and shoulder-button handling*: The tab bar, when collapsed to its thin strip, handles its own Left/Right and shoulder-button (LB/RB) presses to switch tabs, wrapping around at the first and last tab either way. […]
- **tabTitles.tsx** (src/features/plugin-shell/tabTitles.tsx) — *The tab icons Decky actually draws*: Builds the small icon-only element Decky shows for each of bonsAI's tabs, plus every other piece of wording those tabs need: the name read out for accessibility, the short word shown on the collapsed tab bar, and the label shown on the open strip.
- **useAppLogPrefsAndCapturedErrors.ts** (src/features/plugin-shell/useAppLogPrefsAndCapturedErrors.ts) — *Desktop log prefs, captured frontend errors, and the tab-open log line*: Bundles the desktop-log preference object every `appendAppDesktopLogWithPrefs` call needs, the Debug tab's captured-error list that reads it, and the one log line written each time the Developer or Settings tab opens.
- **useBonsaiScopeStyle.ts** (src/features/plugin-shell/useBonsaiScopeStyle.ts) — *The panel's accent + UI-scale style*: Turns the AI character settings into an accent color, merges it with the UI-scale profile's own style, and publishes the merged style for other scopes (a Decky modal, for instance) to read.
- **useCharacterPickerModal.tsx** (src/features/plugin-shell/useCharacterPickerModal.tsx) — *AI character picker popup*: Opens the popup where a person picks the AI's on-screen character (or lets it be chosen at random), and saves that choice.
- **useChatSlotActivityState.ts** (src/features/plugin-shell/useChatSlotActivityState.ts) — *Chat slot activity bookkeeping*: The refs and state `Content` owns about the active chat slot and what is generating in it, before `useChatSlots` itself is even called — the active slot pointer, the row's dot language (hollow cyan ring = generating, solid green = finished while the user was elsewhere), and the…
- **useDesktopNoteSaveModal.tsx** (src/features/plugin-shell/useDesktopNoteSaveModal.tsx) — *"Save this answer to the Desktop" popup*: Opens the confirm popup for saving the last question and answer to a text file on the Steam Deck's own Desktop, and does the actual save once a person confirms a file name.
- **useDeveloperToolActions.ts** (src/features/plugin-shell/useDeveloperToolActions.ts) — *Developer tab one-off actions*: Two actions the Developer tab triggers that do not belong to any more specific hook: jumping to the Steam Input lexicon entry for phase 1's per-game controller config, and installing the seed knowledge base copy onto local storage.
- **useExternalNavigationActions.ts** (src/features/plugin-shell/useExternalNavigationActions.ts) — *Jumping outside the panel*: Two stable callbacks that leave the plugin's own panel entirely -- opening the model policy README in the Steam browser, and opening Steam's own Controller settings page for the keyboard-shortcut setup flow.
- **useHiddenTabHeaderTrap.ts** (src/features/plugin-shell/useHiddenTabHeaderTrap.ts) — *Catching the ring when it lands on an invisible tab button*: When Steam's own highlight ring lands on one of the tab bar's hidden header buttons — buttons that still exist on the page but are not shown — this hands the ring straight to the collapsed tab bar instead, so the invisible button is never where the ring comes to rest.
- **useOllamaConnectionState.ts** (src/features/plugin-shell/useOllamaConnectionState.ts) — *Where the AI is running, and how to reach it*: Keeps track of the address the person typed for a computer running the AI, works out the actual address to use (that typed one, or a fixed local address when the AI is set to run on the Deck itself), and remembers the last time the plugin checked whether that address could be…
- **useOllamaModelsHubModal.tsx** (src/features/plugin-shell/useOllamaModelsHubModal.tsx) — *Models hub popup*: Opens the popup where a person browses, picks, and installs the AI models the plugin is allowed to use, and saves the policy choices made there (which tier of model is allowed, and whether bigger models are allowed on lower-memory hardware).
- **usePluginHelpModal.tsx** (src/features/plugin-shell/usePluginHelpModal.tsx) — *Help popup and its "seen it" chip*: Opens the plugin's help popup, and remembers whether a person has already opened it, so the help chip on the Main tab only offers itself once. […]
- **useSessionResetActions.tsx** (src/features/plugin-shell/useSessionResetActions.tsx) — *Clearing a session, and clearing everything*: The two ways a person can deliberately throw a session away — `resetPluginSession` (the Settings tab's *Clear session*, which forgets the current question and answer but keeps every setting) and `onClearAllPluginData` (*Clear all plugin data*, which resets settings, local Ollama…
- **useSessionRestoreAfterRemount.ts** (src/features/plugin-shell/useSessionRestoreAfterRemount.ts) — *Restoring a session after Decky remounts the plugin*: The two effects that run right after `Content` mounts, to tell a QAM close/reopen (a plain mount, nothing to restore but the saved chat) apart from a Decky modal closing (a real snapshot to restore), and to clear the pending-restore marker once that restore lands.
- **useTabAndModeGuardEffects.ts** (src/features/plugin-shell/useTabAndModeGuardEffects.ts) — *Small settings-driven guard effects*: Two independent effects that each undo one piece of state when a setting no longer allows it: bouncing off the Developer tab if it gets hidden while a person is on it, and clearing the strategy guide's branch state whenever Ask mode leaves "strategy".
- **useUnifiedInputBehaviors.ts** (src/features/plugin-shell/useUnifiedInputBehaviors.ts) — *Ask bar timers and persistence*: Two small, unrelated-but-adjacent behaviors the unified input box needs: the "this is taking a while" warning timer, and writing (or not writing) the typed text back to local storage depending on the persistence mode setting.

## src/features/plugin-shell/tabs

- **useAboutTabPayload.tsx** (src/features/plugin-shell/tabs/useAboutTabPayload.tsx) — *What draws the About tab*: Runs while a person has the About tab open. It builds that screen: the project's links (its code, its issue tracker, and the upstream Ollama project it depends on) and the reply-language picker, and it only rebuilds the screen when something on it actually changes.
- **useDeveloperTabPayload.tsx** (src/features/plugin-shell/tabs/useDeveloperTabPayload.tsx) — *What draws the Debug tab*: Runs while a person has the Debug tab open — a tab meant for troubleshooting and experiments, only shown once a setting turns it on. […]
- **useMainTabPayload.tsx** (src/features/plugin-shell/tabs/useMainTabPayload.tsx) — *What draws the Ask tab*: Runs while a person has the Ask tab open — the plugin's default, always-open tab, and its busiest one. It builds that whole screen: the question box, the running conversation, screenshot attach, and, when the AI-character feature is on, the small avatar shown in the corner. […]
- **useOllamaTabPayload.tsx** (src/features/plugin-shell/tabs/useOllamaTabPayload.tsx) — *What draws the Ollama tab*: Runs while a person has the Ollama tab open — where they point the plugin at the computer running the AI, and adjust how it connects and behaves. […]
- **usePermissionsTabPayload.tsx** (src/features/plugin-shell/tabs/usePermissionsTabPayload.tsx) — *What draws the Permissions tab*: Runs while a person has the Permissions tab open, where they turn each thing the plugin is allowed to do on the Deck on or off.
- **useSettingsTabPayload.tsx** (src/features/plugin-shell/tabs/useSettingsTabPayload.tsx) — *What draws the Settings tab*: Runs while a person has the Settings tab open — screenshot behavior, the AI's on-screen character, voice replies, UI scale, and more. […]

## src/features/preset-carousel

- **carouselState.ts** (src/features/preset-carousel/carouselState.ts) — *Preset carousel state helpers*: Constants and pure functions for the sideways preset carousel: history, timing, and which chips the window shows.
- **composePresetSeedsWithSessionRag.ts** (src/features/preset-carousel/composePresetSeedsWithSessionRag.ts) — *Mixing knowledge-base suggestion chips into the preset carousel*: The row of suggested-question chips on the Main tab can either be built purely from static preset text, or have some of its chips swapped for ones pulled from the current game's own knowledge base. […]
- **presetRowLayout.ts** (src/features/preset-carousel/presetRowLayout.ts) — *Preset row layout*: The numbers that shape the suggestion row — how many chips sit across it, their height and gap, and the scrolling-label settings — plus the hold floor a scrolling label needs.
- **presetRowNav.ts** (src/features/preset-carousel/presetRowNav.ts) — *Preset row D-pad handlers*: The four move handlers a chip button carries: Left/Right walk the chips, Down hands the ring to the Ask field, Up hands it to the transcript.
- **presetSlotRotation.ts** (src/features/preset-carousel/presetSlotRotation.ts) — *Preset slot rotation*: Choose the next prompt for one of the visible preset slots: a pinned QA batch walks in order, then contextual seeds not yet shown, then the sampled pool.
- **sessionRagComposer.ts** (src/features/preset-carousel/sessionRagComposer.ts) — *Deciding which suggestion chips come from the game's own knowledge base*: Given the row of static preset chips and a list of chips pulled from the running game's own knowledge base, decides which of the row's slots actually show a knowledge-base chip instead of the static one — both when the row is first built, and each time the carousel rotates in a…

## src/features/unified-input

- **constants.ts** (src/features/unified-input/constants.ts) — *Unified input layout constants*: Shared pixel constants for Ask bar typography, insets, carousel timing, and search thresholds.
- **useUnifiedInputSurface.ts** (src/features/unified-input/useUnifiedInputSurface.ts) — *Lining up the fake caret with the real question box*: The Main tab's question box is drawn as two layers: an invisible native text field that the Deck's own on-screen keyboard and text handling actually type into, and a visible copy of the typed text with a blinking caret drawn on top of it, positioned to sit exactly over the real…

## src/features/voice

- **useVoiceAskInput.ts** (src/features/voice/useVoiceAskInput.ts) — *The microphone button on the Ask bar*: Runs while a person taps the microphone icon to speak a question instead of typing it. Owns whether recording is on, checks permission before starting, writes the recognized words into the question box as they arrive, and shows an error message if anything goes wrong.
- **useVoiceAskWithReadAloud.ts** (src/features/voice/useVoiceAskWithReadAloud.ts) — *Voice input, wired to Ask and read-aloud*: `useVoiceAskInput` plus the three pieces of glue that connect it to the rest of the Ask flow: mirroring its clear function into a ref other code can call before this hook's own declaration point, keeping the read-aloud completion watchers in step with the live setting, and…

## src/hooks

- **useAskBarInitialRingClaim.ts** (src/hooks/useAskBarInitialRingClaim.ts) — *Ask bar initial ring claim*: On a fresh panel open, where nothing owns Steam's D-pad ring yet, try a few times to place it on the question box rather than leave the first press to land wherever Steam happens to put it.
- **useAskBarMenuToggles.ts** (src/hooks/useAskBarMenuToggles.ts) — *Ask bar menu toggles*: The open/close functions for the Ask bar's two popover menus (the Ask-mode picker and the attach menu), and the effect that marks the nearest `.bonsai-scope` ancestor while either one is open.
- **useAskBarSettingsCardRows.ts** (src/hooks/useAskBarSettingsCardRows.ts) — *Ask bar settings-results card rows*: Everything about how many rows of the settings-search results card can actually fit above the question box, and how the ring gets handed to (and back out of) whichever row is nearest the box.
- **useAskBarSettingsCardVisibility.ts** (src/hooks/useAskBarSettingsCardVisibility.ts) — *Ask bar settings-card visibility state*: Whether the settings-search results card is hidden outright by what was typed, how many of its rows currently fit, and whether a person closed it for the rest of this search (the B / tap-outside behavior).
- **useAskMountRestore.ts** (src/hooks/useAskMountRestore.ts) — *Resume a pending Ask after remount*: On mount, ask the backend once whether a question was still running when the plugin panel last closed, and pick polling back up if it was.
- **useBackgroundGameAi.ts** (src/hooks/useBackgroundGameAi.ts) — *Checking in on an answer that is still being worked on*: Once a question has been handed to the back end, the answer does not arrive all at once — it comes together over the next while. […]
- **useBonsaiAskOrchestration.ts** (src/hooks/useBonsaiAskOrchestration.ts) — *The Ask flow*: Runs the whole life of one question, from the moment Ask is pressed to the moment an answer — or a refusal — is on screen. […]
- **useBonsaiPluginShell.ts** (src/hooks/useBonsaiPluginShell.ts) — *Plugin shell — which tab is open, and surviving a popup*: Tracks which tab the plugin is showing (Ask, Chats, Settings, and so on), and fixes a Steam quirk: opening one of Steam's own popups on top of the plugin (a picker, a confirm dialog) throws the plugin's whole screen away and rebuilds it once the popup closes. […]
- **useCapturedFrontendErrors.ts** (src/hooks/useCapturedFrontendErrors.ts) — *Catching screen crashes before they take the plugin down*: When something on screen throws an error the plugin was not expecting to handle, this file catches it — both an outright crash and a promise that failed with nobody watching for it — and keeps a running list so the Debug tab can show what went wrong. […]
- **useChatSlots.ts** (src/hooks/useChatSlots.ts) — *Saved chats list and switching*: Owns the list of a person's saved chats — creating a new one, switching to a different one, renaming, and deleting — and keeps the which chat is open right now" pointer in sync everywhere at once. […]
- **useDisclaimerAndLocalRuntimeGates.tsx** (src/hooks/useDisclaimerAndLocalRuntimeGates.tsx) — *The two one-time warnings — beta notice and running AI on the Deck itself*: Shows two popups, each only until the person has clicked past it once. The first, on the very first time the plugin is opened, says this is beta software and that AI suggestions should be checked before being trusted. […]
- **useDockClearanceOnFocus.ts** (src/hooks/useDockClearanceOnFocus.ts) — *Dock clearance on focus*: Keep the gamepad-focused element visible above the bottom-pinned preset/Ask dock.
- **useEarlierTurnsPill.ts** (src/hooks/useEarlierTurnsPill.ts) — *The "N earlier" pill's own open/close state*: Own whether the older, finished turns are collapsed behind the "N earlier" pill, and hand the D-pad ring to the first revealed turn header the moment the pill is expanded.
- **useInputTransparencyRefresh.ts** (src/hooks/useInputTransparencyRefresh.ts) — *Show-details refresh*: Fetch the input-transparency snapshot behind the transcript's "Show details" chip and hand it to the state that draws it.
- **useIntentPacks.ts** (src/hooks/useIntentPacks.ts) — *Intent packs*: Intent packs are small, shareable files that teach the plugin's search extra words for the same setting — so typing something the built-in search does not recognize can still find the right toggle. […]
- **useKbNotesFold.ts** (src/hooks/useKbNotesFold.ts) — *The "From the notes" block's own open/closed state*: Own which turns have their "From the notes" block open — independent per turn, since several can reasonably be open at once while scrolling back through a chat — and scroll a block's own header into view the moment it opens.
- **useKidsLock.ts** (src/hooks/useKidsLock.ts) — *The Kids lock — following Steam's own parental control for this session*: Watches Steam's own parental lock and, for as long as it is on, turns off every one of the plugin's five permissions for this session — writing files, the screenshot and video library, Steam's logs, talking to Steam's own servers, and the microphone — no matter what the person…
- **useLocalOllamaSetupFlow.tsx** (src/hooks/useLocalOllamaSetupFlow.tsx) — *The local-on-Deck install and update flow*: Everything that runs when a person installs Ollama on the Deck itself, updates it, or pulls one of the two starting model bundles: the three confirm dialogs (Tier 1 essentials, Tier 2 multimodal, update installed), the Cancel button's RPC, the status-line wording while a setup…
- **useMainTabAskBarFocus.ts** (src/hooks/useMainTabAskBarFocus.ts) — *Main tab Ask bar focus helpers*: A set of "jump the ring to this control" functions for the parts around the Ask bar: the question box, the paperclip, the character picture, the row of suggestion chips, the Ask button, the mic, and the Ask-mode button. […]
- **useMainTabColumnFill.ts** (src/hooks/useMainTabColumnFill.ts) — *Stretching the Main tab so the Ask bar sits at the bottom*: The mockups draw the question box and preset row pinned to the bottom of the panel. On a short conversation, without this file, they would float partway up instead, leaving empty space underneath. […]
- **useMdnsOllamaDiscovery.tsx** (src/hooks/useMdnsOllamaDiscovery.tsx) — *Find Ollama on the LAN over mDNS*: The "Find LAN" button on the where-AI-runs panel — asks first, then searches the local network for other Ollama services advertised over mDNS/Bonjour, and records what it finds (or why it found nothing).
- **useOllamaGameContextSync.ts** (src/hooks/useOllamaGameContextSync.ts) — *Ask-bar footnote game sync*: Keep the Ask-bar footnote's game context (the "Context: <game>" line) in step with whichever game Steam reports running, on its own timer.
- **useOllamaLocalAutostart.ts** (src/hooks/useOllamaLocalAutostart.ts) — *The "Start the AI with the Deck" startup entry*: Reads and flips the per-user Deck startup entry that starts the local AI (and the note-searching part) when the Deck itself starts, so questions come back sooner without a manual step each boot.
- **usePermHintNavTargets.ts** (src/hooks/usePermHintNavTargets.ts) — *Nav targets for the chat's two permission-hint rows*: Register the troubleshooting Ask hint's and the vac-check deny action's own wrapping Focusables as Steam nav targets, for the lifetime of the transcript component.
- **usePermissionJump.ts** (src/hooks/usePermissionJump.ts) — *Jumping straight to the permission that was just denied*: When something is blocked because a permission is off, this is what takes the person straight to the matching switch on the Permissions tab — instead of just dropping them on that tab and leaving them to find the right row themselves — and remembers which tab to bring them back…
- **usePluginSettings.ts** (src/hooks/usePluginSettings.ts) — *Every setting in the plugin — loaded, saved, and kept in step*: This is the one place that owns every setting a person can change anywhere in the plugin — permissions, the Ask model, voice replies, the AI character, timing, and dozens more. […]
- **usePullModelCatalog.ts** (src/hooks/usePullModelCatalog.ts) — *Pull model catalog hook*: Fetch and merge live/cached pull-model catalog overlay with bundled defaults.
- **usePullModelCatalogRefresh.ts** (src/hooks/usePullModelCatalogRefresh.ts) — *Pull models — refreshing installed tags and catalog metadata*: Owns the round trip that learns which tags are actually on this Deck (a connection test against local Ollama) and how big each catalog tag is (live size metadata), run together and run once on mount, plus the small "is this screen still mounted" guard a few other pieces of the…
- **usePullModelCustomTagPull.tsx** (src/hooks/usePullModelCustomTagPull.tsx) — *Pull models — typing a model name by hand*: Owns pulling a tag typed into "Type a name" rather than picked from the table.
- **usePullModelDeleteConfirm.tsx** (src/hooks/usePullModelDeleteConfirm.tsx) — *Pull models — the remove-model prompt*: Owns the confirm dialog PullModelsModal shows before removing an installed model, and the RPC call and busy-state bookkeeping that follow a yes.
- **usePullModelEmbeddedFooterState.ts** (src/hooks/usePullModelEmbeddedFooterState.ts) — *Pull models — reporting footer state to an embedding parent*: When this screen is embedded inside another modal (the AI models hub) rather than drawing its own footer, reports what that footer should say and do every time the queue or the pull-busy flag changes.
- **usePullModelOpenBookkeeping.ts** (src/hooks/usePullModelOpenBookkeeping.ts) — *Pull models — bookkeeping that runs on open*: Two small effects that run without any button press: seeding which tag is shown as used for Ask" from the saved routing order, and updating the client-side "New" badge record whenever the installed-tags list changes.
- **usePullModelPinForAsk.tsx** (src/hooks/usePullModelPinForAsk.tsx) — *Pull models — "Use for Ask"*: Owns moving a tag to the front of the saved try-order so Ask uses it next, for both the text order and, when the catalog says the tag is vision-capable, the vision order too.
- **usePullModelSubmitSelected.tsx** (src/hooks/usePullModelSubmitSelected.tsx) — *Pull models — submitting the queue*: Owns pressing "Pull selected": the registry check that drops any tag the Ollama registry no longer publishes, the batched "enable Tier 2 before pulling" prompt for a queue that holds an open-weight tag under Tier 1, and the RPC call itself.
- **usePullModelTableData.ts** (src/hooks/usePullModelTableData.ts) — *Pull models — the catalog table's derived data*: Turns the raw merged catalog and the screen's filter/installed state into everything the table itself renders: the grouped, sectioned row list, the flat row list the D-pad walks by index, and the header's installed/queued counts and sizes.
- **usePullModelTier2Confirm.tsx** (src/hooks/usePullModelTier2Confirm.tsx) — *Pull models — the "enable Tier 2" prompt*: Owns the confirm dialog PullModelsModal shows before letting an open-weight model through while the licence policy is still Tier 1 (open-source only), plus the small helper both that dialog and the screen's other confirm dialogs use to close themselves.
- **usePullModelToggleSelection.tsx** (src/hooks/usePullModelToggleSelection.tsx) — *Pull models — toggling a model into the pull queue*: Owns pressing a table row's star/checkmark to queue or unqueue an uninstalled model, including the "this one is large" confirm dialog that a "stretch" model shows the first time it is picked in this session.
- **useQamPanelHeightGuard.ts** (src/hooks/useQamPanelHeightGuard.ts) — *QAM panel height guard*: Pin Steam QAM tab pane height and flex column chain to prevent gamescope panel sag on hover.
- **useQamPanelSideBleed.ts** (src/hooks/useQamPanelSideBleed.ts) — *QAM panel side bleed*: Zero horizontal padding and fixed side margins on every ancestor between `.bonsai-scope` and the Steam QAM tab pane, so plugin rows reach the panel edges.
- **useReadAloud.ts** (src/hooks/useReadAloud.ts) — *Read aloud — the button, and reading a finished answer on its own*: Owns the Read aloud / Stop button — the two calls it makes to the back end, and checking in on how the reading is going, several times a second while something is being spoken. […]
- **useReadAloudAutoStop.ts** (src/hooks/useReadAloudAutoStop.ts) — *Read aloud, stopped by a new Ask*: Own the one Read aloud / Stop instance the whole chat transcript shares, and stop whatever is speaking the moment a new Ask begins.
- **useReasoningFoldState.ts** (src/hooks/useReasoningFoldState.ts) — *The Show reasoning row's own open/closed state*: Own which turn's reasoning block is open, if any — closed by default, always, and closed again the moment a different turn is expanded.
- **useReplyFeedbackChips.ts** (src/hooks/useReplyFeedbackChips.ts) — *Reply rating and follow-up chips*: Own the thumbs up or down on the reply that is on screen, and the small chips that rewrite your question for you so you can send a better one.
- **useReplyLanguage.ts** (src/hooks/useReplyLanguage.ts) — *Which language the screen's own text is shown in*: Asks the back end what language Ask replies and the screen's own wording should use — the person's chosen override if they set one, or Steam's own language otherwise — and hands back a small helper, called `t`, that every piece of on-screen text is passed through to be…
- **useScreenshotBrowser.ts** (src/hooks/useScreenshotBrowser.ts) — *Screenshot browser*: Runs the "attach a screenshot" panel on the Ask screen — showing recent game screenshots, taking a new one on the spot, and picking one to send with the next question. […]
- **useSmoothStreamReveal.ts** (src/hooks/useSmoothStreamReveal.ts) — *Smooth stream reveal hook*: Reveal streamed assistant tokens at a capped prose rate with fence burst after close.
- **useSpoilerFenceRecheck.ts** (src/hooks/useSpoilerFenceRecheck.ts) — *Re-render when a spoiler fence opens or closes*: Plan 58 phase 1 — force the chat transcript to re-render whenever a spoiler fence opens or closes anywhere, so a "From the notes" block's own read of whether any fence is open is never stale.
- **useSteamSettingsSearch.ts** (src/hooks/useSteamSettingsSearch.ts) — *Steam settings search hook*: Filter Steam/QAM settings rows from unified input and deep-link via Router or steam:// URLs.
- **useStrategyBranchActions.ts** (src/hooks/useStrategyBranchActions.ts) — *Strategy Guide branch picks and checklist toggles*: Own the two things a person does with the Strategy Guide panel that are not asking a fresh question outright: picking a branch (which sends a composed follow-up) and checking or unchecking an item on the checklist.
- **useStrategyChecklistSession.ts** (src/hooks/useStrategyChecklistSession.ts) — *Keeping the Strategy checklist in step with the running game*: Strategy mode's checklist belongs to whichever game is running, not to the plugin session as a whole. This file notices when the running game changes (checked about every second and a half) and loads that game's own saved checklist from disk. […]
- **useStreamScrollPin.ts** (src/hooks/useStreamScrollPin.ts) — *Stream scroll pin hook*: Follow the end of the transcript while tokens arrive, and hold position once the user scrolls up to read something behind it.
- **useSuggestedPromptChips.ts** (src/hooks/useSuggestedPromptChips.ts) — *Suggested prompt chips*: Own the three question chips above the Ask box — what they say and when they change.
- **useTabStripBodyOffset.ts** (src/hooks/useTabStripBodyOffset.ts) — *Tab strip body offset hook*: Measure Decky tab strip height and reserve space so tab content is not painted into LB/RB titles.
- **useThinkingNoticeGate.tsx** (src/hooks/useThinkingNoticeGate.tsx) — *The one-time notice before Thinking turns on*: The Thinking row on the Ollama tab has four choices: Off, Brief, Balanced, Deep (Off by default). The first time a person moves it off Off, this shows a popup first: "Thinking shows on screen as it happens and is not checked for spoilers. […]
- **useUiScaleProfile.ts** (src/hooks/useUiScaleProfile.ts) — *Sizing the panel for a handheld Deck versus a docked, bigger screen*: Measures how wide the plugin's own panel actually is, and from that (together with the screen's real size) works out whether this counts as a small handheld view or a larger docked one. […]
- **useVoiceTranscription.ts** (src/hooks/useVoiceTranscription.ts) — *Dictating a question with the microphone*: Starts and stops microphone dictation on the back end, checks in several times a second while it is recording, and writes the words heard so far into the question box as they come in — first the words already settled, then whatever is still being worked out.

## src/i18n

- **catalog.ts** (src/i18n/catalog.ts) — *UI string catalog*: Per-language UI string tables with English defaults and partial translation fallback.
- **keys.ts** (src/i18n/keys.ts) — *The list of text bonsAI can show in another language*: bonsAI can show a handful of its own interface messages — "Ask is starting", a couple of toast titles, the reply-language picker's own labels — in the language a person has chosen, separately from whatever language the AI itself answers in. […]

## src/preview

- **previewTestHooks.ts** (src/preview/previewTestHooks.ts) — *Preview test hooks*: Expose window.__bonsaiTestHooks for Decky Plugin Studio automated preview scenarios.
- **replyLayoutReport.ts** (src/preview/replyLayoutReport.ts) — *Reply layout report*: Describe the newest reply's block — stop count, and the rectangle of each control.
- **useDeckyPreviewTestHookRegistration.ts** (src/preview/useDeckyPreviewTestHookRegistration.ts) — *Wiring the preview test hooks into Content*: Registers `window.__bonsaiTestHooks` (see `previewTestHooks.ts`) from live values inside `Content`, but only when running under Decky Plugin Studio's preview harness.

## src/styles

- **bonsaiScopeStylesheet.ts** (src/styles/bonsaiScopeStylesheet.ts) — *Scoped stylesheet builder*: Concatenate all bonsAI scope CSS sections into one injectable string for `.bonsai-scope`.
- **settingsGlassButton.ts** (src/styles/settingsGlassButton.ts) — *The frosted-glass button look*: The style two kinds of button share across the Settings and Ollama tabs — a soft, semi-transparent gradient with a thin light border, matching SteamOS's own look for a secondary action. […]

## src/styles/sections

- **answerBubble.ts** (src/styles/sections/answerBubble.ts) — *The answer bubble in the conversation*: Styles the glass-panel bubble that holds the AI's answer: its background and border, the pulsing border and blinking cursor while an answer is still streaming in, the dashed "waiting on a code block to finish" chip, each answer section's own D-pad outline, the spoiler reveal…
- **answerMarkdownFormatting.ts** (src/styles/sections/answerMarkdownFormatting.ts) — *Formatting for the text inside an AI answer*: Styles the markdown pieces an answer's text is built from once it has been parsed into "chunks" — paragraphs, lists, quotes, links, inline code and fenced code blocks — plus the chunk boxes that hold that text before it lands inside a chat bubble.
- **chatSlotModal.ts** (src/styles/sections/chatSlotModal.ts) — *Rename-a-chat popup styling*: Styles the name field a person types into when renaming a saved chat — the label above it and the text box itself — while leaving the popup's outer frame, footer and buttons drawn by Steam's own stock confirm-popup look.
- **gamepadAndPullModels.ts** (src/styles/sections/gamepadAndPullModels.ts) — *Focus rings, popup style bundle, and the Pull Models table*: A grab-bag file holding three things that each needed a home and did not fit anywhere else: 1. The white ring drawn around a button when the D-pad lands on it, shared by many separate buttons across the plugin (the AI character picker, the Ask bar, the models hub, and more). […]
- **questionBubble.ts** (src/styles/sections/questionBubble.ts) — *The question bubble in the conversation*: Styles the right-aligned bubble that holds a person's own question in the transcript: its shape and background, the small Retry icon tucked into its bottom-left corner, the single-line title at rest, and the wrapped, five-line-capped title once the bubble is open with a fade on…
- **savedChatSlotDots.ts** (src/styles/sections/savedChatSlotDots.ts) — *The saved-chats row -- the small dots underneath it*: Styles the row of small dots marking every saved chat's position (plus the far-left "+" marker for the create position): the dot shapes themselves, the active/pending/unread states, and the matching "spark" markers drawn beside a ghost neighbour.
- **savedChatSlotsRow.ts** (src/styles/sections/savedChatSlotsRow.ts) — *The saved-chats row -- shape, title, and neighbours*: Styles the row of saved chats above the conversation: hiding Steam's own shoulder-button hints while the row has focus (they would otherwise claim the bumpers cycle tabs, when this row has taken them over to cycle slots instead), the row's bumper pills, the centred block that…
- **scopeBase.ts** (src/styles/sections/scopeBase.ts) — *Root scope, QAM host, and Settings-tab spacing*: Styles the plugin's outermost box — the `.bonsai-scope` element every other rule in the stylesheet lives inside — plus Decky's own wrapper around it, so the plugin's column fills the Quick Access Menu instead of being crushed down to the height of the tab strip. […]
- **section-1.ts** (src/styles/sections/section-1.ts) — *Tab area frame, and stopping the tab-switch flicker*: Styles the box that holds the whole tab area — everything below the top of the plugin's window: the strip that used to show Steam's own tab icons (now hidden; the plugin draws its own bar instead, see tabIndicatorBar.ts) and the scrollable body underneath it. […]
- **section-2.ts** (src/styles/sections/section-2.ts) — *Tab strip icon centering (the "ghost nudge" fix)*: Styles the row of tab icons across the top of the plugin — Ask, Chats, Settings, and so on — so each icon sits centered in its own slot instead of drifting sideways ("the ghost nudge") once Steam's own sizing rules get involved.
- **section-3.ts** (src/styles/sections/section-3.ts) — *General spacing and width resets — read the warning below first*: Strips Steam's own padding and margin off the tab body's scroll area and the panel sections inside it, so the plugin's content can run edge to edge instead of sitting inside Steam's usual gutters. […]
- **section-4.ts** (src/styles/sections/section-4.ts) — *The Ask box and the row of suggestion chips under it*: Styles two things that both need to span edge to edge, with no side gutter: the Ask bar (the typing box and its Ask button), and the sideways-scrolling row of suggested-prompt chips above it. […]
- **section-5.ts** (src/styles/sections/section-5.ts) — *The question typing box itself*: Styles the actual field a person types their question into. The real native field is made invisible on purpose — its text is drawn by a separate overlay on top, which is how the plugin gets a custom blinking caret and (when the AI-character feature is on) a small avatar beside…
- **section-6.ts** (src/styles/sections/section-6.ts) — *Chat bubbles, the bottom dock, and the saved-chats strip*: This file started as one thing — the frosted-glass look shared by panels across the plugin — and grew into the largest single piece of styling in the plugin as the conversation screen grew around it. […]
- **section-7.ts** (src/styles/sections/section-7.ts) — *Stripping Steam's own backgrounds so the plugin's show through*: Steam's building blocks stack several backgrounds and shadows of their own on top of each other. This file strips all of that off a handful of spots — the suggestion chips, the Ask box, and the Ask-mode and Attach dropdown menus — so the plugin's own custom backgrounds are what…
- **section-8.ts** (src/styles/sections/section-8.ts) — *The icon row under the Ask box, and its dropdown menus*: Styles the small row of icons under the question box (Ask mode, attach, and so on), the two dropdown menus that open from that row (choosing an Ask mode, and choosing what to attach), and the Ask button itself — including the color change it gets once there is something to send.
- **section-9.ts** (src/styles/sections/section-9.ts) — *Slider and carousel width fixes*: A short grab-bag of leftover width fixes. It stops slider controls — like the sliders on the Settings tab — from refusing to shrink inside a narrow Quick Access Menu column, and keeps the strip that holds the preset chips at full width instead of collapsing down to the width of…
- **tabIndicatorBar.ts** (src/styles/sections/tabIndicatorBar.ts) — *The plugin's own tab bar, at the very top of the screen*: Styles the thin bar that replaced Steam's own tab strip: a small row of dashes and the current tab's name, sitting at a fixed height so nothing around it jumps when a person switches tabs. […]
- **uiScalePx.ts** (src/styles/sections/uiScalePx.ts) — *Scale a pixel size*: Every fixed size in the stylesheet is meant to pass through here, so turning the plugin's own "UI scale" setting up or down resizes it too, instead of only resizing the handful of things Steam already knows how to scale on its own.

## src/types

- **askOrchestration.ts** (src/types/askOrchestration.ts) — *Everything the Ask flow hands back, in one list*: One hook, `useBonsaiAskOrchestration`, runs the whole flow behind asking a question and getting an answer — 52 separate pieces of state and functions come out of it: the answer itself, what it came with, the thread of earlier turns, what is happening right now, and the things a…
- **askOrchestrationArgs.ts** (src/types/askOrchestrationArgs.ts) — *What the Ask flow needs to start*: The one argument `useBonsaiAskOrchestration` takes — every setting, ref and callback it reads in order to ask a question correctly and report back what happened.
- **backgroundAsk.ts** (src/types/backgroundAsk.ts) — *The shapes behind asking a question in the background*: Asking bonsAI a question does not block the screen while the AI is thinking — the question is handed to the computer or Deck side to run in the background, and the panel checks back on it every so often until it is done, even if the panel was closed and reopened in the meantime. […]
- **bonsaiUi.ts** (src/types/bonsaiUi.ts) — *Shapes of things on the Ask screen*: Names the shape of several pieces of data the Ask screen passes around — an attached screenshot, a game-context marker, a branch picker or checklist parsed out of a Strategy answer, and, the largest one, a single finished question-and-answer pair as it is kept in the on-screen…
- **rpcMethods.ts** (src/types/rpcMethods.ts) — *The list of things the screen is allowed to ask the back end for*: The plugin has two halves. The screen half runs in Steam's own interface; the back end half runs as a separate program and does the work that needs a real computer -- talking to the AI, reading files, running commands. […]

## src/utils

- **answerBubbleElRegistry.ts** (src/utils/answerBubbleElRegistry.ts) — *Finding an answer bubble's box on screen*: Every reply the AI gives is drawn on screen as its own box, called a bubble. When the D-pad walk between parts of a long reply needs to jump into a bubble, or needs to know which bubble the player is currently looking at, it needs the real on-screen box for that reply, not just…
- **answerBubbleNavigation.ts** (src/utils/answerBubbleNavigation.ts) — *Answer bubble navigation*: Decides what Up and Down do while the ring is sitting inside one AI answer. An answer is cut into sections — stops the D-pad can walk down one at a time (see the reply-bubble file for why) — and this file works out whether a press should move to the next section, scroll the…
- **answerCopyText.ts** (src/utils/answerCopyText.ts) — *Answer copy text builder*: Build the text a reply's Copy action puts on the clipboard.
- **answerReadableText.ts** (src/utils/answerReadableText.ts) — *Answer readable text builder*: Turn a stored answer (the plugin's markdown, tags and fences) into plain spoken text.
- **answerStopRegistry.ts** (src/utils/answerStopRegistry.ts) — *The stops inside one reply, in order*: A long AI reply is broken into sections on screen — separate headings, paragraphs, code blocks. Before this file existed, the whole reply was one single thing the D-pad could land on, so Down just scrolled the box by a fixed amount and there was no way to land the ring on a…
- **appDesktopLog.ts** (src/utils/appDesktopLog.ts) — *Writing a line to the desktop log file*: When something worth writing down happens on the AI side, this file can save it as one line in a log file under Desktop/bonsAI_logs, sitting right on the player's own computer where they can open and read it directly. […]
- **appliedTuningText.ts** (src/utils/appliedTuningText.ts) — *Wording for "here is what got tuned"*: An older version of this plugin could change the Steam Deck's power limit (called TDP) directly and would show the player a short banner confirming what it had just changed. […]
- **archivedTurnTransparency.ts** (src/utils/archivedTurnTransparency.ts) — *Which "what went into this answer" details to show for an old reply*: The Show details panel tells the player what went into a particular AI answer — which files it read, what game it thought it was answering about, and so on. […]
- **askOrchestrationRestore.ts** (src/utils/askOrchestrationRestore.ts) — *Ask flow restore helpers*: Two small, pure functions that decide what the Ask hook's state should start out as when a survived session snapshot might already answer the question — which turn is expanded, and which game context to show before the first status poll has run.
- **askThinkingPhases.ts** (src/utils/askThinkingPhases.ts) — *"Thinking..." status text, and telling a real answer from a status message*: While the AI is working on a reply, the screen shows a short "starting..." or thinking..." message in the player's chosen language. […]
- **bonsaiAskCompletionWatch.ts** (src/utils/bonsaiAskCompletionWatch.ts) — *Watching a background question for when it finishes*: The player can ask the AI a question, then close the plugin's own panel (or switch to a different Steam tab) while the answer is still being written. […]
- **bonsaiDebugIngest.ts** (src/utils/bonsaiDebugIngest.ts) — *The on-screen debug overlay's short memory*: There is an optional on-screen overlay, off by default, that a developer or a curious player can turn on under Developer settings to watch what the plugin is doing in real time. […]
- **bonsaiPhaseToast.ts** (src/utils/bonsaiPhaseToast.ts) — *Phase toast manager*: Show and replace single-stack Decky toasts for in-flight Ask phase updates.
- **bonsaiReplyReadyToast.ts** (src/utils/bonsaiReplyReadyToast.ts) — *The "your reply is ready" notification*: When a question finishes being answered while the player is not looking at the part of the screen that would show it, this file is what puts up a small notification (a "toast") saying the reply is ready, or that something went wrong. […]
- **bonsaiReplySurface.ts** (src/utils/bonsaiReplySurface.ts) — *Is the player already looking at the reply?*: This file answers one small question for the rest of the plugin: is the part of the screen that would show a finished AI answer currently visible to the player? […]
- **bonsaiSessionSurvival.ts** (src/utils/bonsaiSessionSurvival.ts) — *Keeping the chat on screen when a popup opens and closes*: The plugin's own screen briefly disappears and comes back whenever certain popups open on top of it — Decky, the framework this plugin runs inside, actually removes the screen from the page and puts a fresh copy back afterward. […]
- **buildAnswerBubbleElement.tsx** (src/utils/buildAnswerBubbleElement.tsx) — *The reply bubble*: Builds the bubble holding one answer from the AI — either one still arriving word by word, or a finished one read back from history. […]
- **buildDetailsPanelElement.tsx** (src/utils/buildDetailsPanelElement.tsx) — *The Show details panel*: Builds the panel a turn's "Show details" line opens (plan 62 3c) — on the newest turn, the "This answer / Session · N" tabs row plus whichever tab's own body is active; on any older, hand-expanded turn, today's bare chip ladder with no tabs.
- **buildKbNotesBlockElement.tsx** (src/utils/buildKbNotesBlockElement.tsx) — *The "From the notes" block*: Builds the row that sits under a reply which used a note from the knowledge base or a shared troubleshooting tip (plan 58 phase 1) — closed by default, naming the note and where it came from, and opening to the note's own words.
- **buildReasoningFoldElement.tsx** (src/utils/buildReasoningFoldElement.tsx) — *The Show reasoning line, and the block it opens*: On a turn where the model thought before it answered, a line sits between the question and the answer reading *Show reasoning · 41 s*. […]
- **buildReplyActionsElement.tsx** (src/utils/buildReplyActionsElement.tsx) — *Reply actions row builder*: Builds everything that can sit under one AI reply, below the answer itself: the Helpful / Not really thumbs (with an optional Read aloud speaker at the row's right-hand end), the "what went wrong" chips that appear once someone picks Not really, and the Show details line. […]
- **buildThinkingBlurbTextElement.tsx** (src/utils/buildThinkingBlurbTextElement.tsx) — *Thinking blurb text element builder*: Render thinking status copy with italic prose, upright glyphs, spoilers redacted.
- **buildTurnHeaderElement.tsx** (src/utils/buildTurnHeaderElement.tsx) — *Turn header element builder*: Build the question bubble for a turn — its text, and the Retry icon in its corner.
- **chatPanelScroll.ts** (src/utils/chatPanelScroll.ts) — *Chat panel scroll helpers*: Locate QAM TabContentsScroll, scroll panels by step, and geometry nudges for clipped content.
- **chatSlotTurns.ts** (src/utils/chatSlotTurns.ts) — *Turning a saved chat back into question-and-answer pairs*: A saved chat is stored on disk as a flat list of turns — a question, then an answer, then the next question, and so on. […]
- **chatSlotsApi.ts** (src/utils/chatSlotsApi.ts) — *Asking the back end for saved chats*: The player can keep more than one saved chat with the AI at once, switch between them, and rename or delete the ones they no longer want. […]
- **chatTranscriptNavHelpers.ts** (src/utils/chatTranscriptNavHelpers.ts) — *Small D-pad nav helpers for the chat transcript*: A handful of standalone Focusable move handlers the transcript wires onto specific rows — the permission-hint rows below it, the reply utility row's Down chain, the "N earlier" pill's Left, and the first archived turn header's Up — each one fixing one measured device bug.
- **chatTurnTitle.ts** (src/utils/chatTurnTitle.ts) — *The question shown at the top of each chat turn*: Every question-and-answer pair in the chat has the player's question written above it as a heading. While that pair is closed, the heading shows a short, one-line version of the question. […]
- **clearBonsaiBrowserStorage.ts** (src/utils/clearBonsaiBrowserStorage.ts) — *Wiping the plugin's own browser storage*: The browser the plugin runs in has its own small, private storage the plugin uses for odds and ends that do not belong in the saved settings file — things like "already warned about this once" flags. […]
- **clipboardStash.ts** (src/utils/clipboardStash.ts) — *Reading the clipboard for "attach from clipboard"*: The player can attach whatever text they last copied to their AI question with one tap. This file is what reads that copied text off the computer's clipboard, and does a first, light clean-up pass on it before it is attached — trimming stray control characters, collapsing…
- **clipboardWrite.ts** (src/utils/clipboardWrite.ts) — *Copying a reply to the clipboard*: When the player taps Copy on an AI reply, this file is what actually puts that text on the computer's clipboard so it can be pasted somewhere else. […]
- **contextChipsFromSnapshot.ts** (src/utils/contextChipsFromSnapshot.ts) — *Turning "what went into this answer" into the row of chips the player sees*: The Show details panel shows what went into an AI answer as a row of small labelled chips — one for each file, note, or piece of context the AI actually used. […]
- **createTabLocalSurvival.ts** (src/utils/createTabLocalSurvival.ts) — *The building block behind "the screen remembers itself through a popup"*: Several different tabs in this plugin need to survive the same disappearing-and-coming- back trick a popup causes (see bonsaiSessionSurvival.ts for the fullest example, on the main tab). […]
- **deckSliderMath.ts** (src/utils/deckSliderMath.ts) — *Deck slider math*: Clamp, index/percent mapping, and Deck button direction helpers for discrete sliders.
- **deckyCall.ts** (src/utils/deckyCall.ts) — *Asking the Python side a question, with a deadline*: The screen and the Python back end talk to each other through Decky, the framework this plugin runs inside — the screen asks a named question ("save these settings", "start this AI question") and waits for an answer. […]
- **desktopChatAutosave.ts** (src/utils/desktopChatAutosave.ts) — *Which finished answers already got saved to the desktop note file*: When the "auto-save chat notes to desktop" setting is on, every finished AI answer gets appended to a note file on the player's own computer. […]
- **drgGlossaryAsk.ts** (src/utils/drgGlossaryAsk.ts) — *DRG glossary explain-further Ask composer*: Build the auto-sent Ask question and thread label for a glossary term's "explain further" chip.
- **drgGlossaryTermMatch.ts** (src/utils/drgGlossaryTermMatch.ts) — *Spotting DRG Survivor game terms inside a reply*: For the game DRG Survivor, this plugin keeps a short hand-picked list of the game's own jargon — words a new player might not know. […]
- **drgGlossaryTermRegistry.ts** (src/utils/drgGlossaryTermRegistry.ts) — *Letting the D-pad walk park on a glossary term chip in passing*: A reply can contain a highlighted game-term chip the player can open for a definition (see drgGlossaryTermMatch.ts for how those terms are found). […]
- **focusNavigation.ts** (src/utils/focusNavigation.ts) — *Recognizing which D-pad, stick, or button press just happened*: A Steam Deck press can arrive at this plugin's code in more than one shape, depending on which part of the screen it lands on — sometimes as a keyboard-style key name, sometimes as a Steam-specific numeric button code. […]
- **focusPerTurnRow.ts** (src/utils/focusPerTurnRow.ts) — *Focus a row from its own per-turn registry*: The one move shared by every per-turn row registry in the chat transcript (the "From the notes" block, the details tabs row, the chip ladder) — given the map that registry keeps from a turn key to that turn's own row element, put Steam's ring on it.
- **i18n.ts** (src/utils/i18n.ts) — *Looking up one on-screen label in the player's language*: Small one-function file that looks up a single piece of on-screen text in whichever language the player has chosen for AI replies, falling back to English for anything not yet translated into that language.
- **inputTransparency.ts** (src/utils/inputTransparency.ts) — *The shape of "what went into this answer"*: The Show details panel tells the player what went into a particular AI answer — the exact question sent to the model, which files or notes it read, whether anything was blocked by the content filter, and more. […]
- **intentPackSearch.ts** (src/utils/intentPackSearch.ts) — *Finding a setting by a word that is not its actual name*: Searching Settings normally matches the setting's own name against what was typed. That misses a player typing a word they know instead — "temperature" instead of "thermal", say. […]
- **joinPresetWithRunningGame.ts** (src/utils/joinPresetWithRunningGame.ts) — *Preset text passthrough*: Preserves a single helper for preset chip inject into the Ask field.
- **liveTurnFocusGraph.ts** (src/utils/liveTurnFocusGraph.ts) — *Live turn focus graph*: D-pad focus helpers for the live Ask turn (answer bubble → strategy branches → checklist → reply actions).
- **localOnlyAskCommands.ts** (src/utils/localOnlyAskCommands.ts) — *Questions that do not need the AI, or a PC, to answer*: A few special typed lines are not really questions for the AI at all — turning the content filter on or off, running the Deck controller shortcut setup, or checking for VAC (anti-cheat) problems. […]
- **mediaFormat.ts** (src/utils/mediaFormat.ts) — *Turning screenshot and file info into words a person can read*: When the screenshot browser shows a list of captured files, this file turns raw computer data into the words shown next to each one: the file's path becomes a link that can actually be opened as a picture, the moment it was taken becomes something like "8/14/2026, 3:02 PM"…
- **mergePullModelCatalog.ts** (src/utils/mergePullModelCatalog.ts) — *Combining the built-in model list with an update from the back end*: The Pull Models screen shows a list of AI models a person can download. That list ships with the plugin (the "bundled" list) so it works even with no internet, but the project also publishes updates to it — new models, models that got pulled, corrected details — and this file…
- **modelRoutingOrder.ts** (src/utils/modelRoutingOrder.ts) — *Which installed AI model to try first*: A person can have more than one AI model installed at once. Ask can use any of them, but needs a starting order to try them in, and the Ollama tab needs to show that order as a list. […]
- **navFocusRegistry.ts** (src/utils/navFocusRegistry.ts) — *Sending the controller's highlight to a different part of the screen*: The Steam Deck's controller does not click on things — it moves a highlight ("focus") from one control to the next, and Steam itself, not the plugin, decides which element that highlight sits on. […]
- **ollamaTabLocalSurvival.ts** (src/utils/ollamaTabLocalSurvival.ts) — *Remembering the Ollama tab across a panel close and reopen*: Opening certain modals closes and reopens the quick-access panel, which would normally reset the Ollama tab to its starting state. […]
- **permissionDeepLink.ts** (src/utils/permissionDeepLink.ts) — *Knowing which Permissions row unblocks which action*: When something a person tries to do is blocked because a permission is off — for example, asking to check a Steam ban when "Steam ban lookup" is off — this file knows which row on the Permissions tab controls that permission, and the exact sentence to show explaining what to…
- **permissionJumpRegistry.ts** (src/utils/permissionJumpRegistry.ts) — *Remembering a jump to the Permissions tab, and finishing it*: When a denied permission shows a "go fix this" link — for example from a chat message explaining that a permission is off — pressing it needs to remember two things across the tab switch that follows: which tab to come back to, and which row on the Permissions tab the…
- **persistOllamaIp.ts** (src/utils/persistOllamaIp.ts) — *Remembering the network Ollama address, only when it is actually in use*: The plugin can talk to an AI model running on the Deck itself, or to one running on another PC on the same network. […]
- **presetCarouselInject.ts** (src/utils/presetCarouselInject.ts) — *Turning a back-end "insert this chip" message into something the carousel can use*: The row of suggested-question chips above the Ask box (the "preset carousel") can be told by the back end to drop a specific chip of text into it — for example, after a background check finishes and has something worth suggesting. […]
- **pullModelCatalogRefresh.ts** (src/utils/pullModelCatalogRefresh.ts) — *Pull model catalog refresh bus*: Pub/sub notify listeners to refresh the living pull-model catalog overlay.
- **pullModelFilters.ts** (src/utils/pullModelFilters.ts) — *Pull models table filtering and the Filters panel row model*: The pure matching checks PullModelsModal.tsx runs a catalog entry through (installed? matches the licence tier? matches an Ask-mode filter? on the live registry?), plus the flat, ordered row list that drives the Filters panel itself and the labels each row shows.
- **pullModelNewBadge.ts** (src/utils/pullModelNewBadge.ts) — *Pull models "New" badge history*: Computes and answers questions about the client-side record of when each installed model tag was first seen, which the Pull Models screen uses to badge a recently-pulled model New" for 30 days.
- **pullModelRecommendations.ts** (src/utils/pullModelRecommendations.ts) — *Suggesting which model to download next*: The Pull Models screen can suggest a model to download based on what is missing from what is already installed. […]
- **reasoningDisplay.ts** (src/utils/reasoningDisplay.ts) — *Reading the model's own thinking for the screen*: When a model that can think is asked a question, it writes out its thinking before it writes the answer. This file turns that raw thinking into the small pieces the chat draws: the newest few sentences while the answer is still being made, the "41 s" on the fold row above a…
- **redactThinkingBlurbSpoilers.ts** (src/utils/redactThinkingBlurbSpoilers.ts) — *Thinking blurb spoiler redaction*: Turn model-authored [[spoiler]]…[[/spoiler]] spans in the thinking line into blocks.
- **replyStopRegistry.ts** (src/utils/replyStopRegistry.ts) — *Moving the controller's highlight between a reply's small buttons*: An Ask or Strategy reply on the Main tab has several small controls around it — Retry, Copy, the Helpful / Not really buttons, Read aloud, and Show details — and a person using a controller needs to move the highlight between them with the D-pad. […]
- **runningGameCharacterSuggestions.ts** (src/utils/runningGameCharacterSuggestions.ts) — *Suggesting an AI character based on the game you're playing*: When the AI-character feature is on and a person opens the character picker, this works out up to three characters from the built-in catalog that fit whatever game is currently running — so someone playing a game with catalog characters sees them offered first, instead of having…
- **sessionRagChipCandidates.ts** (src/utils/sessionRagChipCandidates.ts) — *Asking for chip suggestions drawn from this game's own notes*: Before showing the row of suggested-question chips for the game currently being played, the plugin can ask the back end for chip text drawn from that game's own notes (its entry in the plugin's knowledge base), rather than only the fixed set built into the plugin. […]
- **settingsPanelScroll.ts** (src/utils/settingsPanelScroll.ts) — *What "Up" should do at the top of a scrolled Settings section*: On the Settings and Ollama tabs, some sections are taller than the screen and scroll. When a person moving the controller's highlight upward reaches the top control in a scrolled section, pressing Up should not do nothing — it should first reveal more of the section above, and…
- **settingsPayload.ts** (src/utils/settingsPayload.ts) — *Turning the settings on screen into the message that saves them*: The Settings tab holds every setting under a screen-side name, spelled the way screen code spells things. The back end that writes them to disk knows the same settings under different names, spelled its own way. […]
- **settingsTabLocalSurvival.ts** (src/utils/settingsTabLocalSurvival.ts) — *Remembering the Settings tab's open menu across a panel close and reopen*: Opening certain modals closes and reopens the quick-access panel, which would normally reset the Settings tab. […]
- **splitResponseIntoChunks.ts** (src/utils/splitResponseIntoChunks.ts) — *Breaking a long reply into D-pad-sized pieces*: A finished Ask or Strategy reply can be long. Showing it as one solid block would mean a single D-pad press scrolls through all of it with no stopping point along the way, so this file breaks a reply up into a few pieces first, each becoming its own stop the D-pad can land on. […]
- **splitThinkingBlurbItalicSegments.ts** (src/utils/splitThinkingBlurbItalicSegments.ts) — *Thinking blurb italic segments*: Split thinking status copy so emoji graphemes stay upright while prose stays italic.
- **spoilerFenceRegistry.ts** (src/utils/spoilerFenceRegistry.ts) — *Letting the D-pad land on a hidden spoiler instead of scrolling past it*: Reply text can contain a "spoiler" that starts out hidden until a person chooses to reveal it. Normally the whole reply is one connected area for the controller's Down button to scroll through, so a hidden spoiler inside it never gets its own stop — Down just scrolls past it…
- **steamInputJump.ts** (src/utils/steamInputJump.ts) — *Jumping straight to Steam's own controller-setup screens*: A few Ask replies about controller setup can offer a direct jump to Steam's own Input configuration screens instead of only describing where to find them — for example "map a button" or "open the controller layout for this game". […]
- **steamParental.ts** (src/utils/steamParental.ts) — *Watching Steam's own Kids lock*: Steam has its own "Kids" parental lock, and this file watches it for as long as the plugin is open, so the rest of the plugin can lock itself down to match — without ever writing the lock state to the plugin's own saved settings. […]
- **strategyChecklist.ts** (src/utils/strategyChecklist.ts) — *Turning a model's checklist into something the screen can trust*: In Strategy mode, the model can hand back a checklist — a title and a list of steps — instead of only prose, and the person can tick items off as they go. […]
- **strategyChecklistPersistence.ts** (src/utils/strategyChecklistPersistence.ts) — *Saving Strategy checklist progress without saving on every single tick*: Ticking a box on the Strategy checklist should feel instant, but saving every single tick to the back end the moment it happens would mean a flurry of save requests during a quick run through a list. […]
- **strategyGuideBranches.ts** (src/utils/strategyGuideBranches.ts) — *Turning a model's fork in the conversation into something the screen can trust*: A Strategy reply can offer the person a fork in the conversation — "want the early-game plan, or the boss fight?" — as a set of labelled choices instead of plain prose. […]
- **streamMarkdownPrepare.ts** (src/utils/streamMarkdownPrepare.ts) — *Stream markdown preparer*: Progressive markdown layout for token streaming with closed-block safety and wait chips.
- **stripAssistantDisplayTags.ts** (src/utils/stripAssistantDisplayTags.ts) — *Catching any control tag the back end missed before it reaches the screen*: The model writes a couple of tags into its own reply that are only meant for the plugin to read — a status line wrapped in `<bonsai-status>`, and a strategy-branch marker — and those are supposed to be removed by the back end before the reply is ever shown. […]
- **stripSoftContinueCue.ts** (src/utils/stripSoftContinueCue.ts) — *Cleaning up a "Continuing…" note that was still showing when Stop was pressed*: While an Ask is still being answered, the screen shows a small "Continuing…" note at the end of the reply so far, to make clear more text is on the way. […]
- **tabBodyViewport.ts** (src/utils/tabBodyViewport.ts) — *Tab body viewport sync*: Pin TabContentsScroll height via --bonsai-tab-body-height so overflow scroll works on gamescope.
- **thinkingSummaryText.ts** (src/utils/thinkingSummaryText.ts) — *Cleaning up the "thinking" line shown while an Ask is running*: While an Ask is being answered, the screen shows a short line describing what the model is doing — "Thinking…", or something more specific once the back end sends it. […]
- **troubleshootingAskHeuristic.ts** (src/utils/troubleshootingAskHeuristic.ts) — *Noticing a question that sounds like game troubleshooting*: When a question looks like it is about getting a game running well on the Deck — crashing, stuttering, a black screen, Proton issues — the plugin can offer to turn on the permission that lets it read that game's own compatibility logs, rather than turning it on automatically. […]
- **uiDocument.ts** (src/utils/uiDocument.ts) — *UI document*: Resolve the `Document` the plugin's own UI is actually rendered into.
- **uiScaleScopeBridge.ts** (src/utils/uiScaleScopeBridge.ts) — *Sharing the current text size with pop-up windows*: A pop-up window (opened with `showModal`) is drawn outside the plugin's normal on-panel area, so it cannot simply inherit the text and spacing size (the size a person picked, or the plugin worked out automatically for the screen) the same way the rest of the plugin does. […]
- **unifiedInputPersistenceMode.ts** (src/utils/unifiedInputPersistenceMode.ts) — *Deciding the one moment to clear the Ask box for "do not remember"*: The Ask box can be set to remember what was typed in it between openings, or to always start empty ("do not remember"). […]
- **unwrapAskedEntitySpoilerFences.ts** (src/utils/unwrapAskedEntitySpoilerFences.ts) — *Asked-entity spoiler unwrap*: Display-time unwrap of bonsai-spoiler fences per spoiler constitution rules.
