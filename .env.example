# Steam Deck SSH connection (for remote deploy)
DECK_IP=steamdeck.local
DECK_PORT=22
DECK_USER=deck
DECK_DIR=/home/deck

# This machine's IP (for Ollama -- the Deck connects back to this)
PC_IP=192.168.1.100

# Plugin metadata
PLUGIN_NAME=bonsAI
