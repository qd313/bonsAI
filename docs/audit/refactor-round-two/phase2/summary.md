# Phase 2: what the tools found

Generated by `python scripts/phase2_map.py`. Every number here has a list behind it
in the JSON file named at the end of its section. Nothing in this file is a decision.

## Exports nothing imports: 20 values, 6 types
These are not all dead code. Sorted by what actually has to be done:
- 27: named somewhere else after all
- 1: only a test or the harness names it
- 0 whole files can go.
- 12 files are never imported and 6 declared packages are never imported -- check both by hand: a script run as a command, or a package with its own entry point, looks unused to a tool that only follows imports.
- Detail: unused-exports.json

## Back-end code nothing calls: 16 need a decision
- 2: nothing calls it
- 14: only the tests call it
- 63 more were reported and ruled out: on the RPC surface, called by the loader, used by another back-end file, or a name a library owns.
- 3 back-end methods the front end never asks for: ask_game_ai, ask_ollama, dbg_fe_log
- Detail: unused-backend.json

## Copy-pasted code: 1846 lines in the app, 2286 in the back-end tests
- Of the app figure, only 871 lines are really app code; the rest is front-end tests, which the measure counts because they sit in the same folder.
- Of the back-end test figure, 2076 lines are Python; the rest is JSON fixtures.
- App: 84 pairs of files share code; 43 files repeat themselves inside one file.
- Back-end tests: 59 pairs.
- Biggest overlaps: hooks/useBonsaiAskOrchestration.test.ts and hooks/useBonsaiAskOrchestration.test.ts (193 lines); features/preset-carousel/presetRowNav.test.ts and features/preset-carousel/presetRowNav.test.ts (82 lines); components/MainTabAskModeMenuPopover.tsx and components/MainTabAttachMenuPopover.tsx (74 lines)
- Detail: duplicates.json

## Long functions with nothing explaining them: 56 front end, 7 back end
- Worst files: src/components/ContextChipLadder.tsx (2), src/components/MainTabBonsaiAiMarkdownChunk.tsx (2), src/index.tsx (1), src/styles/sections/section-6.ts (1), src/hooks/useBonsaiAskOrchestration.ts (1)
- Detail: long-functions.json

## Files with no purpose line: 20 of 311 checked
- Detail: missing-headers.json

## One setting is spread over many files
- 48 settings. A typical one is named on 9 lines across 6 files.
- 5 files name all 48 settings, so a new setting means editing every one of them; 1 more name at least a quarter.
- Detail: settings-spread.json

## Big files: 42 over 400 lines, 36859 lines between them
- Loops in the back end: 0. Loops in the front end: 0.
- Biggest: main.py (2995), py_modules/backend/services/knowledge_base_service.py (2092), src/index.tsx (1709), py_modules/backend/services/ollama_prompts.py (1571), src/hooks/useBonsaiAskOrchestration.ts (1469)
- Detail: seam-candidates.json

