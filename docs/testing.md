# bonsAI testing

> **Trimmed 2026-09-24 (plan 65).** More finished rows moved to
> [archive/testing-closed-2026.md](archive/testing-closed-2026.md); a few still-open rows had their
> older, superseded history moved to [archive/testing-row-history-2026.md](archive/testing-row-history-2026.md),
> each with a link back on its live row. A second pass moved more. 149,062 to 100,414 bytes —
> still short of the 60 KB goal; what is left is mostly steps and the newest result on each row,
> which a person running the check actually needs, not leftover history.

**Purpose:** Where to test, what to run, and how coverage is tracked — without dumping every historical checkbox into one file.

| Doc | Audience | Contents |
|-----|----------|----------|
| **This file** | Everyone | Overview, PR gates summary, coverage index, Verify pointer |
| [testing-automated.md](testing-automated.md) | Agents / CI | Commands that can run without a human on the Deck |
| [testing-manual.md](testing-manual.md) | Maintainers | On-Deck smokes, Tier 0–4 runbook, Deck-only checklists |
| [test-evidence/](test-evidence/) | CI / agents | Preview-suite artifacts (`--write` / `--evidence`) — see [retention](#evidence-retention) |
| [archive/testing-closed-2026.md](archive/testing-closed-2026.md) | Everyone | Closed rows moved out of the coverage table below |
| [archive/testing-row-history-2026.md](archive/testing-row-history-2026.md) | Everyone | Older, superseded history moved out of still-open rows below |

Related: [roadmap.md](roadmap.md) ([Bugs](roadmap.md#bugs), [Verify](roadmap.md#verify), [Features](roadmap.md#features), [Done](archive/roadmap-done-v0.5.0.md)), [development.md](development.md) (build/deploy), [troubleshooting.md](troubleshooting.md).

### QA-EVIDENCE-GAP-01 — twelve checks whose evidence was never saved

**Found 2026-09-13 during the clean-up. These rows read as proven and nothing backs them.**

Twelve checks cite a saved Deck recording by name. None of those twelve recordings exists, and
checking the project's whole history shows none ever did — they were not lost, they were never
written. That means twelve results were recorded as passing on the strength of a file nobody can
open. Whether the checks really passed is unknown.

**All twelve are to be re-run in the next automated testing session, as one batch.** Until a run
produces real evidence, treat their result as unknown rather than as a pass. Each row carries an
inline flag where it makes the claim.

| Check | Where the row lives | Re-run status |
|---|---|---|
| CONTEXT-LADDER-03, case A (the strip trap) | closed testing rows | **Re-run 2026-09-18, PASS with real evidence:** with the Session context strip open and details closed, Down into the strip and Up out again did not trap — the ring climbed back to the button row. Evidence `docs/test-evidence/plan61-CONTEXT-LADDER-03-caseA.json`. |
| CONTEXT-LADDER-03, case B (details open) | closed testing rows, fixed-bugs archive | **Re-run 2026-09-18 on a real model reply:** the ladder and follow-up menu skip is confirmed. With Show details open, Down from Hide details lands on the new "From the notes" row, then the Session context strip, never on either menu button; Up from the strip skips both the notes row and the menu, landing straight back on Hide details. Evidence `docs/test-evidence/plan61-CONTEXT-LADDER-03-caseB.json`. |
| FOCUS-GRAPH-DEV-KB-01 | this file | **Re-run 2026-09-18, PASS with real evidence** — see the row below. |
| FOCUS-GRAPH-DEV-RAG-01 | this file | **Re-run 2026-09-16, now PASS with evidence** — see the row above |
| MICRO-04-VERIFY, after the fix | this file | **Re-run 2026-09-16** — the old layout is gone; see the row above |
| MICRO-04-VERIFY, left column | this file | **Re-run 2026-09-16** — same as above |
| SPOILER-REVEAL reachability | closed testing rows, fixed-bugs archive | **Tried again 2026-09-18, blocked:** neither God of War nor Hollow Knight is on the Deck's Recent Games row, the only list the launch tool can search, so neither would start and no reply was ever on screen to check. Someone needs to play one of them once by hand first. Evidence `docs/test-evidence/plan61-SPOILER-REVEAL-reachability-retry.json`. **Tried again 2026-09-23 with Hollow Knight running, still owed, for a different reason:** the game launched and the question was asked, but the name-withheld-boss bug (roadmap Bugs) means no spoiler cover ever appeared to reveal — there is nothing hidden to reach. This row stays open until a reply comes back covered at all. Evidence `docs/test-evidence/plan64-SPOILER-REVEAL-reachability.json`. |
| W1-R1 update | wave one session write-up | **Not run on purpose 2026-09-18** — pressing Update knowledge base would replace the 2026.09.18 library under test with the older published one, so it stays owed until the maintainer publishes. Evidence `docs/test-evidence/plan61-W1-R1-update.json`. **Published 2026-09-23:** the 2026.09.18 library now serves from both Hugging Face and the GitHub release, read back to confirm. Pressing the Update button itself is still owed — scheduled in plan 64's flow E. **Pressed 2026-09-23 (flow E), PASS:** the button read version 2026.09.18 and reached the public manifest — it could only say "Version 2026.09.18 is the latest" after reading it — though nothing needed downloading since the Deck already had that version. The proof that a fresh download really comes from the public host came from the two-taps row below instead: removing the library and downloading it again pulled `corpus.db.zlib` from huggingface.co. Both once-owed pieces are done. Evidence `docs/test-evidence/plan64-W1-R1.json`, `docs/test-evidence/plan64-TWO-TAPS-DOWNLOAD.json`. |
| Follow-up memory re-run, 12 Sept | closed testing rows | **Tried again 2026-09-18 with Hades running, blocked:** the same Ask-box freeze written up on the roadmap's three-star focus entry stopped every question in this batch from being sent, so nothing was checked. Evidence `docs/test-evidence/plan61-wave-three-hades.json`. **Settled 2026-09-23 — see KB-FOLLOWUP-01 above, PASS with Hades running.** |
| Model eviction on the Deck | this file | Old reading recovered 2026-09-16 (read 2026-09-12, `docs/test-evidence/plan48-R6-deck-model-eviction.json`) — the Deck has been wiped since, so a fresh run is still owed |
| Start-with-the-Deck check, 12 Sept | closed testing rows | **Re-run 2026-09-18, PASS with real evidence:** the switch was found on with a real start-at-login entry already there; turning it off removed the entry, and turning it back on brought the entry back, with the running AI left alone the whole time. Left switched on, as it was found. Evidence `docs/test-evidence/plan61-KB-AUTOSTART-01.json`. |
| The wave-three Deck evening, 12 Sept | closed testing rows, roadmap, this file | **Tried in pieces 2026-09-18:** the corrected Black Mesa water-note sentence still could not be asked — Black Mesa is not on the Deck's Recent Games row so it could not be launched, and Sifu (used for the coverage rows below) is not the same game the sentence needs; the Hades half of the batch was also blocked by the Ask-box freeze (the three-star focus entry). Still owed. Evidence `docs/test-evidence/plan61-KB-COVERAGE-negative-blackmesa.json`, `docs/test-evidence/plan61-wave-three-hades.json`. **Both halves now run, 2026-09-23:** the Black Mesa water question was asked with the game running (see the roadmap's electrified-water bug, unchanged by the game running) and the Hades follow-up passed (see KB-FOLLOWUP-01 above). |

Why it matters beyond these twelve: the retention rule below says a recording cited by any
write-up is never deleted, because deleting a cited recording turns a link into a lie. That rule
protects recordings that exist. It cannot protect against a row citing one that was never written
in the first place, which is what happened here. A check that writes the row before the recording
lands leaves exactly this trace.

### Evidence retention

`scripts/run-preview-suite.mjs` prunes its own output. It keeps the **3 newest run
folders per batch** and deletes older ones after each evidence run, with two
exemptions: a run **cited by any `docs/**/*.md`** is never deleted, because
evidence is linked by path from the archived QA docs and deleting a cited run
turns a link into a lie; and the run the current invocation just wrote is never
deleted, in case a future-dated folder sorts above it. The rule lives in the
script rather than in this file on purpose — a retention rule that depends on
someone remembering is not a mechanism.

**Reading a `batch-summary.json`: check `ranThisInvocation`.** The run folder is
keyed by date + commit only, so a `--filter` re-run on the same day and commit
writes into the folder an earlier full run already made. Summaries are merged per
scenario id, and `carriedFromEarlierRun` / `ranThisInvocation` say how much of the
row set this invocation actually produced. **Two summaries written before that fix
(2026-08-05) under-report and cannot be trusted for a total:**
`tier2/2026-05-26-9e20a82` says 1 result with 8 case directories beside it — the
archived failures doc independently records that batch as *tier2 (8/8)* — and
`tier2Deep/2026-06-09-a9237e4` says 1 with 11. In both, the per-case
`manifest.json` files are correct; only the batch roll-up is wrong.

**Pruned 2026-08-05 (step 10, D4).** Three runs, all unreferenced and all
carrying no signal: `hookSmoke/2026-05-26-9e20a82` and
`hookSmoke/2026-06-09-a9237e4`, which both failed with
`Error: IPC timeout for callTestHook` — the preview harness being unavailable
headlessly, not a product defect, so **`HOOK-smoke-setTab` has never produced a
real result in this tree** — and `deckOnly/2026-06-09-9e20a82`, whose three cases
all skipped as deck-only. Recoverable with
`git show <commit>^:docs/test-evidence/...`. Everything else was kept: the link
audit found **10 of 13 runs referenced and 0 broken links**, so the earlier
"96% unreferenced" reading in [audit/06-doc-triage.md](archive/06-doc-triage.md) was
an artifact of searching only this file and not the archive.

### Preview-suite evidence invalidated 2026-08-08

**Some recorded preview PASSes tested nothing.** Five independent defects were
found in the harness; each is verified, and none of them failed loudly. Stated
here before the repairs land so the fixes read as scheduled repair rather than
newly-discovered breakage. Plan: [planning/01-qa-automation-plan.md](archive/01-qa-automation-plan.md).

| # | Defect | Evidence |
|---|--------|----------|
| **D1** | **DOM snapshots truncate at 8 KB and never reach rendered markup.** `BonsaiPluginShell` makes a ~110 KB `<style>` block the first child of `.bonsai-scope`, so the snapshot is all CSS. | `test-evidence/tier0/2026-05-26-9e20a82/SMOKE-A-golden-path/dom-final.html` is exactly **8183 bytes**, contains **zero** `</style>`, and ends mid-declaration at `background-color: transparent !i` |
| **D2** | **`previewHook` / `callTestHook` has never worked in this tree.** | `test-evidence/tier3UI/2026-06-09-a9237e4/batch-summary.json` — **0 passed, 8 failed**, every one `Error: IPC timeout for callTestHook` |
| **D3** | **`focusPath`'s no-op value is byte-identical to its success value**, so an assert over it cannot fail. When the focus log is empty, DPS synthesizes `focusPath` from the inputs that were *requested* — and a real log entry for a `Down` press is also the string `onMove(Down)`. | Every stored `focus-path.json` is `["onMove(Down)","onMove(Down)"]`; `active-element.txt` reads `document.body`, so focus never landed on a plugin element. Mechanism confirmed against DPS source 2026-08-08 — see [planning/02-dps-upstream-findings.md](planning/02-dps-upstream-findings.md) § P1-2 |
| **D4** | **`final.png` is a placeholder, not a screenshot.** | Byte-identical within each batch (11202 B tier0, 10220 B elsewhere): a flat dark rectangle reading `Decky preview snapshot` / `505x599` |
| **D5** | **`assertStep` fails open** — six independent `if` blocks with no `default`, so an unknown or misspelled assert type silently passes. | `scripts/run-preview-suite.mjs`, contrast `runStep` which correctly throws. A full audit found **zero** unknown types in the 49 scenarios today, so no recorded result changes because of D5 alone |

**What is invalid, and what still stands** — surgical, because a blanket
invalidation would be as dishonest as leaving it:

| Class | Verdict |
|-------|---------|
| Every `domContains` / `domContainsAny` / `domNotContains`, and the one `focusPathIncludes` | **Invalid** — they matched CSS text, or echoed their own input |
| Every `rpcResult` / `hookResult` assert | **Valid** — coarse, but real |
| Every `shellVitest` / `shellPytest` step | **Valid** — they shell out to the actual suites |
| The 8 `tier3UI` scenarios and `HOOK-smoke-setTab` | **Already recorded FAIL.** Nothing to demote; stop citing them as coverage |
| Every stored `final.png` | **Invalid as evidence** |

**Computed from the scenario JSON, not eyeballed.** Twelve scenarios contain a
DOM or focus assert. Nine of those are `tier3UI` / `hookSmoke` and already record
FAIL. Of the three remaining, **two carry an archived PASS resting on nothing but
invalid asserts** — `SMOKE-A-golden-path` (tier0, 2 DOM asserts, no other check)
and `VISION-V1-spot-dom` (tier2, 1 DOM assert, no other check). Both are marked
`†` in [archive/testing-results-2026.md](archive/testing-results-2026.md).
`THINKING-01-pending-phases` and `FEEDBACK-01-reply-action-chrome` also assert on
DOM but have no archived PASS row, so there is nothing to withdraw.

Note that **`SMOKE-G` (vision attach) is not demoted** — its Verified status comes
from the April 2026 on-Deck vision sweep, not from `VISION-V1-spot-dom`. Only the
preview row is withdrawn.

Everything else stands on its RPC and shell assertions, including the three
SMOKE-F scenarios and the TDP / VAC / mDNS / desktop sets. **No evidence is
deleted** — the retention rule above protects doc-cited runs, and deleting a
cited run turns a link into a lie.

D1–D4 are Decky Plugin Studio behaviour, not ours; per [mcp-setup.md](mcp-setup.md)
§ DPS findings log they are recorded there and go upstream rather than being
forked here.

---

## Quick start

**Every change set (automated):** see [testing-automated.md](testing-automated.md) § Gates.

```bash
pnpm exec tsc --noEmit
pnpm test                  # when src/ touched
pnpm run test:py           # when Python / tests/ touched
pnpm run build             # when src/ or build config touched
```

**Deck-facing PRs:** after `scripts/build.ps1` / `build.sh`, run [testing-manual.md](testing-manual.md) **Tier 0** (SMOKE-A → C → F). Add Tier 1 when Ask / TDP / strategy / game context change.

**Release:** Tier 0–2 + clean-install proof in [testing-manual.md](testing-manual.md) § Tier 4.

**The Deck goes to sleep in the middle of a test run, and it does not fail cleanly.** A run is mostly
waiting — an answer takes tens of seconds, a game takes a minute to launch — and the device dozes off
during that. Presses then land on a sleeping machine and are lost, reads come back empty or stale, and
the session spends its next few steps working out that the Deck went to sleep rather than that the
thing under test broke. Until the tooling can hold it awake, ask a throwaway question before timing
anything, and treat an empty read as "check whether it is asleep" before treating it as a finding.
A pair of tools to turn the sleep timeouts off for a run and put them back afterwards was asked for on
2026-09-07 and is on the Decky Plugin Studio roadmap under *Hold the Deck awake for a test run, then
put the setting back*.

---

## PR contract (summary)

| When | Do |
|------|----|
| Always (if applicable) | Automated gates in [testing-automated.md](testing-automated.md) |
| Touched paths match matrix | Extra unit/preview rows in that doc § PR-scoped matrix |
| `src/`, `main.py`, `plugin.json`, Deck RPC | Tier 0 device smokes ([testing-manual.md](testing-manual.md)) |
| New focusable Settings/QAM control | FOCUS-GRAPH-01…05 + coverage row ([testing-manual.md](testing-manual.md) § Focus graph) |
| Docs-only | State **N/A** for Deck smokes; still run applicable automated steps |

Full policy: MCP `bonsai://policy/documentation` / [AGENTS.md](../AGENTS.md).

---

## Shipped feature coverage (slim)

One smoke often covers many features. Status: **Verified** / **Partial** / **Open** / **N/A**. Update when a smoke or release pass lands. Detail checklists: [testing-manual.md](testing-manual.md).
Rows whose Status is Verified, Passed, Fixed, Retired, Closed, Accepted or Shipped moved out to keep this table short: [archive/testing-closed-2026.md](archive/testing-closed-2026.md).
Some still-open rows below had grown long with dated, superseded history (earlier attempts, fixes since landed). That history moved out whole to [archive/testing-closed-2026.md § History moved out of open rows](archive/testing-closed-2026.md#history-moved-out-of-open-rows-2026-09-21); each row here keeps only its current status, its evidence paths, and a link back.

| Area | Covered by | Status | Notes |
|------|------------|--------|-------|
| Collapsing tab bar (plan 30); open tab strip redesign (plan 59) | TAB-BAR-01…11, TAB-STRIP-2A-01…07 in [testing-manual.md](testing-manual.md); unit: `TabIndicatorBar.test.tsx` (32), `tabBarNav.test.ts` (10), `useHiddenTabHeaderTrap.test.tsx` (9), `tabTitles.test.tsx` (7), `characterUiAccent.test.ts` (13), `icons.bonsaiGeometry.test.tsx` (5) | Open (08 touch, one 09 opener, 11 owed; plan 59's TAB-STRIP-2A-01…07 built 2026-09-17, none run yet) | Steam's header hidden by a hash-free rule, the 20px bar is the strip's one focus stop, the strip floats over the panel. Rows 01–06, 09 (three of four openers) and 10 passed on-Deck 2026-09-02 with evidence under `docs/test-evidence/TAB-BAR-*`; 08 (touch) needs a person at the Deck; the desktop-note opener of 09 needs the *Save files to Desktop* permission on. History: [archive/testing-row-history-2026.md § TAB-BAR-01](archive/testing-row-history-2026.md#tab-bar-01). |
| Permissions gate | SMOKE-C | Open | **Deck 2026-09-05:** the permissions half of this scenario was driven by hand on the device instead — deny surface, *Open Permissions*, the matching toggle, and back (PERM-JUMP-01, all steps). The preview scenario itself is still stale: it asserts the capability key `hardware_control`, removed 2026-07-30. **Demoted from Partial 2026-08-08.** Two reasons. The preview PASS rested on DOM asserts (D1), and separately the scenario's one `rpcResult` assert expects the capability key `hardware_control`, **removed 2026-07-30** — tier0 has not run since, so SMOKE-C will fail on the next run. Fixed in the scenario JSON; recovers with an end-to-end capability gate test through `class Plugin`, which does not exist yet (`tests/test_capabilities.py` is service-level only). **Blocked 2026-09-03 on the *Open Permissions* button not being a D-pad stop; fixed at the desk 2026-09-04, Deck check owed** — see the PERM-JUMP-01 row below. History: [archive/testing-row-history-2026.md § SMOKE-C](archive/testing-row-history-2026.md#smoke-c). **PASS (Deck) 2026-09-23, fixed in `af53b7d`:** the ring reaches the right toggle and stays there, with at most a brief flicker through Back to Main on the way — see the PERM-JUMP-01 row for the timed detail. |
| Reply style (Caveman / Balanced / Detailed) | REPLY-VERB-01 | Open | **Shipped 2026-08-09** (replaces Short). Unit: `settingsContracts.test.ts` (legacy `short` → `caveman`), `test_settings_service.py`, `test_ollama_service.py` (`CAVEMAN REPLY STYLE`; skipped when character roleplay on; depth-relax clause). **On-Deck:** Ollama → Reply style → **Caveman** → Ask → Input handling shows `Reply style: caveman` and reply is terse/caveman-like; **Balanced** → no REPLY VERBOSITY block vs prior; **Detailed** still paragraphs; with **AI characters** on + Caveman, character voice (not caveman grammar). Strategy + Detailed still ends with `bonsai-strategy-branches`. |
| Kids master lock (Steam parental) | KIDS-LOCK-01, KIDS-LOCK-02, KIDS-FOCUS-01, KIDS-REGRESS-01 | Open | **Re-confirmed 2026-09-17:** no lock banner, all four switches on and reachable (`docs/test-evidence/plan57-QA-KIDS-REGRESS-01.json`). **Shipped 2026-08-09.** `steamParental.ts`, `useKidsLock.ts`, `set_kids_lock_state` RPC, `capability_enabled` session flag, Permissions banner + disabled toggles. Unit: `steamParental.test.ts`, `useKidsLock.test.ts`, `PermissionsTab.test.tsx`, `tests/test_capabilities.py`. History: [archive/testing-row-history-2026.md § KIDS-LOCK-01](archive/testing-row-history-2026.md#kids-lock-01). **On-Deck:** **KIDS-LOCK-01** Family View locked on spare adult → banner, toggles grey/off, privileged actions deny; unlock restores prior values. **KIDS-LOCK-02** Steam Families child account (may stay Open). **KIDS-FOCUS-01** D-pad Permissions while locked (banner Focusable; no dead end). **KIDS-REGRESS-01** Deck with no parental controls identical to pre-ship. Live CEF Stage 0 still owed in [08](archive/08-kids-master-lock-feasibility.md) spike results. |
| Permissions cleanup batch (web links always-on; troubleshoot hint) | PERMS-CLEAN-02, 05, 06 | Open | **Narrowed from 01…06 on 2026-08-08 — not demoted, scoped.** Only three of the six are provable by a preview scenario, and only in the positive direction. **05** (About links present with all five capabilities off) and **06** (troubleshooting hint appears, then is gone after Dismiss) assert visible copy, so they cannot false-pass; **02** is checkable only as copy-staleness. **01 / 03 / 04 struck as unprovable** — each was written as "this UI is gone", i.e. a `domNotContains`, and the strings they name do not exist anywhere in the tree, so the assert passes whether or not the code is right. That is true even after D1 is fixed. Those three need an on-Deck look or a source-level test, not a preview row. Remaining on-Deck: About links, Steam Input jump, troubleshooting Ask with game-context on/off + dismiss hint, no power-limits toggle, no Response verification section |
| Deterministic commands / VAC off / shortcut keywords | SMOKE-F | Partial | VAC full matrix → Tier 2 manual. **Re-confirmed (Deck rig) 2026-09-17:** all four built-in commands answered at once with the same fixed wording. Evidence `docs/test-evidence/plan57-QA-SMOKE-F.json`. |
| Destructive mod/save advice guard (compatdata / Wine-Proton prefix / save data) | DESTRUCT-ADVICE-01 | **Passes with streaming ON (Deck) — streaming-OFF half still owed** | The guard reads a finished reply for a destructive verb next to a destructive target (compatdata, Wine/Proton prefix, save data) with no backup mention nearby, and appends a **bonsAI safety check** notice under it; fixed 2026-08-23. Confirmed on the Deck against both a scripted case and a live Ask, with the guard's own log line as a second, independent check. Streaming-off has not been run; filed as its own bug on the roadmap. **This row is expected to shrink:** the maintainer's plan removes the streaming setting once its own bugs are fixed, which would delete the streaming-OFF half outright. Exact test wording is pinned as frozen chips in batch B of [20-frozen-chip-qa-batches.md](archive/20-frozen-chip-qa-batches.md). Full investigation and the original failing case: [archive/testing-closed-2026.md § History moved out of open rows](archive/testing-closed-2026.md#history-moved-out-of-open-rows-2026-09-21), under DESTRUCT-ADVICE-01. |
| Strategy mode + spoilers (spot) | SMOKE-E | Partial | Deeper spoiler/checklist → Tier 2 |
| Strategy spoiler false positives (named-entity display) | STRAT-SPOIL-DRG-01 | Open | **Open — nothing has run on the Deck yet since plan 54 landed 2026-09-15.** The fix now lets the game's own screen name (not just its Steam ID) and the model's own read of which boss was named settle whether a reply should stay behind a spoiler fence, including while the reply is still streaming in. Three device rows are still owed: STRAT-SPOIL-NAME-01, STRAT-SPOIL-FIRST-01, STRAT-SPOIL-TEXT-01, in [testing-manual.md](testing-manual.md). Earlier fixes and device runs, including the *twins*/*exploders* investigation and the mid-stream mask fix: [archive/testing-closed-2026.md § History moved out of open rows](archive/testing-closed-2026.md#history-moved-out-of-open-rows-2026-09-21), under STRAT-SPOIL-DRG-01. Recon and locked decisions: [04-strategy-spoiler-false-positive.md](planning/04-strategy-spoiler-false-positive.md). |
| Spoiler constitution — title profiles + named entity | CONST-SPOIL-01 | Open | **Automated 2026-08-07.** `test_spoiler_title_profiles.py`, `test_ollama_service.py` (Hades protect without entity; `kb_entity_match` does not title-relax; DRG/L4D2 low-narrative), `unwrapAskedEntitySpoilerFences.test.ts`. **On-Deck:** DRG `2321470` unnamed boss ask → open tactics; Hades `1145360` unnamed → fences stay; Hades + *"How do I beat Megaera?"* → Megaera plain, other bosses still fenced; OoT unnamed → conservative. [spoiler-constitution.md](planning/spoiler-constitution.md) |
| Spoiler constitution — consent opens history fences | CONST-SPOIL-CONSENT-01 | Open | **Automated 2026-08-07.** `unwrapAskedEntitySpoilerFences.test.ts`, `buildAnswerBubbleElement.test.tsx` (consent unwrap with masking off), `useBonsaiAskOrchestration` stamps `spoilerConsentEffective` on collapsed turns. **On-Deck:** Strategy ask with *"spoilers are okay"* → all fences plain on live turn; collapse turn → expand from history → fences still plain (not re-masked). |
| Spoiler constitution — Speed + strategy KB inject | CONST-SPOIL-SPEED-01 | Open | **Automated 2026-08-07.** `test_ollama_service.py` `test_build_system_prompt_speed_strategy_domain_injects_compact_constitution`. **On-Deck:** Speed mode, KB on, strategy cards attach on a protect title → reply policy follows constitution (no wholesale boss-name drop without entity/consent); Show details chip still inherent protect band on Hades. |
| Thinking line has one writer (no unprompted rewrite) | THINKING-COPY-01 | Open | **Automated 2026-08-08.** Python composes the opener at accept time and returns it from `start_background_game_ai`; `composeThinkingBlurb.ts` and its pools are deleted, so the client cannot disagree. `useBonsaiAskOrchestration.test.ts` — renders the returned opener, holds the current line when a poll carries none, keeps a lazy-opener summary visible; `test_background_partial_state.py` — the opener equals what a later `"starting"` publish would render, and is in the snapshot before the task starts. **On-Deck:** THINKING-COPY-01 and THINKING-OPENER-01 in [testing-manual.md](testing-manual.md). The second is a judgement call — if the placeholder→opener swap reads as two lines rather than one settling, take the documented fallback. Detail: [06 § 10](planning/06-thinking-blurbs-review.md) **Deck 2026-09-04:** 32 distinct phases sampled across a 212s reply came through as one ordered sequence, no interleaving. |
| Slow Ask phases report progress | THINKING-SLOW-01 | Open | **Automated 2026-08-08.** `building_context` and `connecting_model` had copy but no emitter; both now fire (`test_ollama_ask_service.py` — `connecting_model` on the first attempt, `model_retry` only after). Copy for these two pools is pinned as encouraging: `test_bonsai_stream_tags.py` asserts no discouraging word at any request id in either tone, and that at least one line says the wait is normal. **On-Deck:** Black Mesa `362890`, cold model — see the [test title pool](testing-manual.md). |
| Thinking line updates during generation | THINKING-LIVE-01 | Open | **Automated 2026-08-08, revised same day after a device pass.** First attempt relied on the model emitting later `<bonsai-status>` tags; on-Deck an Expert+screenshot Ask emitted none and the line sat on `Model's warming up…` for the whole run — every other publisher finishes before `ask_ollama` is called. Two model-independent mechanisms now cover it: a `generating` phase on the first content token (`test_ollama_ask_service.py`), and `escalate_static_thinking_line` rotating a line that has not changed in 7s (`test_bonsai_stream_tags.py`, plus an end-to-end walk of the merge path in `test_background_partial_state.py`). Model tags still win and reset the clock. Window lengths are randomised per Ask (7–13s for the first, 4–12s after) from a hash of `(request_id, window index)` — deterministic, because the poll loop re-derives it every ~1.2s and a live roll would strobe the line. **On-Deck:** the line must keep moving during a long generation, on a beat you cannot count along to. A line that predicts completion ("almost done") is a copy bug — asserted in unit tests, but watch for it. Detail: [06 § 10.4](planning/06-thinking-blurbs-review.md) **Deck 2026-09-04 PASS:** the line updated throughout a 212s reply; none of the 32 phases carried a bare emoji. |
| Model status line cannot leak spoilers | THINKING-SPOILER-01 | Open | **Automated 2026-08-08.** Strategy prompts teach `[[spoiler]]…[[/spoiler]]` as a fallback for when the model names something anyway (`test_ollama_prompts_stream_instruction.py`), and `redactThinkingBlurbSpoilers.test.ts` renders that span as blocks, drops markup when masking is off, masks to end of line on an unclosed span, and asserts no raw markup survives six malformed inputs. **On-Deck:** blocks, never plain words, never literal `[[spoiler]]`. Detail: [06 § 2.7](planning/06-thinking-blurbs-review.md) |
| Thinking blurb emoji upright (italic prose only) | THINKING-EMOJI-01 | Partial | **Automated 2026-08-07 (Wave 1 A).** `splitThinkingBlurbItalicSegments.test.ts` (5 tests) — emoji graphemes upright, prose italic. **On-Deck PASS 2026-09-17:** the emoji sits upright in its own spot beside the tilted sentence. Evidence `docs/test-evidence/plan57-QA-THINKING-EMOJI-01.json`. Detail: [wave1.md](archive/wave1.md) |
| The warm-up skips quietly when memory is tight | PRELOAD-02 | Open | **Unit 2026-09-05.** The silent-failure paths are covered in `tests/test_ollama_preload.py`: an unreachable host, no eligible model, and the host declining are all no-ops that never raise. **On-Deck, not yet run:** with a game running and memory under pressure, turn the switch on and restart the loader. Nothing on screen may mention the warm-up — no error, no toast, no stuck status line — and the first question must be no slower than with the switch off. |
| Preset chip inject (no game append) | PRESET-GAME-01 | Open | Unit pass; on-Deck chip → Ask text. **Noted 2026-08-08: fixed at source, so no preview scenario is planned.** `joinPresetWithRunningGame` (`src/utils/joinPresetWithRunningGame.ts`) is an identity function with its own unit test — there is no append path left to regress. The on-Deck check stands as a wiring check, not a logic one |
| Reopened chat header shows the friendly caption | CHAT-HEADER-CAPTION-01 | Open | **Shipped 2026-08-28 (desk).** A branch pick's "I'm at: …" caption is now saved per turn (`display_text`) and the rebuilt header prefers it; spoiler unwrap/copy/follow-up keep the composed prompt. Six new tests incl. one end-to-end through the RPC. Chats saved before the change still show the composed prompt — the caption was never recorded for them, nothing to restore. **On-Deck walk owed:** make a branch pick (costs a generation), reopen the panel, read that turn's header — expect "I'm at: …", not "[Strategy follow-up] …". |
| Strategy Ask placeholder (focus caret) | STRATEGY-PLACEHOLDER-01 | Open | Unit/CSS fix; on-Deck empty Strategy field |
| Ask-bar caret with AI character on | ASK-CARET-CHAR-01 | Open | Layout fix 2026-08-07 (Wave 2 E): avatar is a flex sibling outside the text box; no Ask-bar component test harness — on-Deck with AI character on, focus empty Ask field, confirm native caret aligns with placeholder/text start (not left of `?` badge); D-pad Up from paperclip → avatar, Right → field; character-off path unchanged |
| Ask-bar avatar uses prop-emblem art | AVATAR-PROP-01 | Open | **Automated 2026-08-26.** `CharacterRoleplayEmoticon.test.tsx` (9) — the picker's default art is unchanged, the Ask bar's `art="prop"` path draws the emblem, the rendered box is byte-identical between the two styles at the same size, the corner badge markup matches exactly, and every instance gets its own gradient id with no colons (a shared id makes one disc paint with another character's tint). **The swap was designed to change no geometry**: a true-size mock measured both variants at a 50.0 × 276.8px text row, an 18 × 18 slot and a 119.2px card. **On-Deck:** with AI character on, open the QAM and confirm the Ask-bar corner shows the character's object — Heavy a sandvich, Medic a red cross, Nick Valentine a fedora — and **that nothing else on the row moved**: placeholder still starts in the same place, row the same height, badge letter the same size in the same corner. Then switch characters in the picker and confirm the corner follows, and select **Random** and confirm a crate with a `?` badge rather than the old bare `?`. **The picker itself is deliberately still on the old pixel grids** pending [D33](audit/maintainer-decisions-locked.md) — a picker still showing hooded blobs is correct, not a regression. Detail: [25-ai-character-avatars-handoff.md](archive/25-ai-character-avatars-handoff.md) |
| Global document realm sweep (8 sites) | DOC-SWEEP-01 | Open | **Automated 2026-08-07 (Wave 4 H).** `focusNavigation.test.ts` (`getFocusableWithin`), `uiDocument.test.ts`, `BonsaiPluginShell.test.tsx` (3). **On-Deck per site:** Ask blur before submit; attachment preview L/R; preset carousel pauses while chip focused; About GitHub Up → language dropdown; settings Up at scroll top → tab strip; transcript expand scroll-into-view. **Run the blur and attachment checks on the first Ask after a fresh open** — the document was learned only from answer-time registries until the shell seeding landed the same day, so these paths behaved differently before and after the first reply. Detail: [wave4.md](archive/wave4.md) |
| Suggestion chips as real buttons | CHIP-BUTTON-01…09 | **Partial (Deck) 2026-09-18 — 02, 03, 04, 05, 06 (one chip), 08 pass by measurement; 09 fixed 2026-09-21, Deck recheck owed; 01 and 05 by eye owed; 07 and the help/agent chips owed** | Built under plan 60 and checked on the Deck 2026-09-17 (build `b3c0d52`, merge of seven commits; `python scripts/verify.py --full` passed in 81s, `npx tsc --noEmit` clean, 160 test files / 1460 tests green). History: [archive/testing-row-history-2026.md § CHIP-BUTTON-01-09](archive/testing-row-history-2026.md#chip-button-01-09). **Row 09 failed on the Deck 2026-09-18:** with Half-Life 2 running and the knowledge base on, real note-based tips showed in the chip but the Tip dot never appeared before the label — filed as its own bug, see testing-manual.md's CHIP-BUTTON-09 row. **Row 09 fixed 2026-09-21 (plan 63, commit `895cf0a`):** two copies of the same markup had drifted apart; one piece of code now draws both badges. Deck recheck owed: with a covered game running and the knowledge base on, set the chip animation to the scrambling one and confirm the dot shows. **Tried 2026-09-23, COULD NOT RUN:** the Developer tab's pinned test chips were switched on, so no note chip was ever mixed in to check the dot on — needs the pins cleared first. Evidence `docs/test-evidence/plan64-CHIP-BUTTON-09.json`. **Still owed:** rows 01 and 05 need the maintainer's own look at the screenshots to say whether the new look and the spacing read right; row 07 needs the Deck's reduced-motion setting turned on; and the help and agent chips still need a look by eye. [Plan 60](planning/60-chip-button-restyle.md) · evidence `docs/test-evidence/plan60-QA-chip-button.json`, `docs/test-evidence/plan60-measure-before.json`. |
| Background Ask reopen | SMOKE-H | Partial | Full lifecycle → release. **2026-08-23:** code fix landed for the *"…" live question after a mid-thinking reopen* bug — `applyBackgroundStatusToUi` in `useBonsaiAskOrchestration.ts` now backfills the live header from the polled question whenever it is blank (pending and completed branches), guarded so it never overwrites a caption already on screen. On-Deck re-test owed: reopen while an Ask is still thinking and confirm the question stays visible through to the answer; also re-check whether the reported duplicate-question symptom on the *next* Ask is gone, since it was not independently reproducible by code reading. See [roadmap.md § Bugs](roadmap.md#bugs) for the full trace. |
| Knowledge base retrieve (keyword + hybrid) | KB-SMOKE-02/04 | **Re-opened** | Was Verified on the seed KB, Deck Jul 2026. **Re-opened 2026-08-05 by remediation PR1**, which changed the ranking, the query text and the corpus format — the earlier evidence describes behaviour that no longer exists. Re-run after `python scripts/build_rag_db.py --seed` and a redeploy |
| Phase 4 chip guarantee and Tip badge | PHASE4-CHIPS-01 | **Partial (Deck)** | The carousel now always shows at least one of the running game's own chips, badges them **Tip**, and falls back to shared or static chips for a game the corpus does not cover — all confirmed on the Deck. **Still failing:** a long chip label runs past the right edge of the chip column instead of being cut off with an ellipsis, because the label cannot truncate in its current styling; measured on the Deck 2026-08-29 at 379.8px inside a 300px column, filed as a bug on the roadmap. Owed: a fresh look at the widest labels once a Deck is free again, since the corpus has grown since the last measurement. Earlier attempts and full measurements: [archive/testing-closed-2026.md § History moved out of open rows](archive/testing-closed-2026.md#history-moved-out-of-open-rows-2026-09-21), under PHASE4-CHIPS-01. |
| Frozen test chips (QA) | QA-FROZEN-CHIPS-01 | **Partial (Deck) — badge regressed 2026-09-07** | **Both owed sub-checks closed 2026-08-27** with a six-chip Batch B pinned. *Rotation reaches the tail:* a single D-pad walk visited **all six** chips in order before leaving the carousel — the 2026-08-22 run only ever observed five of seven, which is what left this open. *Chip press:* A on a **TEST** chip put `what happens at the end of the main story` into the Ask field **verbatim** and did **not** submit (0 reply bubbles, not busy) — the split the row specifies. Badging, ordering and session-RAG suppression re-observed as before. **Method note for the next session:** pinning by editing `settings.json` under a running plugin works, but restart `plugin_loader` afterwards or the frontend's debounced save can write its stale list back over yours. **Re-checked 2026-09-07 during the wave-two evening, on a Deck with the chip-decode animation switched on:** the pinned batch still drove the carousel, but no chip showed the amber Test badge, so a pinned batch looked like ordinary chips. Filed as its own bug on the roadmap. Also seen and recorded to reproduce rather than filed: pressing Right on a pinned chip did not bring in the next entry. Evidence `docs/test-evidence/plan47-frozen-chip-findings.json`. |
| Chip rotation reaches past the top three of the candidate list | CHIP-ROTATION-01 | Fixed (desk) 2026-09-04, Deck check owed | `pickNextCarouselChip` used to hand out `available[0]` — the highest-ranked unseen candidate — for both the corpus guarantee and the ~30% roll, so with a stable backend rank order ranks 1-3 came back every rotation and ranks 4-6 only surfaced once 1-3 had cycled through history and been forgotten, which happened rarely because 1-3 kept winning the draw first. It now picks at random among the eligible candidates that share the top priority band — game chips still come before generic Deck tips — and the "nothing corpus on screen, so the next chip is corpus" guarantee is unchanged, just no longer pinned to one candidate. Frontend: 4 new tests in `sessionRagComposer.test.ts` (`pickAt(0.51)` / `(0.68)` / `(0.85)` pin ranks 4, 5 and 6 directly; a sweep of `random()` from 0 to 0.999 asserts the guarantee still returns a corpus chip, never the static fallback, whichever candidate it lands on). **On-Deck:** launch a game the corpus covers, turn the local knowledge base on, open the QAM and watch the carousel for 90 seconds without touching the D-pad — a chip ranked past the top three of that game's candidate list should appear at least once, not just the same three every rotation. |
| KB relevance floor | KB-FLOOR-01 | **Partial (Deck) 2026-09-15** | PR1, 2026-08-05. Weak matches were injected and cited on essentially every Strategy Ask. The floor is deliberately **loose** (R3) and PR2 tightens it, so the on-Deck question is the clean-degrade direction: ask something wholly off-topic in a troubleshooting-shaped Ask and confirm the KB chip reports **skipped** rather than attaching a junk tip. **Watch for the opposite failure too** — if the KB stops attaching on ordinary Asks, the floor is too strict and that is a regression, not a fix. **Run 2026-09-15:** the on-topic half PASSED (`docs/test-evidence/plan55-KB-FLOOR-01-ontopic.json`). The off-topic half FAILED as written — a printer question still attached knowledge-base content, trust tier `fallback_no_source` (`docs/test-evidence/plan55-KB-FLOOR-01-offtopic.json`) — but that is the exact behaviour the maintainer accepted 2026-08-27 in the roadmap's "Unrelated questions still get game cards stapled on" entry. **This row is really two checks bundled as one, pointing opposite ways:** the off-topic half contradicts an accepted decision and is for the maintainer to retire or reword; the on-topic half is a real regression guard worth keeping as is. History: [archive/testing-row-history-2026.md § KB-FLOOR-01](archive/testing-row-history-2026.md#kb-floor-01). **Confirmed fresh on the Deck 2026-09-19: on-topic half PASS.** A Half-Life 2 question got the right note and a normal knowledge-base card. Evidence `docs/test-evidence/plan61-KB-FLOOR-01-ontopic-retry3.json`. |
| KB kill-switch — turning meaning search off and back on | KB-KILLSWITCH-01 | **Partial (Deck) 2026-09-18** | Same switch as **FOCUS-GRAPH-DEV-KB-01** above (Developer tab, *Hybrid retrieval (meaning search)*), reading `rag_hybrid_retrieval_enabled` in the settings file. Steps written 2026-09-18 from the plan 55 run, no row existed before tonight. **Setup:** a covered game running, Strategy mode, knowledge base on. **Do:** with the switch on, ask a covered question and read Show details. Flip the switch off, read the setting file to confirm it changed, ask the exact same question again and read Show details a second time. Flip it back on and confirm the file again. **Pass when:** with the switch on, Show details reads "Keyword + meaning" with an embed time printed; with it off, Show details reads "Keyword search (hybrid disabled)" with no embed time, and the reply still attaches a note and still uses it — the switch changes how the note is found, not whether the game gets an answer. History: [archive/testing-row-history-2026.md § KB-KILLSWITCH-01](archive/testing-row-history-2026.md#kb-killswitch-01). **The Show details wording half PASSED on the Deck 2026-09-19, Hades running:** with the switch off, Show details correctly read plain keyword search, no embed time, and the reply still attached a note; the switch was turned back on and confirmed by reading the settings file again. Both halves of this row now pass. Evidence `docs/test-evidence/plan61-KB-KILLSWITCH-01-retry3.json`. |
| KB compat routing widened (D16) | KB-ROUTER-01 | Open | **Retrieval half PASS by probe 2026-09-03.** Read-only retrieval probe over SSH (corpus `2026.09.01`, Speed mode, no app id given; raw output in `docs/test-evidence/round31-laneB-probes-1.txt`): sentences (1) to (4) below attach the compat tips `storage`, `steam_input`, `anticheat` and `emudeck` respectively, each with `notes=compat_tips`. One caveat: the probe still resolved Deep Rock Survivor (`2321470`) as the game in context, so this was measured with a covered game present rather than with nothing running — the four routed to the tips anyway. Still owed on screen (plan 31 sessions 1 and 2): the **Source: shared troubleshooting tips** line in Show details, the Proton-log permission hint staying absent, and the Strategy-direction sentence with a covered game running. 2026-08-06. Troubleshooting Asks now reach the tip sheet when they **name a topic the corpus covers**, not when they contain the literal word `deck` or `proton`. **Steps:** KB on, corpus installed, then ask each of these in Speed mode with no game running and confirm each attaches tips with **Source: shared troubleshooting tips** — (1) *"I'm out of room and want my installs on the memory card instead"*, (2) *"the game only responds to the touchpad and ignores the sticks"*, (3) *"I can play alone but online kicks me out straight away"*, (4) *"my playstation 2 games run at half speed on the handheld"*. None of the four attached anything before this change. **Then check the other direction, which is the risk:** in Strategy mode with a covered game running, ask *"the slow-motion aiming barely lasts, how do I get more of it"* and confirm you get **strategy** cards, not a troubleshooting tip — that phrasing routed to compat before word-boundary matching went in. Also confirm the **permission hint** for Proton logs did **not** start appearing on ordinary troubleshooting Asks: that gate was deliberately left alone. Backend covered by `tests/test_compat_topic_router.py` (9 tests) **Deck 2026-09-04 PASS, all four sentences** routed to `compat_tips` with `Source: shared troubleshooting tips`. Entry moved to Done. |
| Wiki sources are credited on the reply | KB-ATTRIB-01 | **Partial (both sub-checks now closed; 2026-08-14 date check still owed)** | Every part of this check has now passed on the Deck: sourced cards show the credit line with host, licence and accent (screenshots `DeckCapture_20260817_210521`, `_210738`, `_210917`); a reply built only on bonsAI's own hand-written notes names them under "No source page" instead of showing no credit block; the accent reads as distinct from the amber model-routing chip (maintainer eye on screen, `DeckCapture_20260816_233808_game`); and the capture-date line — checked 2026-09-04, `combineoverwiki.net · CC-BY-SA-4.0 · as of 2026-08-09` over three named cards — is not today's date and does not move when the corpus rebuilds. **Reworded per D116 #8:** the negative case used to expect no credit block at all; the 21 September fix now names bonsAI's own notes under "No source page", and that is the fix working, not a regression. **The Status column above still reads Partial with the date check owed — that looks out of date against these results and is worth a maintainer look.** Full run history: [archive/testing-closed-2026.md § History moved out of open rows](archive/testing-closed-2026.md#history-moved-out-of-open-rows-2026-09-21), under KB-ATTRIB-01. **Confirmed again on the Deck 2026-09-23:** a Hades reply's open notes block named each hand-written note as "From bonsAI's own note" right on its own credit line; "No source page" is a separate group heading seen elsewhere in Show details (on a Black Mesa reply), not the words printed on each note's own line. Evidence `docs/test-evidence/plan64-CREDIT-LINE-NO-SOURCE.json` (+ `.png`). Backed by `tests/test_source_attribution.py`, `contextChipsFromSnapshot.test.ts`. |
| Named chat slots v2 + v3 redesign (LB/RB carousel, disk persistence, inverted layout) | CHAT-SLOTS-V2-01…06, CHAT-SLOTS-V3-01…10 | **Open — fix landed 2026-08-16, unverified on device** | Most of the redesign's checks now pass on the Deck: the bumper-spike row, several slot-switching and disk-persistence rows, and the "12 earlier" expansion all measured correctly on 2026-09-04. **One confirmed bug:** switching slots mid-reply can leave a Strategy branch menu from the old slot showing on the new one, and the new slot's ask bar does not read busy — filed under Bugs as a slot-scoping defect. **Still not run:** the rows that need a real multi-turn Ask in flight (V3-05b, V3-06a/b/c). History: [archive/testing-row-history-2026.md § CHAT-SLOTS-V2-01](archive/testing-row-history-2026.md#chat-slots-v2-01). Evidence `docs/test-evidence/CHAT-SLOTS-V3-01-down-empty-slot.json`, `docs/test-evidence/CHAT-SLOTS-V3-01-up-from-presets.json`, `docs/test-evidence/CHAT-SLOTS-V3-01-down-after-focusable-fix.json`, and more under `docs/test-evidence/`. Full defect history: [archive/testing-closed-2026.md § History moved out of open rows](archive/testing-closed-2026.md#history-moved-out-of-open-rows-2026-09-21), under CHAT-SLOTS-V2-01. Plan: [28-named-chat-slots-v3-implementation-plan.md](planning/28-named-chat-slots-v3-implementation-plan.md); new rows land under `CHAT-SLOTS-V3` in [testing-manual.md](testing-manual.md). |
| Every corpus title has cards | KB-COVERAGE-ALL-01 | Open | **Steps:** with the seed corpus installed and KB on, launch each of the thirteen titles in turn and confirm the coverage chip reports sections rather than **"KB: none for this game"**. Spot-check one Ask per title and confirm the cards attached belong to the running game — the corpus is now 133 sections across 13 titles, so a cross-game leak has far more room to hide than it did at 39. **Then the alias path:** with no game running, ask something naming a title (*"hl2 ravenholm"*, *"drg survivor what class"*) and confirm the right game's cards attach and no other game's do. Backed by `test_every_corpus_title_has_a_spoiler_profile`, `test_every_label_names_a_card_that_exists`, and the profile contract fixture. History: [archive/testing-row-history-2026.md § KB-COVERAGE-ALL-01](archive/testing-row-history-2026.md#kb-coverage-all-01). **Partly covered on-Deck 2026-08-22:** the alias path at the end of this row passes (see the D19 row); the thirteen-title coverage-chip sweep still needs the device, and the corpus is now **133** sections across 13 titles, not 119. |
| Plainly-worded problems reach the tips | W2-R4 | **Partial (Deck) 2026-09-07** | **Setup:** nothing running. Pinned Batch R4, nine chips — eight problem sentences plus one control. **Do:** ask each in Speed mode. **Pass when:** the eight attach a tip on the right subject and *"thank you very much"* attaches nothing. [Plan 47](planning/47-kb-wave-two-session.md) § 8, row W2-R4. **Ran 2026-09-07:** all eight problem sentences reached a tip and the control attached nothing, but one of the eight — the question about the TV picture being in the wrong spot — got a tip about Big Picture Mode versus Desktop Mode, which does not answer it. Evidence `docs/test-evidence/plan47-R4-problems-reach-tips.json`. |
| The two bug fixes | W2-R6 | **Failed (Deck) 2026-09-07, both halves** | **Setup:** Speed mode, Deep Rock Galactic: Survivor running, voice on. Pinned Batch R6, three chips. **Do:** ask all three, then exit the game and read the line under the question box. **Pass when:** no reply carries a code-like line, and the line under the box stops naming the game within a few seconds of exiting. [Plan 47](planning/47-kb-wave-two-session.md) § 8, row W2-R6. **Game-naming half, ran 2026-09-07:** Hades was exited with the panel open, and the line under the question box still named it straight after, 31 seconds later, about four minutes later, and after closing and reopening the Quick Access Menu. **The opposite failure was measured the same evening:** with the panel already open, Deep Rock Galactic: Survivor was launched and the line read *no active game detected* for 48 seconds and through a close and reopen of the menu, while Steam's own list had the game running throughout. Restarting the plugin corrected the line at once, both times — so the panel reads this once at start-up and never listens for a change. **Stray-computer-text half, ran later the same evening and failed:** two of the three questions were asked with the game running and the panel recognising it; both replies ended in a code box holding nothing but the raw power line, `{"tdp_watts": 7, "gpu_clock_mhz": null}` and `{"tdp_watts": 13, "gpu_clock_mhz": null}`, read out of the page. The cleanup that was meant to remove that line deliberately leaves anything in a code box alone, and the plugin's own instruction asks the model to put the block in a code box — so the one shape the plugin asks for is the one it cannot clean. Evidence `docs/test-evidence/plan47-R6-bug-fixes.json`, `docs/test-evidence/plan47-R6-stray-computer-text.json`. |
| The ring behind the corner icons, measured | W2-R7 | Open | **Setup:** a long reply on screen. No pinned chips. **Do:** walk down the whole reply with the D-pad. **Pass when:** this row only measures — the fix is not built yet. Record the visible percentage at every stop, especially the size of the stop that earlier read 67% behind Retry, which no evidence file yet holds. A stop only counts as a failure if a word of text is hidden; a corner point under an icon is where the icon is meant to be. [Plan 47](planning/47-kb-wave-two-session.md) § 8, row W2-R7. |
| The five August rows from the retrieval rework (optional) | W2-R8 | Open | One batch per row, run as each row already says elsewhere in this table: **KB-VARIANT-01**, **KB-FLOOR-01**, **KB-FOLLOWUP-01**, **KB-TRANSPARENCY-01**, **KB-KILLSWITCH-01**. Passes when each of those rows' own conditions pass. [Plan 47](planning/47-kb-wave-two-session.md) § 8, row W2-R8. |
| What the Deck is holding in memory around one question | KB-SPEED-02 | **Answered (Deck) 2026-09-18** | **The cause of the one-second wait.** Read at three moments: after a reply, the note-searching model is gone from memory; after the next search, the answering model is gone in turn. They evict each other because the Deck is set to hold one model at a time. Measured with that raised to two, on a temporary second Ollama started and stopped inside one session: the search after a reply costs 24 thousandths of a second instead of 732, and both models fit with room to spare. The maintainer's own Ollama was not touched and was confirmed unchanged afterwards. Changing it for good is still the maintainer's call. Where it is set is now known — the plugin's own two starting paths disagree, one asking for one model and the other for two — but which one the Deck is running today is unknown until someone reads it off the device. Evidence `docs/test-evidence/plan48-R6-deck-model-eviction.json`. **Re-read on the Deck 2026-09-18, as set up today, with real evidence:** the chat model was in memory before a question, gone from memory partway through answering it, and back in memory once the answer finished — caught happening on this Deck, on this build. Evidence `docs/test-evidence/plan61-KB-SPEED-02-model-eviction.json`. |
| The "No tip for this" line | W3-R2 | **Blocked — still nothing to fire on (Deck, 2026-09-15)** | **Do not pin this row until the line has something to fire on.** History: [archive/testing-row-history-2026.md § W3-R2](archive/testing-row-history-2026.md#w3-r2). **Checked again on the device 2026-09-15, still blocked, and now blocked with device evidence rather than an off-device measurement.** All seven of W3-R3's problem sentences attached a tip on the Deck, so none of them can show the line, and the line appears nowhere in that whole chat. Nothing about the tip side has changed. Do not pin this row until a sentence exists that reaches the tip sheet and comes back empty. Evidence `docs/test-evidence/plan48-deck-evening-2026-09-15.json`. |
| KB download Cancel (step 9) | KB-CANCEL-01 | **Open — not testable as written** | **Attempted on-Deck 2026-08-16 and abandoned: there is no cancel window.** The published corpus is 758502 bytes, so on a healthy LAN the whole download-decompress-install cycle takes **~0.9 s** — the Deck log shows `Downloading…` at 23:32:37.711 and `Knowledge base installed` at 23:32:38.610. Nothing in the plugin log recorded a cancel on any of the three downloads that session; the row cannot be exercised by hand at this corpus size. History: [archive/testing-row-history-2026.md § KB-CANCEL-01](archive/testing-row-history-2026.md#kb-cancel-01). **To actually test this, the download has to be slowed:** throttle the Deck's link (`tc qdisc`), point `rag_corpus_path` fetching at a stalled local host, or add a dev-only delay. Until then the six frontend tests are the only coverage and the focus half is unproven. Cancel takes the action row's **second slot** while a download runs, so the row is Download/Update + Cancel, and reverts to Download/Update + Remove when it ends. **The focus half is the part unit tests cannot judge, and it is why this is more than a feature:** while downloading, the primary button is `disabled` and Remove is not rendered, so before this change the row had **no focusable control at all** — the D-pad could not enter it. **On-Deck steps:** start a download (Ollama → Knowledge base → Update, or a first Download), then (1) D-pad **Down** from *Use local knowledge base* must land on **Cancel**, (2) D-pad **Up** from *Reply verbosity* must land on **Cancel** rather than passing through to the toggle, (3) **A** on Cancel shows *Cancelling…* and a second press must do nothing, (4) within a few seconds the row returns to Download/Update + Remove and the status line reads *Download cancelled* in grey — **not** the backend's raw exception text in red, (5) starting a new download after a cancel works. 6 frontend tests cover the RPC call, the double-press guard, the slot swap and the cancelled-vs-failed status text; all three mutation checks turn them red |
| Spoiler confidence chip (Show details band estimate) | SPOILER-RISK-CHIP-01 | Open | **Unit 2026-08-07 (Wave 3 C).** `tests/test_spoiler_risk_service.py` — band scoring, model-tag blend, manifest chip `spoiler_risk`. **On-Deck:** Speed, Strategy, and Expert Asks each show **Spoiler risk: low\|med\|high** under Show details; band does not change when spoiler masking or consent toggles change; incomplete `<bonsai-spoiler-risk>` in a stream must not blank the answer |
| Reply micro-actions / D-pad graph | MICRO-01…05 | Partial | Bug: Strategy live-turn skips. History: [archive/testing-row-history-2026.md § MICRO-01-05](archive/testing-row-history-2026.md#micro-01-05). **Reworded 2026-09-20: that shape is gone again.** Read aloud is no longer its own stop below Not really — a small speaker now sits at the right-hand end of the Helpful / Not really row itself, reached with Left/Right along that row, and Down from that row goes straight to Show details. Not yet run on the Deck under this new shape. **Still open:** MICRO-01/02/03/05, and the *branches* half of MICRO-04. **Corrected 2026-08-26:** an earlier note here called that half "unreachable rather than broken" without checking. It is neither — instrumentation added the same day shows the prompt *does* request the fence (`branch fence requested in prompt=True`) and `gemma4:e2b-it-qat` *does not emit it* (`branch_marker=False`) on two Strategy first turns with no game running. So it is model non-compliance, and it is now one log line to check rather than a guess. See the branch-picker entry in [roadmap.md](roadmap.md) for the full chain and the scope of the claim. |
| A chat carries what it has already covered | CHAT-MEMORY-01 | **Failed (Deck) 2026-09-23, with nothing running** | Built 2026-09-21. A question now carries what the chat has already covered, sized by the budget rather than by whatever is on disk. **Proved on the Deck through the deployed back end**, same model, same chat, one difference: asked *"and what about the boots?"* with no memory it replied *"Please tell me which game you are referring to"*; with the memory it replied *"You should keep the Iron Boots off during the fight in Ocarina of Time so you can move freely and effectively use your Longshot to yank the nucleus out of the water."* **What is owed is the screen.** **Setup:** a game running, any Ask mode, one chat with at least one question and answer already in it. **Do:** ask a follow-up that names nothing of its own — *"and what about that?"*, *"what next?"*. **Pass when:** the answer stays on the same game and subject as the earlier turn, and the plugin log's budget line shows turns carried above zero. **The spoiler half needs its own read and matters more than the rest:** ask something in a game with a spoiler profile until an answer comes back with a hidden block, then ask a follow-up and confirm the hidden text appears nowhere — not in the new answer, and not in Show details. Unit: `tests/test_chat_memory_service.py` (17), `tests/test_prompt_budget_service.py` (16) and four wiring tests in `tests/test_ollama_ask_service.py`; every rule was proved by breaking it. **Run 2026-09-23, with nothing running, FAIL:** in the Hades chat "how do i beat megara in hades" got a real answer; the follow-up "what about her second phase" knew "her" meant Megaera but gave only generic advice, and Show details read "No game is running, so there is nothing to look up" — no search ran for it at all. A third question, "what weapon works best against her", lost the subject entirely and asked which game and character were meant. `ollama ps` and the Ollama journal show both models stayed loaded the whole time, so this is not a model-eviction problem. Evidence `docs/test-evidence/plan64-FOLLOWUP-MEMORY-EVICTION.json`. **Run again 2026-09-23 with Hades running, still FAIL by this row's own rule:** 34 earlier turns were carried, but the reply still opened with "I ain't got no idea what you're talkin' about without a name… tell me which part" before guessing the right subject (Sandtraps) and giving two lines on it — the same deflect-then-recover shape as with nothing running. Evidence `docs/test-evidence/plan64-CHAT-MEMORY-01.json`. |
| Session tab inside Show details | SESSION-TAB-01 | **Passed (Deck) 2026-09-23 for steps 1–6, two new bugs split out** | Decided 2026-09-20 (roadmap Features, plan 62): opening Show details will show two tabs, *This answer* and *Session · N*, so the separate Session context row goes away. Nothing has been built for this yet, so there are no Deck steps to run. [Plan](planning/62-feature-session-five.md). History: [archive/testing-row-history-2026.md § SESSION-TAB-01](archive/testing-row-history-2026.md#session-tab-01). **Steps (starting from a chat with a finished answer already on screen — reopen an existing saved chat rather than asking a fresh question, to skip the pinned-chip and streaming variables entirely):** (1) Down-walk to **Show details**, press A — the label flips to **Hide details** and the panel opens. (2) Keep walking Down: **From the notes** block, then the **This answer / Session · N** tabs row (its own stop, `.bonsai-details-tabs-row`). (3) With the tabs row focused, press **Right** — the active tab must change from *This answer* to *Session · N* and the session row list must appear below it; press **Left** — it must change back to *This answer*. (4) With **Session · N** active, press **Down** — the ring must enter the row list on its first row. Keep pressing Down through every row to the last one (11 rows measured 2026-09-21; the count may differ tonight) — each row must take the ring and the list must scroll to keep it clear of the bottom dock, never covered. (5) Past the last row, Down again must reach the **Clear** button at the end of the body (`.bonsai-details-session-clear`). Press **A** — a confirm box titled *"Start the next question fresh?"* must open, with a **Clear** button and a **Cancel** button; press **B** or move to Cancel and press A — the box must close **without** clearing anything (do not press the confirm box's own Clear button; that is a real destructive action and this check only proves the box opens and cancels). (6) With the ring back in the Session tab's row list, press **B** — this must close the whole Show details panel (not just collapse the row you are on) and hand the ring back to the **Show details / Hide details** line, per `SessionContextTabBody`'s own `onCancelButton` wiring. **Run 2026-09-23, PASS on steps 1–6, moved to Done — two new bugs split out:** both tabs are reachable and Left/Right switch them; all 27 session rows and the Clear button take the ring, 100% visible; Clear's confirm box ("Start the next question fresh?") opens on pressing A. Two new focus bugs found in the same pass, filed separately on the roadmap with a fix already being built this session: the confirm box starts with the ring on the destructive Clear button rather than Cancel, and cancelling it (log only, nothing cleared) puts the ring on the tab bar with the whole details panel closed rather than back on the row list. Evidence `docs/test-evidence/plan64-SESSION-TAB-01.json` + screenshot, saved check `checks/plan64-SESSION-TAB-01-B-close.json`. **The Clear-box bug was fixed the same night (`e163d8c`); tried again 2026-09-23, COULD NOT RUN:** Claude Code's own permission check refused the rig's walk to the Clear button before any press went out, and the log shows 0 "clear" lines, so nothing was tried either way. Owed: a by-hand check by the maintainer — open Show details, the Session tab, then Clear, and confirm the ring starts on Cancel and stays on the Session tab after Cancel is pressed. Evidence `docs/test-evidence/plan64-SESSION-CLEAR-BOX-01.json`. |
| D-pad answer scroll | D-PAD-SCROLL-01/02 | Partial | Viewport fix; choppy scroll bug open |
| Character voice / Pyro | CHAR-VOICE, PYRO-EGG | Open | Tier 2 |
| Voice STT | VOICE-01…07 | Open | Tier 2; see troubleshooting § Voice. **2026-08-03: voice was completely broken since `742db60` (2026-07-17)** — `VoiceTranscriptionSession.status()` had been deleted signature-only, leaving its body as unreachable code, so every start raised `AttributeError`. Fixed with two mutation-checked guards (`test_voice_session_status.py`, `test_no_unreachable_code.py`) and verified on the deployed plugin. **A live recording round-trip on-Deck is still unverified** — that is VOICE-01. Deferring this tier is what let a total outage sit for two and a half weeks |
| Voice diagnosis note | — | — | Plugin-side RPC exceptions appear in `sudo journalctl -u plugin_loader`, **not** in `homebrew/logs/bonsAI/*.log` — the loader catches them before the plugin's own logger runs. Check the journal first when a UI action reports a Python error with nothing in the plugin log |
| Model routing pickers | ROUTING-01…02 | Partial | Fetch-on-open + save OK; focus/chrome → Bugs (**ROUTING-FOCUS-01**) |
| Ollama local-setup focus / Install label | OLLAMA-FOCUS-01…03, KB-FOCUS-01 | Open | Auto-probe + vertical chain + KB pair; re-check KB equal height |
| Ollama keep-alive slider gpfocus ring | OLLAMA-KEEPALIVE-FOCUS-01 | Open | Keep models loaded thumb: white ring vertically centered on dot (1px regression) |
| UI scale focus graph | UI-SCALE-01…05 | Open | Template for new controls |
| Soft `num_predict` + thinking budget (C1) | SOFT-PREDICT-01…05 | Partial | **Shipped 2026-08-10.** Caps Speed 800 / Expert 1200 / Strategy 1600; soft auto-continue on `done_reason=length` (max 2) with ephemeral **`Continuing…`** stripped from the final reply; C1 `resolve_ask_token_budgets` keeps `think: false` until Thinking effort control. History: [archive/testing-row-history-2026.md § SOFT-PREDICT-01-05](archive/testing-row-history-2026.md#soft-predict-01-05). Unit: `tests/test_ollama_ask_budgets.py` (7 cases incl. `medium` effort and `None` fallback), soft-continue cases in `tests/test_ollama_service.py` (cancel-mid-continue, strategy-fence stitch boundary, `ask_budgets` contract), throttle/shrink cases in `tests/test_background_partial_state.py`, `src/utils/stripSoftContinueCue.test.ts`. **Automated (Partial → confirm on-Deck):** **01** stitch + cue-absent-from-response, **03** cancel republishes cue-free text, **04** fence stays hidden across the stitch boundary — all pinned by the unit tests above; still need the on-Deck visual/timing confirm. **Verified (fully automated):** **02** empty continue stops quietly. **Still Open — needs a real thinking model on-Deck:** **05** thinking model with default off still returns visible text (no empty-reply regression). Reply verbosity remains prompt-only. Detail: [16-soft-num-predict-thinking-budget.md](planning/16-soft-num-predict-thinking-budget.md) **Deck 2026-09-04:** SOFT-PREDICT-01 PASS (a 10,707-character reply, the `Continuing` cue caught mid-stream, absent from the finished text and from the saved history after a tab round-trip). **Deck 2026-09-06: SOFT-PREDICT-03 PASS.** Asked for a very long weapons guide, pressed *Stop generation* while it was still arriving: the turn kept the half answer and shows **Stopped — partial answer kept.** *Helpful* and *Not really* are present and greyed (`bonsai-chat-secondary-btn Disabled`), *Retry*, *Show details* and *Copy* live — which is the corrected 2026-09-05 behaviour. Backend stopped cleanly: `abort_background_game_ai: stop requested` then the model unloaded. Screenshot `screenshots/round35-stopped-notice-and-greyed-buttons.png`. The greyed buttons still take a dead D-pad press — the separate greyed-button bug, second sighting. |
| Thinking effort control (Phase 1) | THINK-EFFORT-01…05, EXPERT-CAP-01 | Partial | THINK-EFFORT-05 PASS 2026-09-03 (D-pad row, plan 31). **Shipped 2026-08-15.** Ollama tab → **Thinking**: Off / Brief / Balanced / Deep, default **Off**. `think: true` for every on level (named levels are gpt-oss-only; qwen3 / deepseek-r1 reject a string — **D21**, superseding doc 16), with effort carried by the reserved budget 256 / 512 / 1024 added to `num_predict`. A model rejecting `think` gets one silent retry with thinking off, is remembered for the plugin session, and toasts once per model. History: [archive/testing-row-history-2026.md § THINK-EFFORT-01-05](archive/testing-row-history-2026.md#think-effort-01-05). Unit: `tests/test_ollama_ask_budgets.py` (11 cases), `tests/test_ollama_service.py` (think wire, fallback, known-bad-model skip, unrelated-400 non-retry), `tests/test_settings_service.py`, `useBonsaiAskOrchestration.test.ts` (warn-once-per-model), both settings contracts. **Verified (fully automated):** **01** Off still sends `think: false`; **02** levels send `think: true` with the budget added; **03** a refusing model still answers, once per session. **Still Open — needs a Deck:** **THINK-EFFORT-04** real thinking model per level (**and capture the actual 400 body from a non-thinking model to tighten `_is_thinking_unsupported_error`, whose matcher is deliberately loose because the wording is not a stable API**); **THINK-EFFORT-05** D-pad the new row; **EXPERT-CAP-01** Expert Asks now visibly longer before a soft continue. Detail: [16-soft-num-predict-thinking-budget.md](planning/16-soft-num-predict-thinking-budget.md), [D21](audit/maintainer-decisions-locked.md) **Deck 2026-09-04:** THINK-EFFORT-04 PASS with `qwen3.5:4b` (already installed, reports the `thinking` capability, so no model pull was needed) — Deep thinking, 212s, Show details read `Routed qwen3.5:4b`; all four segments of the Thinking row took the ring by Left/Right. |
| Token streaming (experimental) | STREAM-01…11 | Partial | Several preview PASS; Strategy spoiler stream open. **2026-08-07 — Phase A (P1–P5) and Phase B (multi-stop + follow) both landed**, see [05-token-streaming-review.md §§ 3.1–3.2](planning/05-token-streaming-review.md). Needs on-Deck: **STREAM-04** (rewritten — Stop now *keeps* the draft and shows a Stopped notice; the old expectation of a cancel literal is gone), **STREAM-09** (rewritten below for multi-stop), **re-run STREAM-01/02** because P2/P3 both changed the flag-off path, and watch first-paint latency on STREAM-02 (P3 leaves the poll at 1200ms until the first token). New **STREAM-11** below. **Noted 2026-09-16:** the "Developer → Token streaming" precondition every row in this family names is stale — that toggle is gone from the Developer tab, and streaming was on for every question asked during the 2026-09-16 Deck pass, so it can no longer be turned off to compare against. |
| Reply language (About override) | LANG-01…03 | Partial | **LANG-01** regression fixed Jul 2026 — closed dropdown shows **Follow system** on load (`selectedOption` = option `.data`); on-Deck confirm |
| RPC timeout wrapper (behavior change) | RPC-TIMEOUT-01 | Open | 2026-08-02: bounded RPCs moved to `callDeckyWithTimeout` (15s). Verify on-Deck that settings save/load, Ask submit, background status/abort, intent packs, strategy checklist, screenshots and voice status all still succeed normally — and that the 4 deliberately unbounded calls (`clear_plugin_data`, `install_rag_corpus_local`, `start`/`stop_voice_transcription`) still complete on slow paths (multi-GB model teardown, corpus copy, long recording) without a spurious timeout |
| Stop mid-Ask keeps a readable answer | STOP-PARTIAL-01 | **Re-open (2026-08-07)** | **The 2026-08-04 pass proved less than it looks.** It confirmed the *debris* case only: the frontend wrote *"Request cancelled."* unconditionally, so seeing that literal in the bubble could not distinguish a kept draft from a discarded one. Phase A (A5) changed the behaviour — Stop now keeps the streamed text and shows a small *"Stopped — partial answer kept."* line above it, and the literal appears only when nothing readable had arrived. **Re-run with real streamed text:** stop mid-answer after a few sentences (text must stay), and stop within the first instant (question must return to the ask field, no thinking blurb). Original entry: 2026-08-04, on-Deck and in the UI, stopping mid-Ask shows *"Request cancelled."* — the probe checks `_background_state`, the maintainer checked the reply bubble |
| Streaming follows the newest text | STREAM-FOLLOW-01 | **Verified (Deck) 2026-09-04, D-pad half — the touch-drag half stays owed to the maintainer** | B4 changed this from pin-only to follow-plus-pin, and both halves need a look because they share one scroll listener. With streaming on and a long answer arriving: (1) sitting at the bottom, new text must stay on screen without any input; (2) scrolling up mid-answer (**touch and D-pad, separately**) must hold position — the next partial must not yank you back down; (3) scrolling back to the bottom must resume the follow; (4) D-pad specifically: walking down through the sections pins, and walking to the bottom resumes — no fight with STREAM-09's stay-put rule. Worth watching for **overshoot**: the follow targets the end of the transcript, not the bottom of the panel, so the session context strip and Save chat should *not* be dragged into view. History: [archive/testing-row-history-2026.md § STREAM-FOLLOW-01](archive/testing-row-history-2026.md#stream-follow-01). **The D-pad half passed on the Deck 2026-09-04:** the scroll position tracked the growing answer, holding a constant distance above the true bottom (the sticky Ask bar). The touch-drag half needs a finger and stays on the maintainer's own checklist. Moved to [archive/roadmap-completed.md](archive/roadmap-completed.md). |
| ~~`main.py` extractions — original plan~~ | | | 2026-08-03: background-state shape, Ollama health probe, cancel-and-await, and Stop transport moved out of `main.py` (2865 → 2743). **Automated side is the strongest of any refactor step so far** — 60 new tests where these paths had almost none, every file mutation-checked, 486 Python tests green. **Not deployed.** Three things the unit tests cannot reach, in priority order: (1) **Stop** during a real streaming answer — press it mid-reply and confirm the reply stops promptly and the partial text is kept, since item 5 moved the handle-close that unblocks the worker read; (2) **plugin unload** — `sudo systemctl restart plugin_loader` and confirm `bonsAI plugin unloaded!` then `bonsAI plugin loaded!` with no traceback, since item 4 changed both teardown paths; (3) **Connection test** against a reachable host and an unreachable one, plus the on-Deck local runtime, since item 2 moved all three HTTP calls. `scripts/probe_deck_rpc_surface.py` covers the admission and abort RPCs but uses an unreachable port, so it never exercises a successful probe |
| Character picker avatars at the locked size | CHAR-AVATAR-26-01 | **Partial (Deck)** | **D33 locked at 26px 2026-08-27 (option C) and implemented the same day.** The picker was five hardcoded sizes (22 on the OK preview, 24 on both character grids, 26 on Random and Custom) and still drew the *old* pixel-grid faces; it now draws the prop-emblem artwork at one size, `PICKER_AVATAR_PX` in `CharacterPickerModal.tsx`. 26 is the design's badge line — in-disc at 26 and above, beside the disc below — so every avatar is on the same side of it. **Measured on device 2026-08-27** (bundle hash-verified, picker open on the Deck): **34 avatars, all `width=26`, all on the `0 0 32 32` emblem viewBox** — no pixel grids left. Character rows measure **38px, unchanged**, so no fewer characters fit per screen; avatar boxes 26×26, badges 14×14, **nothing clipped** by its row. Capture: `screenshots/DeckCapture_20260827_192047_game.png`. **Still owed — the judgement call, which is the maintainer's:** whether the props read clearly at 26 (the art was reviewed at 44), and whether the OK button's slightly taller row is acceptable. The maintainer said they may pick a different size after seeing it. **Regression direction:** the art style is an opt-in prop that falls back to the old pixel grids, so a new avatar call site that forgets `art="prop"` renders something plausible and wrong — two tests in `CharacterPickerModal.test.tsx` assert one size and one art style across the whole modal, both confirmed red without the change |
| Focus ring clipped on grid layouts (character picker) | CHAR-PICKER-RING-01 | Open | **Fixed at the desk 2026-09-04** (roadmap: "The focus ring is clipped on grid layouts"). Every entry button filled its column's full width with no inset, and the column div itself clips overflow (`overflow: hidden`), so a focused tile flush against its column's top, bottom, left or right edge had its D-pad ring cut off before it could render — rings paint outside an element's own border box, and `overflow: hidden` clips at that box's edge regardless. Each grid column now carries 6px of inner padding (`PICKER_GRID_RING_PAD_PX` in `CharacterPickerModal.tsx`), inside the same box that clips, so the ring has room without ever reaching the boundary. 6px covers both reference points at hand: bonsAI's own documented "outer ring" recipe in design-tokens.md reaches about 5px past the element, and Steam's native ring on this modal's plain, unstyled `Button` (none of the custom-ring CSS in `gamepadAndPullModels.ts` targets a class this modal uses) has been read on device as roughly 2-3px plus a soft glow. This is inner padding for the ring, not a decorative gutter (design-language.md Rule 1) — it costs a few px of label width on every entry, not only the ones at an edge, because every column clips independently of its neighbours. Unit test in `CharacterPickerModal.test.tsx` pins the padding value; see [testing-manual.md](testing-manual.md) row **CHAR-PICKER-RING-01** for the on-Deck screenshot check this desk fix owes |
| Clear cache actually clears the session | CLEAR-CACHE-01 | **Partial (Deck)** | **2026-09-03, plan 31: main and reopen halves PASS again on 3b0e9d7 — log line `forget_background_game_ai: stored answer dropped (stopped=False)` at 01:05, Main empty at the [+] position after the clear, after a QAM close/reopen and after a true remount (`docs/test-evidence/CLEAR-CACHE-01-b-after-modal-back-to-main.json`, `docs/test-evidence/CLEAR-CACHE-01-c-close-panel-for-remount.json`). Orphan half measured: the saved chat stays — 8 slots on disk before and after, the echo chat still in the carousel with its two turns; a clear moves the panel to [+] and leaves the chat behind, so each clear-and-reask cycle leaves one more chat. Mid-generation half still owed (deferred to the thinking-model step).** History: [archive/testing-row-history-2026.md § CLEAR-CACHE-01](archive/testing-row-history-2026.md#clear-cache-01). **Passing steps, run on device with the bundle and `main.py` hash-verified:** ask *"reply with only the word echo"*, confirm one reply bubble; Settings → **Clear cache…** → **Clear**; switch to Main — `bubbles: 0`, `turnIds: []`, no *echo* anywhere. Then close the QAM entirely and reopen the plugin — the full remount that reads `get_background_game_ai_status` — and Main must still be empty. **The log line is the tell:** `forget_background_game_ai: stored answer dropped (stopped=…)` in `~/homebrew/logs/bonsAI/`. Without it you cannot distinguish "the clear worked" from "the panel simply did not repaint", because the evidence for this fix is an absence. **Still open — the mid-generation half.** Clearing while a reply is *still being written* must stop the generation and leave the panel empty (not a "Request cancelled." bubble). Four unittests cover it ([tests/test_background_forget_on_clear.py](../tests/test_background_forget_on_clear.py)); it was **not** reproducible by hand, because `gemma4:e2b-it-qat` finishes in 6–32s (`num_predict` caps at 1600 tokens) while the D-pad walk from **Ask** to **Clear cache…** takes 35–95s over the bridge. Retry with a slower model, or park the ring on the button first. **BLOCKED a second time 2026-09-18:** the shoulder-button tab switch got to Clear cache in 63 seconds, but the high-thinking reply had already finished at about 50 seconds, so Clear again landed on a finished answer. Clear itself worked cleanly — empty transcript, a fresh ask started a clean session. Two tries tonight, both lost the race; this needs a reply over about 70 seconds (a running game, or the sixty-tip question from SOFT-PREDICT-01), or the maintainer's word to close this half as covered by its unit test. Evidence `docs/test-evidence/plan61-CLEAR-CACHE-01-midanswer-retry.json`. |
| D-pad reaches everything below the reply row | REPLY-DOWN-01 | **Partial** | **Demoted from Verified 2026-08-28** — the Ask diagnostics stop this row's verbose-logging case verified is gone (see the addendum at the end of this row and **DIAG-FOLD-01**); the rest of the walk is unaffected but wants a fresh on-Deck pass before Verified again. History: [archive/testing-row-history-2026.md § REPLY-DOWN-01](archive/testing-row-history-2026.md#reply-down-01). **Steps:** ask anything, then from the answer press Down to **Helpful**, Down again to **Retry**, and keep going: focus must continue to **Session context** and **Save chat to Desktop** instead of stopping. Then Up back through the same stops. **Column behaviour must survive:** Helpful → Down → Retry, and Not really → Down → Show details, each staying in its own column. **2026-08-28: that block is gone** — folded into the chip ladder's **Developer details** chip (roadmap: "Fold Show diagnostics into Show details"), so this stop no longer exists and a re-run should confirm Down goes straight from the ladder (or the collapsed hint) to Session context with nothing between. See **DIAG-FOLD-01**. |
| Clean install / release zip | Tier 4 | Open | Before tag |
| Windows deploy prune + verify | DEPLOY-VERIFY-01…03 | Partial | 2026-08-03 (**D8**): `scripts/build.ps1` wipes the Deck plugin dir before copy, exit-code checks every `ssh`/`scp`, and SHA-256 compares every shipped code file after upload. **The count is computed, not fixed** — `$VerifyPaths` is 4 named files plus every `py_modules/**/*.py` outside `__pycache__`, which is **57** as of 2026-08-08 (this row previously said 52, the value at the time D8 landed; do not treat it as a target). **01 Verified** — two deploys 2026-08-03 (00:47, 00:51), both `Verified 52 files on the Deck.` + exit 0 + `bonsAI plugin loaded!` + zero `Traceback`/`ERROR`. **03 Verified** — planted `STALE_SENTINEL.py` and `py_modules/backend/stale_dir/ghost.py` on the Deck, deployed, both files and the directory gone. (Do **not** use `refactor_helpers.py` as the witness — it was hand-deleted on-device 2026-08-02, so its absence proves nothing.) **02 Verified 2026-08-03** — happened for real rather than staged: a deploy attempted while the Deck had gone to sleep failed at the first `ssh` (exit 255) and the script printed *"Creating remote temp directory failed (exit code 255). Deploy aborted - the Deck may be asleep or unreachable."* with exit 1. Pre-D8 that same run would have attempted every `scp`, ignored all of them, and printed *Deployment complete!*. The `STALE` branch of the hash compare remains simulation-tested only. **Re-run with output redirected** (`.\scripts\build.ps1 2>&1 \| ...`) if the script's error handling is ever touched — a `$ErrorActionPreference = "Stop"` regression made a `pnpm` deprecation warning fatal under redirection and was caught only that way |
| Command reply saves to its chat slot (turn header / chat title) | CMD-REPLY-TITLE-01 | Fixed (desk) 2026-09-04, Deck check owed | A deterministic local-command reply (`bonsai:vac-check`, `bonsai:shortcut-setup-*`, the sanitizer on/off phrases) used to return from `_finalize_immediate_background_local_command` without ever writing to the active chat slot, so the slot never gained a first turn: its title stayed **New chat**, and the frontend's post-completion reload (`useChatSlots.ts`'s `applySlotTranscript`) found nothing and blanked the just-set live question back to `""`, which is what showed as a `…` turn header (trace: [roadmap-details.md](archive/roadmap-details-closed.md#a-command-reply-leaves-the-turn-header-blank-and-the-chat-titled-new-chat)). All three local-command branches now persist the question and the reply through the same `_chat_slots_record_user_turn` / `_chat_slots_record_assistant_turn` steps a normal Ask's completion uses, so the slot renames itself off the command text and the reload finds a real turn instead of an empty slot. **No frontend change was needed** — `turnsToCollapsedTurns` already builds a proper collapsed turn from a complete pair (existing test: `chatSlotTurns.test.ts`, "returns empty pending when turns end on assistant"), so the header stops depending on the live-question path once the turn is archived. Two new backend tests in `tests/test_chat_slot_ownership.py`: `test_vac_check_deny_reply_persists_turn_and_renames_slot` (ban lookup off) and `test_shortcut_setup_reply_also_persists_to_chat_slot` (the second call site). **On-Deck:** from the [+] position with the ban lookup off, send `bonsai:vac-check` — the turn header reads the command, not `…`; the chat's title in the slot row is the command, not *New chat*; after a QAM close and reopen the header still reads it. |
| Reading answers aloud: sound reaches the speaker from the background | TTS-FEAS-02 | **Partial — machine half PASS 2026-09-12, the ear check is owed** | A program with no screen of its own found the Deck's sound connections the same way the microphone does, and the sound service accepted them; it played a five-and-a-half-second file and one playback stream showed up on the built-in speaker. Owed to the maintainer's ear: hearing it on speakers, on headphones, over Bluetooth, and over a running game. Run by `scripts/probe_deck_read_aloud.py`. [Memo](planning/42-read-aloud-feasibility.md) § 7. |
| Reading answers aloud: works beside a running game | TTS-FEAS-05 | Open — owed | Not run yet. Needs the same speaking check with a game running, to see whether the game slows down. [Memo](planning/42-read-aloud-feasibility.md) § 7. |
| Read answers aloud: pressing it starts and stops the speech | READ-ALOUD-02 | **Old shape PASSED by machine 2026-09-12; not yet re-run since the shape changed 2026-09-20** | **Old shape, machine-checked 2026-09-12:** pressing Read aloud changed the line to Stop, a speaking process started, and one playback stream showed up on the built-in speaker while the next sentence's sound was made ready ahead of time; pressing Stop ended the process and the stream within a second, nothing left behind. Confirmed by watching the process and the sound system, not by ear. **Reworded 2026-09-20:** Read aloud is now a small speaker at the right end of the Helpful / Not really row, quiet until the ring reaches it, full strength once it does, and it turns into a red stop icon while the Deck is talking — same mechanism underneath, a new look. Not yet run again under this shape. Still owed: actually listening to it over speakers, headphones, Bluetooth, and over a running game. |
| Read answers aloud: a spoken question reads its answer back, a typed one does not | READ-ALOUD-05 | Open — owed (needs the microphone; the rule itself is unit-tested) | Not run on the Deck yet. With Voice replies set to When I asked by voice, ask one question by voice and a different one by typing; only the spoken question's answer should read itself. |
| Read answers aloud: a new question stops a reading in progress | READ-ALOUD-06 | Open — owed (unit-tested) | Not run on the Deck yet. Start a reading, then ask a new question before it finishes, and check the old reading stops. |
| Down and Up through a reply visit the same stops | REPLY-STOPS-MIRROR-01 | Open — re-measured 2026-09-23, still mismatched | **Fixed 2026-09-15 (plan 55, lane D).** On a reply with two paragraphs, a spoiler block and a two-button menu, check the Down stops and the Up stops are exact mirrors of each other. History: [archive/testing-row-history-2026.md § REPLY-STOPS-MIRROR-01](archive/testing-row-history-2026.md#reply-stops-mirror-01). **Re-measured 2026-09-23, with the details panel both closed and open:** the two directions still differ the same way — going down visits the question row and the question box, going up skips both and stops instead on Attach screenshot and Choose AI character; everything else matches. A 22-line answer section showed only a third of itself on landing because the question box covers it — expected for a section taller than the screen, not a new fault. Evidence `docs/test-evidence/plan64-REPLY-STOPS-MIRROR-01.json`. |
| The AI models hub passes the D-pad between its groups at the edges | HUB-EDGE-01 | **Verified (Deck) 2026-09-16, now describes a screen that no longer exists** | **Measured 2026-09-16 (plan 56 block 0).** Up from the first tier choice lands back on the Policy button; Down from the last advanced switch reaches the README button, then Done. Both edges already hand the highlight to the next group. `docs/test-evidence/plan56-M-hub-edges.json`, `docs/test-evidence/plan56-M-hub-edges-policy.json`, `docs/test-evidence/plan56-M-hub-edges-advanced.json`. **Note added 2026-09-20:** the Policy button this row measured is gone — Policy is now the Licence group inside the Filters panel (see **MODELS-FILTERS-01** below). These edges have not been re-measured against the new screen. |
| Every filter on the AI models screen behind one Filters button, and five changes that give the list more room | MODELS-FILTERS-01 | **Done 2026-09-23 for the filters themselves; the "more room" half stays open under MODELS-LIST-CAP-01** | **Corrected 2026-09-22: the blocker is fixed and the screen works.** History: [archive/testing-row-history-2026.md § MODELS-FILTERS-01](archive/testing-row-history-2026.md#models-filters-01). Earlier notes on the build and what to expect: [archive/testing-closed-2026.md § History moved out of open rows](archive/testing-closed-2026.md#history-moved-out-of-open-rows-2026-09-21), under MODELS-FILTERS-01. **Run 2026-09-23, PASS on what this row asks:** Installed only took the model count from 4 to 1, Essentials only off took it from 4 to 25, and Recently added took it to 0. Speed, Strategy, Expert, Vision and Open source only could not change tonight's list because all three essential models are open source, carry every tag and are rated six stars; "Any installed" stayed greyed out until unlocked, as designed — neither is a bug. One behaviour change, not filed as a bug: A on the "Filters · N on" button while the panel is open now moves the ring into the panel instead of closing it (it closed the panel on 2026-09-22); "Close filters" and Up still close it. The room the list gets is a separate matter, tracked under **MODELS-LIST-CAP-01**. Evidence `docs/test-evidence/plan64-MODELS-FILTERS-01.json`, screenshot `plan64-MODELS-FILTERS-01-essentials-off.png`. |
| A on a settings result jumps there, and coming back matches | SETTINGS-CARD-05 | Partial — Deck 2026-09-16 | **Landed 2026-09-16 (commit `cdc3759`).** Press A on a result and check it opens that Steam setting; coming back finds the same words and the same card. **Passed on the Deck 2026-09-16 for the jump itself:** A on a Bluetooth settings row opened Steam's Bluetooth page — though the highlight landed one toggle above the row that was pressed, the same shape as the PERM-JUMP-01 bug, not new here. **Did not match the row's own expectation for the return:** reopening the plugin after the jump showed the Main tab with the question box empty and no card, rather than the same words and card as before. Whether the words should survive the jump is a question for the maintainer. Evidence `docs/test-evidence/plan56-SETTINGS-CARD-DPAD-01.summary.json`. |
| A one-result settings search still shows its one line | SETTINGS-CARD-06 | Open — owed | **Landed 2026-09-16 (commit `2d92240`), the hide rule's let-out clause.** Type `steam client update channel` and check it still shows its one result at four words even though the hide rule would otherwise hide the card past three words. Owed on the Deck. [Plan](planning/45-settings-shortcut-card.md) § 7. |
| The settings card grows with the box and never reaches the tab bar | SETTINGS-CARD-07 | Open — owed | **Landed 2026-09-16 (commit `2d92240`).** Check the card's top rises as the box grows, and never reaches the tab bar; on the Deck's own screen it should show about six rows and name the rest as "N more" in its heading. Owed on the Deck. [Plan](planning/45-settings-shortcut-card.md) § 7. |
| The Spy's advice can be wrong on purpose, and Show details says so | SPY-REVEAL-01 | Partial — Deck 2026-09-16 | **Landed 2026-09-16 (commits `3cb00fe`, `4c422a9`, `45e9d57`, `1039154`).** Pick the Spy at the Heavy or Unleashed accent setting, ask a strategy question, and check Show details for a "Spy" entry reading "The Spy was on" with what he lied about, or "The Spy was on and did not confess". At Balanced or below, check no Spy chip appears. **Passed on the Deck 2026-09-16, at Heavy:** a Gonarch strategy question came back with two wrong-sounding tips inside a normal 141-word reply, the closing tag naming the lies was stripped from the visible text, and the Show details chip for that turn — read from the saved chat on disk — carried "The Spy was lying" with both lies named. **Still owed:** reading the chip's own body on screen by D-pad, blocked by the separate chip-row bug on the roadmap (Down from Hide details skips the whole chip row); and the Balanced "no chip" half, not re-run to keep the Deck's character changes to one for this pass. Evidence `docs/test-evidence/plan56-SPY-REVEAL-01.json`, `docs/test-evidence/plan56-SPY-REVEAL-01-ladder-walk.json`. |
| The very first model ticked in a fresh download picker only queues, it does not start downloading | PULL-FIRST-TICK-01 | **Done 2026-09-23 for this half; the try-order half stays open as its own bug** | **Fixed in `ec4d007` (plan 63, wave one).** Decky's buttons render as form submit buttons and the screen sits inside Steam's own form, so any click that did not cancel its default action submitted that form and took the screen's Done path — true of twelve of the screen's thirteen controls, the same shape of fault found and fixed once before in the try-order picker on 2026-09-04. History: [archive/testing-row-history-2026.md § PULL-FIRST-TICK-01](archive/testing-row-history-2026.md#pull-first-tick-01). The model's second half — a model pulled this way never joining the saved try order — is **not** fixed; that lives in the back end. **Run 2026-09-23, PASS for this half:** ticking the first tickable model (qwen3.5:4b; the literal first row is the installed model, which carries a "Use for Ask" star instead of a tick) only queued it — "Queue 1 · 3.2 GB", footer "Pull selected (1)" — nothing downloaded, and Ollama's own folder was unchanged. The second half — a model pulled this way never joining the saved try order — is still not fixed and now has its own, smaller bug entry on the roadmap. Evidence `docs/test-evidence/plan64-PULL-FIRST-TICK-01.json` (+ `.png`). |
| A preset chip's icon and its text are coloured the same way | CHIP-COLOR-BYEYE-01 | Open — measured 2026-09-23, owed the maintainer's eye | **Fixed in `895cf0a` (plan 63, wave one).** The words on a chip now use the same plain colour every other chip style already used; the dot's own colour is unchanged. **Measured on the Deck 2026-09-23:** the label colour reads rgb(196,211,226) at rest and rgb(220,235,248) with the ring on the chip. **Owed:** a by-eye check by the maintainer, from `docs/test-evidence/plan64-BYEYE-01-preset-chip.png` — a model cannot judge pixels. |
| A preset chip's bright blue underline is toned down | CHIP-UNDERLINE-BYEYE-01 | Open — measured 2026-09-23, owed the maintainer's eye | **Fixed in `895cf0a` (plan 63, wave one).** The line's strength went from 0.85 down to 0.55, the resting strength every other highlighted control already uses. **Measured on the Deck 2026-09-23:** strength reads 0.55 as intended, drawn only while the ring is on the chip. **Owed:** a by-eye check by the maintainer, from `docs/test-evidence/plan64-BYEYE-01-preset-chip.png`. This line is the chip's only highlight cue left, since Steam's own white ring has been clipped off chips since 2026-09-01, so it must stay clearly visible, not just calmer. |

**Evidence snapshots (keep):** [tier0/2026-05-26](test-evidence/tier0/2026-05-26-9e20a82/), [tier1Core](test-evidence/tier1Core/2026-05-26-9e20a82/), [tier2Deep/2026-06-09](test-evidence/tier2Deep/2026-06-09-a9237e4/). Historical PASS narration: [archive/testing-results-2026.md](archive/testing-results-2026.md). Orphan evidence folders may be pruned later when nothing links them.

---

## Verify (from roadmap)

These are **not** missing features — deferred maintainer testing. VAC still lives in the roadmap's
[Verify § Features](roadmap.md#verify) list under the `[platform]` **VAC check** entry. The `[QA]` **Deferred
manual QA** entry that used to sit beside it closed 2026-09-18 once its last owed check passed on the Deck —
see [Done](archive/roadmap-completed.md#deferred-manual-qa) for the full record:

1. Device QA Tier 0–1: A, F, E and H all pass, and all three Tier 1 extras now pass too (a Q&A that survives a
   reopen, one Ask each in Speed and Expert, and "What game am I playing?" with a game focused, the last of
   the three, passed 2026-09-18). C stays blocked by the Open Permissions jump bug (roadmap Bugs), not owed as
   a test of its own. SMOKE-B retired 2026-09-03 (D57 #6)
2. VAC-02…06 on-device — done 2026-09-23, all four remaining checks passed with the maintainer's own key; see [Done](archive/roadmap-completed.md#vac-check-bonsaivac-check-on-device-qa)
3. Broader prompt-testing pass

The QAMP verification matrix was withdrawn 2026-09-02 for the same reason as SMOKE-B (no TDP apply to test).

See [roadmap.md](roadmap.md) and [testing-manual.md](testing-manual.md).

---

## New focusable controls

Before shipping any new Settings/QAM control, complete the focus-graph checklist in [testing-manual.md](testing-manual.md) and add a coverage note above. Policy: `bonsai://policy/decky-ui-focus`.
